# 鲲鹏阅读器 测试版 1.1

这是重新开发项目的首个三平台公开预发布版本。请先在非关键环境安装，并保留
原有数据的本机备份；测试版不代表同步服务已经上线。

## 本版重点

- 智能管理收敛为三类能力：语义模型、Agent 模型、生图/视频模型。普通页面只
  展示可执行的准备、暂停、继续和状态；重排、BGE-M3、索引维护和 ComfyUI
  工作流位于各自模型卡的设置与维护中。
- 语义搜索默认采用 Qwen3 Embedding 0.6B，可自动在 CPU/GPU 之间选择，并支持
  增量索引与暂停/继续。27B Agent 和 H3 先检查显存、内存、磁盘及运行状态；条件
  不足时不会下载或显示无法执行的安装操作。
- 资讯中心按账号和设备隔离缓存正式发布包，支持 SSE 游标增量、正文和图片
  SHA-256 校验后 ACK、分类/重要性/通知范围筛选和可重试的历史回源。客户端不
  上传原始正文、模型提示词、推理或凭据。
- 阅读器继续修复连续开书、预加载、页码/章节进度、双页中缝实时预览、书单与
  收藏合并及批量加入书单等日常体验。
- 更新页和“关于”页统一展示当前版本、可用版本和完整更新说明；已经撤销的虚拟
  更新不会因旧缓存而在重启后再次出现。

## 安装与校验

- Windows：提供单文件便携版和 NSIS 安装包；使用 `SHA256SUMS-Windows.txt` 校验。
- macOS（Apple Silicon）：提供 App ZIP 和 DMG；本次为临时签名的早期开发测试包，
  首次打开可能显示 Gatekeeper 提示，使用 `SHA256SUMS-macOS.txt` 校验。稳定版才要求
  Developer ID 签名与 Apple 公证。
- Linux x86_64：提供 AppImage 和 Debian 包；使用 `SHA256SUMS-Linux.txt` 校验。

每个平台只下载适合自己的文件与对应清单。若安装包名称、版本号或 SHA-256
校验不一致，请不要继续安装，并通过项目仓库提交问题。

## 已知边界

- 同步协议 v5 的源码与客户端兼容测试仍不等于正式同步服务部署；公开测试包不
  内置真实同步服务地址。
- EPUB/PDF 的真实样本、性能和视觉验收仍按隔离 profile 的人工验收流程进行；
  CI 构建通过不替代这些阅读样本证据。
