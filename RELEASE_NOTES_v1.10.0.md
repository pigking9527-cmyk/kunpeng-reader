# 鲲鹏阅读器 v1.10.0

桌面端版本为 **v1.10.0**，Android 客户端版本为 **v0.2.0**。本次重点是阅读模式切换稳定性、私密配置加密同步、Android 阅读能力完善，以及首次提供 Linux 安装资产。

## 桌面端

- 新增端到端加密的私密配置同步与账号恢复。普通设置和智读问答历史可按需同步；API Key 只有在用户设置独立同步密码后才以 PBKDF2 + AES-256-GCM 密文保存，并支持密钥包世代撤销。
- 修复滚动/整页、单页/双页切换后的阅读位置漂移和首行缩进，切换前第一行内容会保持为切换后的第一行。
- 底部全书进度条与顶部工具栏统一同现同隐，支持拖动跳转与返回拖动前位置。
- 桌面与阅读页动画开关归入同一设置入口；展开书架语义搜索时会提前预热模型。
- GitHub 更新检查失败时可读取服务器发布元数据。

## Android v0.2.0

- 新增离线中英文词典、生词本、熟悉度和复习队列。
- 新增智读提问、总结与脑图；只使用已读范围内容，API Key 保存在 Android 安全存储。
- 新增邮箱绑定/换绑、修改/找回密码，以及智读与翻译设置、问答历史和加密 API Key 的可选同步。
- 新增关于页与更新检查，以 GitHub Releases 为主、服务器发布元数据为回退。
- EPUB 改为后台安全解析和按章缓存；书架延后读取大段标注数据，进入章节后预热前后章节。
- 降低滚动惯性，跨章只在边界手势触发并从下一章顶部进入。

## 下载与兼容性

- Windows x64：NSIS 安装包、单文件便携版和 `SHA256SUMS.txt`。当前尚无 Authenticode 签名。
- macOS Apple Silicon：DMG、App ZIP 和 `SHA256SUMS-macOS.txt`。当前为临时签名，尚未经过 Apple Developer ID 公证。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`。兼容基线为 Ubuntu 24.04 / glibc 2.38+，当前未签名。
- Android v0.2.0：Full Profile 测试 APK 和 `SHA256SUMS-Android.txt`。当前没有生产 keystore，不作为 Google Play 正式签名包。

同步协议版本仍为 `1`，本次没有破坏性契约升级。图书正文、原始书文件、原始封面、语义模型与索引仍不会进入同步实体。
