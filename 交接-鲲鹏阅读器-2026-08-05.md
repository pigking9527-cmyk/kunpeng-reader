# 鲲鹏阅读器交接说明

日期：2026-08-05  
仓库：`C:\Users\pigki\Documents\Codex\2026-07-21\claude-projects\ebook-reader-tauri`  
桌面验收包：`C:\Users\pigki\Desktop\鲲鹏阅读器.exe`

## 当前交付状态

- 桌面端基线为 **v1.11.0**；Android 当前公开测试版本为 **v0.3.0**。
- 本工作区有大量**未提交但已构建验证**的协作改动。继续开发前请先阅读 `AGENTS.md`、`docs/coordination/CURRENT.md` 和相关 ADR/契约；不要用 reset、checkout 覆盖现有修改。
- 当前桌面验收包：55,910,400 bytes，SHA-256：

  ```text
  819A37A0834C9FF391AD682CA09C38A432E605DF77AA5F95F36E7BD0A03992EF
  ```

## 已实现的主要功能

### 书库问答 / RAG

- 书库问答已集成进主窗口，不再额外打开窗口。
- 默认检索全书库，展示最多 20 本、每本 1 段的候选依据；可按标签、收藏夹、选中图书限定范围，也支持跨书对比。
- 支持单书深度问答：显式书名或单选图书时，优先正文/目录、多段召回、证据筛选、回答后的引用自检。
- 回答支持 Markdown、脚注来源、悬浮预览与跳转原文；字体大小可在问答页调整并保存在本机。
- 书籍分类会生成大模型暗标签，参与书库问答的检索排序和回答上下文；模型标签与用户手工标签分开保存、展示和同步。

### 智读

- 智读以覆盖层呈现，打开/关闭不会压缩正文列宽或改变当前阅读位置。
- 已接入选句及邻近正文、当前章节、已读范围内混合检索、证据筛选与重排、引用自检、会话短摘要等流程。
- 智读回答支持 Markdown 标题、列表、代码和来源跳转；来源正文只保留下方“本次依据”区域，避免重复浮框。
- 阅读页仅能切换已配置的大模型；接口地址和 API Key 统一在书架设置的“大模型与翻译 API”里维护。

### 大模型与翻译 API

- 可保存多个常用大模型配置（DeepSeek、OpenAI、Claude、OpenAI 兼容接口）。
- 书库问答右上角、阅读页智读可切换已保存模型；“设为智读与问答当前模型”决定默认模型。
- 翻译服务可单独配置和切换；密钥仅保存在本机安全存储，除非用户显式设置同步密码并开启密钥同步。

### 同步与历史

- 同步实体包括阅读进度、书签、高亮、批注、评分、手工标签、收藏夹、生词本、阅读统计，以及 `model_book_tags_v1` 大模型书籍分类标签。
- 书库问答与智读历史会本机保存；用户开启“同步智读历史”后，才同步问题、回答和脱敏来源索引。
- 历史记录均支持删除。删除采用**条目级墓碑**（只含记录 ID 与删除时间）同步，防止其他设备的旧缓存复活已删除记录。
- 书库历史跨设备不同步来源正文、本机书籍 ID、书籍路径或原始文件；同一设备可从本机书架按书名/章节恢复正文预览。
- 已修复旧本机 ID 重用造成的历史来源错书：恢复时优先按书名和内容身份核对，避免《南明史》被误指向《三国志》。来源卡片和悬浮预览已限长并清理控制字符。
- 账户面板“同步内容”已列出：大模型书籍分类标签、API 配置（不含密钥）、可选智读/书库问答记录、可选加密密钥。

## 关键文档与代码入口

| 范围 | 位置 |
| --- | --- |
| 协作基线 | `AGENTS.md`、`docs/coordination/CURRENT.md` |
| 同步实体约定 | `contracts/sync/README.md` |
| 同步历史决定 | `docs/adr/0005-synced-library-ai-history.md` |
| 私密同步、历史合并与墓碑 | `src/private_sync.rs` |
| RAG、智读、模型配置、分类 | `src/ai_reader.rs` |
| 命令注册 | `src/main.rs` |
| 书库问答 UI | `ui/library-ai.js`、`ui/library-ai-entry.js`、`ui/styles.css` |
| 阅读页智读 UI | `ui/reader.js`、`ui/reader.html` |
| 同步账户面板 | `ui/index.html`、`ui/sync-ui.js` |
| API 配置面板 | `ui/api-settings-ui.js` |

## 最近验证

已通过：

```powershell
$env:ORT_LIB_PATH=(Resolve-Path 'target\local-ort-link').Path
Remove-Item Env:CARGO_NET_OFFLINE,HTTP_PROXY,HTTPS_PROXY,ALL_PROXY -ErrorAction SilentlyContinue
cargo fmt --check
cargo test --bin ebook-reader-tauri private_sync::tests
node --test ui\tests\library-ai-ui.test.cjs ui\tests\reader-toolbar.test.cjs ui\tests\sync-ui.test.cjs
cargo build --profile fast
```

结果：Rust 私密同步测试 5/5；前端相关测试 48/48；`fast` 构建成功。

## 后续建议与注意事项

1. **不要同步书籍正文、封面、向量索引、语义模型、本机路径或明文 API Key。** 这是既定边界。
2. 继续新增同步语义时，先更新 ADR、`contracts/`、fixture/兼容性测试，再改客户端和服务端。
3. 书库历史来源正文仅是本机缓存：历史记录在另一台设备打开时，应根据本机是否有相同书名/章节决定可否恢复，不可将正文写入同步 payload。
4. 发布前运行 `scripts/check.ps1 -Release`；Windows 快速构建依赖 `target\local-ort-link` 提供 ONNX Runtime。
5. 每次给用户桌面验收时，统一覆盖 `C:\Users\pigki\Desktop\鲲鹏阅读器.exe`。若被占用，只能关闭路径完全匹配的该桌面进程，不要结束其它同名或未知进程。

