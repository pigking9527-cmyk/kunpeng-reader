# 鲲鹏阅读器 v1.14.1

发布日期：2026-08-09

本版仅发布 Windows 包。macOS、Linux 继续使用 v1.14.0，Android 继续使用 v0.3.0。

## 重点更新

- 书库问答继续提供短、中、长三档作答长度；中、长档分别扩展连续来源窗口，并可选用 BGE-M3 长上下文二阶段重排。
- 语义检索支持标准、高精度和实验性 BGE-M3 混合模式；重排模型可以独立下载、删除和恢复状态。
- 鼠标手势升级为多手势管理器，可配置动作、精度和提示样式；识别更侧重归一化方向和形状，不再要求末段补足额外绝对距离。
- 跳转回退图标支持连续大小和屏幕位置设置，并修复位置回到左上角、拖动边界与实际显示位置不一致的问题。
- 双页分页新增 0–120 px 中缝调节，单页与滚动阅读保持原有版式。
- EPUB 分页图片区分“下一页完整显示”和“跨页连续显示”；切换策略时保持当前文字位置，补充跨页候选、裁剪和几何诊断。
- 资讯来源、贴吧来源和书库问答偏好加入跨桌面设置同步；同步仍使用协议 v1，并继续保留旧客户端未知字段。

## 修复与稳定性

- 改进较长分页图片的前页上部预览和后页裁剪，减少图片压字、图文大间隔、上部缺失和翻页后跳动。
- 修复跳转回退预览与阅读页的图标样式、尺寸和坐标不一致。
- 恢复点增加经过过滤和容量限制的 WebView 偏好快照，不复制完整浏览器配置。
- 加固原生拖拽、图片拖动、文字选择和单实例窗口恢复边界。

## Windows 发布资产

- `Kunpeng-Reader-v1.14.1-portable.exe`：Windows x64 单文件便携版
- `Kunpeng-Reader-v1.14.1-x64-setup.exe`：Windows x64 NSIS 安装包
- `SHA256SUMS.txt`：Windows 资产 SHA-256 校验清单

## 已知说明

- Windows 安装包尚未进行 Authenticode 签名，首次运行时可能显示系统安全提示。
- 本次不发布 macOS、Linux 或 Android 新资产。
