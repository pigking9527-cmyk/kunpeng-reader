# 鲲鹏阅读器 v1.9.3

v1.9.3 增加书架标签/收藏夹与本机 BYOK 智读，并继续改善高亮菜单、翻页、图文分页和全书页数缓存。

## 主要更新

- 新增图书标签与收藏夹：支持右键整理、全局管理、漏斗面板多选过滤，并随图书轻数据同步。
- 新增「智读」侧栏：对已经读到的内容提问、总结或生成脑图；支持 DeepSeek、OpenAI、Anthropic 与 OpenAI 兼容接口。
- 高亮菜单支持横排/九宫格、四种颜色和百度/Google 搜索，多行、跨页与页末选区定位更稳定。
- 整页动画统一为新旧页面同步左移的水平翻页，修复跨章回闪。
- 修复滚动/整页模式与智读侧栏切换时的阅读位置漂移，并继续改进页末图片分页。
- 全书页数采用稳定版式宽度增量缓存；开关智读不再清空缓存或重复测量，首次统计也减少固定等待。
- 修复中文输入法下书内搜索框自动收起等交互问题。

## 隐私

智读采用用户自备 API Key。配置经系统能力保护后仅保存在本机，不进入同步实体；发送内容限制为当前书已经读到的相关章节和用户明确选择的文字。

## 下载

- Windows：NSIS 安装包、单文件便携版。
- macOS Apple Silicon：DMG、App ZIP。

macOS 包当前使用临时签名，尚未经过 Apple 公证。首次启动如被系统拦截，请在“系统设置 → 隐私与安全性”中允许打开。

## 校验

请使用 Release 中的 `SHA256SUMS.txt` 核对最终下载文件。
