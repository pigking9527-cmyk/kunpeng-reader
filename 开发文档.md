# 鲲鹏阅读器开发文档

> 鲲鹏阅读器（Kunpeng Reader）是一个面向 Windows、macOS 与 Linux 的本地电子书阅读器。当前候选版本为**测试版 1.1 修订版**（技术版本 `1.1.0-beta.2`），项目地址：[github.com/pigking9527-cmyk/kunpeng-reader](https://github.com/pigking9527-cmyk/kunpeng-reader)。

## 一、产品范围与发布状态

| 项目 | 当前状态 |
| --- | --- |
| Windows x64 | 生成单文件便携版与 NSIS 安装包 |
| macOS Apple Silicon | 生成 App ZIP 与 DMG；当前为开发签名，尚未公证 |
| Linux x86_64 | 基于 Ubuntu 24.04 生成 AppImage 与 deb |
| 许可 | 第一方代码为 source-available；公开仅供学习、评估与交流，复制、修改、分发、商用或发布衍生版本须取得书面许可 |
| 同步协议 | 开发基线为 `syncProtocolVersion = 5` |

本仓库只维护桌面端。移动端不属于本次测试版范围，也不在本文档描述或发布。

测试包用于功能验收，不等同于正式商业发行：Windows 尚未进行 Authenticode 签名，macOS 尚未完成 Developer ID 签名与公证，Linux 尚未签名。公开二进制发布还须完成许可证、产权和平台签名门禁。

## 二、技术栈

| 层 | 选型 | 职责 |
| --- | --- | --- |
| 桌面框架 | Rust + Tauri 2 | 窗口、文件、数据库、同步、系统能力与高耗时任务 |
| 用户界面 | HTML + CSS + JavaScript（系统 WebView） | `ui/` 为书架、阅读、设置、账户、统计与资讯的唯一可视实现 |
| 类型化边界 | TypeScript + Vite | 业务规则、控制器、Tauri 适配与阅读协议构建，不维护第二套页面 |
| 阅读引擎 | 命令式 DOM / Canvas | EPUB/PDF、分页、选区、手势与高频渲染 |
| 本地数据 | SQLite + WAL | 轻数据、同步实体、恢复点与设置 |
| 本地智能能力 | FastEmbed / ONNX + HNSW | 语义检索、模型管理与本机向量索引 |

桌面端始终只有一套用户可达 UI。技术迁移、类型化或性能优化不得新增候选页面、隐藏备用 DOM、iframe 或新旧界面切换入口。

## 三、项目结构

```text
kunpeng-reader/
├─ apps/desktop-ui/            # TypeScript 业务边界、适配层与构建配置
├─ capabilities/               # Tauri 权限
├─ contracts/                  # 跨端同步 schema、fixture 与兼容说明
├─ crates/epub-parser/         # EPUB 解析兼容层
├─ docs/                       # 架构、ADR、测试、法律与协作资料
├─ packages/tauri-api/         # 类型化 Tauri 调用边界
├─ reader-core/                # 可复用领域模型与同步规则
├─ server/reader-sync-api-rs/  # Rust + Axum 同步服务源码（未切生产流量）
├─ src/                        # 桌面 Rust 逻辑与 Tauri command
├─ ui/                         # 唯一桌面 UI 与阅读页运行资源
├─ .github/workflows/          # CI 与三平台构建
├─ scripts/                    # 检查、构建、安装与资料安全脚本
├─ Cargo.toml                  # Rust 工作区与发布配置
└─ tauri*.conf.json            # 通用与 Linux 打包配置
```

## 四、核心架构

### 阅读与数据通路

- 前端通过自定义 URI 协议按需读取封面、书籍外壳、章节、EPUB 资源、TXT/Markdown/MOBI/AZW 正文与 PDF 原文件。
- 控制类操作（导入、打开、搜索、进度、书签、标注、统计、设置与同步）通过 `#[tauri::command]` 调用 Rust。
- EPUB 普通章节按章加载；大章节按标题或段落拆分为虚拟小章，避免一次把整本书放入 WebView。
- 阅读页的样式、布局、高亮批注和运行时脚本在编译期组合，不额外增加运行时网络请求。

### 本地可靠性

- SQLite 启用 WAL；同步游标、统计与确认基线按“标准化服务器地址 + 账号”隔离。
- 恢复事务以大小与 SHA-256 清单进行预检和回滚；每日自动恢复点最多保留 7 个，手动恢复和数据包导入均先创建保护点。
- 单实例在 Windows 使用系统互斥量，在 macOS/Linux 使用文件锁；第二个进程只转发待打开图书。
- 后台任务按耐久检查点恢复，索引任务只在完整提交一本书后推进进度；内存预算统一限制搜索、向量和缓存占用。

### 搜索、词典与智能功能

- 全书架全文检索使用压缩逐章索引、Bloom 预筛选、有界 LRU 缓存与精确扫描兜底。
- 语义检索在本机运行，索引、模型、正文和书籍路径不上传；向量与索引文件均带版本、尺寸和 SHA-256 校验。
- 离线词典支持中中、中英、英中、英英，以及 TSV、CSV、JSON、StarDict 和常见未加密 MDX 外置词典。
- 智读与书库问答使用用户自行配置的模型服务；仅发送用户明确选择的文本或已经读到的相关内容，API Key 默认只保留本机。

## 五、功能清单

### 书架与阅读

- 导入 EPUB、PDF、TXT、Markdown、MOBI、AZW、AZW3；支持封面墙、评分、阅读进度、标签与书单。
- 支持全文搜索、语义搜索、书内搜索、跨书搜索、阅读统计、笔记汇总和 Markdown 导出。
- EPUB 支持单页、双页与连续滚动；支持目录、书签、脚注、主题、字体、排版、高亮、批注、朗读和沉浸模式。
- PDF 使用 PDF.js 渲染，支持连续滚动、文字选择、目录、缩放、双页、搜索与批注。
- 资讯在应用内以本机资讯流展示；正文提取后只展示标题、正文、图片、来源与发布时间。

### 账户、同步与隐私

- 同步对象包括阅读位置、书签、高亮、批注、评分、标签、书单、生词本、统计、设置、主题背景及已授权的智能功能配置。
- 不同步图书正文、原文件、原始封面、模型、语义索引和本机路径。API Key 与翻译凭据默认不同步；用户主动设置同步密码后才会以加密密文同步。
- 协议 v5 客户端使用 `/v1/auth/*` 和 `/v1/sync/*`，并显式发送协议版本。旧版或缺少版本的同步请求应被拒绝升级。
- Rust + Axum 同步服务的源码与桌面 wire 已对齐，但尚未完成受保护 PostgreSQL 的端到端、备份恢复、反代切换与生产部署验收；公开构建不内置真实同步地址。

## 六、构建、检查与安装包

日常检查：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/check.ps1
```

本机 macOS 验收构建：

```bash
scripts/install-macos-app.sh --build
```

该脚本会构建完整应用、覆盖 `/Applications/鲲鹏阅读器.app` 并启动应用；不会删除书库、账户或用户数据。

GitHub Actions 在 `main` 的 push 与 pull request 运行跨平台前端门禁和 Windows 检查。三端安装包工作流仅在 `v*` 标签或手动触发时运行，避免每次源码提交都重复完整打包；新的提交会自动取消同一分支上已过时的 CI。

| 平台 | 产物 |
| --- | --- |
| Windows x64 | `Kunpeng-Reader-v<版本>-Windows-x64-portable.exe`、`Kunpeng-Reader-v<版本>-Windows-x64-setup.exe`、`SHA256SUMS-Windows.txt` |
| macOS Apple Silicon | `Kunpeng-Reader-v<版本>-macOS-Apple-Silicon.app.zip`、`Kunpeng-Reader-v<版本>-macOS-Apple-Silicon.dmg`、`SHA256SUMS-macOS.txt` |
| Linux x86_64 | `Kunpeng-Reader-v<版本>-Linux-x86_64.AppImage`、`Kunpeng-Reader-v<版本>-Linux-x86_64.deb`、`SHA256SUMS-Linux.txt` |

Windows 便携版为单个可执行文件，ONNX Runtime 以静态链接方式进入程序，不再要求旁置 DLL。每次下载都应先核验相应的 SHA-256 清单。

## 七、当前完成项与后续门禁

已完成：唯一桌面 UI、三端测试构建、Windows 便携版/安装包、macOS DMG/App ZIP、Linux AppImage/deb、哈希清单、跨平台前端门禁、Rust 单元测试与公开源码仓库。

仍需完成：第一方资产与第三方依赖的许可证复核、Windows 签名、macOS Developer ID 签名与公证、Linux 分发签名、受保护 PostgreSQL 的迁移/E2E/备份恢复演练，以及真实 EPUB/PDF 样本的隔离人工验收。上述门禁完成前，测试包不得表述为正式商业发行版。

*最后更新：2026-08-21*
