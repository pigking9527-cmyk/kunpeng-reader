# 鲲鹏阅读器 v1.11.0

桌面端版本为 **v1.11.0**，Android 客户端版本为 **v0.3.0**。本次重点是让书库问答真正基于图书正文与目录回答，并把模型书目分类和问答历史纳入可靠的本地与同步流程。

## 书库问答与检索

- 新增主窗口内的书库问答：默认精检全部已建语义索引的图书，展示最相关的前 20 本书，每本只保留 1 段证据；标签、书单和人工选书可收窄范围。
- 专门询问某本书时，系统优先识别书名，按“目录/正文优先 → 同书多段 → 全库补充”的层级检索，避免用作者后记、书评或相邻材料代替书的内容。
- 所有模式都在回答后检查引用：不被来源支撑的断言会被删改或明确标为推断；脚注悬浮即可查看对应原文，并可跳回来源章节。

## 模型书目分类与同步

- 「书籍分类」可由大模型在后台生成结构化暗标签，分类进度具有耐久检查点，应用退出后可以恢复续建。
- 模型标签与用户手工标签并存；模型标签始终纳入同步和图书信息页展示。“使用大模型分类的标签”仅控制本机筛选是否使用它，书库问答仍会把它作为检索与回答上下文。
- 书库问答自动本地保存，支持查看历史。只有开启“同步智读历史”时，才同步问题、回答及脱敏来源索引；不会上传书籍正文、本机书 ID、路径或来源原文。

## 其他改进

- 新增资讯入口，改善书库问答阅读宽度和脚注来源体验。
- 修复长书名分类时的进度布局、分类中断恢复和右键整理弹层的滚动定位。
- Android v0.3.0 新增本机全文索引、搜索结果页和语义索引构建，书籍正文、语义模型和向量仍仅保留在设备本地。

## 下载与兼容性

- Windows x64：NSIS 安装包、单文件便携版和 `SHA256SUMS.txt`。当前尚无 Authenticode 签名。
- macOS Apple Silicon：DMG、App ZIP 和 `SHA256SUMS-macOS.txt`。当前为临时签名，尚未经过 Apple Developer ID 公证。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`。兼容基线为 Ubuntu 24.04 / glibc 2.38+，当前未签名。
- Android v0.3.0：Full Profile 测试 APK 和 `SHA256SUMS-Android.txt`。当前没有生产 keystore，不作为 Google Play 正式签名包。

同步协议版本仍为 `1`，本次没有破坏性契约升级。图书正文、原始书文件、原始封面、语义模型与索引仍不会进入同步实体。
