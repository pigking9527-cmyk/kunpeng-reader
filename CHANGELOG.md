# 更新日志

## v1.1.0-beta.3 - 2026-08-31

- **测试版 1.1 修订发布**：修正 Linux 发布流程中恢复构建缓存覆盖许可证清单的问题；许可证清单现在在缓存恢复后生成，供 Rust 打包步骤可靠使用。
- **三平台候选**：Windows、Linux 与 macOS Apple Silicon 均从同一修订源码标签构建。macOS 是临时签名、未公证的早期开发测试包。

## v1.1.0-beta.2 - 2026-08-31

- **测试版 1.1 修订**：以新的预发布候选整合近期桌面端修复，同时修复错误的跨平台安装包目标选择。
- **修正版打包**：Cargo、Tauri 与 Windows/macOS/Linux 三套发布工作流均显式选择 `ebook-reader-tauri` 阅读器主程序；不会再把无阅读窗口的辅助进程当作桌面应用打包。
- **安装包验收**：macOS 会验证 App ZIP 与 DMG 的 `CFBundleExecutable` 和主程序；Linux 会验证 AppImage 与 deb 中的主程序；Windows 便携版也要求主程序符合最低体积门槛。
- **GitHub 候选发布**：修正版将使用新的预发布标签，保留错误测试包的历史记录并清楚标注其不可用状态。
- **近期修复收录**：纳入阅读器跨章排版与脚注裁切、macOS 同步凭据持续授权、同步地址恢复、资讯中心与设置等近期桌面端改动。
- **发行说明**：补充修正版范围、旧测试包处置和安装包验收要求。公开二进制仍受许可证、产权与平台签名门禁约束。

## v1.1.0-beta.1 - 2026-08-27

- **测试版 1.1**：将当前开发成果收敛为新的三平台公开预发布基线；版本号、Tauri 包元数据和“关于”页保持一致。
- **三端发布**：GitHub Actions 为 Windows x64、macOS Apple Silicon 和 Linux x86_64 生成安装包及 SHA-256 清单。
- **智能管理**：普通界面收敛为语义模型、Agent 模型、生图/视频模型三类能力。
- **资讯消费链**：已登录账户可按账号和设备隔离缓存正式资讯包，使用 SSE 游标增量接收；正文和图片完成校验后才确认接收。
- **阅读与书架**：继续加固阅读页预加载、连续开书、页码/章节进度、双页中缝预览、书单与收藏合并等使用路径。
- **更新体验**：启动后的更新检查只依据本次成功结果显示独立更新页；“关于”页可在同一页面展示当前与可用版本以及完整新版更新说明。

## v1.0.0-beta.1 - 2026-08-20

- **测试版 1.0**：桌面端进入公开源码前的三平台测试阶段；应用“关于”页以“测试版 1.0”显示当前版本。
- **三端持续构建**：GitHub Actions 在 `main` 分支分别验证 Windows x64、macOS Apple Silicon 与 Linux x86_64，并生成各平台测试构建产物和 SHA-256 清单。
- **GitHub 入口**：“关于”页最下方显示项目地址，可直接在系统浏览器打开源码仓库。
- **同步凭据稳定**：macOS 同步凭据改用稳定应用身份授权，已验证重启、重新构建覆盖安装及再次重启均可无提示自动同步。
- **情报中心完善**：菜单栏设置纳入情报中心入口，并改进情报快照续跑、资讯正文打开与返回路径。

## v1.0.0 - 2026-08-12

- **全新产权基线**：移除旧发行链路、旧仓库坐标、内置部署地址与个人标识，公开仓库及更新地址改由构建环境注入。
- **停止旧版同步**：新 1.0 客户端改用 `/v1` 认证与同步 API；旧客户端访问无前缀路径会收到 `426 CLIENT_UPGRADE_REQUIRED`，无需客户端世代请求头。
- **版本重置**：桌面产品版本重置为 `1.0.0`，服务端 API 标识同步调整为 `1.0`。
- **依赖许可整改**：EPUB 解析切换到 Apache-2.0 许可实现及本仓库兼容层，发行门禁继续阻止未通过产权审查的公开资产。

## v1.16.0 - 2026-08-12

- **桌面端技术栈迁移完成**：主窗口、设置、账户、同步、统计、资讯等业务页面采用 React + TypeScript + Vite；新增类型化 Tauri 调用边界、严格类型检查、lint、类型测试与跨平台构建门禁。所有桌面业务页面均为新实现，不保留旧页面入口。
- **迁移规模**：相对于 v1.15.0 的 Git 跟踪源码 diff，新增代码 35,488 行、删除代码 5,855 行，留存代码 29,633 行；v1.16.0 代码总量为 129,522 行。
- **阅读引擎保持独立**：Rust + Tauri 2 继续负责窗口、文件、数据库、同步和系统集成；EPUB/PDF、分页、选区、手势和高频渲染保持命令式阅读引擎，并补齐 TypeScript 边界与回归测试。
- **同步与恢复**：完善软件设置、主题背景、手势、书签/批注/标签/书单和 AI 历史的契约兼容；恢复状态、确认恢复、历史保留与数据世代保护进一步接入，避免旧离线数据覆盖恢复后的云端状态。
- **阅读稳定性与预览**：改进长章节测量、单双页分页、中缝、跨页图片、批注/高亮、脚注、进度与跨章定位。双页中缝支持拖拽时的实时阅读页预览、恢复默认，并在松手后再应用到正文，修复预览白闪和右侧露出第三页的问题；脚注弹窗内的引用可继续跳转，跳回当前页面不会计入阅读跳转历史。TXT 会合并固定宽度换行造成的断句并保留诗词分行；修复浅色正文看似空白、章节末尾整页空白和跨章节双页重复/跳页。
- **阅读偏好、主题与 PDF**：阅读页设置重新整理外观、分页、工具栏和高级选项；调整主题、配色和排版时提供即时预览，避免直接打断当前正文布局。自定义主题扩展至最多 15 个并补充常用色板，颜色调整不会让偏好页关闭或闪白重开；PDF 桥接改为显式模块依赖并兼容完整长度图书标识，修复 PDF 空白或安全桥未就绪。
- **桌面体验**：修复统计日期切换时封面闪动、柱状图/折线图高度变化和设置项布局跳动；移除热力图配色下多余的说明文字。调整账户概览服务状态点的展开/收起动画，避免悬停导致窗口高度变化或边缘抖动；修复启动增强下系统首次启动后窗口首次点击不显示的问题。
- **同步流量与账户**：同步实体和恢复版本继续压缩、去重并细化额度统计；账户中心、同步状态、鼠标手势设置与跨平台启动/窗口行为更稳定。手势管理支持收起编辑项，录制与执行提示的内容、位置、字号、透明度和动画反馈均进一步调整，降低误触和提示滞后。
- **阅读交互与诊断**：阅读偏好快捷面板、朗读控件和音源文案重新排布；语速步进改为 0.05。修复 macOS 阅读器触控板惯性滚动造成的额外连续翻页，顶部状态栏通过固定占位和等宽数字减少数字变动时的视觉抖动。问题记录容量增至 4 MiB，并保留更多操作与手势事件；书架和设置清理重复入口，多彩高亮开关、账户概览同步信息、资讯本地化和 macOS 标题栏细节同步修正。
- **PDF、语义与启动**：补强 PDF 阅读桥接、语义/GPU 运行时启动和降级路径、书库问答来源跳转及内存预算处理。
- **更新说明富文本**：版本更新通知与“关于”中的本版更新内容支持 Markdown 渲染，不再以纯文本显示标记符号。
- 发布 Windows x64 便携版/NSIS 安装包、macOS Apple Silicon App ZIP/DMG、Linux x86_64 AppImage/deb，并分别提供 SHA-256 校验清单；Android 保持 v0.3.0。

## v1.15.0 - 2026-08-10

- **全新桌面 UI 版本**：重做主窗口工具栏、设置中心、账户中心、图书信息、书架筛选、阅读偏好、大模型与翻译 API、更新提示与通用弹窗；界面统一为更紧凑的分区、卡片和侧栏导航设计。
- 主窗口工具栏支持图标大小、显示/隐藏、拖动排序和避让动画；账户入口一并纳入配置，设置入口始终保留。设置中心按基础、菜单栏、书架与导入、阅读与交互、智能功能、数据与系统分类，并支持折叠侧栏。
- 菜单栏显示设置可同步：图标大小、显示/隐藏、图标/文字形态和顺序随软件设置交换；“设置”本身始终显示。所有设置子页都在设置框架内打开，返回不会隐藏设置页。
- 阅读偏好提供精简/经典两种设置布局；默认主题支持分别调整正文、链接、选中文本和脚注颜色。全界面禁用浏览器原生拖拽与文字选中，避免书架和工具区误拖选。
- **阅读统计 UI 重构**：日、月、年、总览与贡献热力图改为统一信息层级；可直接切换阅读时长/字数、柱状图/折线图和热力图配色，统计项目显示偏好会保留。
- 账户中心改为概览、同步、账户安全、数据与隐私四页：概览显示服务连接、总存储额度、每日同步额度和最近同步；同步、安全、隐私页使用统一的较高内容区。
- 同步升级为按项目选择，并升级到 `syncProtocolVersion` 3：智读/书库问答历史逐条同步、书单元数据独立同步，减少重复上传；API Key 和翻译密钥仍只在用户设置同步密码后以加密密文交换。
- 新增书单推荐：本机语义检索先生成有限候选，再由用户配置的大模型精选；不会将整座书库正文发送给模型。书单支持简介、封面书、顺序和逐书评语。
- 阅读页新增可保存多套方案的正文点击区域；完善手势作用范围、绘制过程实时提示、连续恢复跳转前位置和重新打开任意已关闭阅读页。精简/经典阅读布局归入阅读偏好的工具栏与操作设置。
- 跳转回退图标支持更细的像素级大小调节，并与位置、显示条件一并保存/同步。
- 阅读偏好中的单页、双页共用同一套排版设置；双页模式额外提供 0–120 px 的中缝调节，单页与滚动阅读不受影响。
- 重做大模型配置：可保存多个模型，并独立分配给智读、书库问答和其它分类用途；翻译配置独立管理。书籍分类成为智能功能的独立入口，模型标签为其子功能。
- **同步与额度**：修复 AI 历史、书籍状态等轻数据会整包重复推送的问题；改为逐条实体、按确认基线去重与准确额度统计，避免一次同步再次推送两千余条未变化记录。
- **同步完成与冲突**：修复本机与服务端确认状态不一致后停在“自动同步失败”的路径；同步结果会区分已接受、重复/冲突、拒绝和拉取，并保留待确认状态而不把未结算数据误标成功。
- **更新页**：修复更新页手势关闭会退出整个软件、关闭后下次启动不再提示、启动后提示过晚的问题；更新页现在独立关闭/恢复，启动优先显示版本号和更新说明。
- **手势与阅读位置**：修复绘制期间提示滞后、不同手势相互误命中、目录/链接/脚注/进度条跳转记录各自割裂，以及只能重新打开由手势关闭页面的问题。
- **分页尾部**：修复页底已能完整容纳一行正文、却因为段后空隙被提前推到下一页的情况；仅收紧恰好跨栏处的段后间距，不改变普通段落、标题、脚注或滚动阅读的间距。
- **菜单栏与设置**：修复菜单项无法拖动、拖动过程中没有避让动画、账户入口未纳入排序、折叠设置侧栏后图标缺失，以及资讯入口存在重复功能开关的问题。
- **账户中心**：修复“同步”页无法打开、概览外三页高度不足导致内容被截断，以及页面顶部重复账号/同步/退出操作的问题。
- **模型与阅读偏好**：修复单模型时取消所有功能分配会导致智读/书库问答不可用；功能分配改为保留至少一个可用模型。精简/经典布局与阅读偏好、正文点击区域多方案的保存和切换也统一了状态。
- **阅读统计**：修复统计设置按钮关闭状态不同步、图表和热力图切换后偏好不保留的问题；新的指标、图形和配色选择会立即重绘并持久化。
- 发布 Windows x64 单文件便携版/NSIS 安装包、macOS Apple Silicon App ZIP/DMG、Linux x86_64 AppImage/deb，并分别提供 SHA-256 校验清单；Android 保持 v0.3.0。

## v1.14.1 - 2026-08-09

- Windows-only 功能与稳定性更新：仅发布 Windows x64 单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；macOS、Linux 继续 v1.14.0，Android 继续 v0.3.0。
- 书库问答保留短、中、长三档作答长度，并让中、长档分别扩展到约 800、1200 字的连续来源窗口；新增可选 BGE-M3 长上下文二阶段重排，未完成模型与索引准备时给出明确设置路径。
- 语义检索增加标准、高精度和实验性 BGE-M3 混合模式；重排模型可独立下载、删除、恢复状态，索引任务与界面状态刷新更稳定。
- 鼠标手势升级为可管理的多手势方案，支持新建、编辑、启停、删除、搜索、全局/独立精度、动作选择和提示样式；识别以归一化方向轨迹为主，不再要求末段达到额外绝对距离。
- 跳转回退图标支持像素级大小与屏幕位置同步，修复打开设置时位置回到左上角、预览拖动边界与阅读页实际位置不一致，以及预览/正文图标样式不统一的问题。
- 双页分页新增 0–120 px 中缝调节，并纳入版式缓存签名；单页和滚动模式不受影响。
- EPUB 分页新增“移到下一页完整显示”和“允许跨页连续显示”两种图片策略，切换策略时保留当前文字锚点；补充跨页图片候选、裁剪与几何诊断，减少图片压字、图文大间隔和进入图片下半页时的跳动。
- 资讯来源、贴吧来源和书库问答偏好纳入 `app_settings_v1` 增量同步；旧客户端未知字段继续保留，`syncProtocolVersion` 仍为 1。
- 恢复点增加经过白名单过滤的 WebView 偏好快照，不复制完整浏览器配置；背景资产恢复、单实例窗口与原生拖拽/文字选择边界进一步加固。

## v1.14.0 - 2026-08-08

- Windows、macOS 与 Linux 同步升级：新增完整阅读偏好页面，可区分总体和单书设置，管理主题、分页、工具栏显示项和高级选项。
- 新增独立书内跳转回退：目录、脚注、书内链接和底部进度条分别维护历史；正文右侧回退图标支持按时间/页数隐藏及 1～10 级大小。
- 新增启动增强与 Windows 开机自启；关闭窗口后可保留进程快速恢复，并可选择是否继续语义索引等高消耗任务。
- 智读新增三档侧栏宽度、脑图收起、最近 100 条自动同步与手动云端记录；本机历史不设活跃上限，云端保留删除墓碑。
- 自定义阅读配色、顺序和背景图支持桌面端同步；背景图改为独立二进制资产，支持哈希去重、分块/断点传输和完整性校验。
- Windows GPU 组件支持非阻塞检测、下载状态恢复和按需安装大型 CUDA 运行库；检测不再闪命令行，失败自动回退 CPU。
- Linux 主窗口和阅读窗口支持八方向边框拉伸；更新提示按当前平台和实际发布资产判断，三端相关界面补齐十种语言。
- 修复词典增强开关无法保持关闭、阅读工具栏隐藏无效、进度圆球拖动弹动、空 EPUB 首章、翻页动画和多处阅读偏好滚动问题。
- 加强阅读窗口关闭、书架首击打开、跨章定位和续读锚点保存，减少首次点击无响应和关闭重开后的阅读位置偏移。
- 用户标签与收藏夹拆为独立同步实体；服务端增加 90 天压缩实体历史、7 日/4 周/12 月整库快照、邮箱验证、资源配额、持久化限流及安全审计。
- 发布 Windows x64 便携版/NSIS、macOS Apple Silicon App ZIP/DMG、Linux x86_64 AppImage/deb，并分别提供 SHA-256 校验清单；Android 继续 v0.3.0。

## v1.13.1 - 2026-08-07

- Windows-only 稳定性更新：仅发布 Windows x64 NSIS 安装包和 `SHA256SUMS.txt`；macOS、Linux 继续 v1.13.0，Android 继续 v0.3.0。
- 修复关闭阅读页后首次点击图书可能没有反应的窗口销毁竞态；旧阅读窗口注销完成前保持关闭状态，不再用延迟强制聚焦抢占书架点击。
- 修复 EPUB 单页/双页恢复与关闭重开后的阅读位置偏移；关闭前等待稳定页面锚点和进度写入，初次恢复不再用临时分页结果覆盖准确进度。
- 优化双页章节末尾与跨章翻页：等待章节样式和最终分栏稳定后再显示新页，恢复位置与双页左页锚点保持一致。
- 移除书架封面按需写入 URL 的策略；所有封面立即获得地址，首屏封面优先解码，其余交给浏览器原生延迟调度，减少空白框架和分批闪现。
- 修复软件启动后“全书架正文检索”开关状态与搜索框提示不一致；阅读正文优先于大型目录加载，目录改为分批生成。
- 问题记录新增阅读窗口创建、图书信息、首屏正文、关闭销毁以及最近 12 次软件启动耗时统计；记录继续排除正文、路径、账号和凭据。
- 恢复 AI 历史记录来源预览和点击跳转原文；账户/同步子页增加遮罩与点击外部关闭，避免点击穿透。
- 云端智读与书库问答历史各保留 100 条，每类最多保留 200 条删除墓碑；仅这两类同步实体的单条容量提高至 4 MiB。

## v1.13.0 - 2026-08-07

- 桌面端 Windows、macOS 与 Linux 同步发布。新增 BGE-M3 与 Multilingual-E5-Small 语义模型选择，支持全文与稠密向量融合、可选重排、BGE-M3 长文精读、GPU 状态检测与更快的非阻塞模型/索引状态读取；所有索引续建必须由用户显式开始。
- 书库问答、资讯、阅读统计、排序过滤、账户和设置子页完成十种界面语言覆盖；阅读页菜单、浮动提示和 Microsoft TTS 会按文本语言自动选择合适语音。
- 问题反馈整合阅读器与主窗口最近两分钟的脱敏操作记录，可一键插入 Bug 反馈或保存到桌面；保留截图补充入口，并明确要求填写复现步骤和操作。
- 资讯来源管理改为独立页面；新增可自定义轨迹的右键返回手势、灵敏度设置和资讯正文手势返回。资讯卡片继续优先使用真实正文图片，无可靠图片的来源保持纯文本。
- 书架新增封面首屏加载策略：可在设置中选择固定数量或记住上次可见数量；禁用浏览器原生文字选区和封面拖拽，减少启动与拖选干扰。
- 新增自动目录导入连续监控、阅读字数桶与反馈/迁移兼容契约、fixtures 和服务端兼容测试；同步协议版本仍为 1，书籍正文、原始书文件、模型和索引仍不同步。
- 修复语义索引面板打开/切换模型时可能阻塞整个主窗口、打开多本阅读器窗口、已下载模型仍可重复下载、问答/资讯设置未随语言切换，以及多处阅读页交互与状态显示问题。
- Windows 安装包现在随程序安装语义检索运行时，避免安装后因缺少 ONNX Runtime 无法启动。
- GitHub Release 另附 Windows x64 的可选 CUDA Provider 组件，方便符合 NVIDIA 驱动要求的用户预先下载；不包含驱动、CUDA Toolkit 或 cuDNN。

## v1.11.2 - 2026-08-05

- Windows-only 体验更新：重做主窗口「资讯」，支持按来源选择（最多 24 个）、横排 / 方格与混合 / 按来源排列；新增 3DM、游民星空新闻。
- 资讯卡片按需懒加载真实封面，修复多来源图片、防盗链与无图补位；正文在阅读器浏览器内打开，右侧仅保留一个返回资讯流按钮。
- 资讯流加入本地缓存与可选后台预取：应用空闲后每 5 分钟刷新已启用来源，最多 6 路并发，打开时优先显示旧缓存；缓存不会进入书库、备份或同步。
- 书库问答输入框支持 Enter 提问、Shift+Enter 换行，以及只含复制、剪切、粘贴的右键菜单；历史详情直接显示回答正文，移除冗余标题。
- 修复关闭软件偶发延迟、非沉浸阅读模式工具栏与底部进度条的显示和隐藏交互。
- 仅发布 Windows x64 单文件便携版、NSIS 安装包与 `SHA256SUMS.txt`；macOS/Linux 继续 v1.11.0，Android 继续 v0.3.0。

## v1.11.1 - 2026-08-05

- Windows-only 补丁发布：提供 Windows x64 单文件便携版、NSIS 安装包和独立 `SHA256SUMS.txt`；macOS/Linux 维持 v1.11.0，Android 维持 v0.3.0。
- 智读改为“选句与邻近正文、已读范围混合检索、证据筛选重排、引用自检”的多阶段流程；回答支持 Markdown、可点击来源与本机会话记忆。
- 新增“大模型与翻译 API”统一设置：可保存并切换 DeepSeek、OpenAI、Claude 和 OpenAI 兼容接口等多套模型配置；阅读页仅显示已配置的翻译服务。
- 书库问答可切换当前模型、调整并记忆回答字号；历史记录支持删除，旧记录可从本机书架按书名/章节恢复引用正文。
- 智读和书库问答历史使用稳定条目 ID 与删除墓碑同步，避免旧设备重新上传已删除记录；墓碑不含问题、回答、书名或正文。
- 修复智读覆盖层切换时的首行/段距和阅读位置漂移、双击打开图书时的短暂选中闪动，以及资讯入口再次点击无法返回书架的问题。

## v1.11.0 - 2026-08-05

- 新增「书库问答」：全书库检索可返回前 20 本书、每本 1 段证据；支持标签、书单和手动选书限定，并以脚注悬浮展示可追溯的原文来源。
- 单书问题采用“目录/正文优先”的分层检索和同书多段召回；所有问答在输出前执行引用自检，避免把书评、后记或不相干片段当作正文结论。
- 新增可续建的书籍分类任务：模型生成结构化书目标签，优先用于问答检索和提示词；标签与用户手工标签分离、可选参与本机筛选，并作为同步实体始终保留。
- 书库问答记录自动本地保存，可在设置中按现有“同步智读历史”开关同步；同步时只上传问题、回答和脱敏来源索引，不上传原文、本机书籍 ID 或路径。
- 书籍信息页显示模型分类标签；修复长书名分类进度换行、进度恢复和右键整理弹层随滚动跑到菜单栏的问题。
- 新增资讯入口，并改善书库问答布局、来源脚注和结果可读性。
- Android v0.3.0 新增本机全文索引、搜索结果页和语义索引构建；书籍正文、索引和向量仍不参与同步。
- 发布 Windows 便携版/安装包、macOS Apple Silicon DMG/App ZIP、Linux AppImage/deb、Android Full Profile APK，并分别提供 SHA-256 校验清单。

## v1.10.0 - 2026-07-29

- 新增端到端加密的私密配置同步与账号恢复：普通设置、智读问答历史可按需同步；API Key 只有在用户设置独立同步密码后才以 PBKDF2 + AES-256-GCM 密文跨设备保存，并支持密钥包世代撤销。
- 修复滚动/整页、单页/双页模式切换时的阅读位置漂移与首行缩进；切换前的可见文本首行会保持在切换后的第一行。
- 底部全书进度条与顶部阅读工具栏统一同现同隐，并支持拖动跳转和返回拖动前位置；不再出现两个组件交替显示。
- 桌面与阅读页动画设置归入同一配置入口；书架语义搜索在打开搜索时提前预热模型，减少首次搜索等待。
- 新版检查在 GitHub 不可达时可读取服务器发布元数据；新增 Ubuntu 24.04 x86_64 的 AppImage/deb 构建、链接检查和启动烟测。
- Android v0.2.0 新增离线中英文词典、生词本、智读、账号安全、加密密钥同步与更新检查；后台解析 EPUB、延后读取标注并预热相邻章节，改善启动和切章速度。
- 发布 Windows 便携版/安装包、macOS Apple Silicon DMG/App ZIP、Linux AppImage/deb、Android Profile APK，并分别提供 SHA-256 校验清单。

## v1.9.5 - 2026-07-27

- 书架多选增加“标签”和“加入收藏书单”：可追加已有或新建分类，不会覆盖各图书原有的标签、书单；已有关系会明确提示。
- 修复高亮菜单靠近页末时切换横排/九宫格布局后的定位：菜单按新的真实高度重新判断上/下方向，避免隐藏、裁切或遮住选中文字。
- 修复 macOS Apple Silicon 的整页/滚动分页边界：按文本边界和完整行测量，减少滚动裁切、首尾半行和图文分页异常；测量逻辑已拆为独立模块。
- Android 首版完成书架六入口、EPUB/TXT 系统导入与 Download 自动导入、后台 EPUB 解析缓存、筛选/布局持久化，以及网格封面阅读进度显示。
- Android 阅读页支持滚动和整页左右翻页、自定义划词高亮/批注/书签/词典/翻译/web 搜索菜单；修复 EPUB 封面与正文图片、章节头图、脚注展开和大字号菜单兼容。
- 建立跨端 `contracts/`、兼容样本、ADR 与协作状态文档，统一桌面、Android 与 Apple 端后续共享的数据语义和同步协议边界。
- 发布 Windows 便携版/安装包、macOS Apple Silicon DMG/App ZIP、Android Profile APK，并分别提供 SHA-256 校验清单。

## v1.9.4 - 2026-07-25

- 书架交互改为单击打开图书、双击选中图书；进入选择状态后，单击书卡可连续多选。
- 收藏夹升级为书单：支持书单简介、封面和拖动排序；图书信息、漏斗筛选和同步数据统一使用标签/书单。
- 新增“提交 Bug / 功能提议”反馈入口，支持文字与压缩图片；服务端校验反馈载荷并转发到配置的反馈邮箱。
- 新增三款按需下载的开源中文字体，以及集中动画设置。
- 修复阅读统计时间线、语义索引状态与书架筛选等问题；移除项目内代码统计页面。
- 修复单双页、滚动模式与智读侧栏互切后的阅读位置恢复；已验证正文视口不再跳到章节开头。

## v1.9.3 - 2026-07-25

- 新增图书标签与收藏夹：支持右键整理、全局重命名/删除、漏斗面板多选过滤，并纳入图书轻数据同步。
- 新增「智读」侧栏：可对已读内容提问、总结、生成脑图和查看本机历史；支持 DeepSeek、OpenAI、Anthropic 与 OpenAI 兼容接口，API Key 仅在本机受保护保存。
- 重做划词/高亮菜单：支持横排与九宫格、四种高亮颜色、百度/Google 切换，并改善多行、跨页和页末选区的菜单定位。
- 整页翻页动画收敛为水平左移，修复跨章动画回闪；继续修复滚动/整页模式切换、图文分页和智读侧栏开关时的阅读位置漂移。
- 全书页数统计改用稳定阅读窗口宽度；智读开关不再清空或重建缓存，后台逐章统计减少固定等待并继续保留增量结果。
- 修复阅读页中文输入法搜索框自动收起，以及若干搜索状态、消息校验和工具栏弹层交互问题。

## v1.9.2 - 2026-07-23

- 语义模型收敛为 BGE Small（默认轻量）与 BGE Large（高精度）两档，移除自定义模型转换包与 GPU 运行时分发路径。
- 语义检索支持暂停、按完成图书续建，主窗口与阅读页的检索交互和结果展示得到优化。
- 修复全书页数计算与缓存，阅读页右上角显示当前页/总页数。

## v1.9.1 - 2026-07-20

- 首次提供 Apple Silicon（M 系列芯片）macOS 构建，并改进 macOS 启动与阅读器稳定性。
- 统一阅读器菜单、弹层与工具栏的状态管理，修复阅读模式切换后可能残留的界面状态。
- 改善阅读页图片、分页和搜索/索引的稳定性。
- 将设置窗口默认宽度由 720 px 收窄至 600 px。
- 恢复同步服务 HTTPS 证书并启用自动续期。

## v1.9.0 - 2026-07-15

- 改进搜索、存储治理、数据迁移与同步。
