# 鲲鹏阅读器 iOS / iPadOS 界面交接

> **先读跨端规则**：开始 Apple 端工作前，先阅读仓库根目录 `AGENTS.md`、`docs/coordination/CURRENT.md`、`docs/architecture/跨平台协作规则.md` 和 `contracts/README.md`。涉及同步实体或 API 时，`contracts/` 是唯一事实来源；不得以桌面 SQLite 表结构推断协议。

> 用途：供 iOS 与 iPadOS 端复刻当前桌面版交互。本文描述的是产品行为与界面层级；数据、同步、阅读内核可按移动端技术栈重新实现，不要求沿用桌面端实现。
>
> 桌面版当前实现参考：`ui/index.html`、`ui/reader.html`、`ui/reader-page-annotations.js`。

## 一、主页面：六个一级入口

主页面是书架。顶部工具栏从左到右的六个一级入口如下；窗口最右侧的最小化、最大化、关闭只属于桌面壳层，移动端不需要实现。

| 位置 | 按键 | 作用 | iPhone / iPad 建议 |
| --- | --- | --- | --- |
| 左上角 | **账号与同步**（头像） | 登录、注册、查看当前账号、手动同步、退出登录。同步阅读进度/续读位置、书签、高亮、批注、评分、标签、书单、生词本、阅读统计；不上传图书正文或原文件。 | iPhone 放导航栏左侧；iPad 可放侧边栏顶部或导航栏左侧。账号页用 sheet / navigation push。 |
| 右上角第 1 个 | **搜索书籍**（放大镜） | 展开书架搜索框，按书名、作者等元数据筛选本地书架；输入框内还可打开“全书架正文检索”。 | iPhone 点后进入独立搜索页，键盘自动聚焦；iPad 以内嵌搜索框或顶部 search field 展开。 |
| 搜索右侧 | **阅读统计**（柱状图） | 打开日/月/年/总阅读统计：热力图、阅读时长、字数、速度、读过图书，并显示当日读过图书的封面、阅读时长/字数、高亮和批注数。 | iPhone 用全屏页；iPad 用 sheet 或居中大卡片。 |
| 右上角第 2 个 | **漏斗筛选** | 打开排序、阅读状态、评分、标签、书单、视图布局和列数设置。已启用筛选时，漏斗有蓝色呼吸提示，并悬浮提示“已启用筛选”。底部显示“当前结果/全部图书”，例如 `36/798`。 | iPhone 用底部抽屉；iPad 用锚定在漏斗下方的 popover。不要因打开标签/书单选择器而丢失筛选页状态。 |
| 右上角第 3 个 | **常用设置**（齿轮） | 控制书架层设置：显示阅读进度、显示评分、显示书名、自动导入目录，以及进入语义索引、词典、动画设置。 | iPhone 用设置页；iPad 用 popover 或 sheet。动画总开关关闭时，入口名称显示为“讨厌动画”。 |
| 右上角第 4 个 | **更多菜单**（三点） | 包含：随机打开一本书、导入书籍、书库体检、笔记汇总、全选（批量删除）、关于。关于页还应提供“提交 Bug”“功能提议”两个反馈入口。 | iPhone 使用 overflow menu；iPad 使用菜单或 popover。菜单较窄，以文字为主，不为每项另做装饰图标。 |

### 1. 搜索入口的细节

放大镜先展开输入框，而不是立刻进入搜索结果。输入书名/作者时搜索书架；输入框旁的“全书架正文检索”开关用于切换为跨所有图书的全文检索。切换时要保留当前输入内容，不要因中文输入法组合文字而收起搜索框。

移动端建议：

- iPhone：进入独立搜索页，顶部放搜索框和“书架 / 全文”分段控制器。
- iPad：可在书架顶部展开搜索框；外接键盘 `⌘F` 聚焦它。
- 搜索结果渲染应增量/分页，不阻塞输入框和滚动。

### 2. 漏斗筛选的细节

筛选弹层分为三列（手机可改为分组纵向列表）：

- **排序**：书名、作者、导入时间、存储目录。
- **全部图书**：最近阅读、阅读时间、大小、阅读进度。
- **阅读过滤**：未读、正在阅读、已读，以及五颗星评分过滤。
- **标签**：点击“选择标签”进入多选器；按钮显示“全部”或“已选 N 项”，旁边 `×` 可清空。
- **书单**：点击“选择书单”进入多选器；同样支持清空。书单与标签的组合模式可切换为“匹配任一 / 匹配全部”。
- **布局/列数**：列表与宫格两种书架布局；宫格列数支持默认值、减、加。

标签和书单选择器点击“确定”后，应返回仍展开的漏斗页，而不是关闭筛选页；用户可继续调整其他条件。

### 3. 书架的基础手势

- 单击/单点封面：打开图书。
- 双击/双点封面：进入该书选中状态。
- 已选中一册后再单点其他图书：多选，用于批量操作。
- 右键（iPad 指针）或长按封面：打开图书整理菜单，可添加/移除标签、加入/移出书单、管理标签/书单。

## 二、阅读页：高亮菜单的定位与触发

阅读页支持滚动、整页、双页等排版模式。**高亮菜单是阅读页的文本选择工具栏**，不应与顶部导航栏混淆。

### 1. 两种触发状态

| 状态 | 触发 | 菜单内容差异 |
| --- | --- | --- |
| 新选中文字 | 用户拖动选中一段尚未高亮的正文后松手。 | 可执行“高亮”“书签”等新建操作。 |
| 已高亮文字 | 鼠标悬停/点击高亮文本；移动端对应轻点高亮。 | “高亮”替换为“取消高亮”；已存在的内容可重新查词、搜索、批注等。 |

桌面端只有“拖动选择”才自动弹出菜单，普通单击/双击不应误弹。移动端用系统文本选择手柄完成选区后再显示自定义菜单。

### 2. 菜单位置规则

1. 普通单行选区/高亮：菜单贴近当前指针或选区，优先显示在文字**下方**。
2. 同页多行高亮：菜单显示在**最后一行下方**，不要盖住中间文字。
3. 高亮跨越两页：菜单显示在前一页页末那部分高亮的**上方**，避免被下一页裁切。
4. 如果下方没有足够空间，则翻到高亮上方；若上方也不足，只能在安全边距内钳制。
5. 菜单与文字间距为 **6 pt（逻辑像素）**。
6. 水平位置优先以触点/指针为中心，再做左右防越界；不要只使用几个固定弹出点。
7. 滚动、翻页、切换章节、点击页面空白处后收起菜单。菜单本身可点击，不能因为离开文字瞬间消失。

移动端建议：iPhone 上以选区 rect 为锚点使用自定义气泡；iPad 鼠标/触控板模式沿用指针锚定。系统原生菜单若无法承载全部功能，可只保留“复制”，其余进入鲲鹏自定义工具栏。

## 三、阅读页：高亮菜单动作

### 1. 可配置动作清单

以下动作可以在“高亮菜单设置”中单独开关、排序。新选区菜单默认可出现全部 12 项；已高亮菜单不再显示“高亮”和“书签”，其中“高亮”会变成“取消高亮”。

| 动作 | 新选区 | 已高亮 | 行为 |
| --- | --- | --- | --- |
| web 搜索 | 是 | 是 | 用选中文字打开外部网页搜索。搜索引擎由设置页选择百度或谷歌。 |
| 词典 | 是 | 是 | 使用选中文字查询离线/已配置词典，并显示上下文释义。 |
| 翻译 | 是 | 是 | 调用用户已配置的翻译服务，显示原文与译文；凭据仅保存在本机。 |
| 复制 | 是 | 是 | 复制选中或高亮文本。 |
| 高亮 | 是 | 否 | 保存文字范围、所在章节、上下文与颜色。 |
| 取消高亮 | 否 | 是 | 删除该高亮；相关批注是否保留须有明确确认或按现有数据模型处理。 |
| 改错 | 是 | 是 | 基于选区创建校对/改错草稿。 |
| 书摘 | 是 | 是 | 把选区保存为书摘，并可进入书摘页面。 |
| 跨书搜索 | 是 | 是 | 用选中文字在本地书架正文中检索。 |
| 相似语义 | 是 | 是 | 使用本地语义索引查语义相近段落；语义索引未建立时给出可理解提示。 |
| 智读 | 是 | 是 | 打开“智读”阅读助手，并把选区作为问题上下文。只发送当前图书中已读的相关文字，不自动上传整本书。 |
| 批注 | 是 | 是 | 为选区创建高亮与批注，打开批注编辑界面。 |
| 书签 | 是 | 否 | 在当前位置创建书签，默认名称取选中文字前 40 个字符。 |

右端始终保留一个**齿轮**，用于打开高亮菜单设置；齿轮不是可隐藏动作。

### 2. 多彩高亮

开启“多彩高亮”后，菜单下方/末尾出现四个颜色按钮，用于新建高亮或修改当前高亮颜色：

- 黄色：默认；
- 绿色；
- 蓝色；
- 粉色。

颜色是高亮记录自身的属性，必须与高亮、批注和同步实体一起保存；关闭“多彩高亮”只隐藏调色按钮，不应抹掉已有颜色数据。

### 3. 智读的移动端呈现

点击“智读”后：

- iPad：优先在阅读页右侧打开可关闭的侧栏，正文在剩余宽度内重排；开关侧栏后应尽量保持原文字仍处于视口中。
- iPhone：使用底部抽屉或全屏页，不要把正文压缩到不可读。
- 侧栏内容：提问、总结、生成脑图、历史；输入框回车可提问，同时保留可点击的“↵ 回车提问”按钮。
- 历史保存本地；API 地址、模型和 Key 仅保存在本机。DeepSeek、OpenAI、Anthropic 及 OpenAI 兼容 API 都是可配置提供者。

## 四、高亮菜单设置页

该页从高亮菜单右端齿轮进入。其职责是决定“菜单长什么样、显示什么、按什么顺序显示”，不是全局阅读设置。

### 1. 顶部外观区

第一行：

- **显示**：`图文` / `文字` / `图标`。
  - 图文：图标加名称；
  - 文字：只显示名称；
  - 图标：只显示图标，但要有 VoiceOver 标签和长按说明。
- **多彩高亮**：开关。文字靠左，开关靠右；开关动画与其他高亮菜单设置的开关一致。

第二行：

- **布局**：`横排` / `九宫格`。
  - 横排：按用户定义的动作顺序横向排列，超出宽度时可水平滚动或智能换行；不要丢失按钮。
  - 九宫格：每行 3 个动作；桌面称“九宫格”，实际动作超过 9 个时继续向下排布。
- **大小**：`小` / `中` / `大`，影响按钮高度、字体、图标和间距，不改变动作语义。

### 2. 动作列表

外观区下方列出全部动作。每行包括：

- 动作名称；
- 显示/隐藏开关；
- 拖动手柄，用于改变菜单动作顺序；
- 仅 `web 搜索` 额外显示 `百度 / 谷歌` 分段选择。

动作的显示状态和排序是用户偏好，应跨设备同步还是仅本机保存可以后续决定；桌面版当前为本机偏好。移动端至少应在本机持久化。

### 3. 动画与无障碍

- 高亮菜单设置页的出现、动作拖动排序动画受“动画设置 > 高亮菜单设置页动画”控制。
- “减少动态效果”或全局关闭动画时：关闭列表位移动画、菜单淡入缩放与开关过渡；功能和排序仍完全可用。
- 所有仅图标按钮必须提供 VoiceOver 文本（如“词典”“高亮菜单设置”）。
- 拖动排序必须有非拖动替代：iPhone 提供“编辑/上移/下移”，iPad 键盘或辅助功能动作同样可排序。

## 五、iPhone / iPad 复刻优先级

### 第一优先级（必须一致）

1. 书架六入口的信息架构与筛选行为。
2. 选区、已有高亮的两种菜单状态及其数据写入。
3. 高亮、批注、书签、阅读进度、标签、书单的本地保存与同步模型。
4. 高亮菜单的 6 pt 间距、多行/跨页位置规则和不遮挡正文。
5. iPad 智读侧栏与 iPhone 智读抽屉的可用性。

### 第二优先级（体验完善）

1. 菜单图文/文字/图标、横排/九宫格、大小、多彩高亮和动作排序。
2. 语义搜索、跨书搜索、翻译、词典、改错、书摘的逐项接入与不可用状态。
3. 动画设置、键盘快捷键、指针悬停与无障碍排序。

## 六、验收场景

1. 长按一段未高亮正文，选择文字后出现自定义菜单；点“高亮”后文本着色且菜单关闭。
2. 轻点已有高亮，菜单出现“取消高亮”而不是“高亮”。
3. 选中跨两行的文字，菜单在最后一行下方；选中跨页文字，菜单在前一页页末高亮上方。
4. 开启多彩高亮，选择粉色，重新进入该高亮时颜色仍为粉色；关闭多彩后历史颜色不丢失。
5. 将动作切为“九宫格 + 图标 + 大”，所有已开启动作仍可点击，齿轮仍存在。
6. 在 iPad 打开/关闭智读侧栏，阅读位置应保持同一段文字附近；在 iPhone 打开智读不应把正文压得过窄。
7. 漏斗页选择两个标签和两个书单后，点击确定返回漏斗页；底部立即更新 `当前/全部` 图书数。
