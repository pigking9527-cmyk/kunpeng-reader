//! Compatibility adapter for Kunpeng Reader's narrow EPUB needs. It uses the
//! Apache-2.0 `rbook` parser for format validation and performs bounded
//! resource extraction for the existing reader protocol. This crate contains
//! no former `epub-rs` source.

pub mod doc;
