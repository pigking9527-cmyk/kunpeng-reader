# 开发记录 / CHANGELOG

鲲鹏阅读器（Kunpeng Reader）桌面测试版的功能、工程和发布准备记录。

## 测试版 1.0（2026-08-21，新增代码 364 行，删除代码 164 行，留存代码 200 行，代码总量 196,048 行，Windows / macOS / Linux）

测试版 1.0 将桌面产品收敛为 Windows、macOS 与 Linux 三端的公开源码测试基线，技术版本为 `1.0.0-beta.1`，同步协议开发基线为 5。本版不描述或发布移动端。

> 统计口径：新增、删除与留存值来自测试版公开快照与 2026-08-21 `main` 的 Git 跟踪文件差异；代码总量为当前 Git 跟踪的 Rust、TypeScript、JavaScript、CSS、HTML、PowerShell 与 Shell 文件行数，排除 Markdown、锁文件、依赖缓存和构建产物。

### 🧱 桌面工程与稳定性

- 保持 `ui/` 为桌面端唯一可视实现；TypeScript/Vite 仅承载规则、控制器、类型化 Tauri 边界和阅读协议构建，不引入第二套页面或界面切换。
- 保留 Rust + Tauri 2 的本地能力与命令边界；阅读、数据、同步和系统操作继续由 Rust 侧承载，避免把高耗时工作放入页面运行时。
- 常规 CI 会自动取消同一分支的过时检查；三端安装包工作流只在版本标签或手动触发时构建，减少日常反馈时间。

### 📦 三端测试包与校验

- Windows x64 已生成单文件便携版与 NSIS 安装包；ONNX Runtime 静态链接，不再要求用户同时下载 DLL。
- macOS Apple Silicon 已生成 App ZIP 与 DMG；Linux x86_64 已生成 AppImage 与 deb；三端均附 SHA-256 清单。
- 三个平台安装包工作流改为仅在版本标签或手动触发时构建，日常 `main` 提交只运行必要检查，从而缩短反馈时间。
- macOS、Linux、Windows 产物均已通过对应清单的 SHA-256 核验；当前仍是未完成商业签名与公证的测试包。

### 🔐 同步、许可与发布边界

- 桌面同步客户端采用协议 v5，使用 `/v1/auth/*` 与 `/v1/sync/*`；公开构建不内置真实同步服务地址。
- 图书正文、原文件、封面原图、模型、语义索引、本机路径与默认 API Key 不进入同步实体；仅用户主动开启时才同步加密密钥包。
- 项目公开源码采用 source-available 许可。新增公开二进制发行仍受第一方资产、第三方许可、历史资产处置和平台签名门禁约束。

### 📝 文档与仓库整理

- 移除 `更新.md`；版本变更统一由本文件、`CHANGELOG.md` 与 GitHub Release 说明承载，避免同一版本存在多份可能不一致的更新文本。
- 开发文档改为当前桌面测试版事实：删除过时的全量 React UI、移动端发布和旧发行流程描述，补齐唯一 UI、协议 v5、三端产物、签名状态和发布门禁。
- 公开仓库地址、测试版版本号和三端支持范围已在 README 与“关于”页保持一致。

### 📌 后续事项

- 完成第一方资产与第三方依赖的许可证复核后，才可公开发布二进制资产。
- 完成 Windows 代码签名、macOS Developer ID 签名与公证、Linux 分发签名和干净环境安装验收。
- 在受保护 PostgreSQL 环境完成同步服务 migration、认证/同步端到端、备份恢复与切换/回滚演练后，再申请生产同步服务部署。

*最后更新：2026-08-21*
