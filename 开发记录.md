# 开发记录 / CHANGELOG

鲲鹏阅读器（Kunpeng Reader）开发期间对功能的增删与调整记录。

---

## v1.15.0（2026-08-10，Windows / macOS / Linux）

v1.15.0 是桌面端的**全新 UI 版本**。Windows、macOS 与 Linux 统一更新到 v1.15.0；Android 保持 v0.3.0。同步协议升级为 `syncProtocolVersion` 3，以支持逐项同步选择、逐条 AI 历史与独立书单元数据。

### 🎨 全新 UI 与界面重构

- 主窗口工具栏、设置中心、账户中心、图书信息、书架筛选、阅读偏好、大模型与翻译 API、更新提示与通用弹窗全部重做，统一为更清晰的分区、卡片和侧栏导航。
- 菜单栏支持调整图标大小、显示或隐藏、拖动排序及避让动画；账户入口纳入同一套配置，设置入口始终可见。
- 菜单栏显示设置随软件设置同步：图标大小、显示/隐藏、图标/文字形态和顺序会在账户设备间交换；“设置”本身始终显示。
- 设置页改为“基础、菜单栏、书架与导入、阅读与交互、智能功能、数据与系统”分类侧栏，支持收起侧栏；删除重复标题和关闭控件。所有子页均在设置框架内打开，返回不会隐藏设置页。
- 阅读偏好提供精简/经典两种设置布局；默认主题可分别修改正文、链接、选中文本和脚注的颜色。全界面关闭浏览器原生拖拽和文字选中，避免书架、菜单和工具区误操作。
- 图书信息页改为封面、基础信息、评分、标签与书单、AI 分类、操作和简介的分区布局；书架筛选/布局面板改为更紧凑的排序、状态、评分、标签、收藏夹与列数控制。
- 阅读统计页同步重做：日、月、年、总览和阅读贡献热力图采用统一布局；设置面板可切换时间/字数、柱状图/折线图、热力图配色和统计项目，并记住选择。
- 账户中心改为概览、同步、账户安全、数据与隐私四页。概览保持紧凑，另外三页使用同一较高内容区，完整显示内容并在小屏时独立滚动。

### ✨ 新增功能

- 新增书单推荐：先由本机语义检索选出有限候选，再交由用户配置的大模型精选推荐；模型不可推荐候选外图书，也不会获得整座书库正文。书单支持简介、封面书、顺序和逐书评语。
- 阅读页正文点击区域支持新建、命名、保存、切换和删除多套操作方案。
- 单页和双页共用同一套阅读偏好；仅在双页分页时额外提供 0–120 px 的中缝调节，单页和滚动阅读不受影响。
- 跳转回退图标支持更细的像素级大小调节，并与位置和显示条件一并保存、同步。
- 新增“恢复跳转前位置”手势，可连续回退目录、书内链接、脚注和底部进度条跳转；所有已关闭阅读页均可重新打开。
- 大模型配置支持保存多个模型，并分别分配给智读、书库问答、其它分类用途；翻译 API 继续独立配置和切换。
- 书籍分类成为“智能功能”独立入口；“使用大模型分类的标签”作为其子页。
- 更新提示改为居中更新页，显示当前版本、目标版本和更新说明；启动时优先检查可用更新。

### 🔐 同步、账户与数据

- 同步内容改为逐项选择：进度/时间线、笔记与标签/书单、生词、统计、软件设置、主题背景、分类标签、AI 配置、AI 历史和加密密钥均可独立开启。
- `ai_reader_history_entry_v2` 将智读与书库问答历史拆成逐条实体，避免新增一条记录时重复上传整个历史；`booklist_v1` 独立保存书单元数据。
- 账户概览显示总存储额度、每日同步额度、下次重置时间、服务连通状态和最近同步结果。邮箱绑定/换绑、密码修改、数据清理和账号删除流程同步重做。
- 图书正文、原文件、原始封面、语义模型、语义索引和本机路径仍不上传；API Key 与翻译凭据默认保留本机，仅在设置同步密码后以加密密文同步。

### 🐛 修复与稳定性

- 修复 AI 历史、书籍状态等轻数据可能整包重复推送的问题，改为更细粒度实体、去重和额度统计。
- 修复本机与服务端确认基线不一致后停留在“自动同步失败”的问题；结果明确区分已接受、重复/冲突、拒绝和拉取，未结算实体会保留待确认状态。
- 修复阅读页手势提示滞后、手势相互误命中、跳转回退记录与水平滚动记录不一致，以及只能恢复手势关闭页面的问题。
- 修复页底明明可完整容纳一行正文，却因段后空隙被提前换页的问题；只收紧恰好跨栏的段后间距，不影响普通段落、标题、脚注和滚动阅读。
- 修复更新页通过手势关闭时退出整个软件、关闭后再次启动不再提示的问题。
- 修复菜单栏拖动不生效、无避让动画、账户入口未纳入排序、设置侧栏图标缺失和资讯重复开关等问题。
- 修复账户同步页无法打开、非概览页高度不足和顶部重复操作；修复单模型时取消全部功能分配会让智读/书库问答没有可用模型。
- 修复阅读统计设置按钮状态不同步、指标/图形/热力图配色切换后偏好不保存的问题。

### 📦 发布

- Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`。
- macOS Apple Silicon：App ZIP、DMG 和 `SHA256SUMS-macOS.txt`。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`。
- Android 保持 v0.3.0，不随本次桌面 UI 版本更新。

---

## v1.14.1（2026-08-09，Windows-only）

v1.14.1 是仅面向 Windows 的书库问答、语义检索、鼠标手势、阅读分页图片和设置恢复更新；macOS 与 Linux 保持 v1.14.0，Android 保持 v0.3.0，`syncProtocolVersion` 仍为 1。

### ✨ 新增与改进

- 书库问答沿用短、中、长三档作答长度；中、长档分别使用约 800、1200 字连续来源窗口，在控制生成长度的同时扩展可核查上下文。
- 新增可选 BGE-M3 长上下文二阶段重排：先以小分片快速召回，再合并命中点附近连续正文，对有限候选重排；未准备好模型或索引时显示具体设置路径。
- 语义检索区分标准、高精度和实验性 BGE-M3 混合模式；重排模型支持独立下载、删除、自动加载与状态恢复。
- 鼠标手势升级为多手势管理器，支持新建、编辑、启停、删除、搜索、动作选择、全局/独立精度及提示文字的大小、颜色、透明度和位置。
- 手势识别改为以归一化方向与形状为主，降低末段绝对长度对识别结果的影响；完成目标方向后无需为补足距离继续拖动。
- 跳转回退图标支持 30–160 px 连续大小和千分比屏幕坐标，并与资讯来源、书库问答偏好一起纳入增量设置同步。
- EPUB 分页提供“移到下一页完整显示”和“允许跨页连续显示”两种图片策略；切换时以可见文字锚点恢复位置，避免设置本身改变当前文字页。

### 🐛 修复 / 稳定性

- 修复跳转回退位置设置反复回到左上角、预览上下边界无法贴边、预览和阅读页位置不一致，以及图标样式被替换的问题。
- 改进分页图片的候选探测、前页上部预览和后页裁剪；减少图片浮在文字上、图注与正文间距过大、较长图片漏掉上部和翻到图片下部时跳动。
- 问题记录新增分页图片来源页、候选页、几何尺寸、可用高度、预览高度及探测结果，便于区分没有重算与 Chromium 多栏几何异常。
- 恢复点新增经过白名单和容量限制的 WebView 设置快照，不复制可能包含缓存、凭据或正文的完整 WebView 配置目录。
- 统一主窗口与阅读页对原生拖拽、图片拖动和文字选择的保护，进一步收紧单实例启动和恢复后的窗口行为。

### 🔐 同步与兼容

- `app_settings_v1` 改为字段级增量合并，新增回退图标像素大小/位置、资讯来源/贴吧来源和书库问答长度、历史同步模式、字号及长上下文开关。
- Schema、fixtures、ADR、兼容说明和服务端兼容测试同步更新；旧客户端未知字段继续保留，不升级同步协议版本。

### 📦 发布

- Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；当前仍未进行 Authenticode 签名。
- macOS、Linux 与 Android 本次不发布新包。

---

## v1.14.0（2026-08-08）

v1.14.0 在 v1.13.1 的阅读稳定性更新之上，集中升级阅读偏好、书内导航、智读历史、启动增强、GPU 组件管理、跨端设置同步和云端数据恢复。Windows、macOS 与 Linux 同步发布，Android 保持 v0.3.0，`syncProtocolVersion` 仍为 1。

### ✨ 新增功能

- 新增完整“阅读偏好”页面，设置分为“总体”和“本书独立”两个范围；外观、分页、工具栏和高级设置集中管理。
- 工具栏支持分别显示或隐藏简繁、目录、上一章/下一章、朗读、批注、生词本以及页码与章节进度，切换后即时生效。
- 新增独立书内跳转回退：目录、脚注和书内链接使用独立历史栈，正文右侧显示无背景半透明回退图标；支持按经过时间或翻过页数自动隐藏，并提供 1–10 级图标大小。
- 底部整书进度条保留另一套完整跳转历史，可连续逐级返回多次点击或拖动前的位置，不与目录和链接跳转串台。
- 新增启动增强及 Windows 开机自启：关闭主窗口后可保留进程以快速恢复，并可独立决定是否继续语义索引等高消耗后台任务；关闭启动增强仍为完全退出。
- 智读侧栏新增三档宽度，保持覆盖正文而不触发重分页；脑图支持收起并返回历史。
- 智读历史新增“最近 100 条自动同步”和“手动选择云端记录”两种模式；提问、总结和脑图显示实际云端状态，跨全部图书共用账号级额度。
- 自定义阅读配色、排序和背景图支持桌面端同步；背景图改为独立二进制资产引用，而不是把 Base64 原图写入主题实体或阅读页。
- Windows GPU 设置支持检测显卡、驱动、CUDA Provider 和上次下载状态，并可从固定发布资产按需安装大型 CUDA 运行组件；失败时自动回退 CPU。
- 阅读页与资讯页共用可配置轨迹手势，可用于页面返回和关闭阅读页。

### 🎨 阅读与界面改进

- 图片背景主题卡片直接显示对应缩略图；自定义主题过多时仍可连续滚动和排序。
- 外观与配色区域隐藏内部原生滚动条但保留滚动操作；阅读偏好主滚动条改为无白色轨道、无两端三角按钮的浮动滚轴。
- 修复主题列表滚动到边界后必须移动鼠标才能继续滚动外层页面的问题。
- “高级”页移除解释性说明；跳转回退设置展开后，点击设置框和齿轮以外的位置自动收起。
- 简繁开关关闭显示“简”、开启显示“繁”；双页和滚动模式文字按当前状态显示可切换到的模式。
- 底部进度圆球拖动改为本地预览，松手后等待目标位置确认，避免正文回传旧进度造成圆球弹动。
- 软件动画开关直接控制翻页动画，不再被 WebView 继承的系统减少动态效果设置二次关闭。
- 资讯正文图片提取与缓存继续改进，补充虎扑等来源的正文首图。
- 自动导入增加路径、完整内容 SHA-256 和采样指纹三层去重；同内容换路径时更新原图书条目并保留阅读数据。

### 🚀 性能与平台改进

- 启动增强记录冷启动、热恢复、窗口显示和关闭耗时，便于分别衡量真实进程启动与驻留进程恢复速度。
- GPU 检测改为异步状态刷新，避免阻塞整个设置页；外部检测进程使用无窗口方式，不再闪出命令行。
- GPU 状态区和按钮区预留稳定空间，重新检测时不再因说明文字变长或变短导致设置页面高度跳动。
- Linux 主窗口和阅读窗口增加八方向原生边框拉伸处理。
- 更新提示按当前平台和实际发布资产判断；没有 Linux/macOS 对应新包时不再展示 Windows 版本更新提示。
- 启动增强、更新提示、阅读偏好和读后推荐补齐十种界面语言及动态状态文案。

### 🐛 修复 / 稳定性

- 修复词义提示、可能义项、结合当前句子和上位词四个词典增强开关无法保持关闭状态。
- 修复阅读偏好中的简繁等工具栏开关误写入单本外观、设置隐藏后控件仍显示的问题。
- 修复阅读偏好入口文字被齿轮样式压缩成逐字竖排，以及英文等长文案在窄列中重叠的问题。
- 修复启动增强状态下右上角关闭按钮偶发无效、窗口不能再次唤醒，以及切换开机自启时闪出命令行的问题。
- 修复 EPUB 空 spine 或首章无正文时停在空白页，自动跳到第一个有内容的页面。
- 加强跨章切换、双页续读锚点和关闭前稳定位置保存，减少新正文定位前闪现及关闭重开后的阅读位置偏移。
- 加强阅读窗口 closing 状态、书架首击聚焦和打开重试，减少关闭阅读页后第一次点击图书无响应的问题。
- 修复进度条或回退按钮每操作一次就立即消失，以及目录/脚注/书内链接跳转后缺少正文右侧回退入口的问题。
- 修复 GPU 已下载组件后仍要求再次安装、检测过程卡顿、缺少依赖提示过长和设置布局跳动的问题；缺少运行库时统一显示“缺少 CUDA 组件”。
- 修复读后推荐阈值“超过/低于”语义不一致的问题。

### 🔐 同步、恢复与数据安全

- 用户手工标签和收藏夹拆分为 `user_book_tags_v1`、`book_collections_v1` 独立实体；旧客户端仍可同步阅读状态，但不能再用旧状态中的空数组清除新版分类关系。
- 服务端为实际接受的实体变化保存 zlib 压缩完整版本，滚动保留 90 天并保留窗口前锚点；恢复后提升 `data_generation` 并撤销旧令牌，避免旧设备重新上传错误状态。
- 增加经过一致性与 SHA-256 校验的整库 gzip 快照，按 7 个日备份、4 个周备份和 12 个月备份轮换；异地对象存储副本仍需私密运维配置。
- 自定义主题最多同步 10 个；背景图资产支持内容哈希去重、分块上传、断点续传、Range 下载、大小限制和完整性校验，不上传图书正文、本机路径或原始书文件。
- 新增 `app_settings_v1/default`，在 Windows、Linux、macOS 同步跳转回退开关、隐藏规则和图标大小，并保留旧客户端不认识的字段。
- 智读与书库问答本机活跃历史不设上限；云端分别最多保留 100 条活跃记录和 200 条删除墓碑，删除语义可防止旧设备复活记录。
- 新账号完成邮箱验证后才允许同步；服务端增加账号存储和每日写入预算、共享限流、封禁审计与待告警记录。外部告警通知渠道仍需在私密运维环境验收。

### 📦 发布

- Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；当前仍未进行 Authenticode 签名。
- macOS Apple Silicon：DMG、App ZIP 和 `SHA256SUMS-macOS.txt`；当前为临时签名，尚未经过 Developer ID 签名与公证。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`；兼容基线为 Ubuntu 24.04 / glibc 2.38+。
- Android：继续使用 v0.3.0 Profile 测试包，本次不发布新 APK。

---

## v1.13.1（2026-08-07，Windows-only）

v1.13.1 是仅面向 Windows 的阅读稳定性、封面加载和诊断更新；macOS 与 Linux 保持 v1.13.0，Android 保持 v0.3.0，`syncProtocolVersion` 仍为 1。

### ✨ 改进

- 阅读正文框架优先导航，大型 EPUB/PDF 目录在空闲帧中分批创建，降低打开大目录图书时的首屏阻塞。
- 书架不再通过 IntersectionObserver 按需写入封面 URL；所有书卡直接获得封面地址，首屏使用高优先级同步解码，其余使用浏览器原生 lazy 调度。
- 问题记录汇总阅读窗口创建、图书信息读取、首屏正文、窗口销毁和最近 12 次软件启动耗时，提供最小、平均、最大和最近样本。
- AI 历史来源恢复预览与点击跳转；账户和同步子页使用遮罩并支持点击外部关闭。

### 🐛 修复 / 稳定性

- 关闭阅读页后保持 closing 标记直到旧窗口从 Tauri 注册表注销，移除销毁后的延迟聚焦，减少首次点击图书无响应。
- 关闭前向正文页请求稳定位置快照并等待进度保存；初次恢复消息不再覆盖已保存位置，修复重开后阅读进度向前偏移。
- 双页跨章时等待样式、图片和最终分栏稳定，再切换到新章节；双页恢复锚点统一落到左页。
- 修复全书架正文检索开关已开启但启动时仍显示关闭状态提示的问题。

### 🔐 同步 / 诊断

- 问题记录继续保持脱敏和容量边界，不记录图书正文、选区、文件路径、账号数据、URL 或 API 凭据。
- 智读与书库问答云端历史上限分别调整为 100 条，删除墓碑各最多 200 条；对应两类同步实体允许 4 MiB，其他实体仍保持 1 MiB。

### 📦 发布

- 仅发布 Windows x64 NSIS 安装包与 `SHA256SUMS.txt`，当前仍未进行 Authenticode 签名。
- 不新建 macOS、Linux 或 Android 资产；它们继续使用 v1.13.0 / v0.3.0 发布包。

---

## v1.13.0（2026-08-07）

v1.13.0 是 Windows、macOS 与 Linux 同步发布的语义检索、国际化和可诊断性更新；Android 保持 v0.3.0，`syncProtocolVersion` 仍为 1。

### ✨ 新增 / 改进

- 语义检索新增 BGE-M3 与 Multilingual-E5-Small，可结合全文、稠密向量、重排和 BGE-M3 长文精读；模型与索引状态改为非阻塞读取，续建仅在用户明确操作后开始。
- 完成十种界面语言在设置子页、书库问答、资讯、阅读统计、排序过滤、登录及阅读页菜单中的覆盖；Microsoft TTS 按朗读文本自动选取对应语言声音。
- “提交 Bug”可附加最近两分钟的脱敏问题记录，记录阅读器和主窗口操作；支持直接插入反馈框或单独保存到桌面，并保留截图补充。
- 资讯管理独立为页面，右键自定义轨迹手势可关闭资讯子页或返回书架；手势支持开关、编辑面板和低/中/高匹配精度。
- 书架封面首屏加载增加“固定数量 / 记住上次可见数量”策略，取消浏览器原生拖拽和多封面文字高亮。

### 🔐 契约 / 兼容

- 新增 ADR-0008 至 ADR-0015、反馈 JSON 附件、阅读位置历史、核心数据迁移包、毫秒时钟、资讯缓存、诊断快照、任务中心和阅读问题记录相关契约及 fixtures。
- 服务端接受受限的反馈 JSON 附件并返回明确确认；核心数据迁移包只包含可同步实体，不包含书籍文件、路径、模型、索引、密钥或私密阅读正文。

### 📦 发布

- Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`。
- macOS Apple Silicon：DMG、App ZIP 和 `SHA256SUMS-macOS.txt`。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`。

---

## v1.12.0（2026-08-06）

v1.12.0 是 Windows、macOS 与 Linux 同步发布的桌面体验和数据治理版本；Android 保持 v0.3.0。同步协议仍为 v1，并新增账号数据生命周期契约。

### ✨ 新增 / 改进

- 书库问答历史支持横排 / 方格切换，卡片改为显示回答的核心首句；输入区与历史记录一起滚动，扩大长记录浏览空间。作答和字号选项统一归入“设置”，配置齐全时不再显示冗余状态提示。
- 重做资讯工具栏和布局状态：分类完整展示，管理来源、刷新、布局与排序集中在同一行；返回资讯流后保持横排 / 方格和混合 / 按来源选择一致。
- 资讯封面只使用正文真实图片，不再用来源统一图标冒充内容图；稀土掘金、知乎热榜等无可靠封面的来源保持纯文本。少数派正文恢复内嵌显示与返回按钮，微博、酷安等受登录或 App 限制的页面改用安全说明和外部打开入口。
- 新增资讯正文提取、图片代理与本地缓存改进，打开资讯时优先使用提取后的标题、正文和图片，后台刷新不再干扰当前阅读。
- 新增阅读结束推荐、书籍信息整理、多个目录自动导入和更细的阅读布局、动画、统计设置。
- 设置新增“数据与隐私”：可清除本设备数据、清除本设备及云端数据，或永久删除账号。危险操作采用二次确认，并明确不删除用户自行导入的原始书文件。

### 🔐 同步 / 数据安全

- 新增 ADR-0006、账号数据生命周期契约和兼容 fixture。云端清除会原子删除同步实体与私密密钥包、递增 `data_generation` 并撤销所有登录令牌。
- 客户端同步请求携带数据世代；旧设备、旧令牌和旧游标不能在清除后重新上传历史数据。永久删除账号会清除账号及其全部服务端从属数据。
- 清除本机数据覆盖数据库、配置、缓存和 WebView 浏览数据；检测到原始书文件位于应用数据目录时会拒绝清除并提示用户先迁移，避免误删原书。

### 🐛 修复 / 稳定性

- 修复空书架误显示全书问答、书库问答页滚动时标题和筛选栏一起漂移，以及历史输入区占用固定空间的问题。
- 修复资讯首次进入无图、从正文返回后布局与图标状态不一致、部分来源统一占位图、少数派白屏，以及微博 / 酷安跳转登录或 App 下载页的问题。
- 修复阅读页末、书架交互、导入提示、语言切换和动画设置中的多项状态一致性问题。

### 📦 发布

- Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；当前仍未进行 Authenticode 签名。
- macOS Apple Silicon：DMG、App ZIP 和 `SHA256SUMS-macOS.txt`；当前为临时签名，尚未经过 Developer ID 签名与公证。
- Linux x86_64：AppImage、deb 和 `SHA256SUMS-Linux.txt`；兼容基线仍为 Ubuntu 24.04 / glibc 2.38+。
- 三个桌面平台从同一 v1.12.0 提交构建；Android 继续使用 v0.3.0 Profile 测试包。

---

## v1.11.2（2026-08-05，Windows-only）

v1.11.2 是仅面向 Windows 的桌面体验更新；macOS 与 Linux 保持 v1.11.0，Android 保持 v0.3.0。同步协议仍为 v1。

### ✨ 新增 / 改进

- 重做主窗口内的「资讯」：来源由用户按类别选择和管理，最多同时启用 24 个；支持横排 / 方格和混合 / 按来源排序，新增 3DM 与游民星空新闻来源。
- 资讯流与正文均使用阅读器内浏览器渲染。点击卡片进入独立的干净正文页，只展示标题、正文、正文图片、来源和发布时间；站点导航、广告、评论、推荐和登录浮层会被剔除。
- 资讯卡片使用按视口进入时加载的封面；补充多站图片解析、防盗链与无图卡片补位，方格布局改为瀑布流以减少留白和首屏重排。
- 新增「资讯设置」中的后台预取开关：应用空闲后每 5 分钟刷新已启用来源，最多 6 路并发；先展示磁盘缓存，再静默更新，网络失败仍保留旧内容。缓存不进入同步、备份或书库。
- 书库问答支持 Enter 提问、Shift+Enter 换行；右键菜单精简为复制、剪切、粘贴，问答历史详情直接显示正文内容。

### 🐛 修复 / 稳定性

- 修复书库问答语义检索与部分 OpenAI 兼容接口响应无法解析的问题。
- 修复退出时被同步任务等待导致的偶发延迟，以及非沉浸阅读模式中工具栏、底部进度条被中部点击错误收起或无法再次隐藏的问题。
- 资讯正文页只保留一个返回资讯流按钮，避免网页原生控件与阅读器导航同时出现。

### 📦 发布

- 仅发布 Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；当前仍未进行 Authenticode 签名。
- 不新建 macOS、Linux 或 Android 资产；它们继续使用此前的 v1.11.0 / v0.3.0 发布包。

---

## v1.11.1（2026-08-05，Windows-only）

v1.11.1 是仅面向 Windows 的桌面补丁版本；macOS 与 Linux 保持 v1.11.0，Android 保持 v0.3.0。同步协议仍为 v1。

### ✨ 新增 / 改进

- 智读将选中文本、邻近正文、当前章节开篇和已读范围检索结果分层取证，再由模型筛选和重排；回答完成后执行引用自检，并展示检索阶段和材料类型。
- 新增“大模型与翻译 API”统一设置，可保存并切换多个 DeepSeek、OpenAI、Claude 或 OpenAI 兼容模型；书库问答、阅读页智读共用当前模型，翻译下拉框只显示已经配置的服务。
- 书库问答新增模型快速切换、字号记忆和记录删除；历史来源正文只保存在本机，旧记录会按内容身份/书名与章节恢复，跨设备仍只同步脱敏来源索引。
- 智读和书库问答历史为每条记录分配稳定 ID；删除使用无正文、无问答内容的条目墓碑同步，避免旧设备缓存复活已删记录。

### 🐛 修复 / 稳定性

- 智读侧栏改为覆盖层；开关时以当前视口真实首行作为阅读锚点，避免压缩正文列宽、丢失段落上下文或改变阅读位置。
- 修复双击打开图书时首击短暂选中、资讯入口无法切回书架等交互问题。

### 📦 发布

- 仅发布 Windows x64：单文件便携版、NSIS 安装包和 `SHA256SUMS.txt`；当前仍未进行 Authenticode 签名。
- 不新建 macOS、Linux 或 Android 资产；它们继续使用此前的 v1.11.0 / v0.3.0 发布包。

---

## v1.11.0（2026-08-05）

v1.11.0 聚焦书库级 RAG 质量和跨端发布：桌面端新增可追溯的书库问答、单书深度检索、可续建模型书目分类和问答历史同步；Android 独立升级为 v0.3.0。

### ✨ 新增 / 改进

- 新增主窗口内的“书库问答”：默认全书库精检，返回前 20 本相关书、每本 1 段证据；标签、书单和人工选书可以限定范围。
- 单书提问识别书名后优先召回目录与正文，执行同书多段检索，并在生成后验证每个来源是否支撑对应结论。
- 书籍分类任务由大模型生成时代、体裁、篇幅、主题等结构化标签，支持中断后续建；模型标签与用户手工标签并存、始终参与同步。
- 图书信息页展示模型标签；“使用大模型分类的标签”开关只控制本机书库问答/筛选是否采用它，不影响同步或问答本身的标签上下文。
- 书库问答自动保存、可查看历史；开启“同步智读历史”时同步脱敏后的书库问答记录和来源索引。
- 新增资讯入口；调整书库问答结果为更宽的阅读布局，来源改用可悬浮查看原文的脚注。

### 🐛 修复 / 稳定性

- 修复书库问答把单书问题错误扩展到周边材料的问题；未检出正文或目录证据时会明确说明，不再虚构书中结论。
- 修复分类任务中长书名把进度行撑高、恢复后从头计数或不显示进度的问题。
- 修复右键标签/收藏夹整理弹层在滚动书架时跑到菜单栏的问题。

### 📦 发布

- Windows：NSIS 安装包与单文件便携版；当前尚无 Authenticode 签名。
- macOS Apple Silicon：DMG 与 App ZIP；仍为临时签名，尚未经过 Apple Developer ID 公证。
- Linux x86_64：AppImage 与 deb；兼容基线为 Ubuntu 24.04 / glibc 2.38+，当前未签名。
- Android v0.3.0：Full Profile 测试 APK；当前没有生产 keystore，不作为 Play 正式签名包。
- 四个平台分别提供独立 SHA-256 校验清单。

---

## v1.10.0（2026-07-29）

v1.10.0 是跨四端发布、私密同步和阅读位置稳定性版本：桌面端首次一并提供 Windows、macOS 与 Linux 安装资产，Android 客户端独立升级为 v0.2.0。

### ✨ 新增 / 改进

- 新增私密配置端到端加密同步：普通智读/翻译设置和问答历史可按需同步；API Key 只有在用户设置独立同步密码后才会使用 PBKDF2 + AES-256-GCM 加密上传。
- 新增账号邮箱绑定/换绑、修改密码和找回密码流程；密钥包采用世代编号，撤销后旧设备不能把旧密文重新写回。
- 底部全书进度条支持点击/拖动跳转，并可返回拖动前位置；可见性由 ReaderShell 与顶部工具栏统一管理。
- 桌面和阅读页动画设置合并到同一设置入口；书架语义搜索框展开时提前预热模型。
- 新版检查增加服务器发布元数据回退；Linux 工作流在 Ubuntu 24.04 x86_64 构建 AppImage/deb，检查动态链接并执行 15 秒启动烟测。
- Android v0.2.0 增加离线中英文词典、生词本、智读、账号安全、加密密钥同步、关于页和更新检查。
- Android EPUB 后台安全解析并按章缓存；书架列表延后读取高亮/书签大字段，打开章节后预热前后章节。

### 🐛 修复 / 稳定性

- 修复滚动/整页模式互切时回到章节首页的问题，使用源文本锚点恢复当前可见内容。
- 修复单页/双页模式互切后原第一行跑到栏中部或章首的问题；临时 DOM 拆分会无损回滚，续接段不会重复产生首行缩进。
- 修复顶部阅读工具栏与底部进度条状态分离造成的交替显示。
- Android 降低阅读滚动惯性，跨章只在边界手势触发并从新章节顶部进入；补充 Zip Slip 和异常解压大小防护。

### 📦 发布

- Windows：NSIS 安装包与单文件便携版；当前尚无 Authenticode 签名。
- macOS Apple Silicon：DMG 与 App ZIP；仍为临时签名，尚未经过 Apple Developer ID 公证。
- Linux x86_64：AppImage 与 deb；兼容基线为 Ubuntu 24.04 / glibc 2.38+，当前未签名。
- Android v0.2.0：Full Profile 测试 APK；当前没有生产 keystore，不作为 Play 正式签名包。
- 四个平台分别提供独立 SHA-256 校验清单。

---

## v1.9.5（2026-07-27）

v1.9.5 是跨端发布与阅读页边界稳定性版本：首次将 Windows、macOS Apple Silicon 与 Android APK 一并发布，并补齐跨端协作契约。

### ✨ 新增 / 改进

- 多选书架新增“标签”和“加入收藏书单”，可批量追加已有或新建分类；不会覆盖原有整理结果，重复加入会逐书提示。
- Android 首版实现账号、搜索、统计、筛选与布局、设置、更多六个书架入口；支持 EPUB/TXT 系统导入、Download 自动导入、批量导入、解析缓存与封面阅读进度。
- Android 阅读页提供滚动/整页左右翻页、点击翻页以及高亮、批注、书签、词典、翻译、Web 搜索等自定义划词菜单；兼容 EPUB 图文、章节头图和脚注开关。
- 建立 `contracts/`、同步实体样本、ADR 和跨端协作状态文档，桌面、Android、iOS/iPad 后续以统一产品语义和同步协议推进。

### 🐛 修复 / 稳定性

- 高亮菜单在页末切换横排/九宫格时会基于新菜单高度重新定位，确保菜单完整可见、可点击且不压住选区。
- macOS 完善整页与滚动分页的文本边界/完整行测量，修复滚动裁切、页首页末半行和图片分页边界问题；测量代码拆分为独立模块并通过治理检查。

### 📦 发布

- Windows：NSIS 安装包与单文件便携版。
- macOS Apple Silicon：DMG 与 App ZIP；仍为临时签名，尚未经过 Apple Developer ID 公证。
- Android：Profile APK；首版已包含书架、导入、阅读和划词工具，移动端源码独立维护，按本仓库 `contracts/` 与同步协议兼容。

---

## v1.9.4（2026-07-26）

v1.9.4 聚焦书架整理、用户反馈与阅读位置连续性；发布 Windows 与 macOS Apple Silicon 两套安装资产。

### ✨ 新增 / 改进

- 收藏夹升级为可独立浏览的书单：支持书单简介、封面、拖动排序和图书信息回显；标签与书单继续随轻数据同步。
- 书架卡片调整为单击打开、双击选中；已有选中项后单击可直接增减多选，便于批量操作。
- 新增 Bug / 功能提议反馈入口，支持图文反馈；客户端会先把图片压缩至受限大小，服务端再校验并转发邮件。
- 新增三款可按需下载、离线使用的开源中文字体；常用设置提供集中动画开关。

### 🐛 修复 / 稳定性

- 修复单双页、滚动模式和智读侧栏互切时的阅读位置恢复，避免正文跳回章节开头。
- 修复阅读统计时间线、语义索引完成状态、书架标签/书单筛选与书单排序等交互问题。
- 移除项目内代码统计页面，减少无关维护入口。

### 📦 发布

- Windows：NSIS 安装包与单文件便携版。
- macOS Apple Silicon：DMG 与 App ZIP；仍为临时签名，尚未经过 Apple Developer ID 公证。

---

## v1.9.3（2026-07-25）

v1.9.3 继续扩展书架整理、辅助阅读与阅读页交互，并以 Windows 与 macOS Apple Silicon 两套安装资产发布。

### ✨ 新增 / 改进

- 图书模型新增标签与收藏夹；书架右键菜单可为单本书添加/移除分类，也可全局重命名或删除，漏斗面板以多选弹层过滤。
- 标签与收藏夹进入图书同步实体 v3；旧数据默认使用空集合，不影响现有书架。
- 阅读页划词菜单新增「智读」入口，右侧栏只使用当前书已经读到的内容，支持提问、总结、脑图和本机问答历史。
- 智读支持 DeepSeek、OpenAI、Anthropic 及 OpenAI 兼容接口；API 配置通过系统能力保护后仅存本机，不进入同步数据。
- 高亮菜单支持横排/九宫格布局、四种颜色、多彩高亮开关与百度/Google 搜索引擎切换；多行、跨页和页末选区采用可见行与鼠标位置联合定位。
- 整页翻页只保留水平动画：旧页和新页同步向左移动，跨章时复用同一动画事务。

### 🐛 修复 / 性能

- 修复滚动/整页模式切换、打开/关闭智读侧栏时正文跳页；侧栏宽度稳定后按源文本偏移和视口纵向位置恢复。
- 修复图片位于页底时覆盖正文的问题；分页器按当前栏的真实文字片段保留正文空间，并在下一页完整显示图片。
- 全书页数改用不含智读侧栏的稳定阅读宽度作为缓存签名；开关智读不再清缓存、重测或把右上角切回加载状态。
- 全书页数仍按章增量落盘，后台统计由每章固定等待改为短章连续处理、每 8 章主动让出界面线程。
- 修复阅读页中文输入法输入时搜索框自动关闭，以及全文/语义检索输入状态回退等交互问题。

### 📦 发布

- Windows 提供 NSIS 安装包与单文件便携版。
- macOS Apple Silicon 提供 DMG 与 App ZIP；当前仍为临时签名，尚未经过 Apple Developer ID 公证。
- 两个平台的最终资产统一列入 `SHA256SUMS.txt`。

---

## v1.9.2（2026-07-23）

v1.9.2 是检索与阅读页稳定性版本：语义功能收敛为 BGE Small 和 BGE Large 两档，全文检索与语义索引的会话、暂停和续建行为更可靠，并补齐全书页数缓存与显示。

### ✨ 新增 / 改进

- 语义模型收敛为 BGE Small（默认轻量）与 BGE Large（高精度）；移除自定义模型转换、显卡运行时与独立大模型分发路径。
- 语义索引支持按已完成图书暂停、续建，容量、进度与完成状态实时展示；加速索引可将超大图书分段构图。
- 主窗口全文检索关闭后会清理输入与结果页会话，重新展开搜索框不会复现旧关键词或残留焦点。
- 阅读页全书页数采用按书籍、字号和页面尺寸的增量缓存，右上角显示当前页/总页数。

---

## v1.9.1（2026-07-20）

v1.9.1 是跨平台发布与阅读器交互稳定性维护版本：首次发布 Apple Silicon macOS 包，并将阅读页顶层工具栏、菜单和弹层收敛为统一状态机；同时完成 Windows/macOS 双平台 Release 与 SHA-256 校验清单。

### ✨ 新增 / 改进

- 首次提供 macOS Apple Silicon 构建，GitHub Actions 在 `macos-14` 上生成 `.app.zip` 与 `.dmg`。
- 新增 `ReaderShell` 顶层状态机，统一设置、目录、书内搜索、生词本、书籍信息、批注与跨书搜索等阅读器弹层的打开、关闭和生命周期处理。
- macOS 针对 WebKit 的阅读模式切换、指针交互和大章节布局做兼容性调整，提升启动与阅读稳定性。
- 设置窗口默认宽度由 720 px 调整为 600 px，常用设置在桌面端更紧凑。
- 同步服务 HTTPS 证书已恢复并启用自动续期。

### 🐛 修复 / 验证

- 修复阅读模式切换后工具栏或弹层状态可能残留、互相覆盖的问题；关闭钩子仅执行一次。
- 修复阅读页图片、分页与大章节布局链路中的稳定性问题，并保留当前书的搜索/索引命中。
- 增加设置窗口宽度和阅读器状态互斥的前端回归测试；v1.9.1 发布前 UI 测试 45/45、Rust 测试 113/113 通过。
- 修复 Windows 发行构建的 ONNX Runtime 发现与链接环境，已实际启动验证生成的 Windows 可执行文件。

### 📦 发布

- 版本号统一升级至 `1.9.1`，同步更新 `Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、README、更新日志与发布说明。
- GitHub Release `v1.9.1` 同时发布 Windows 便携版、Windows 安装包、macOS Apple Silicon App ZIP、macOS DMG 及统一 `SHA256SUMS.txt`。
- macOS 包目前使用临时签名，尚未经过 Apple 公证；首次启动如被拦截，需在“系统设置 → 隐私与安全性”中允许打开。

---

## v1.9.0（2026-07-15）

v1.9.0 是检索性能、数据可靠性与同步稳定性升级版，重点治理全文索引和 SQLite 体积，恢复旧版语义画像/HNSW 兼容，修复账号启动状态和阅读页搜索，并补齐自动恢复点、依赖升级与正式发布流程。

### ✨ 新增 / 改进

- 全文检索改为压缩二进制逐章索引、Bloom 预筛选和有界 LRU 缓存；索引磁盘上限 3 GB，原文/小写缓存上限 384 MB，Bloom 缓存上限 128 MB。
- 书库体检新增全文索引磁盘、内存、旧文件和孤儿文件统计，并支持一键清理孤儿索引和执行配额治理。
- 新增每日自动恢复点和设置页手动备份入口，最多保留 7 个；数据包导入前自动创建恢复点，导入后立即应用到当前书架。
- 超大 EPUB 章节启用快速分页、批量几何测量和重复布局跳过，降低大章节首次排版成本。
- 阅读页注入代码按样式、布局、高亮批注和运行时职责拆分，编译期合并，不增加运行时请求。
- 语义索引任务中心统一模型、语义索引和加速索引的任务状态；新增查询结果缓存、多中心画像候选和逐书向量缓存。

### 🐛 修复 / 数据治理

- 修复软件重启后账号状态未恢复的问题；已登录账号启动后自动同步，退出前执行有超时限制的同步，并防止启动、手动和退出同步并发。
- 修复阅读页关键词跨书搜索和语义搜索主动过滤当前书的问题；当前书命中现在会显示并在现有阅读窗口内跳转。
- 修复依赖升级后旧版 bincode 合并画像和 HNSW v2 分片被误判失效的问题；旧索引可直接读取并自动迁移，新索引使用 MessagePack。
- 777 本真实书库的合并语义画像冷加载由逐本文件扫描约 5.7 秒降至约 28 毫秒；原有 6 个 HNSW 分片已验证覆盖 777/777 本，无需重建。
- reader.db 升级为 v3 核心库：移除废弃 SQLite n-gram 倒排表，保留迁移前副本，并启用 WAL 自动 checkpoint 和日志大小治理。
- 本地与同步服务器统一为 `book_state_v2`、`vocab`、`reading_bucket_v2` 三类可移植实体；清理旧实体前强制建立恢复点。
- 修复点击书架封面后主菜单、筛选、账号或搜索浮层未自动收起的问题。
- 关键词检索继续以精确原文扫描保证完整性，避免旧索引、部分索引或 Bloom 预筛选造成漏书和漏命中。

### 🔧 依赖 / 测试 / 发布

- 升级 `zip 3.0 → 8.6`、`fastembed 4 → 5.17`、`ureq 2 → 3.3`、`sha2 0.10 → 0.11` 和 `actions/checkout 4 → 7`；临时内置 epub 2.1.5 以兼容 zip 8.6。
- CI 增加 Python 3.13 与同步服务器测试，检查脚本改为所有原生命令失败即停止，并新增阅读器模块、缓存预算、账号恢复和当前书搜索回归测试。
- Release 流程新增 SHA-256 校验清单；v1.9.0 同时发布单文件绿色版、Windows NSIS 安装包和 `SHA256SUMS.txt`。

---

## v1.8.9（2026-07-13）

v1.8.9 是阅读连续性与公开发布整理版，重点改善图文分页、滚动/整页切换、脚注交互和设置页返回路径，并移除公开仓库中的固定部署信息。

### ✨ 新增 / 改进

- 图书信息中的阅读时间线按日汇总阅读时长、阅读字数和当日最后进度，柱状图按各自峰值比例显示，悬停可查看具体数据。
- 书架横向列表加大图标与行高；封面单击、双击与布局/搜索浮层收起的交互进一步统一。
- 阅读页图片分页支持：当前页优先展示可容纳的图片顶部，下一页从图片顶部完整呈现；图片无需跨页时不会重复显示。
- 语义索引和外置词典设置页关闭后返回常用设置页，避免误退回书架。

### 🐛 修复 / 调整

- 整页与滚动模式切换改为按正文锚点恢复位置，降低切换时前跳、轻微上浮和章节内错位。
- 正文脚注「注」再次点击会收起已展开的注文。
- 阅读进度明细改为“每天最后读到哪里”的日摘要，避免逐章记录长期膨胀。
- 更新检查只使用 GitHub Release；同步服务地址不再内置，需由用户在设置中填写 HTTPS 地址。
- 移除公开仓库中的真实服务器地址、部署路径、系统服务与证书配置，避免发布部署信息。

---

## v1.8.8（2026-07-10）

v1.8.8 是 v1.8.7 之后的书架、词典、语义索引和阅读页交互修复版，重点把语义索引迁入主设置，增加外置词典管理与优先查询，并集中修复滚动/双页模式切换、语义索引状态、批注/高亮展示、脚注标记和书架设置页的一批体验问题。

### ✨ 新增 / 改进

- 主设置新增「语义索引」管理页，支持语义模型下载/删除、语义索引和加速索引建立/删除、后台运行、续建、状态本地持久化和占用空间显示。
- 主设置新增「词典」管理页，支持添加词典、删除词典、启用/禁用和调整优先级；外置词典优先于内置词典查询，查词结果显示来源并支持多结果折叠。
- 外置词典导入支持 TSV / CSV / JSON、StarDict `.ifo/.idx/.dict/.dict.dz`、常见未加密 zlib/未压缩 MDX；补完整 MDX/MDD 块读取框架，LZO 或加密 MDX/MDD 会给出明确转换提示。
- 书架筛选面板新增「列数」设置；默认保持封面自适应，手动调整后按固定列数等比缩放封面。
- 阅读页设置新增「仿真翻页」动画选项；双页和滚动模式改成同一行互斥开关，减少模式状态误解。

### 🐛 已修复问题

- 修复语义索引窗口关闭后状态丢失的问题；语义模型、语义索引和加速索引状态会持久化在本地，重新打开设置页可直接显示上次状态。
- 修复进入语义索引页时 UI 卡住的问题；模型、索引和加速索引状态改为快照读取和后台刷新，避免打开弹窗时同步阻塞。
- 修复语义模型和语义索引已就绪时，点击建立加速索引仍重复读取/加载已就绪项的问题；建立加速索引只准备必要的加速索引任务。
- 修复语义索引与加速索引进度不可续建、窗口关闭后任务中断感强的问题；正在运行的任务可继续后台执行并恢复进度显示。
- 修复主设置页语义索引入口、封面显示设置和自动导入目录说明混排的问题；删除多余说明文案，保留原有设置项。
- 修复阅读页滚动模式与整页模式切换时正文轻微位移或跳页的问题；模式切换时按可见锚点恢复当前位置。
- 修复滚动模式自由滚动后再点击翻页，会把上一页末行推进到下一页第五六行的问题；滚动翻页改用真实正文 DOM 与底部裁剪，减少重复行。
- 修复滚动/整页模式下正文脚注「注」大小、颜色不一致的问题；脚注标记统一使用整页模式的视觉规则。
- 修复高亮颜色遮挡文字、让正文发糊的问题；高亮改为更浅的下半部刷痕效果，降低透明色覆盖正文的影响。
- 修复批注/高亮列表展示上下文过长的问题；列表只显示真正高亮的文字，跳转和编辑能力保持不变。
- 修复阅读页设置里「页面」「切换」按钮语义绕的问题；删除旧按钮，改为「双页」「滚动模式」互斥开关。
- 修复词典增强设置浮层受词典释义高度影响、短释义时设置内容被截断的问题；设置面板改为独立浮层，并只保留近义、反义两项常用开关。

### 🔧 工程 / 发布

- 新增 HowNet 词义增强数据源、来源说明和导出脚本，内置词典可补充近义/反义信息。
- 书架全文检索页旧的语义索引建立入口移除，统一迁入主设置页，避免同一任务入口分散。
- 版本号统一升级到 v1.8.8，作为 v1.8 系列词典、语义索引和阅读页交互增强节点。

---

## v1.8.7（2026-07-09）

v1.8.7 是 v1.8.6 之后的阅读页体验、大书加载与主窗口细节修复版，重点收束滚动模式点击翻页、脚注/注释/高亮、翻页动画、超大 EPUB 加载、阅读统计和自动导入目录设置。

### ✨ 新增 / 改进

- 阅读页整页模式继续支持单页/双页显示；滚动模式保持真实连续滚动，并支持鼠标/键盘按虚拟页片段进行整页翻页。
- 阅读页设置新增「纸张效果（Google）」翻页动画和动画速度调节；其他不稳定动画先收敛，默认仍可关闭动画。
- 超大 EPUB 章节支持按标题/段落拆成虚拟小章，并缓存 EPUB 元数据、章节路径映射和处理后的章节 HTML；首次打开会建立缓存，后续打开同一本未修改书籍更快。
- 主窗口图书信息集中单本操作，「相似图书」「更换封面」移入图书信息弹窗；全书架正文检索开关会记住上次状态。
- 阅读统计口径收紧：设置/目录/搜索/生词弹窗、窗口失焦、快速回翻等场景不再虚增阅读时长，并补充平均阅读速度提示。

### 🔧 修复 / 调整

- 滚动模式点击翻页改为轻量虚拟页渲染，文字按测得行片段重排，图片/图表支持页底预览且下一页仍从图片顶部开始。
- 修复正文脚注「注」在虚拟页中丢失、独占行、不可点击或位置偏移的问题；长注释弹窗支持内部滚动，注释正文字号可在阅读设置中调整。
- 修复虚拟页高亮选区映射、已高亮文字菜单、取消高亮、改错、书摘下载图片和批注上下文显示等交互问题。
- 正文后注释序号按章节只编号一次，翻页/重排只刷新显示层，避免反复更新。
- 自动导入目录弹窗补齐启用开关、关闭按钮和事件隔离，避免打开/关闭互相误触。
- 版本号统一升级到 v1.8.7，作为 v1.8 系列阅读页体验与大书加载优化节点。

---

## v1.8.6（2026-07-08）

v1.8.6 是 v1.8.5 之后的阅读页分页与模式切换修复版，重点处理滚动模式、整页模式、单双页显示、图片分页、总页数统计，以及阅读页工具菜单和语义/跨书搜索体验的后续收束。

### ✨ 新增 / 改进

- 阅读页设置面板的「页面」和「切换」按钮改为显示当前状态：单页/双页、整页模式/滚动模式不再显示相反目标。
- 滚动模式下强制页面模式为单页，并禁用双页切换；只有整页模式允许切换单页/双页。
- 阅读页划词和高亮菜单补充「相似语义」入口，继续完善跨书搜索、语义检索和长摘要展示体验。

### 🔧 优化 / 调整

- 重做滚动模式单击翻页的页片段计算：文字行按完整行处理，避免页底切字、上下翻页重叠或丢行。
- 图片/图表分页增加预览规则：上一页底部可露出图片上半部分，下一页从图片顶部完整显示。
- 整书总页数按物理单页统计，避免滚动/整页/双页视图切换时页数从六百多跳到三百多。
- 阅读页 iframe 结构增加 pager/scroller/root 分层，滚动模式与整页模式的滚动容器边界更清晰。
- 版本号统一升级到 v1.8.6，作为 v1.8 系列阅读页分页稳定性修复节点。

---
## v1.8.5（2026-07-05）

v1.8.5 是 v1.8.4 之后的阅读页工具与统计修复版，重点处理划词菜单、翻译、跨书搜索、阅读统计贡献图和页数加载状态，继续收束 v1.8 系列的体验问题。

### ✨ 新增 / 改进

- 阅读页划词菜单设置页支持调整菜单项显示方式、大小、显示/隐藏和拖拽排序；「高亮」与「取消高亮」作为一组保持同位置逻辑。
- 划词菜单新增「复制」和「翻译」能力；翻译面板支持原文/译文、语言选择、服务切换，并接入 DeepL / Google / 百度 / 腾讯翻译的 API 配置与请求路径。
- 跨书搜索窗口加宽加高，结果摘要改为多行显示，按书分组结果可展开更多，并继续优化跳转后的返回来源书页逻辑。
- 主窗口全书架关键词检索改为在主窗口浮层中展示，减少独立窗口切换带来的割裂感。

### 🔧 优化 / 调整

- 阅读统计贡献图月份标签改为固定 53 周列定位，修复月份文字挤到左侧或末尾错位的问题。
- 阅读统计柱状图继续支持阅读时长 / 阅读字数单位切换，并补充悬停数据提示。
- 阅读页窗口尺寸变化时的页数状态改为加载提示，避免旧页数和新页数交替闪烁。
- 高亮设置页调整关闭逻辑、背景、拖拽图标样式和拖拽换行动画。
- 修复若干搜索框、书架检索切换、跨书搜索结果宽度和 TXT 章节标题重复显示问题。
- 版本号统一升级到 v1.8.5，作为 v1.8 系列阅读页工具与统计修复节点。

---

## v1.8.4（2026-07-04）

v1.8.4 是 v1.8.3 之后的阅读页交互与诊断增强版，重点处理跨书搜索、划词菜单设置、沉浸模式切章和调试开关，为继续排查复杂卡顿与回归问题留出工具。

### ✨ 新增 / 改进

- 阅读页划词菜单新增「跨书搜索」，可在当前阅读窗口里按书分组查看全书架命中结果。
- 跨书搜索结果窗口改为自适应尺寸，支持点击书名折叠/展开单本结果，超过默认展示数量时可分批展开更多结果。
- 从跨书搜索跳到其它书后，左下角提供返回按钮；连续跨书搜索时仍可回到最初来源书页和搜索结果。
- 阅读页划词/高亮菜单新增设置入口，并补上关闭按钮与点击外部自动关闭逻辑，后续可继续放置高亮相关设置项。
- 隐藏调试设置页上线：关于页连续点击版本号 5 次，或主窗口搜索 `--debug-ui` 可打开；支持后台任务开关、阅读页开关、安全模式、性能面板和诊断导出。

### 🔧 优化 / 调整

- 沉浸模式下点击上一章 / 下一章后，底部工具栏保持可见，避免连续切章时反复唤出。
- 阅读页拖动卡顿排查后去掉原生标题栏相关路径，保留更稳定的自定义菜单/标题区域。
- 后台任务与阅读页上报进一步接入调试开关，方便单独关闭自动导入、封面预加载、全文索引、同步、更新检查、TTS 缓存、阅读统计上报、页数测量等功能。
- 版本号统一升级到 v1.8.4，作为 v1.8 系列的阅读页交互和诊断工具节点。

---

## v1.8.3（2026-07-03）

v1.8.3 是 v1.8.2 之后的工程稳定版，重点处理代码拆分、测试补强和安全边界，为后续域名 HTTPS 与多端同步继续铺路。

### ✨ 新增 / 改进

- 后端主入口继续拆分为导入、搜索、同步、统计、TTS、阅读协议、阅读命令、更新检查等模块，降低 `main.rs` 的维护压力。
- 前端书架与阅读页继续拆分为搜索、书架、同步、统计、生词本、阅读设置等独立脚本，减少单文件全局状态堆叠。
- 新增真实容器烟测：动态生成 EPUB / PDF 文件，验证 EPUB3 目录解析和 PDF 元数据读取链路。
- 固定检查脚本扩展到 71 个 Rust 测试，并继续检查 UTF-8、版本一致、图标资源、CSS、安全基线和 release 文件。
- 阅读统计柱状图新增“时间 / 字数”切换，鼠标悬停柱子可看到带单位的数据提示。

### 🔒 安全 / 稳定

- 外部链接打开不再走 `cmd /C start`，改为 HTTPS 校验后通过 Windows `ShellExecuteW` 打开，避免 shell 注入风险。
- 检查脚本新增禁止 `Command::new("cmd")` 回归规则。
- 继续收紧同步地址、CSP、阅读页 iframe sandbox、MOBI/EPUB HTML 清洗和本机账号密钥保存边界。

### 🔧 优化 / 调整

- 域名尚未生效前保留服务器更新清单兜底逻辑；GitHub 更新检查仍为优先路径，服务器检测仅作为备用。
- 阅读统计窗口改为稳定高度，切换 2026 / 2027 等数据量不同的年份时不再因为内容多少而缩短或跳动。
- 阅读页窗口大小变化时先显示加载图标，避免旧页数停留一会儿再跳成新页数。
- 主窗口搜索框关闭时主动释放焦点，避免菜单栏附近残留白色竖条。
- 发布流程补齐 GitHub Release 资产，v1.8.3 同时提供单文件绿色版和 Windows 安装包。
- 版本号统一升级到 v1.8.3，作为 v1.8 系列工程化加固节点。

---

## v1.8.2（2026-07-02）

v1.8.2 是 v1.8.1 之后的稳定修复版，重点处理大批量导入、启动响应、自动导入目录和发布检查流程。

### ✨ 新增 / 改进

- 主菜单「导入书籍」支持大批量导入进度提示，导入大量图书时会显示当前进度、已新增数量和当前文件名。
- 拖拽导入与菜单导入统一走同一套批量导入流程，完成后刷新书架并后台更新全文索引。
- 启动性能日志补充到前后端，记录书架加载、封面渲染、账号设置、自动导入、全文索引、更新检查等耗时，方便后续定位卡顿。
- 固定检查脚本继续增强，覆盖 UTF-8、版本一致性、图标资源、CSS 常见错误和 release 文件存在性。

### 🔧 优化 / 调整

- 手动导入的 EPUB / PDF / TXT 等解析、封面提取、指纹计算移到后台阻塞线程执行，并降低后台线程优先级，减少对前台 UI 的抢占。
- 批量导入不再长时间持有书架全局锁，只在去重、插入和保存时短暂加锁。
- 导入过程改为批量间隔保存，避免每本书都写盘，也避免大批量导入完成前完全没有持久化。
- 启动阶段继续延后全文索引、指纹补全、字数统计和更新检查，减少刚打开软件、拖动窗口或点击菜单时的卡顿。
- 自动导入目录设置改为即时更新界面，扫描和导入单独后台执行，避免设置窗口等待导入完成。
- 「关于」页更新说明区域间距收紧。

### 🐞 修复

- 修复通过主菜单一次性导入大量图书时，整个界面卡住且没有进度的问题。
- 修复自动导入目录添加 / 删除后界面不立刻显示路径变化的问题。
- 修复打开软件后短时间内工具栏按钮和拖动窗口偶发卡顿的问题。
- 修复普通书架搜索切到全书架关键词搜索时可能错误回到封面列表的问题。

---
## v1.8.1（2026-07-02）

v1.8.1 是 v1.8 的 Windows 稳定收尾版，重点处理启动卡顿、账号窗口、阅读统计、生词本、搜索框、图标与发布工程问题。

### ✨ 新增 / 改进

- 主窗口搜索框右侧新增清空按钮，普通书架搜索会立即恢复完整书架，全文书架搜索模式会清空输入并保留历史下拉。
- 阅读统计页新增六项统计显示开关；贡献图、统计卡片和时间显示继续打磨。
- 新应用图标换为书本图标，图标资源重新生成透明边缘。
- 新增 `scripts/check.ps1` 固定检查脚本，覆盖 Rust、前端 JS 语法和 UTF-8 编码。
- 新增 `scripts/build-release.ps1` 发布脚本：构建 release、复制单个桌面 exe、校验 exe 内部图标、刷新 Windows 图标缓存。

### 🔧 优化 / 调整

- Windows 项目重新聚焦桌面版，Android 试验方向从当前项目发布说明中移出，后续另起工程处理。
- 账号窗口保持普通居中窗口，减少反复点击账号按钮时的卡顿和浮层遮挡。
- 书架启动路径继续减负：减少启动阶段不必要的重刷、扫描和封面阻塞。
- 阅读统计页隐藏右侧滚动条，贡献图右侧月份标签禁止换行并做边缘对齐。
- 阅读页生词本居中显示，约占阅读器高度五分之四，并隐藏右侧滚动条。
- 关于页检查更新时，如果已是最新版，不再弹系统提示框，而是把按钮文字改为“最新版本”。
- 发布桌面只保留一个 `鲲鹏阅读器.exe`，安装包作为 GitHub Release 资产单独上传。

### 🐞 修复

- 修复 Windows 桌面同名 exe 因图标缓存继续显示旧图标的问题；发布脚本会自动清理 Explorer 图标缓存。
- 修复阅读统计贡献图最右侧 `7月` 被挤成两行的问题。
- 修复搜索框清空后书架筛选状态残留的问题。
- 修复生词本继承目录抽屉布局导致不居中、尺寸过大的问题。

---
## v1.8.0（2026-07-01）

v1.8.0 重点升级**账号同步、本地 SQLite 数据层、阅读统计与生词本体验**，并为后续多端互联打基础。

### ✨ 新增功能

**账号 / 同步**
- 新增账号入口：主窗口标题栏头像按钮打开居中账号窗口。
- 支持注册、登录、退出登录；登录成功后使用服务端 token 同步轻数据。
- 同步服务端支持按用户隔离数据，旧固定 token 仍兼容默认用户。
- 同步内容包括书籍元数据、阅读进度、高亮、批注、书签、生词本、阅读统计与设置等轻数据。
- 账号窗口支持本机保存多个账号信息，可点击填入；账号 / 密码输入框支持回车登录。

**本地数据 / 迁移**
- 新增 SQLite 数据层 `src/db.rs`，统一保存同步实体与关键元数据。
- 为多端同步预留 `updated_at / deleted_at / device_id / sync_version`。
- 新增数据包导入 / 导出，用于轻数据备份与迁移。
- 书架全文搜索增加 SQLite 倒排索引，为关键词检索提速。

**词典 / 生词本 / 发音**
- 生词本支持学习熟悉度：陌生 / 认识 / 掌握。
- 英文生词可点击自动发音。
- 英语查词可选自动发音；可选缓存微软在线语音到磁盘。
- 高频词语音包支持本机生成前 10,000 高频英文词，支持暂停、继续、进度显示和删除。
- 词典查询结果显示时可后台预取语音，用户稍后点发音时优先播放缓存。

**阅读统计 / 笔记**
- 阅读统计移到主窗口工具栏，加入阅读贡献图。
- 贡献图按阅读时长分级展示，阅读统计页去除目标设置，界面更简洁。
- 新增笔记汇总页：按书汇总高亮、批注、查词记录，并支持导出 Markdown。

### 🔧 优化 / 调整

- 账号窗口从标题栏下拉浮层改为居中普通窗口，避免与搜索、漏斗、菜单浮层互相遮挡。
- 账号窗口开关改为纯前端 UI 操作，不再每次点击都读取 SQLite，减少反复点击时的卡顿。
- 启动书架增加前端缓存，第二次启动可立即显示上次书架，减少白屏等待。
- 自动导入扫描延后并放到后台阻塞线程执行，避免抢占首屏加载。
- 书架首屏不再逐本做文件存在性检查，避免慢盘 / 移动盘偶发卡顿。
- 关闭封面资源请求的高频 debug 日志，减少封面加载时的磁盘写入。
- 阅读页搜索框点击标题栏、菜单栏时会自动隐藏。
- 生词本改为瀑布式双列布局，条目高度按内容自适应。
- 生词本排序设置移到设置窗口顶部，复习/普通列表排序行为统一。
- 语义搜索的相似段落推荐默认收起，避免完整索引时仍显得耗时。

### 🐞 修复

- 修复阅读页搜索框关闭后仍残留的问题。
- 修复词典查询内容过多时文本排布不统一的问题。
- 修复生词本条目因同一行高度互相撑开的布局问题。
- 修复启动时空书架提示闪现、封面纯色占位闪烁等体验问题。
- 修复同步、登录、账号面板与其他工具栏浮层互相遮挡导致的交互异常。
- 修复全文搜索关键词查询的一些慢路径，减少无意义书架重绘。

---

## v1.7.0（2026-06-30）

v1.7.0 重点升级**离线词典、生词本与语义检索稳定性**，同时补充更多文本格式支持，并调整 release 构建配置以提升日常迭代速度。

### ✨ 新增功能

**词典 / 生词本**
- 阅读页划词或点击已高亮文字，可打开离线词典查询。
- 词典支持 **中中 / 中英 / 英中 / 英英**：中文词可在中中释义与英文释义间切换，英文词提供英中释义与英文释义。
- 查过的词自动进入**生词本**，中英文分栏管理。
- 生词本改为居中宽面板，双列展示；支持查询次数显示/隐藏，并支持按**查询时间**或**查询次数**排序。

**格式与阅读**
- 增加 MOBI / AZW / AZW3 的导入与阅读支持。
- Markdown 渲染改为 HTML 渲染，支持标题、列表、代码块、表格等常用语法。

### 🔧 优化 / 调整

- Release 构建配置从 full LTO 单代码单元调整为 ThinLTO + 多 codegen units + incremental，在保留 `opt-level=3` 的同时加快本地 release 构建。
- 词典查询弹窗与生词本布局经过多轮调整，减少内容截断和空间浪费。
- 主窗口单实例逻辑修正互斥量句柄保存方式，消除 Rust 编译警告。

### 🐞 修复

- 语义索引空正文书籍会写入空索引元信息，避免一直被判断为未完成。
- 单本语义索引构建失败会在进度状态中提示，不再静默吞掉。
- 全库 HNSW 加速索引的新鲜度判断加入源文件修改时间，源文件变化后会触发重建。
- 构建全库加速索引时同步扣减语义缓存计数，避免缓存预算长期偏高。

---

## v1.6.0（2026-06-29）

在 v1.5.0 的完整阅读器基础上，加入**图书评分体系、多目录自动导入、书架显示设置、新版更新提示**，并对书架封面加载做了显著提速。

### ✨ 新增功能

**评分**
- 在「书籍信息」弹窗给书打分，**支持半星（0.5 刻度）**：点星的左半=半星、右半=整星，再点同一处清除。
- **评分过滤**：书架漏斗面板里按评分筛选（N 星及以上，支持半星）。
- **封面显示评分**：评分小星叠在封面左下角（只读、半星、带描边在深浅封面上都清晰），可在「我的书架」里开关。

**自动导入**
- **多目录自动导入**：在「我的书架 → 自动导入目录」里设置任意多个目录，每次启动自动扫描（含子目录）把其中的电子书加入书架，已在架的不重复添加。

**「我的书架」设置页**（漏斗面板右上角 ⚙）
- 三个封面显示开关：**封面阅读进度 / 封面评分 / 封面下书名**，各自独立开关；网格视图可只显示封面。

**更新提示**
- 启动后台检查 GitHub 最新发行版，有新版时书架顶部弹横幅（查看更新 / 忽略此版本 / 关闭）。
- 「关于」里新增「检查更新」按钮与「本版更新内容」（拉取对应版本的发行说明，带本地缓存以便离线显示）。
- 检查在 **Rust 后端发请求**（避免前端跨域被拦），结构上方便以后扩展更多源。

### 🔧 优化 / 调整

- **书架封面加载大幅提速**（修复"封面比 1.0 还慢"的回归）：
  - 封面 / EPUB 内嵌资源加 `Cache-Control`，再次渲染直接命中缓存、不再走异步协议重取；
  - 封面请求**取到路径后立刻放锁再读盘**（原先读 167KB 图片时一直占着书架全局锁，几百张并发互相阻塞）；
  - 自动导入扫描改为**锁外扫描 + 过滤**，稳态下不再持锁遍历整个目录（原先扫描期间封面全在排队）；
  - 封面 URL 的缓存参数由「stat 文件 mtime」改为「封面版本号」，去掉每次渲染数百次系统调用。
- 网格视图默认**只显示封面**（书名、进度可在「我的书架」开关），更清爽。
- 更新检查由「6 小时节流」改为**每次启动查一次**（避免"刚发新版却不自动弹"）。
- 「关于」版本号、检查更新等统一从后端 `CARGO_PKG_VERSION` 取，单一来源。
- 仓库加 `.gitattributes` 把 PDF.js 标为 vendored，GitHub 语言统计回归真实手写比例。

### 🐞 修复

- 《万历十五年》等书**脚注弹窗空白**：注释 `id` 落在只含角标「[52]」的内联 `<a>` 上、正文是它的兄弟节点；改为取其所在段落的正文。
- 封面首次显示「**黑一下**」：图片字节到达前先露出 `<img>` 黑底；改为命中缓存即直接出图、未缓存时显示占位底色而非黑块。

---

## v1.5.0（2026-06-29）

从 v1.0.0 的「能看 EPUB 的本地阅读器」迭代为一个**带语义检索、PDF.js 全功能阅读、高亮批注、朗读、详细统计**的完整阅读器。下面按「新增 / 优化 / 修复」分类汇总这一阶段的全部改动。

### ✨ 新增功能

**检索**
- **书架全文检索**：对整个书架（或选中的若干本）做正文关键词检索，结果在独立窗口展示。底层用 `memchr::memmem` 字节级匹配 + 多线程并行 + 逐书纯文本内存缓存，全库秒搜。
- **语义检索**：基于 `fastembed`（ONNX Runtime）+ `bge-small-zh-v1.5` 向量模型，找「意思相近」而非「字面相同」的内容。
  - 全库 **HNSW 近邻索引**（`instant-distance`）实现毫秒级检索；
  - **分片索引**：按整本书切片建图，一次只建一片→建图内存恒定，任何机器/任何库大小都不会爆内存（取代了原先会失败的「单图 + 上限」方案）；
  - **按可用内存自动调配**：能装多少分片走近邻就装多少，装不下的书自动退回并行暴力，结果合并；
  - 建索引期间**给打开窗口让路**（暂停几秒）+ 后台线程优先级，开书不卡。

**PDF（PDF.js 自渲染）**
- 连续滚动、文字层选择/复制/划词、内置目录、主题（浅/深/护眼）。
- **缩放**：默认按窗口宽度铺满；`－/＋` 细粒度缩放；**记住每本 PDF 的缩放与双页状态**。
- **双页模式**：两页并排，翻页键整对翻。
- **PDF 内搜索**、**PDF 高亮/批注**（按页+归一化坐标）。
- PDF 文字纳入书架检索与语义检索。
- 翻页键精确翻一页（瞬移，避免平滑滚动竞态）。

**高亮 / 批注**
- 选中文字 → 高亮 / 批注 / web 搜索 / 加书签。
- 大批注页：列出全部条目（含上下文，被批注文字高亮），可点编辑、跳转。
- 高亮 ID 改用**内容指纹**（防文件移动导致数据丢失）。

**朗读（TTS）**
- 从当前页开始朗读，**逐词高亮**（CSS Custom Highlight API，不改 DOM），**自动翻页/跳章**。
- 两种音源：**系统语音**（离线，Web Speech API）/ **在线·微软神经**（edge-tts，免费 Azure 级中文音色）。
- 在线音源**流水线预取**下一两句，句间几乎无缝。
- 设置可选音源、音色（晓晓/云希/云扬/晓伊/东北话）、语速。

**阅读统计**
- 由「只存总量」升级为**按（本地日 × 小时 × 书）的小时桶流水**。
- 统计窗口分 **日 / 月 / 年 / 总** 四个视图：时长、字数、读过/读完本数、高亮/批注数；SVG 柱状图（日=24 时段、月=每天、年=每月）；本期读过的书清单。
- 「已读字数」改为**真正读过的字数**（页面停留≥3 秒且逐页前进才计入），不再用「进度 × 总字数」高估。

**其它**
- **更换封面**：选中一本书 → 工具栏「更换封面」→ 选图片替换。
- **关于页**（⋮ 菜单）：软件名、版本、GitHub 链接。
- **沉浸模式**：工具栏淡出但保留占位（切换零重排、绝不跳页），沉浸下右上角阅读统计仍显示。
- **脚注弹窗**：点注释角标就地弹出注释正文，而非跳走。
- **txt 章节化**：网文按「第X章/节/回」自动切章建目录，大文件虚拟化加载（打开秒开）。
- EPUB 与 PDF **各自记忆**阅读窗口的大小/位置。
- 每本书的**页数缓存**：版式一致则复用，免重复测量。
- 选中文字一键 **web 搜索**（系统默认浏览器）。
- 禁用 F12 / 浏览器右键菜单。

### 🔧 优化 / 调整

- **阅读位置稳定性**：顶部锚点改为字符级 caret，并**只在导航时记录、之后冻结**——改字号/边距/缩放时顶部文字不再累积漂移。
- **正文默认两端对齐**：修复部分 EPUB 把对齐写在 `<body>` 上、被阅读器丢弃导致排版异常的问题。
- **逻辑章节按所在文件去重**：金庸全集（每回独立文件）保留到「回」级；Python Cookbook（上千个 #锚点小节）合并回章级，不再显示一千多章。
- **滚动条精确定位**：按全书页位置映射，同章内直接翻页（平滑、不重载），跨章才加载。
- 书架封面整排居中（原先空隙堆在右侧）。
- 「全选」只选当前过滤/搜索后显示的书。
- 工具栏高度 35px、图标居中；书内搜索框改为贴着搜索键的下拉面板。
- 页边距夹到非负且有上限（负边距会破坏分栏排版）。
- 语义索引建立时 CPU 让路 + 后台优先级，打开阅读器不再变慢。
- PDF 默认按窗口宽度铺满，开箱即舒适尺寸。
- 编译：开发期用「快速档」加速迭代，正式发布改回满优化（`opt-level=3, lto=true`）。

### 🐞 修复

- 金庸全集只显示 12 章 → 显示完整目录（数百回）。
- 沉浸模式切换跳页（尤其金庸）→ 保留工具栏占位、零重排。
- 设置面板：每调一个数值就自动关闭；拖滑块时面板消失 → 已修。
- PDF 选中点书签**闪退** + 高亮后叠菜单 → 操作后清空选区。
- PDF PageDown 要按两下、双页模式要按两次 → 瞬移翻页 + 双页整对翻。
- 大 txt（百万字）加载慢 / 加载圈不消失 → 章节化虚拟加载、走合并阅读页。
- 拖动滚动条经过正文时卡住 → 拖动期间 iframe `pointer-events:none`。
- 在线朗读 403 Forbidden → 更新 `Sec-MS-GEC-Version` 至 Edge 143 + 补 `ConnectionId`。
- PDF 书签错显示为「章」→ 显示为「页」。
- 天下格局正文字号极小（书内联 font-size）→ 让其继承阅读器字号。
- 末页正文整体右移（Chromium 多栏尾页 padding）→ 占位空栏 + 页数修正。

---

## v1.0.0（2026-06-28，首个发布）

- Rust + Tauri 2 + 系统 WebView2 的本地 EPUB 阅读器。
- 书架（导入/封面/进度）、自定义 URI 协议按章虚拟化加载、CSS 多栏分页、阅读设置（字体/字号/行距/边距/主题）、目录跳转、书签、记忆窗口几何、续读。
