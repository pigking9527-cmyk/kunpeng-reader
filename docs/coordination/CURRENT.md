# 当前跨端协作状态

> 只记录正在进行且会影响多个端的事实；不要把日常开发日志堆在这里。

**更新时间：2026-07-27**

## 协议基线

- `syncProtocolVersion`：**1（契约抽取阶段）**。
- 现有桌面同步服务与桌面客户端是当前行为基准；schema/fixtures 尚未接入运行时校验，后续修改需先与真实服务端实现逐项对齐。
- 同步范围：阅读进度与续读位置、书签、高亮、批注、评分、标签、书单、生词本、阅读统计。
- 不同步：图书正文/原文件、原始封面、语义模型、语义索引、本机 API Key 和本机路径。

## 平台状态

| 平台 | 状态 | 当前重点 |
| --- | --- | --- |
| Windows | 已有正式桌面端 | 保持稳定、持续修复和发布。 |
| macOS | 已有桌面构建链路 | 签名、公证与实际发布完善。 |
| Linux | 未作为正式发布重点 | 后续补 AppImage/deb 与 CI。 |
| Android | 已有第一版构建 | 本地阅读、导入、账号与基础同步；继续按 contracts 校验兼容性。 |
| iPhone / iPad | 启动阶段 | SwiftUI 书架、阅读页和 iPad 侧栏体验。 |

## 当前跨端工作项

1. 为现有服务端 API 和同步实体补齐正式 schema、请求/响应 fixture 与兼容测试。
2. Android/iOS 先实现本地阅读、进度、高亮、书签、标签与书单；语义索引、智读和脑图不阻塞第一阶段。
3. 为移动端确定 HTTPS 正式地址与认证/Token 生命周期；不得在生产版放开明文 HTTP。

## 开工提醒

开始跨端任务前：阅读根目录 `AGENTS.md`、本文和相关 `contracts/`。若修改实体语义或 API，先写 ADR 与契约；若仅改本端 UI，不必等待其他端。
