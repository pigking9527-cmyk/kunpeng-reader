# 多会话进行中工作

此文件记录**尚未完成**且可能与其他会话冲突的工作。它不是工作日志；完成后将对应行移到提交记录或删除。

## 使用规则

1. 开始会修改多个文件、共享契约或原生层的任务前，先阅读本文件与 `git status --short`。
2. 在下表登记会话/分支、任务范围和预计触及的目录或文件；不要登记私密资料。
3. 已被占用的文件默认不得并行修改。需要协作时，先拆分到互不重叠的文件或由同一会话串行合并。
4. 完成后记录提交（或明确“未提交，原因”）和验证结果，再删除该行。
5. 影响协议的任务必须先完成 ADR 和 `contracts/`，不能只登记在此文件。

## 当前占用

| 会话/分支 | 任务 | 占用范围 | 状态 | 备注 |
| /root/interstellar_progress_v1 | P1 情报中心星际旅行进度首版 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、必要生成物与本表 | 已完成，未提交 | 在现有唯一情报中心中扩展第四种布局；首版展示 17% 人工基线、三项支柱、六类瓶颈、人类/AI 归因、速度快照规则，并对当前资讯执行带航天技术门槛的候选筛选，不自动把新闻计入进度。类型检查、8 项定向测试、完整前端构建、macOS Release 覆盖安装和实机验收通过；全量 typed 测试 394 项仅有其他会话的新闻规则等价测试 1 项失败。 |
| /root/reader_chapter_opening_snapshot_v10 | P1 预制相邻下一章章首过渡快照，减少跨章首屏等待 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 仅在 macOS 滚动兼容分页下复用已预取正文制作非交互的章首临时快照；真实章节仍按现有精确分页落地，设置、视口和章节键变化自动失效。TypeScript 专项 24/24、Legacy 阅读器回归 62/62、typecheck、ESLint、完整前端构建、macOS 覆盖安装与签名验证通过。 |
| /root/reader_latency_range_chunks_v9 | P0 撤回会造成正文重叠的跨章首屏 Range 分段测量 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 用户实机发现文字片段重叠；已立即回退至已验证的逐行源偏移测量，不保留该优化路径。29 项 TypeScript 阅读器专项、typecheck、ESLint、完整前端构建、macOS 覆盖安装与签名验证通过。 |
| /root/reader_latency_exact_short_nodes_v8 | P1 兼容排版短节点首屏精确测量加速 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 单行文本节点现直接复用其行框，短多行普通文本也进入按行源偏移反查；仅跨行退化矩形和异常 caret 才逐字回退。专项 77 项、typecheck、ESLint、完整前端构建、签名验证、macOS 覆盖安装和启动均通过。 |
| /root/reader_latency_exact_first_screen_v7 | P1 兼容排版长章节章首精确测量加速 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 长文本节点现用整段行框＋caret 源偏移反查替代首屏逐字 Range 测量；任一行边界不可靠时仍自动逐字回退。新增专项回归，typecheck、ESLint、TS/Legacy 阅读器专项 77 项、完整前端构建、签名验证及 macOS 覆盖安装启动均通过。未改视觉、分页语义、脚注或用户数据。 |
| /root/reader_latency_queue_adjacent_prefetch_v6 | P1 兼容排版跨章输入排队与相邻章节预取 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-page-modules,reader-large-chapter}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 保持单一阅读页面和现有排版语义；跨章加载期间保留最后一次净翻页意图，预取相邻章节正文，当前章连续页预取采用独立队列而不因快速点按被取消。TS/专项 77 项、完整前端打包、签名、覆盖安装和启动均通过。 |
| /root/reader_trace_autosave_page_latency_v5 | P1 兼容排版翻页预取与问题记录自动持久化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}`、`ui/tests/{reader-bug-trace,reader-toolbar,reader-large-chapter,reader-page-modules}.test.cjs`、`src/app_commands.rs`、`AGENTS.md`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 已取消跨章落地后 320 ms 的点击盲区，以坐标去重同一次 macOS 指针事件；章首精确扫描先覆盖一屏、仅在页尾异常留白时扩展，下一页/再下一页预取提前到 36/72 ms。最新两分钟脱敏快照现原子覆盖持久化到应用缓存，关闭前也会强制检查点；新会话按 `AGENTS.md` 直接读取快照和 `debug.log`。定向 TypeScript 18/18、相关阅读器 109/109、Rust 定向 4/4、Rust 全量 577/577、typecheck、ESLint、前端完整构建、Release 打包/签名/覆盖安装和持久化文件实机校验均通过。全量 typed/legacy 各剩一项任务外资讯规则断言，Release 总门禁被其他会话的 `private_sync.rs:948` 默认值重赋值 Clippy 告警阻断；本任务未改契约、视觉或用户数据。 |
| /root/reader_compat_cross_chapter_latency_v4 | P1 兼容排版跨章与顺序翻页响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 最新问题记录基线：普通首次顺序翻页约 40–274 ms；章节 121 首次首屏 1128 ms、整章 1308 ms，正文读取仅 2 ms；章节 122 落地 171 ms 后收到补点并进入第二页。现跳过已就绪活动字体的无效全局字体等待，macOS 快速章节阈值从 16 KiB 降至 4 KiB，当前页显示后 80 ms 空闲预取下一页，并在跨章落地后 320 ms 内仅消费同方向补点。定向 TypeScript 22/22、相关阅读器 50/50、目标 ESLint、typecheck、前端源码门禁、全部前端产物、许可证门禁及完整 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改契约、视觉设置或用户数据。 |
| /root/reader_compat_chapter_first_paint_v3 | P1 兼容排版大章跨章首帧与章首空白修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 基线：普通页 45–70 ms、缓存页 13–22 ms、小章跨章 133 ms，54 KiB 大章完整分页 566–690 ms。现按虚拟排版收紧的标题空段动态扩展精确候选带，修复首页仅 17 行的大块留白；向前进入大章时先提交精确首屏并记录 `chapter_first_page`，下一帧再完成整章页表。定向 20/20、相关阅读器 50/50、目标 ESLint、typecheck、全部前端产物、完整 macOS Release 构建与覆盖安装启动通过；另有任务前已存在的旧脚注正则断言 1 项失败。本任务未改契约、阅读设置或用户数据。 |
| /root/reader_compat_page_latency_v2 | P1 兼容排版逐页与跨章延迟优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 问题记录基线：普通页 48–160 ms，跨章 573 ms、返回 925 ms，正文读取仅 2–3 ms。现在精确排版只处理粗分页标出的当前页候选节点，同时 LRU 复用最近 48 个精确页；保留无索引时的全文兼容回退。定向 19/19、阅读器回归 110/111（唯一失败为任务前已有的旧脚注正则断言）、目标文件 ESLint、全部前端产物生成/校验、完整 macOS Release 构建与覆盖安装通过；已启动 `/Applications/鲲鹏阅读器.app`。全仓 typecheck 受共享工作区 `news-ui.ts` 的未完成 `canInvoke` 变更阻断，视觉门禁受共享 `ui/index.html` 未登记改动阻断；本任务未改两者、未改契约。 |
| /root | P1 跨设备继续阅读与可选自定义订阅同步 | `contracts/sync/{README.md,entities.schema.json}`、`contracts/fixtures/sync-handoff-and-subscriptions.v1.json`、`docs/adr/0021-synced-app-settings.md`、`src/{db.rs,db/entities.rs,db/backup_port.rs,private_sync.rs,library_commands.rs,newsnow.rs,main.rs,sync.rs}`、`apps/desktop-ui/src/legacy-ts/{sync-ui/sync-ui.ts,main-news/news-ui.ts}`、`ui/index.html`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 新增仅含稳定内容 ID 的续读交接实体；自定义 RSS/Atom 地址按独立、默认关闭的选项同步，均不含正文、文件、路径、缓存或阅读历史。短暂断网后的手动/启动同步加入无交互、指数退避恢复，恢复成功会清除失败 UI；钥匙串拒绝、认证和限额错误不重试。客户端/服务端单测、Clippy、完整前端与 macOS 签名覆盖安装启动通过。 |
| /root | P1 登录历史按点击展开与登录后账户概览保留 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`ui/tests/sync-ui.test.cjs`、必要生成物与 macOS 构建、本表 | 已完成，未提交 | 登录表单的程序化聚焦不再打开账号历史；用户点击账号框才展开。认证成功后保留账户面板并选中概览，首轮同步状态可见。同步 UI 定向 12 项、Legacy 同步页 17 项、72 项脚本构建/校验、完整 macOS Release 构建与覆盖安装通过；全局 TypeScript 检查仍被其他会话的阅读器测试只读属性赋值阻断。 |
| /root/reader_compat_boundary_latency | P1 兼容排版跨章首屏响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 问题记录显示正文接口仅 3–5ms，约 0.33–0.37s 延迟集中在 macOS 兼容分页。现保证缓存目标首屏先提交两帧，再开始阻塞式整章重排；首次跨章跳过不可用且随后会被丢弃的隐藏态分页。验证：定向 18/18、阅读器回归 95/95、typecheck、lint、source policy、生成物校验、`npm run desktop-ui:build`、`scripts/install-macos-app.sh --build` 均通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改契约。 |
| /root/reader_compat_cross_chapter_footnote | P1 兼容排版跨章注文解析与无效查找优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 原书注文存在；虚拟角标现已拆解 `#c目标章~片段` 并直取目标章，明确目标不存在时也只检查该章，不再串行扫描全书；无目标章的旧格式仍保留兼容回退。9 项定向及 95 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_compat_note_pointerup | P1 macOS 兼容排版脚注点击优先级修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 新增诊断确认 macOS 快速 pointerup 在 click 前把虚拟“注”当正文翻页；已将虚拟注释标记排除出快速翻页路径，交回脚注监听器。9 项定向及 94 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_footnote_trace | P1 脚注点击脱敏诊断链 | `apps/desktop-ui/src/legacy-ts/{reader-page-modules/{reader-page-bug-trace.ts,reader-page-bug-trace.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts},reader-messages/{reader-message.ts,reader-message.test.mts},reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 已记录脚注标记命中、链接/片段存在、虚拟监听器拦截、注文本章/跨章查找与弹窗显示结果；三层白名单仅允许布尔值、阶段和数量，不记录正文、注文、片段名或链接。24 项定向与 108 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端及 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_compat_footnote_click | P1 兼容排版脚注标记点击修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 虚拟排版片段现在会恢复包裹它的脚注链接，“注”点击由脚注弹窗消费，不再落入左右翻页热区。24 项定向测试及 226 项相关阅读器回归通过；typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过，已覆盖并启动 `/Applications/鲲鹏阅读器.app`。全量 typed 测试中的 1 项任务外资讯规则等价性测试仍受其他未提交修改影响。 |
| /root/reader_compat_boundary_cache | P1 兼容排版章首章尾往返响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，未提交 | 已缓存最多 4 个与当前视口和阅读设置精确绑定的稳定章边界快照；再次往返立即显示目标章页，真实章节仍按原门禁重排并无缝替换。54 项定向与 110 项相关阅读器回归、typecheck、ESLint、前端源码门禁、完整前端及 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root | P0 账户页钥匙串按需读取 | `src/{sync.rs,secret_store.rs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`ui/tests/sync-ui.test.cjs`、本表 | 已完成，未提交 | 打开账户页不再请求额度或同步 token；只有显式同步才允许钥匙串读取，且一次取消不会阻止后续手动重试。已完成 Rust/UI 定向验证、完整 macOS 构建并覆盖安装。 |
| /root | P0 同步凭据恢复、账户页在线探测与 inventory 向后兼容修复 | `src/{secret_store.rs,sync.rs,sync/commands.rs,main.rs,profile.rs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、必要同步测试、本表 | 已完成，未提交 | 无交互钥匙串探测不再缓存 macOS“需要交互授权”为进程级拒绝；显式同步成功会把旧 v2-v4 开发签名钥匙串条目迁移至当前 v5 槽位，使同一已安装版本后续重启可静默恢复。账户页会安全探测服务在线状态；旧服务 inventory 400 时只延后新可选类别。Rust 全量 577 项、Clippy、类型检查、同步 UI 13 项、完整前端构建及 macOS 签名覆盖安装启动通过。 |
| /root | 账户概览同步成功提示去重 | `ui/index.html`、`apps/desktop-ui/src/legacy-ts/sync-ui/sync-ui.ts`、同步 UI 测试、本表 | 已完成，未提交 | 顶部已表达同步成功；概览底部保留失败与进行中信息，不再重复显示成功结果。同步 UI 13 项、类型检查、完整前端构建、macOS 签名覆盖安装启动和差异检查通过。 |
| /root | P0 macOS 钥匙串重复授权与账户页恢复 | `src/{profile.rs,secret_store.rs,sync.rs}`、钥匙串定向测试、本表 | 已完成，待实机验收，未提交 | 已移除读取后改写 Keychain ACL 造成的第二次弹窗；旧 v2-v5 条目可读，首次显式同步迁移至本次应用的 v6 凭据槽位，之后账户页无提示检查连接并在凭据可静默读取时自动合并同步。开发临时签名跨后续重装仍需正式 Developer ID 签名才能永久稳定，不能以放宽 Keychain 访问替代。profile/同步/凭据定向测试、Clippy、完整前端构建与 macOS 签名覆盖安装通过。 |
| /root/reader_compat_tail_probe | P1 兼容排版章首页尾精确行扫描修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-large-chapter.test.cjs`、必要生成物与前端构建、本表 | 已完成，未提交 | 已将 macOS 兼容排版页尾精确测量带由“仅段距”扩大为“一整行＋段距＋字形余量”，使基础分页已消费的页尾行也能进入精确重绘。23 项专项和 109 项相关阅读器测试、typecheck、ESLint、前端源码门禁、完整前端与 macOS Release 构建均通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改视觉设置、翻页语义或协议。 |
| /root | P1 来源目录紧凑说明与无上限选择 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/{legacy-ts/main-news/{news-ui.ts,news-ui.test.mts},legacy-ts/main-rules/news-rules.ts,features/news/{news-rules.ts,news-rules.test.mts}}`、`src/{newsnow.rs,app_settings/{rules.rs,patch.rs}}`、`contracts/sync/entities.schema.json`、`docs/adr/0021-synced-app-settings.md`、`README.md`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 删除来源页顶部链路说明，改为短悬停提示和完整手势说明；去掉 24 条选择限制并保持来源请求的 12 路并发节流。同步设置 schema 与本地规则已升级，避免跨设备截断选择；类型/Rust/前端验证及 macOS 覆盖安装启动通过。 |
| /root | P1 来源悬停说明与检查移除 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/{main-news/news-ui.ts,gesture-ui/gesture-ui.ts}`、`src/{newsnow.rs,main.rs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 已删除来源检查 UI、状态及原生命令；来源悬停显示完整概览，来源卡片阻断信息提取手势，空白区域页面说明保留。类型检查、定向新闻测试、Clippy、完整桌面构建与 macOS 安装验收通过。 |
| /root | P1 来源目录紧凑布局与手势说明 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 自定义 RSS/Atom 改为默认收起的紧凑编辑区；来源悬停/键盘聚焦显示简介；信息提取手势在来源上显示该来源的概览、在空白处显示来源页总览。已通过类型检查、定向来源页测试、前端构建及 macOS 安装验收。 |
| /root/page_open_fix | P0 主窗口页面入口诊断 | `apps/desktop-ui/src/legacy-ts/main-app/{app.ts,main-app.test.mts}`、必要前端构建/测试、本表 | 已完成，未提交 | 以当前唯一 `ui/index.html` 与 72 个 generated classic 脚本在 Chromium 中模拟 Tauri 验证：统计、书库问答、旧资讯、设置与情报中心入口均可打开，无主窗口初始化异常。用户所述“页面打不开”不在主导航复现，已转交按情报中心的资讯详情/原文来源路径处理；未改产品代码。 |
| /root/source_ui_catalog | P1 来源目录单页视觉重设 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 直接重设唯一既有“管理来源”页为自适应分类目录；保留搜索、已选上限与按需可用性检查，新增 provider 汇总/筛选挂点及 `data-provider` 分组样式，不创建第二套页面。来源页专项静态测试与前端单 UI 门禁通过。 |
| /root | P1 多提供方来源筛选与目录行为 | `apps/desktop-ui/src/legacy-ts/main-news/{news-ui.ts,news-ui.test.mts}`、`src/newsnow.rs`、`ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 在唯一既有来源管理页中接入 provider/kind 的筛选、汇总和受限可用性检查；新增 Horizon 的 ReliefWeb、CISA 与 WorldMonitor 的 USGS、NASA EONET、GDACS 真实公开来源。来源页按提供方与中文类型检索/分组，保留 24 个同时启用的上限；来源检查沿用每源 6 秒、最多 12 路并发。五个新增公开端点实测均返回 HTTP 200；完整桌面前端构建、33 项 newsnow 单测、Clippy、签名安装及启动通过。 |
| /root/horizon_worldmonitor_sources | P1 Horizon 与 WorldMonitor 公开来源目录 | `src/newsnow.rs`、必要 `newsnow` 单元测试、本表 | 已完成，未提交 | 新增 Horizon 的 ReliefWeb 人道动态、CISA 网络安全公告，以及 WorldMonitor 的 USGS 显著地震、NASA EONET 自然事件、GDACS 灾害预警；均为公开免凭据直连源，并输出 `reader|horizon|worldmonitor` provider 与稳定 kind。复用现有 6 秒、12 路来源检查；弃用实测超时的 GDELT。33 项 newsnow 单测、Clippy、格式和差异检查通过。 |
| /root/custom_source_ui | P1 自定义 HTTPS RSS/Atom 来源单页接入 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、本表 | 已完成，未提交 | 在唯一既有“管理来源”页加入本地持久化的自定义 RSS/Atom 表单与列表：仅无凭据 HTTPS URL（≤2048）、名称≤80、分类≤48、本机最多200条；支持启停/删除，所有启用来源仍与内置/贴吧共用24条上限。已启用自定义源以 `custom/rss` 纳入统一目录、搜索、分类、健康检查，以及 list/refresh/availability 请求的 `customSources`；定义不跨设备同步，避免另一设备取得无法解析的裸 ID。类型检查、前端整包构建及专项来源回归通过。 |
| /root | P1 全量公开来源与可配置来源 | `src/newsnow.rs`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 已接入 614 条 WorldMonitor 公开直连 HTTPS RSS 与 4 条 Horizon 无密钥 RSS/Atom，内置目录共 657 条；唯一来源页支持本地自定义 RSS/Atom（受 URL/私网/重定向、2 MiB、6 秒、24 个启用上限约束）。只验证用户启用的来源，逐项反馈可用/空/不可用；不进行全量外网探测。桌面前端与 Rust 回归已通过，待安装验收版。 |
| /root | P1 情报中心来源复用与可用性页 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 复用唯一既有“管理来源”选择器；情报中心读取当前选择、可打开既有安全正文阅读器，并提供来源目录与已选源可用性检查。Horizon/WorldMonitor 当前无自有采集源，页面明确其复用链路。类型检查、前端构建、定向 UI/原生测试、Clippy 与 macOS 覆盖安装启动通过。 |
| /root | P5 2核4G 同步服务稳定吞吐突破 | `server/reader-sync-api-rs/src/{auth.rs,rate_limit.rs,sync.rs,error.rs,middleware.rs,account_mutation.rs,config.rs}`、`server/reader-sync-api-rs/migrations/0022_*`、`server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,test-capacity-monitor.py,run-capacity-test.sh,test-run-capacity-test.sh}`、`src/sync{.rs,/retry.rs}`、相关测试、容量文档、本表 | 已完成，未提交 | 认证/准入/业务 SQL 复用每请求同一连接，checkpoint 改账户单行高水位，push 合并 SQL 往返；准入租约自适应且竞争不再持连接等待。负载模型现用固定 seed 哈希相位、首请求错峰、3 秒排空和严格请求账目门禁；旧 20 分钟报告有 13 个 cutoff，按现行门禁只是历史诊断。新增 error/busy/database/queue 来源指标、有界写等待位 48 和数据库 acquire 超时 300 ms。最终默认执行预算收敛为普通读/checkpoint/写=12/18/10，合计 40；桌面同步客户端截止为 20 秒，长于服务端默认 15 秒，超时/5xx 最多三次抖动重试，push 保持 mutation ID 幂等。新预算的隔离 40 VU 三轮共 250,896 请求全成功，0 个 503/无响应/Busy/数据库失败/队列拒绝，平均 1,393.87 成功 RPS、P99 51.63 ms。隔离测试服务已生效，正常服务未改；完整 macOS 应用已构建、覆盖安装并启动。未改 v5 wire 语义。 |
| /root | P0 退出后登录失败可见性与流程修复 | `ui/{index.html,styles.css,tests/sync-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 登录请求不再在认证前关闭账户面板；在登录卡片中显示进行中/失败原因，避免失败无提示而无法诊断。本机到服务的隧道、服务健康/就绪、登录流程回归、完整 macOS Release 覆盖安装及签名均通过。 |
| /root | P1 同步首次上传与日额度一致性 | `server/reader-sync-api-rs/src/{sync.rs,assets.rs,auth.rs}`、`ui/{index.html,tests/sync-ui.test.cjs}`、`contracts/sync/README.md`、`docs/adr/{0019-account-security-quotas-and-verified-sync.md,0028-registration-and-resource-abuse-hardening.md,0029-rust-sync-api-v4.md}`、服务端测试、本表 | 已完成，未提交 | 每日“实际接受写入”已提升至 25 MiB（下载恢复不计入），账户页明确显示“今日上传”；总存储 25 MiB、请求/注册防护不变。服务与 macOS Release 已部署、签名和健康检查通过。 |
| /root | P1 单账户同步准入边界 | `server/reader-sync-api-rs/src/config.rs`、本表 | 已完成，未提交 | 将默认的已认证单账户一分钟上限由 120 调整为 600，避免首次同步被误判为滥用；全局 75 并发背压不变。配置测试、Clippy、服务健康/就绪检查通过。 |
| /root | P1 同步单飞进行中状态 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、本表 | 已完成，未提交 | 将已有同步单飞返回的忙状态显示为进行中，避免误标为同步失败；不改变同步协议或服务端。typed/legacy 同步 UI 回归、类型检查与 macOS Release 覆盖安装启动通过。 |
| /root | P0 首次大同步与卡住状态修复 | `src/sync/{client.rs,retry.rs}`、`server/reader-sync-api-rs/src/{sync.rs,auth.rs,config.rs,lib.rs}`、`server/reader-sync-api-rs/migrations/0021_storage_ledger_history_cleanup_v5.sql`、`server/reader-sync-api-rs/tests/postgres_e2e.rs`、`apps/desktop-ui/src/legacy-ts/{main-app/app.ts,sync-ui/{sync-ui.ts,sync-ui.test.mts}}`、本表 | 已完成，未提交 | 修正升级后账本触发器继续引用已移除恢复历史列，避免实体/资产写入 503；连续迁移已在正常服务应用，迁移开关恢复关闭。首次 push/pull/replay、数据重置/会话撤销、资产断点续传及账本在隔离 PostgreSQL 全部通过；客户端会在后台单飞结算事件后更新 UI，429 快速结算。 |
| /root | P5 账户新密码边界调整 | `server/reader-sync-api-rs/src/account_validation.rs`、服务端认证测试、`apps/desktop-ui/src/legacy-ts/sync-ui/`、`contracts/auth/`、ADR-0028、本表 | 已完成，未提交 | 新注册、手机号注册、重置及改密统一为 8–32 Unicode 字符；既有登录兼容不变。服务端边界/HTTP、前端 typed/legacy、schema 解析与完整 macOS Release 覆盖安装均通过；当前开发服务已替换并以协议 5 健康/就绪检查验证。 |
| /root | P1 登录卡片与账户概览尺寸对齐 | `ui/styles.css`、`ui/tests/sync-ui.test.cjs`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 登录外框为与概览一致的 760×302，360px 居中表单；注册页改为 520px 内容自适应卡片，窄屏保持自适应。17 项 sync UI、完整 desktop-ui 构建、macOS Release 覆盖安装、签名与实机验收通过。 |
| /root | P1 未登录同步摘要去重 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、本表 | 已完成，未提交 | 未登录概览仅保留“尚未同步”一行，隐藏标题已表达的重复“尚未登录”；不改账户、认证或同步请求。5 项 typed sync UI、17 项 legacy sync UI、完整桌面构建、macOS Release 覆盖安装、签名与实机验收通过。 |
| /root | P5 75 并发读写背压预算 | `server/reader-sync-api-rs/src/config.rs`、`server/reader-sync-api-rs/DEPLOYMENT.md`、`server/reader-sync-api-rs/scripts/{run-capacity-test.sh,test-run-capacity-test.sh}`、本表 | 已完成，已被 40 预算取代 | 历史的普通读/checkpoint/写=33/24/18（合计 75）已由后续隔离证据收敛为 12/18/10（合计 40）；不得再将本行的 75 执行预算当作当前配置。 |
| /root | P5 可指定并发的一分钟隔离同步冒烟 | `server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,capacity-client-monitor.py,run-capacity-test.sh,test-run-capacity-test.sh}`、本表 | 已完成，未提交 | 新增 `--smoke CONCURRENCY` 一键非容量冒烟（1–500）：独立测试账号、60 秒、隔离服务/测试库和三端资源观测；不改变 20 分钟容量曲线。100 并发：61,856/61,900 成功、1,030.93 成功请求/秒、P50/P95/P99=65.66/100.44/130.91ms，44 个受控 503；75 并发：55,575/55,575 成功、926.25 成功请求/秒、P50/P95/P99=54.33/83.02/105.60ms，无无响应或拒绝。 |
| /root/login_registration_redesign | P1 未登录账户入口与登录注册二级页 | `ui/{index.html,styles.css,tests/sync-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 已精简登录卡片并删除账号上方标题说明和返回概览；登录主操作改为低强调灰色；注册点击后立即进入独立状态并显示邮箱。保留邮箱验证码注册协议、原账户概览和单一 UI。定向 lint、typecheck、源码门禁、17 个 legacy 测试、362 个 typed 测试、macOS Release 构建、签名安装及正式应用实机验收均通过；完整 `scripts/check.ps1` 仅剩其他会话涉及的反单体预算与手势反馈断言失败。 |
| /root | P5 本机 SSH 隧道同步临时端点 | `build.rs`、`src/runtime_support.rs`、本表；仓库外本机 LaunchAgent/安装入口 | 已完成，未提交 | 已配置自动重连的本机 SSH 回环隧道；构建期仅允许严格的 `127.0.0.1` HTTP 端点、拒绝公网 HTTP。仓库外 `run-kunpeng-macos-install.sh --build` 为本机验收构建注入该地址，避免退出登录后丢失服务配置；公开构建仍不内置地址。Release 覆盖安装、签名和隧道健康检查通过。 |
| /root | P5 P99 队列/连接/SQL 归因与分级背压收敛 | `server/reader-sync-api-rs/{src/{middleware.rs,config.rs,state.rs,lib.rs,auth.rs,rate_limit.rs,sync.rs},scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,run-capacity-test.sh,test-capacity-monitor.py,test-run-capacity-test.sh}}`、必要服务端测试、本表 | 已完成，未提交 | 仅隔离候选服务：按模板路由/读写/轻读槽记录已接纳排队、快速拒绝、连接获取和 SQL 区间；成功 2xx 延迟与总体延迟分列，固定 330 秒短测要求每阶段有样本，否则整轮无效。候选完成一轮完整报告；不记录 URL 参数、账号、Token、请求体或数据库连接资料。 |
| /root | P5 带数据同步传输 5 分钟隔离冒烟 | `server/reader-sync-api-rs/scripts/{data-transfer-k6.js,data-transfer-report.py,run-capacity-test.sh,test-data-transfer-report.py}`、本表 | 已完成，未提交 | 仅可销毁测试库和独立服务；固定 300 秒、约 256 KiB 同步实体交替 push/pull。最终有效记录为 1,024/1,024 成功、无超时/429/503，已采集 wire 字节、P50/P95/P99、服务/PG/客户端资源；不接入反代或正式服务。 |
| /root | P5 同步 checkpoint 桌面接入与隔离短测 | `src/{sync.rs,sync/protocol.rs}`、必要同步测试、`server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py}`、本表 | 已完成，未提交 | 无待上传变更、完整 cursor 且一小时内做过完整清点时走轻量 checkpoint；任何否定/错误回退完整 v5 同步。已部署独立测试服务并完成固定 330 秒短测。 |
| /root | P1 手势恢复动作重启丢失修复 | `src/app_settings/{rules.rs}`、`src/app_settings.rs`、`contracts/sync/entities.schema.json`、`docs/adr/0021-synced-app-settings.md`、本表 | 已完成，未提交 | v5 app-settings 已接受规范 `undo_last`；旧 `reopen_last` / `restore_jump` 可读并写回归一。应用设置 14 项、手势 7 项、类型检查、前端与 macOS Release 构建、覆盖安装启动通过。完整 rustfmt 仍被同目录既有共享格式差异阻断，未重排任务外内容。 |
| /root | P1 阅读偏好高级页排版引擎界面重设 | `ui/{reader.html,reader-preferences.css,tests/reader-preferences.test.cjs}`、`apps/desktop-ui/src/legacy-ts/reader-preferences-ui/{reader-preferences-ui.ts,reader-preferences-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，未提交 | 已删除弹窗内重复标题行；排版引擎改为与跳转回退同风格的图标+标题+双选控件，保留 legacy/modern 设置语义。已同步更新经用户授权的 UI 视觉基线；类型检查、偏好回归、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root | P1 新版排版章节内翻页计数修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与前端构建、本表 | 已完成，未提交 | 新版 EPUB 多栏布局在 WebKit Range 几何退化为首栏时，改以 scrollWidth 计数作只增不减回退，避免章节误判为单页而让点击/滑动跨章；不改手势映射。专项 3 项、类型检查、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root | P1 新版排版空白尾栏修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与前端构建、本表 | 已完成，未提交 | 新版 WebKit Range 几何失效不再参与尾栏修剪，避免把 scrollWidth 已确认的章节页数裁回单页；保留 legacy 的可见内容尾栏修剪。专项 3 项、类型检查、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root/reader_compat_bottom_space | P1 兼容排版页尾空白收敛 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与前端构建、本表 | 已完成，未提交 | 已移除固定整行保护，并让大章节精确逐字重绘向视口外探测可回收段距，避免页尾空白、整行漏读及下一页重复；相关 56 项测试、typecheck、lint、完整 Release 构建通过，已覆盖 `/Applications/鲲鹏阅读器.app` 并在第 50/84 章第 1→2 页实机验证连续且无切字。全量 `scripts/check.ps1` 仅被任务外既有 `news-rules-equivalence` 期望差异阻塞（实际 `['b','a','3']`，期望 `[]`）。 |
| /root/reader_compat_chapter_turn | P1 兼容排版跨章闪页与章首页快照修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与前端构建、本表 | 已完成，未提交 | 关闭翻页动画时的跨章遮挡层已改为复制 macOS 兼容排版真正可见的逐行虚拟页，不再短暂显示页界不同的原始滚动正文；章首页页尾与撤下遮挡后的正文保持一致。新增专项回归，相关 94 项阅读器测试、typecheck、ESLint、完整前端和 macOS Release 构建通过，已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/pdf_gesture_forwarding | PDF 书页接入阅读器手势 | `apps/desktop-ui/src/legacy-ts/pdfview/pdfview.ts`、`ui/pdfview.js`、`ui/tests/pdf-engine-bridge.test.cjs`、本表 | 已完成，未提交 | PDF iframe 已通过固定来源桥转发右键拖动的 start/move/end/cancel，覆盖 Pointer Events、WebKit 鼠标兼容和失焦取消；专项 4 项、Typed 355 项、完整桌面前端构建、Lint、类型检查、Release 打包/签名/覆盖安装及真实 PDF 打开通过。全仓检查另被占用中的 `window_commands.rs` 三项 Clippy 拦截；Legacy 全量另有两项任务外迁移断言失败。 |
| /root/server_sync_hotpath_impl | P5 同步服务 PostgreSQL 热路径尾延迟优化 | `server/reader-sync-api-rs/src/sync.rs`、`server/reader-sync-api-rs/migrations/0016_sync_hot_path_v5.sql`、必要服务端测试与本表 | 已完成，未提交 | push 收据改为精确 90 天读/写边界与每进程 5 分钟一次、最多 2,000 行的全局清理；inventory 空结果改为单一 account-left-join 快照并加入覆盖索引；pull 使用同语义的 stored `envelope_bytes`。迁移静态检查、35 服务单元、22 HTTP、16 PG 门禁、Clippy 与差异检查通过；未部署或连接数据库。 |
| /root/capacity_pg_stats | P5 容量测试 PostgreSQL 脱敏观测与内存指标校正 | `server/reader-sync-api-rs/scripts/{capacity-monitor.py,run-capacity-test.sh,test-capacity-monitor.py,test-run-capacity-test.sh}`、本表 | 已完成，未提交 | 仅隔离容量测试链路；从运行中的可销毁服务推导并前缀校验其测试库，报告聚合前后 SQL 计数、连接/等待及 PSS；不含 SQL、凭据、地址或账户数据。`test-capacity-monitor.py`、Shell 语法、Python 编译、容量脚本自测与差异检查通过；未连接、部署或执行远端测试。 |
| /root/client_sync_core | 客户端同步核心安全快路径 | `src/{sync.rs,data_migration.rs}` 及必要同步核心测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 稳态 inventory/reconcile 改为只读 LWW manifest，不解码实体 JSON；pull/reconcile/push conflict 在与 cursor/实体同一事务中写 runtime projection marker，稳态无变更时跳过完整 runtime 投影和重复 JSON→SQLite 迁移，异常中断仍会在下次 no-op pull 恢复。保留初次投影、cursor/LWW、每轮 inventory 自愈和顺序 push。`cargo test` 548 项、`cargo clippy --all-targets -- -D warnings`、格式和差异检查通过。 |
| /root/sync_dirty_scheduler | 客户端持久脏同步调度 | `src/{app_state.rs,sync.rs,db.rs,db/metadata.rs}`、必要调用点/测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 复核已有 generation 调度并补齐“重启不主动读取钥匙串”：自动启动只可使用当前进程中已解锁的同 marker token cache；手动同步失败也会持久化同一指数退避，拒绝钥匙串时不会再次自动弹窗。保留本地 upsert/tombstone/导入同事务 generation、45/10 秒 trailing、5 分钟上限、同步期新写保留、恢复重绑 notifier 与远端 projection 静默。`cargo fmt --all -- --check`、`cargo test sync::`（52）、`cargo test db::metadata::`（6）、`cargo check`、`cargo clippy --all-targets -- -D warnings`、差异检查均通过。 |
| /root/catchup_cursor | k6 推进式 catchup 容量档位与 cursor-zero 对抗回放隔离 | `server/reader-sync-api-rs/scripts/{capacity-k6.js,run-capacity-test.sh,capacity-k6-report.py,test-run-capacity-test.sh}`、`docs/testing/sync-capacity-baseline.md`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 单 controller VU 的 `http.batch` 每轮使用阶段指定数量的不同账户，持久保存 2048 个账户各自的 `nextCursor`；报告明确阶段数是 `http-in-flight` 而非 k6 VU。显式 `cursor-zero-replay` 标为对抗且不可作默认容量结论。脚本自测、演练工具总自测、Python 编译、Shell 语法、JS 解析和差异检查通过；本机未安装 k6，真实短测待具备 k6 的压测机执行。 |
| /root/server_queue_metrics | P5 服务请求队列与处理时延观测 | `server/reader-sync-api-rs/src/middleware.rs`、必要 middleware 测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 队列等待、在执行槽内 handler 耗时、活跃/排队数及按路由的状态码均用 MatchedPath 模板和 read/write 类别标注；不记录 URL 参数、身份、Token 或请求体。定向 middleware 4 项、格式和范围差异检查通过；全目标 Clippy 暂被并行 `sync.rs` 缺失字段阻断。 |
| /root | P5 多账号同步 P95/P99 六项优化 | `server/reader-sync-api-rs/src/{auth.rs,rate_limit.rs,state.rs,config.rs,sync.rs,assets.rs,recovery.rs}`、`server/reader-sync-api-rs/migrations/`、`server/reader-sync-api-rs/{tests,scripts,README.md}`、`docs/testing/sync-capacity-baseline.md`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已完成认证单 CTE、跨实例保守租约式单账号准入、pull 单查询、事务用量账本、批量 push/history 与 cursor 稳态档位。压测一键脚本现使用 2048 个账户、并发去重与测试迁移。隔离 catchup 短测完成：500 并发 P95 1012.61ms、P99 6637.29ms、0 个429/断连、1 个503；未触碰正常服务/数据库或公网入口。 |
| /root | P5 单账户写入快速限流及多账户 k6 容量复测 | `server/reader-sync-api-rs/src/{account_mutation.rs,sync.rs,assets.rs,recovery.rs,lib.rs}`、`server/reader-sync-api-rs/{scripts,tests,README.md}`、`docs/testing/sync-capacity-baseline.md`、`AGENTS.md`、`docs/coordination/ACTIVE_WORK.md` | 短测完成，未提交 | 保持跨实例账户数据锁，但改为立即 503/Retry-After；k6 以 500 个独立、可销毁测试账户和保活连接执行既定 20 分钟曲线，并采样客户端/API/PostgreSQL 负载。2026-08-14 短测完整跑完 330 秒、358,529 请求，仅 1 次无响应；完整 20 分钟容量验收仍待单独执行。 |
| /root | P5 读写分级、背压与异步维护优化 | `server/reader-sync-api-rs/src/{middleware.rs,config.rs,state.rs,sync.rs,assets.rs,lib.rs}`、必要迁移/测试/容量文档、本表 | 已完成，未提交 | 读写现有独立执行槽和独立有界等待位，满/超时即返回可重试 503；过期收据和孤儿资产已从 push 关键路径移至有界后台任务。保持同账户写入锁、协议与配额精确性；候选仅部署到隔离测试服务并已运行固定 330 秒短测。 |
| /root/read_write_backpressure | P5 服务端读写分级与有界背压 | `server/reader-sync-api-rs/src/{middleware.rs,config.rs,state.rs,lib.rs}`、必要服务端测试/文档、本表 | 进行中，未提交 | 在父任务授权范围内补齐独立读写执行槽与有限等待队列；仅记录模板路由和读写类别，不记录请求参数、身份或内容。 |
| /root/async_maintenance | P5 同步写后资产/收据异步维护 | `server/reader-sync-api-rs/src/{sync.rs,assets.rs}`、服务端测试/容量文档、本表 | 已完成，未提交 | push 事务只保留 LWW、配额账本和幂等 receipt；单个后台循环每 5 分钟最多清理 2,000 条过期 receipt、每小时最多检查 100 个资产账户。账户锁忙即跳过，无同账户队列积压，失败下周期重试；指标仅使用固定任务标签/聚合数量。`cargo test --lib` 37、HTTP 25、格式和差异检查通过；全库 Clippy 仅被任务外 `config.rs` 的 doc_markdown 阻断。无需 migration。 |
| /root/sync_write_batch | P5 同步写入/恢复历史 SQL 往返收敛 | `server/reader-sync-api-rs/src/{sync.rs,recovery.rs}`、必要 migration/服务端测试、本表 | 已完成，未提交 | `0017` 将实体、资产与恢复历史的存储账本触发器改为 statement-level transition-table 聚合，批量源表写入由每实体账本更新收敛为每账户一次；恢复 history 用持久 `last_recorded_at` 原子分配单调时间戳，删除每 push 的 `MAX(history)` 扫描，并把保留裁剪和元数据更新合为一条 SQL。`cargo fmt --check`、36 单元、22 HTTP、16 PG 门禁、Clippy、迁移/演练工具自检和差异检查通过；本机无 PostgreSQL 可执行文件，真实 migration 留待隔离测试库验收；未部署或连接服务。 |
| /root | P5 注册账号与新密码长度统一 | `server/reader-sync-api-rs/src/{account_validation.rs,auth.rs,registration.rs,phone_registration.rs,password_reset.rs}`、`server/reader-sync-api-rs/tests/http_baseline.rs`、`contracts/auth/{registration-v2.md,password-reset-v2.md,phone-registration-v1.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 新账号保持 3–32 个 ASCII 标识字符；新密码统一为 12–128 个 Unicode 字符。登录保留旧密码兼容边界，避免锁死已注册用户。服务端 35 个单元、22 个 HTTP、14 个受保护 PG 门禁测试和 Clippy 均通过。 |
| /root/main_ui_ts_migration | 原版主窗口首批严格 TS 无视图迁移 | `apps/desktop-ui/src/legacy-ts/main/**` | 进行中，未提交 | 只迁移纯规则、状态与边界，不改 `ui/`、HTML、CSS、构建配置或 Rust，不定义第二套 UI。 |
| /root/reader_ui_ts_migration | 原版阅读器首批严格 TS 无视图迁移 | `apps/desktop-ui/src/legacy-ts/reader/**` | 进行中，未提交 | 只迁移阅读外壳的纯规则、状态、guard、导航、偏好和消息边界，不改 `ui/`、HTML、CSS、构建配置或 Rust。 |
| /root/ts_build_pipeline | 原版页面严格 TS 到 classic IIFE 的确定性构建管线 | `package.json`、`package-lock.json`、`tsconfig.json`、`apps/desktop-ui/vite*.ts`、`apps/desktop-ui/scripts/**`、新增前端源码门禁/校验脚本 | 进行中，未提交 | 构建产物仍供唯一原版页面加载；不改 `ui/`、Tauri/Rust、文档，不引入 React/TSX 或双入口。 |
| /root/reader_loading_delay | 阅读窗口加载指示器延迟显示 | `ui/reader.html`、`apps/desktop-ui/src/pages/reader/reader.css`、`ui/tests/reader-loading-delay.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 现行与迁移中阅读页的加载遮罩均延迟 100ms 显示；定向 46 项、类型检查、四入口构建、Rust 483 项与前端 Lint 通过。完整检查仅被任务外尚未收口的 React 源码迁移门禁拦截；Release 整包构建、签名、覆盖安装和实机正文就绪检查通过。未改图书加载、排版、翻页、数据或协议。 |
| /root/reader_full_page_text_fix | 整页翻页图片页正文缺失回归修复 | `ui/{reader-page-runtime.js,reader-page-layout.js,tests/reader-large-chapter.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 连续图片来源页不再改真实媒体高度；带正文的旧式背景图 table 会拆为普通图片和原正文节点。定向 15 项、完整工程检查（476 项 Rust 及 TS/语法）和 Release 安装通过；《爱丽丝》实机确认问题页下一页完整正文立即显示。 |
| /root/reader_background_art_text_fix | EPUB 背景插图与正文重叠修复 | `ui/{reader-page-layout.js,tests/reader-large-chapter.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将含正文的 EPUB 背景图表拆为独立插图和普通正文流；专项 15 项通过、Legacy UI 409/410（唯一失败为任务外 React 第二 UI 治理迁移）、完整 Release 构建/签名/安装通过，同书同章实机确认不再重叠。 |
| /root | P3 同步只读 SQLite 观测边界收敛 | `src/sync.rs`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | 仅将 4 个已存在的只读锁快照改接 `with_db_read`，补齐锁等待/锁内区间指标；不改变大 JSON 构造、账户 generation、网络、备份/恢复或同步 wire。 |
| /root | P5 CI 上传物离线暂存门禁接线 | `.github/workflows/sync-api-v5.yml`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | CI 在上传前必须用已验证的暂存工具生成、复验仅含二进制和溯源清单的目录；不连接部署环境。 |
| /root | P2 AI 阅读证据纯规则拆分 | `src/ai_reader.rs`、`src/ai_reader/reading_evidence.rs`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | 仅移动已读章节排序、选区邻近、章节范围提示、去重与上限；不改 Tauri、SQLite、书架、网络、provider 或同步。 |
| /root/p5_offline_bundle | P5 离线候选物受限暂存包 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 新增最小传输目录门禁：仅复制已复验的候选二进制与清单、拒绝符号链接/额外文件/内容漂移，并在复制后复验；无上传、服务器、数据库、SMTP、反代或部署能力。`bash -n`、独立 bundle 自测、演练工具自测、迁移静态检查与差异检查通过。 |
| /root | P5 受保护空 PostgreSQL 演练尝试 | 受保护服务器临时数据库/角色/源码目录、`docs/coordination/{CURRENT,ACTIVE_WORK}.md` | 已安全停止 | 仅创建前缀受限的临时库和最小权限角色；Docker Hub 获取 Rust 1.97.1 镜像超时，测试和迁移均未开始。已立即销毁临时库、角色和源码目录；旧服务、反代和正式数据未改。需可用的受信 Rust x86_64 构建镜像或 CI 候选物后重试。 |
| /root/sqlite_next_safe | P3 实体 JSON 读取 SQL 计时补齐 | `src/db/entities.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 单条与批量 JSON 投影现记录固定 `entity_json`/`entity_json_many` SQL 标签与结果行数，不采集键或 JSON；不改查询语义、同步、备份、连接池或 AppState 生命周期。定向 3 项实体读取测试、全目标 Clippy 与范围差异检查通过。 |
| /root/reader_image_overlay_fix | 爱丽丝插图翻页首帧与全局弹窗顺序修复 | `ui/{reader-page-layout.js,reader-page-runtime.js,overlay-stack.js,tests/{reader-large-chapter,overlay-stack}.test.cjs}`、`docs/{architecture/desktop-frontend-and-rust.md,coordination/ACTIVE_WORK.md}` | 已完成，已进入当前提交 | 程序化翻页后主动稳定媒体合成；连续图片裁切按首次原高幂等计算；信息窗与操作窗按打开顺序叠放，确认/反馈仍保留更高语义层。完整检查、Release 构建和安装通过，连续模式目标页首帧已实机检查。 |
| /root | P5 v5 发布清单跨目录 fixture 完整性 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 编译期引入的 v5 契约 fixture 已纳入发布清单的 Git 清洁度与 SHA-256 复验，避免服务目录之外的输入漂移；自测覆盖 fixture 漂移拒绝。未连接服务器、数据库或 SMTP。 |
| /root | P5 v5 服务 Linux 可复现构建产物 | `.github/workflows/sync-api-v5.yml`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交 | CI 现以 Rust 1.97.1 在 Ubuntu 24.04 锁定构建服务端，运行格式、测试、Clippy、迁移/演练工具，并上传通过源码/二进制/迁移/Cargo.lock 复验的 Linux x86_64 候选产物；不连接服务器、数据库或 SMTP，不配置反代或切流。提交 `17261520`。 |
| --- | --- | --- | --- | --- |
| /root/public_source_catalog | P1 Horizon/WorldMonitor 可直连公开来源目录与自定义 RSS 安全边界 | `src/newsnow.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已扩展本地资讯目录、通用 HTTPS RSS/Atom 解析与每请求自定义源验证；不改 UI、同步、服务端或外部部署。 |
| /root/reader_annotation_rules | P1 阅读页高亮菜单纯矩形规则拆分 | `ui/{reader-page-annotations.js,reader-page-highlight-rules.js,tests/{reader-page-modules,reader-toolbar}.test.cjs}`、`src/reader_page.rs`、`ui/tests/governance.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将高亮矩形包络、指针最近命中、同页行分组及跨页/末行菜单锚点移至冻结、无 DOM/存储/IPC 依赖的编译期规则脚本；批注渲染、选区、分页、事件、IPC 和 EPUB/PDF 命令式阅读引擎留在原模块。定向 157 项与全量 409 项 Legacy UI、`cargo test reader_page` 和差异检查通过；全仓 rustfmt 仅被任务外正在修改的 update 模块差异阻断。 |
| /root/p5_deploy_manifest | P5 Rust 服务离线发布产物溯源门禁 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交 | 新增仅本地的二进制/源提交树/Cargo.lock/迁移 SHA-256 清单创建与复验；拒绝服务目录未跟踪、未提交或二进制不匹配，避免错误归属。服务源码及 v5 fixture 已作为提交 `dc7502fc` 入库，脚本自测、迁移/拒绝路径自测、服务端测试、格式、Clippy 与差异检查通过；未连接服务器、数据库或读取部署机密。 |
| /root/update_rules_split | P2 更新版本与发布元数据纯规则拆分 | `src/update.rs`、`src/update/rules.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将版本比较、发布 JSON 字段兼容、tag/changelog 解析、HTTPS endpoint 与仓库标识校验移至无网络/Tauri/构建注入依赖的 rules；父模块保留 HTTP、超时、构建配置和命令。5 项更新定向测试、格式、全目标 Clippy 与差异检查通过；未加入部署端点。 |
| /root/registration_ui_audit | 注册端点缺失的安全用户提示审计 | `src/sync/validation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 默认端点不存在时改为明确说明客户端未配置同步服务、不能注册或登录，并建议联系服务提供方或使用已配置版本；不泄露地址、不添回退、未改端点、服务端或 contracts。`cargo fmt --all -- --check`、3 项定向校验测试和差异检查通过。 |
| /root/p3_metadata_metrics | P3 高频 metadata 前缀查询 SQL 计时 | `src/db/metadata.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | `metadata_with_prefix` 现从 SQL 执行到结果收集记录固定的 `metadata_with_prefix` 标签及行数，不记录调用前缀或内容；新增定向测试。`cargo fmt --check`、定向 1 项、全目标 Clippy 与范围差异检查通过；未改 entities、schema、索引、连接池、同步或备份。 |
| /root | 注册默认同步端点构建注入 | `build.rs`、`src/runtime_support.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已提供仓库外 `KUNPENG_DEFAULT_SYNC_URL` 的 HTTPS 构建注入机制；未设置时客户端明确提示尚未配置服务。线上仍是旧协议，正式 macOS 应用未注入任何端点，等待 v5 服务部署、受保护 E2E 与注册验收后才可启用。构建注入验证、472 项 Rust、408 项 Legacy UI、81 项 Typed、桥接构建、Clippy、格式和差异检查通过。 |
| /root/p4_runtime_boundary | P4 PDF 受控渲染器替代打开请求生命周期保障 | `packages/pdf-engine/{src/index.ts,test/performance-baseline.test.mts}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 修正被替代打开请求在 finally 中误清后继 PDF.js loading task 的竞态；close/abort 现原子取得任务所有权后恰好 destroy 一次。新增无内容双打开合成回归，确认旧请求结算不丢失新任务、close 仍清理后继任务。`npm run test:typed`（81 项）、`npm run desktop-ui:build`、PDF bridge 3 项与差异检查通过；未用真实样本、未改 PDF.js Canvas、视觉、手势或页面入口。 |
| /root/gesture_rules_split | P1 手势提示配置纯规则拆分 | `ui/{gesture-ui.js,gesture-hint-rules.js,index.html,tests/gesture-hint-rules.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将提示框配置规范化、形状路径和自由绘制点压缩移至冻结的 classic-script 规则；外壳保留手势输入、Canvas、DOM、存储、IPC 和阅读器引擎命令式执行，并在规则缺失时逐项保持等价回退。定向 62 项、全量 403 项 Legacy UI、Node 语法和范围差异检查通过。 |
| /root/app_settings_rules_audit | P2 应用设置纯规范化规则拆分 | `src/app_settings.rs`、`src/app_settings/rules.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 新闻、工具栏、手势和阅读排版的无状态 JSON 规范化已迁至 `rules`；父模块保留 v5 像素字段/退役字段清理、payload 合并、Tauri 命令和 SQLite 生命周期。新增 2 项纯规则测试；`cargo fmt --check`、10 项 app-settings 测试、全目标 Clippy 和差异检查通过。 |
| /root/reader_page_layout_split | P1 阅读页滚动分页纯几何规则拆分 | `ui/{reader-page-layout.js,reader-page-scroll-rules.js,tests/reader-page-modules.test.cjs}`、`src/reader_page.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 滚动分页的索引、对齐起点、切片底边和断点查找已移至冻结、无 DOM/设置/IPC 的编译期 classic-script 规则；布局模块仅桥接既有全局 API，保留 EPUB/PDF、视觉层、选区、手势和 Canvas 命令式引擎。Node 语法、31 项阅读页模块、24 项治理与差异检查通过；Rust 定向测试被其他会话未完成的 `background_tasks`/`db` 编译错误阻断。 |
| /root/background_registry_split | P2 后台任务持久化注册表低耦合拆分 | `src/background_tasks.rs`、`src/background_tasks/registry.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将持久化任务快照的序列化/加载、恢复归一化、终态、日志与历史裁剪辅助迁至低耦合 registry 模块；调度器、Tauri 命令、后台优先级和锁生命周期保留父模块。29 项后台任务定向测试与全目标 Clippy 通过；全仓 rustfmt 被任务外 `src/db.rs` 的既有格式差异阻断，范围差异检查通过。 |
| /root/i18n_more_split | P1 i18n 语义运行时文案目录纯规则拆分 | `ui/{app-i18n.js,app-i18n-semantic-runtime-catalog.js,index.html,tests/app-i18n.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将语义模型、检索分级、GPU/下载/索引运行态的无 DOM/存储/IPC 十语种 catalog 移至预加载 classic script；入口保留严格 applier 形状校验与无前置脚本时的等价本地回退。定向 10 项、全量 401 项 Legacy UI、Node 语法与差异检查通过；未改 AI 调用、账户/同步语义、React 或契约。 |
| /root/rust_module_audit | P2 书架展示投影低耦合拆分 | `src/library_commands.rs`、`src/library_commands/presentation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 审计后迁移书名首字母及其 GBK/标点规则至无 Tauri、SQLite、文件写入或同步依赖的纯模块；命令签名、书库锁、同步和阅读窗口生命周期保持不变。定向 2 项测试、全目标 Clippy、格式和差异检查通过。其余书架命令均直接编排持锁持久化、同步墓碑或阅读窗口生命周期，不作无价值拆分。 |
| /root/search_next_split | P1 搜索历史纯规则拆分 | `ui/{search.js,search.html,search-history-rules.js,tests/search-history-rules.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 最近搜索规范化、去重、长度上限与常搜排序已移至冻结、无 DOM/存储/IPC 依赖的 classic script；搜索外壳保留 localStorage、DOM、事件和检索命令，并在脚本缺失时走同形状回退。定向 15 项与全量 396 项 Legacy UI、Node 语法检查通过；未改搜索命令、页面入口、React 或契约。 |
| /root/news_next_split | P1 资讯瀑布流纯布局规则拆分 | `ui/{news-ui.js,news-layout-rules.js,index.html,tests/news-layout-rules.test.cjs,tests/news-ui.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已将列数、卡片估高和稳定最短列分配移至冻结、无 DOM/存储/IPC 依赖的 classic script；外壳保留 DOM 创建、图片队列、来源分组、页面与资讯命令，并在规则脚本缺失时逐项使用原公式。定向 19 项与全量 399 项 Legacy UI、Node 语法与差异检查通过。 |
| /root/sync_next_split | P2 同步地址校验与账户 scope 纯规则拆分 | `src/sync.rs`、`src/sync/validation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | sync/auth base 规范化、本机 HTTP 边界、账户 scope 与 data generation 校验已迁至无 HTTP/SQLite/Tauri/恢复依赖的模块；v5 wire/HTTP、账户恢复、命令签名及 sync generation 语义未变。34 项 `cargo test sync::`、全目标 Clippy、rustfmt 与差异检查通过。 |
| /root/builtin_chinese_dictionary | 恢复合规的内置汉语词典与中英释义切换 | `src/dict.rs`、`src/dict/{zh_zh.tsv.gz,ZHWIKTIONARY_SOURCE.md}`、`tools/generate_zhwiktionary_asset.py`、`THIRD_PARTY_NOTICES.md`、`README.md`、`ui/tests/reader-toolbar.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 旧萌典繁转简资产因禁止改作条款保持移除；现以固定哈希的中文维基词典/Kaikki CC BY-SA 4.0 快照生成 108848 个中中查询键，并保留 CC-CEDICT 中英释义。修正中文英文释义字段映射后，划词卡恢复“中文/英文”切换，生词本双语标签实机可见。3 项真实词典测试、400 项 Legacy UI、全目标 Clippy、许可证门禁、确定性生成/差异检查与 Release 整包构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/backup_next_split | P2 备份剩余低耦合边界审计 | `src/backup.rs`、`src/backup/`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 审计确认 catalog/retention/snapshot/restore/transaction-log 已覆盖现存无状态边界；剩余函数直接执行文件暂存/替换/回滚、durable manifest 恢复分支或 SQLite 独占关闭/重开，继续拆分会割裂同一原子事务，故未作无价值移动。36 项备份定向测试、全目标 Clippy 与差异检查通过。 |
| 未登记的既有修改 | 工作区存在多个历史未提交改动 | 以 `git status --short` 为准 | 注意 | 新会话不得假定这些文件可自由修改。 |
| /root/db_next_split | P2 SQLite 便携核心数据包纯规则拆分 | `src/db.rs`、`src/db/portable_package.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 核心数据包 JSON 格式、资源上限和校验已迁至无 SQLite/文件依赖模块；AppDb 保留查询、导入事务、WAL、备份与恢复生命周期。新增纯规则有效 v2 包与标准化敏感字段拒绝测试；24 项 db 定向测试、全目标 Clippy、格式和差异检查通过。 |
| /root/ai_next_split | P2 AI 阅读证据上下文纯规则拆分 | `src/ai_reader.rs`、`src/ai_reader/context.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已迁移无状态的证据上下文格式化、来源 ID 解析与引用充分性规则；修正上下文上限边界（包括末尾分隔符），新增独立 2 项测试。父模块保留命令、数据库、语义检索和 provider 编排，不改协议或 Tauri 签名。定向 60 项 AI 测试、全目标 Clippy、格式与差异检查通过。 |
| /root/reader_next_rules | P1 阅读器显式导航历史纯规则拆分 | `ui/{reader.js,reader.html,reader-navigation-rules.js,tests/reader-navigation-rules.test.cjs,tests/reader-toolbar.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 已把导航点规范化、去重、有界历史与分页消失判断移至冻结、无 DOM/存储/IPC 的经典脚本；外壳保留 DOM、计时、事件、命令和等价缺失脚本回退。未触及 EPUB/PDF 分页、选区、手势、PDF.js Canvas、设置或同步契约。聚焦 47 项和全量 392 项 Legacy UI、Node 语法及范围差异检查通过。 |
| /root/sqlite_preflight_immutability | P3 SQLite 只读预检副本不变性自检 | `scripts/test-sqlite-reader-db-preflight.sh`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 临时测试数据库现断言预检前后 SHA-256 一致，且前后均不存在 WAL/SHM sidecar；不修改 SQLite、同步或备份生命周期。`bash -n`、脚本自测和差异检查通过。 |
| /root/i18n_next_split | P1 i18n 资讯文案目录纯规则拆分 | `ui/{app-i18n.js,app-i18n-news-surface-catalog.js,index.html,tests/app-i18n.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 资讯页十语种纯 classic-script catalog 已在 `app-i18n.js` 前加载；兼容入口保留独立加载时的等价回退与目录形状校验。9 项 i18n 定向、390 项 Legacy UI、语法与差异检查通过。 |
| /root/shelf_next_split | P1 书架自定义滚动条纯几何规则拆分 | `ui/shelf-ui.js`、`ui/shelf-ui-rules.js`、`ui/tests/shelf-ui.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 无状态 thumb 比例、轨道点击和拖动映射已进入既有 `ReaderShelfRules`；外壳负责 DOM/拖拽状态，规则函数未加载时逐项走原公式。未改 `ui/index.html`、书架命令、契约或 React。25 项书架定向、390 项 Legacy UI、Node 语法和差异检查通过。 |
| /root/reader_startup_blocker | 阅读页启动阻断与关闭保险 | `ui/{reader.html,reader.js,reader-startup-guard.js,tests/reader-startup-guard.test.cjs}`、`ui/tests/reader-toolbar.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 实机根因是 `reader-settings-ui.js` 的全局函数与 `reader.js` 新拆分别名同名，macOS WebKit 在解析阶段报语法错误，导致 frame 停在 about:blank 且主关闭逻辑未安装。主脚本现直接通过 `ReaderJumpBackRules` 模块对象访问，已扫描阅读页 27 个经典脚本无其他顶层重名；启动守卫覆盖同步脚本、book_info、有效 EPUB/PDF URL 与引擎 ready，并提供原生关闭兜底。全量 388 项 Legacy UI 回归、Node 语法、差异检查及 Release 整包构建通过；已按规范覆盖 `/Applications/鲲鹏阅读器.app` 并实机验证正文、设置面板和关闭窗口。 |
| /root/sync_v5_desktop | P5 桌面 Rust sync v5 Axum wire 对齐 | `src/sync.rs`、`src/sync/client.rs`、`src/sync/protocol.rs`、`src/reader_backgrounds.rs` 与其内嵌测试 | 已完成，未提交 | v5 DTO 现在要求 camelCase 的 dataGeneration、嵌套 user、恢复元数据与资产续传偏移，拒绝 Python/snake_case 形状；push/pull/inventory/reconcile/资产和通用 sync 请求均使用 v5 header。32 项同步、2 项资产、全目标 Clippy、格式和差异检查通过。 |
| /root/news_legacy_rules_split | P1 资讯来源/内容纯规则拆分 | `ui/news-ui.js`、`ui/news-rules.js`、`ui/tests/news-rules.test.cjs` | 已完成，未提交 | 已抽取无 DOM/存储/IPC 的 HTTPS 与图片边界、结果包/预览判断、来源/Tieba 约束；现有资讯壳在规则脚本未预加载时保留等价内嵌回退，未改入口、React、样式或资讯命令。3 项定向规则、13 项资讯回归与全量 381 项遗留 UI 测试、语法及范围差异检查通过。 |
| /root/stats_legacy_rules_split | P1 统计日期范围与导航纯规则拆分 | `ui/stats-ui.js`、`ui/stats-rules.js`、`ui/tests/stats-rules.test.cjs` | 已完成，未提交 | 已抽取无 DOM/存储/IPC 的日期范围、锚点和导航边界；现有 stats-ui 在规则脚本未预加载时保持等价内嵌回退，未改入口、React、样式或统计命令。聚焦 16 项与全量 372 项遗留 UI 测试、语法及范围差异检查通过。 |
| /root/search_legacy_rules_split | P1 搜索结果纯规则拆分 | `ui/search.js`、`ui/search-result-rules.js`、`ui/tests/search-result-rules.test.cjs` | 已完成，未提交 | 已抽取转义、CJK 高亮与排序的无 DOM/存储/IPC 规则；独立搜索窗口保留等价内嵌回退，未改 `search.html`、React、搜索命令或 WebView 加载顺序。新增 4 项规则测试、相邻 4 项会话测试和全量 369 项遗留 UI 测试通过。 |
| /root/db_backup_port_split | P2 SQLite 备份端口边界拆分 | `src/db.rs`、`src/db/backup_port.rs` | 已完成，未提交 | 已抽取 AppDb 的 SQLite 快照与旧实体清理端口；`backup.rs` 的目录暂存、文件回滚和独占连接生命周期未改。`rustfmt --check`、21 项 db 定向测试、全目标 Clippy 与范围差异检查通过。 |
| /root/asset_sync_integration | 背景资产同步 v4 闭环 | `server/reader-sync-api-rs/src/sync.rs`、`src/reader_backgrounds.rs`、对应测试 | 已完成，未提交 | 桌面端 init/PUT/GET 使用 `/v1/sync/assets/*` 与协议 v4 头，init JSON 使用 `dataGeneration`；服务端重置 E2E 覆盖已完成资产、恢复历史和当日额度清理。服务端 40 项、桌面定向 2 项、两端 Clippy、格式和差异检查通过；未改 contracts、未连接或部署服务。 |
| /root/sync_v4_get_header_audit | P5 桌面同步 GET v4 请求头审计 | `src/sync.rs`、`src/sync/client.rs`、对应内嵌测试 | 已完成，未提交 | 审计确认账户通用 JSON 请求和资产 GET 已有路径受限 v4 头；补齐直连 `push`、`pull`、`inventory`、`reconcile` 请求，共用单一头名/版本常量。32 项 sync、2 项资产定向测试、格式、全目标 Clippy 及范围差异检查通过；未改认证、公共端点或 UI。 |
| /root/sync_client_split | P2 同步 HTTP 客户端传输边界拆分 | `src/sync.rs`、`src/sync/client.rs` | 已完成，未提交 | 已抽取带超时的 ureq client 与认证 JSON 请求/响应解码，保留既有端点、认证、v4 header 作用域和外层中文错误映射；32 项 sync 定向测试、全目标 Clippy、格式和范围差异检查通过；未改 contracts。 |
| /root/sync_commands_split2 | P2 同步薄命令适配边界拆分 | `src/sync.rs`、`src/sync/commands.rs` | 已完成，未提交 | `sync_get_settings`、注册/登录与安全/额度概览的 Tauri 适配编排已迁移；父模块保留领域、HTTP、账户切换和 DB 逻辑。Tauri handler 所需私有宏符号留在父模块的单行转发器，命令签名不变。18 项 sync 测试、全目标 Clippy、全仓格式检查和范围差异检查通过；未改 contracts。 |
| /root/sync_reconcile_split | P2 同步 reconcile 本地决策边界拆分 | `src/sync.rs`、`src/sync/reconcile.rs` | 已完成，未提交 | 已迁移 manifest 请求投影及服务端要求补传时的本地实体选择；父模块保留 HTTP、SQLite、数据迁移、账户恢复与公开命令。3 项 reconcile 定向测试、18 项 sync 测试、全目标 Clippy 与差异检查通过；未改 contracts。 |
| /root/sync_protocol_split | P2 同步协议纯数据边界拆分 | `src/sync.rs`、`src/sync/protocol.rs` | 已完成，未提交 | 已迁移无状态 wire 模型、库存摘要、游标和批处理；父模块保留账户恢复、HTTP 编排及 reconcile 流程。6 项协议定向测试、18 项现有 sync 测试、全目标 Clippy 与范围差异检查通过；未改 contracts。 |
| /root/sync_modularization | P2 同步 retry 低耦合边界拆分 | `src/sync.rs`、`src/sync/retry.rs` | 已完成，未提交 | 仅迁移重试策略、退避和诊断；未触碰账户恢复、协议语义、命令或 UI。`cargo fmt --check`、435 项 Rust 测试、Clippy 通过。 |
| /root | P3 同步 SQLite 访问边界收敛 | `src/sync.rs` | 已完成，未提交 | 登录、注册、设置、登出及需认证请求的短 SQLite 临界区已统一为 `with_db_read/write`，网络调用前释放锁；保留备份/恢复独占连接生命周期、协议与账户语义。31 项同步测试与 Clippy 通过。 |
| /root | P2 SQLite metadata 边界拆分 | `src/db.rs`、`src/db/metadata.rs` | 已完成，未提交 | metadata CRUD、设备 ID 与同步作用域已拆分；格式、21 项 db 测试和 Clippy 通过；不改变连接生命周期。 |
| /root | P3 应用命令 SQLite 边界收敛 | `src/app_commands.rs` | 已完成，未提交 | 翻译凭据读写改用 `with_db_read/write`，网络翻译前已释放 SQLite 锁；定向测试和 Clippy 通过。 |
| /root | P2 AI provider 低耦合拆分 | `src/ai_reader.rs`、`src/ai_reader/provider.rs` | 已完成，未提交 | 网络协议、端点、响应解析和脱敏错误已拆分；42 项 AI 测试和 Clippy 通过。 |
| /root/ai_error_types_audit | P2 AI provider 内部结构化错误审计 | `src/ai_reader/provider.rs`、其内嵌测试 | 已完成，未提交 | 传输、响应读取、HTTP 拒绝、JSON 解析及空回答已收敛为 provider 私有结构化错误；`call`/`call_async` 仍在模块边界输出原有中文 `Result<String, String>`，未触碰 Tauri command、父模块或契约。7 项 provider、42 项 AI 测试及全目标 Clippy 通过。 |
| /root | P3 数据命令 SQLite 边界收敛 | `src/data_commands.rs` | 已完成，未提交 | 数据包导入/导出改用 `with_db_read/write`；大 JSON 写盘在释放锁后执行，恢复排他连接生命周期未变。 |
| /root | P1 i18n catalog 拆分 | `ui/app-i18n.js`、`ui/app-i18n-reranker-catalog.js` | 已完成，未提交 | 保持兼容入口/API，纯 reranker catalog 已移出；51 项相关旧 UI 测试通过。 |
| /root/i18n_legacy_split2 | P1 i18n statistics catalog 拆分 | `ui/app-i18n.js`、`ui/app-i18n-stats-catalog.js`、`ui/tests/app-i18n.test.cjs` | 已完成，未提交 | 已新增图表、详情、热图的无 DOM/存储/IPC 经典脚本目录，并给兼容入口增加可选委托与严格形状检查；因 `ui/index.html` 被 `/root/single_ui` 占用，尚未加入实际 script 标签，现有 WebView 仍走等价内置回退。8 项 i18n 测试、Node 语法检查与范围差异检查通过。 |
| /root | P2 备份 snapshot 边界拆分 | `src/backup.rs`、`src/backup/snapshot.rs` | 已完成，未提交 | manifest 兼容性和完整性校验已拆分；父模块仍独占文件系统、SQLite 生命周期和恢复事务；28 项备份测试及 Clippy 通过。 |
| /root/backup_transaction_log_split | P2 备份恢复事务日志边界拆分 | `src/backup.rs`、`src/backup/transaction_log.rs` | 已完成，未提交 | 耐久 manifest/日志序列化、完整性刷新及加载路径/白名单校验已迁移；父模块保留文件暂存、复制/重命名、回滚编排和 SQLite 生命周期。30 项备份测试、定向 rustfmt、全目标 Clippy 和差异检查通过；全仓 `cargo fmt --check` 仍受任务外 `src/private_sync.rs` 格式差异阻断。 |
| /root/backup_restore_split | P2 备份恢复计划边界拆分 | `src/backup.rs`、`src/backup/restore.rs` | 已完成，未提交 | 已迁移无状态的恢复计划路径、事务日志计划投影、同目录约束、运行时投影名单与 SQLite 目标识别；父模块保留文件、SQLite 独占生命周期、复制重命名与回滚。33 项备份测试、全目标 Clippy、定向 rustfmt 与范围差异检查通过；全仓 `cargo fmt --check` 仍受任务外 `src/sync.rs`/`src/sync/protocol.rs` 格式差异阻断。 |
| /root | P5 Axum v4 合约缺口核对 | `server/reader-sync-api-rs/` | 已完成，未提交 | 已实现 feedback、密码变更、数据重置、密码找回、背景资产分块与 90 天实体恢复历史。恢复只记录实际接受的非密钥包版本，校验压缩内容、保留窗口前锚点；确认恢复生成墓碑、递增世代并撤销会话。数据重置会物理清空同步状态、资产和恢复历史。所有功能尚未部署。 |
| /root/postgres_rehearsal_tools | P5 PostgreSQL 演练受保护入口 | `server/reader-sync-api-rs/scripts/`、`server/reader-sync-api-rs/README.md`、`docs/testing/sync-api-v5-postgres-rehearsal.md` | 已完成，未提交 | 已增加离线迁移静态检查、显式破坏性确认/测试库前缀门禁，以及不连接数据库的拒绝路径自检；未执行真实 PostgreSQL E2E、部署或备份恢复。脚本自检、服务端测试和 Clippy 通过。 |
| /root | P2 AI history/retrieval 低耦合拆分 | `src/ai_reader.rs`、`src/ai_reader/history.rs`、`src/ai_reader/retrieval.rs` | 已完成，未提交 | 纯检索策略已拆分；Tauri commands、配置与网络调用仍留在父模块。已通过全量 Rust 测试、Clippy 及旧 UI 回归。 |
| /root/ai_commands_split | P2 AI 配置命令适配边界拆分 | `src/ai_reader.rs`、`src/ai_reader/commands.rs` | 已完成，未提交 | 配置档案的 6 个 Tauri command 已迁移为薄 State 适配器；父模块保留领域校验和持久化，命令名称/参数/返回值不变。`cargo fmt --check`、51 项 AI 测试、全目标 Clippy 及差异检查通过。 |
| /root | P2 SQLite entities 边界拆分 | `src/db.rs`、`src/db/entities.rs` | 已完成，未提交 | 同步实体查询/投影辅助已迁移；连接生命周期、schema 和 metadata 边界保留。已通过全量 Rust 测试和 Clippy。 |
| /root/db_migration_split | P2 SQLite 旧库紧凑迁移边界拆分 | `src/db.rs`、`src/db/migration.rs` | 已完成，未提交 | 旧 keyword 索引库的 checkpoint、快照校验和可回滚文件替换已迁入迁移模块；AppDb API、schema/entities/metadata、便携导入语义与备份恢复生命周期未变。`cargo fmt --check`、21 项 db 定向测试和全目标 Clippy 通过。 |
| /root/db_schema_split | P2 SQLite schema 初始化边界拆分 | `src/db.rs`、`src/db/schema.rs` | 已完成，未提交 | DDL、WAL/连接 PRAGMA 和 `dirty` 兼容列初始化已迁移；AppDb API、文件生命周期和备份/恢复编排未变。22 项 db 定向测试、全目标 Clippy、格式和范围差异检查通过。 |
| /root/sqlite_metrics_audit | P3 SQLite 锁等待与慢操作观测收敛 | `src/app_state.rs`、`src/db.rs`、`src/diagnostics.rs` | 已完成，未提交 | 已将锁等待、helper 锁内区间和有明确 SQL 边界的 AppDb 操作分为独立、带类型的脱敏诊断；不改变单连接/WAL 或备份恢复生命周期。定向 27 项测试、全目标 Clippy 与范围差异检查通过。 |
| /root | P4 EPUB/PDF 真实样本验收 | `packages/reader-engine/`、`packages/pdf-engine/`、`docs/testing/` | 待隔离 profile 人工验收 | 自动验收基线和仓库外 SHA-256/性能记录工具已完成；当前仓库不保存 EPUB/PDF 二进制，不能伪造真实打开、截图、计时或内存结果。已验证隔离配置能从空书架经真实 UI 导入受控 EPUB；Computer Use 不能按 PID 附着隔离进程，剩余阅读/PDF/性能/视觉验收须在直接启动的隔离进程中人工完成。 |
| /root/sqlite_explain_preflight | P3 授权大书库 SQLite 只读预检 | `scripts/sqlite-reader-db-preflight.sh`、`scripts/test-sqlite-reader-db-preflight.sh`、`docs/testing/sqlite-reader-db-preflight.md` | 已完成，未提交 | 只读预检要求显式授权确认、仓库外新建输出，并在 `-readonly` 与 `query_only` 双重门禁下运行 quick_check、聚合及固定 EXPLAIN 探针。报告只留汇总数与计划分类；脚本自检验证脱敏、拒绝路径与不修改临时 DB。 |
| /root/reader_legacy_split2 | P1 阅读器 AI 历史纯规则拆分 | `ui/reader.js`、`ui/reader.html`、`ui/reader-ai-history-rules.js`、`ui/tests/reader-ai-history-rules.test.cjs`、`ui/tests/reader-toolbar.test.cjs` | 已完成，未提交 | 历史 ID、墓碑合并、会话摘要和长度限制已迁至冻结、无 DOM/存储/IPC 依赖的经典脚本；外壳仅保留存储、同步与 DOM 编排。聚焦 50 项与全量遗留 UI 361 项测试通过；未改 EPUB/PDF、分页、选区、手势或 PDF.js Canvas。 |
| /root/shelf_legacy_split2 | P1 书架首屏封面加载纯规则拆分 | `ui/shelf-cover-loading-rules.js`、`ui/tests/shelf-cover-loading-rules.test.cjs` | 已完成，未提交 | 已抽取首屏封面数量、上下限及 eager/lazy 加载优先级的无副作用规则；`ui/index.html` 由 `/root/single_ui` 占用，故没有接入或改动 `shelf-ui.js`，避免引入未加载依赖。定向 26 项旧 UI 测试和 Node 语法检查通过。 |
| /root/axum_deploy_audit | P5 Rust 服务离线部署配置自检 | `server/reader-sync-api-rs/src/bin/`、`server/reader-sync-api-rs/scripts/`、`server/reader-sync-api-rs/{README.md,DEPLOYMENT.md}`、`docs/testing/sync-api-v5-postgres-rehearsal.md` | 已完成，未提交 | `config_check --offline` 只解析运行配置而不连接 PostgreSQL 或监听端口；拒绝路径覆盖缺少数据库、过短 HMAC、未显式允许的公网 bind。迁移静态检查、工具自检、32 项服务端测试与全目标 Clippy 通过；未执行真实 E2E、备份恢复、部署或切流。 |
| /root/sync_load_rehearsal | P5 受保护 PostgreSQL 幂等/限流负载演练 | `server/reader-sync-api-rs/{scripts,tests}/`、`docs/testing/sync-api-v5-postgres-rehearsal.md`、`README.md` | 已完成，未提交 | 新增独立确认词、测试库前缀及 URL 静默门禁；固定本地 Router 演练验证 12 个并发相同 mutation 只留一条实体/收据，以及匿名反馈第 4 次返回 429、只保存前三条。拒绝路径自检、2 项定向测试（未提供数据库时跳过连接）、全目标 Clippy、格式与范围差异检查通过；未连接、部署或接触外部服务器。 |
| /root/reader_sample_schema_check | P4 仓库外阅读样本记录校验 | `scripts/{validate,test-validate}-reader-sample-record.mjs`、`scripts/record-reader-sample.mjs`、`docs/testing/reader-pdf-*`、`package.json` | 已完成，未提交 | 记录升级为 schema v3，强制格式所需计时/视觉证据，独立校验器拒绝未知字段、路径/URL/内容泄漏及伪造的汇总数；全程不读取或提交真实 EPUB/PDF、路径、正文、截图或录像。`npm run test:reader-sample-record`、Node 语法检查和范围差异检查通过。 |
| /root/reader_prefs_rules_split | P1 阅读偏好颜色纯规则拆分 | `ui/reader-preferences-ui.js`、`ui/reader-preference-color-rules.js`、`ui/reader.html`、`ui/tests/reader-preference-color-rules.test.cjs` | 已完成，未提交 | 已提取冻结、无 DOM/存储/IPC 依赖的 Hex/HSL 规则；偏好页在脚本预加载时委托，在脚本缺失时保持等价内嵌回退。未触碰 EPUB/PDF、分页、选区、手势、PDF Canvas、样式或主窗口入口。定向 21 项、全量 380 项遗留 UI 测试、Node 语法与范围差异检查通过。 |
| /root/backup_split_audit | P2 备份目录册低耦合边界拆分 | `src/backup.rs`、`src/backup/catalog.rs`、对应备份测试 | 已完成，未提交 | 已抽取恢复点目录发现、状态投影、稳定排序与安全 ID/路径约束；父模块仍独占创建、轮转删除、恢复事务、文件替换、SQLite 生命周期与回滚。35 项备份定向测试、454 项全量 Rust 测试、全目标 Clippy、格式及差异检查通过。 |
