# 多会话进行中工作

此文件记录**尚未完成**且可能与其他会话冲突的工作。它不是工作日志；完成后将对应行移到提交记录或删除。

## 使用规则

1. 开始会修改多个文件、共享契约或原生层的任务前，先阅读本文件与 `git status --short`。
2. 在下表登记会话/分支、任务范围和预计触及的目录或文件；不要登记私密资料。
3. 已被占用的文件默认不得并行修改。需要协作时，先拆分到互不重叠的文件或由同一会话串行合并。
4. 完成后记录提交（或明确“未提交，原因”）和验证结果，再删除该行。
5. 影响协议的任务必须先完成 ADR 和 `contracts/`，不能只登记在此文件。

## 当前占用

| 会话/分支 | 任务 | 占用范围 | 状态 | 备注 |
| /root/github-release-ci | P0 修复测试版 1.1 GitHub CI 的 Rust Clippy 阻断并核对三平台发布边界 | `src/{ai_reader/intelligence_store.rs,newsnow.rs,pdf_support.rs,semantic/{profile.rs,vector.rs},translate/signing.rs}`、本表 | 已完成，待提交 | Windows CI 的 7 处 `chunks_exact_to_as_chunks` 已替换为等价固定数组读取；`cargo fmt --check` 和完整 `cargo clippy --bin ebook-reader-tauri --all-targets --no-default-features -- -D warnings` 已通过。未改发布门禁或创建公开资产。 |
| /root/clippy_reader | P1 阅读器 Rust Clippy 收敛 | `src/profile.rs`、`src/semantic/**`、`src/window_commands.rs`、`src/newsnow.rs` 与必要对应测试 | 已完成，未提交 | 修复 reparse-point 的 needless return、Qwen reranker 的 matches 表达式、资讯缓存 then/条件链、仅测试的资讯外壳 URL，以及测速参数聚合，未用 blanket allow；同步修复启动增强静态断言以对应当前主线程激活和 `background_work_allowed_at` 语义。`node --test ui/tests/startup-enhancement.test.cjs` 10/10、定向 `cargo fmt --check`、范围 `git diff --check` 通过。全量/定向 Rust Clippy 与测试暂被任务外 `main.rs` 的 host_inference_lifecycle cfg 导入错误及 `ai_reader/intelligence_store.rs` 测试缺失函数阻断，已通知协调会话。 |
| /root/clippy_core | P0 核心阅读器与 AI 存储 Clippy 收敛 | `src/ai_reader.rs`、`src/ai_reader/**`、`src/diagnostics.rs`、`src/epub_runtime.rs`、本表 | 已完成，未提交 | 清理无效转换、无必要复制、`let-else` 与生产路径中的遗留 queue skeleton；测试专用归档/队列投影收敛到 `#[cfg(test)]`，并将检索配置保存聚合为请求对象。为保留现有 Tauri flat invoke 兼容性，事件写入适配器使用窄范围、带理由的 `#[expect(clippy::too_many_arguments)]` 后立即转为请求结构。`cargo fmt --check`、范围 `git diff --check` 及 `cargo test --bin ebook-reader-tauri intelligence_store --no-default-features` 20/20 通过。完整 Clippy 仍受其它会话的 host/worker 模块告警阻断；本范围告警已清零。 |
| /root/beta-1-1-release-coordination | P0 测试版 1.1 版本收敛、文档、验证、源码推送与三平台测试构建 | `Cargo.toml`、`Cargo.lock`、`tauri.conf.json`、更新显示/测试、`CHANGELOG.md`、`README*.md`、`开发{记录,文档}.md`、`docs/{history,coordination}` | 已完成，待提交 | 已按已验证代码、Git 提交和协作记录整理 1.1；`scripts/check.ps1` 全量通过。不宣称服务端已部署、真实阅读样本已验收或公开二进制可发布。提交后仅运行 `publish_release=false` 的三平台临时构建，绝不绕过许可证门禁。 |
| /root/clippy_intel_host | P0 情报主机/worker 生命周期 Clippy 收敛 | `src/{intelligence_host/**,intelligence_worker_lifecycle.rs,intelligence_worker/**,host_inference_lifecycle.rs,host_inference_relay_client.rs,host_inference_crypto.rs,atomic_file.rs,secret_store.rs}` 及必要定向测试 | 已完成，未提交 | 已收敛 host/worker 生命周期、归档、受控 relay 和测试路径的范围内 Clippy；正式桌面仅保留真实配对，私有 relay 保持测试覆盖直到完整 claim/result/ACK transport 接线。host/worker Clippy、worker tests Clippy、relay 5/5、host 56/56、fmt 与范围 diff 均通过；worker 完整测试另有 processing 2 项既有行为失败，已报告协调会话。 |
| /root/update-discovery-about-inline | P1 自动更新发现与关于页内更新说明 | `apps/desktop-ui/src/legacy-ts/main-{app/app.ts,settings/about-ui.{ts,test.mts}}`、`ui/{index.html,styles.css,tests/update-ui.test.cjs}`、本表 | 已完成，未提交 | 已撤销虚拟 v1.1.0/v1.1.1 测试源；修复 WebView `pendingUpdateV1` 旧缓存复活：启动只显示本次成功检测到的真实新版本，成功检测为无更新时清理同版本旧缓存。静态 6/6、严格 TS 5/5、73 项生成物校验和 fast 构建通过；桌面快捷方式已覆盖并启动。 |
| /root/intelligence_client_consumption | P0 阅读器客户端正式资讯消费链：账号隔离缓存、SSE 游标、偏好/过滤、历史回源与阅读展示 | `src/intelligence_client.rs`、`apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.{ts,test.mts}`、`ui/{index.html,styles.css}`、本表 | 已完成 | 已完成账号/设备隔离缓存、游标持久化、正文及图片校验后 ACK、段落“注”阅读展示、分类/重要性/通知范围本地筛选与历史回源状态/重试界面；未登录不显示入口。补齐真实 API 的设备登记前置：刷新与 SSE 均先登记设备，才允许设备游标与 ACK。`cargo test --bin ebook-reader-tauri intelligence_client` 18/18 通过；隔离合成会话实测设备登记、feed、正文、图片 SHA-256、SSE 和 ACK，通过后已清理账号及关联记录。仅消费既有 `/v1/intelligence/*`；不改服务端、worker、host 或本机采集/全文回填/8B/27B 处理。 |
| /root/client_delivery_status | P0 阅读器正式资讯交付状态可视化 | `src/intelligence_client.rs`、`apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.{ts,test.mts}`、必要 `ui/{index.html,styles.css}`、本表 | 已完成，未提交 | 已补固定脱敏 delivery/SSE 状态、最近尝试/成功和 fetched/persisted/acked；明确仅显示服务端正式包，本机草稿或未配对内容不属于账号资讯，且客户端不探测主机状态。`cargo test --bin ebook-reader-tauri intelligence_client --no-default-features` 19/19、workspace UI 22/22、typecheck、legacy-ts verify、`cargo build --bin ebook-reader-tauri --no-default-features` 与范围 diff 检查通过；未改服务端。 |
| /root/smart_management_workspace_redesign | P1 智能管理三模型卡片内设置、手势说明与语义任务状态 | `ui/{index.html,styles.css,tests/semantic-model-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/{main-semantic/semantic-ui.ts,gesture-ui/gesture-ui.test.mts}`、`src/semantic/model.rs`、本表 | 已完成，未提交，已部署本机 | 已修复模型下载被误显示为“搜索库 100%”：模型下载显示独立的百分比、已下载/总量和启动核对阶段；Qwen 权重读取其受控运行时实际文件大小。搜索库进度在模型下载时隐藏，防止借用旧的完成百分比；“暂停建立”只在可断点建库时显示；“清除搜索库”在无任务且有索引时直接显示；共用的 Qwen 模型删除入口隐藏，避免无效灰按钮。语义模型选择框现只保留 Qwen 0.6B、Qwen 8B 和 BGE-M3，未删除已有本地模型文件或旧索引兼容代码。静态 UI 25/25、严格 TS 11/11、Rust model 7/7、`cargo fmt --check`、`git diff --check` 和 fast 构建通过；桌面目标已更新，SHA-256 为 `51CA77FFABD0BB1CDEF1D8FD23D9D30A280863044C97DC47D88B9B66CDEC89FF`。 |
| /root/reader_converted_disk_cache | P0 缓存外图书复用持久化繁简转换结果 | `src/{epub_runtime.rs,epub_runtime/cache_paths.rs,epub_runtime/chapter_cache.rs}`、`ui/tests/reader-open-performance.test.cjs`、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 保持最近 10 本与 64 MiB 内存预算不变；已处理章节的繁简结果按书籍、源文件代次、章节、缓存版本与转换模式写入本机磁盘，内存淘汰或软件重启后优先读取，首次未命中才转换。Rust EPUB 23/23、打开链路 18/18、typecheck、73 项生成物校验、格式和范围 diff 检查通过；fast 产物与桌面目标 SHA-256 均为 `AEB4FC46647EEC77ADD812315334D5BC14B44856ACB5E830C2F4F1B0245A7F70`，PID 4724 已启动且响应。不改视觉、排版、同步或跨端契约。 |
| /root/capacity_one_click | P5 Windows 一键可参数化直连隔离短测与 50 请求执行预算 | `AGENTS.md`、`server/reader-sync-api-rs/{src/config.rs,DEPLOYMENT.md,README.md,scripts/{capacity-k6.js,run-capacity-test.sh,run-capacity-test-windows.ps1,capacity-fixture-seed.py,capacity-direct-control.sh,capacity-monitor.py,test-*capacity*}}`、本表 | 已完成，未提交 | 本地交叉编译并一次性上传获批候选；2026-08-25 最新 `verified-candidate` 直连 HTTPS 50 VU/60 秒实测 83,922 请求且全部 200、无响应为 0，吞吐 1,398.70 请求/秒，P50/P95/P99 为 30.68/63.11/101.48 ms。客户端/主机/API/PostgreSQL CPU 峰值 84.23%/100%/65.32%/133.72%；结果仅为非容量链路短测。随后 50/75/100/150 VU 各 60 秒四档：首次在第 4 档 SSH 短暂断连且清理失败；重试时启动恢复已收敛旧状态、最终清理通过，但 150 VU 的远端监控只采到 59/60 个必需样本，故无正式 manifest。原始诊断显示各档 503 为 0/990/3893/15781，服务端 `request_queue` 的 full/timeout 背压在 75 VU 起出现，150 VU 为主导；所有档均无无响应、账户覆盖和 shard 认领完整，锁/I/O 等待为零、服务端可用内存约 2.36 GiB 以上。这些分档文件只能用于归因，不能作为容量结论。当前按用户要求将隔离测试服务的读/checkpoint/写执行槽从 12/18/10 调为 15/23/12，合计 50；等待位、200ms 过载保护与 48 条数据库连接预算不变。已重启隔离 `dev-test` 服务并从其进程环境回读三项值，服务为 active；后续一键脚本会维持同一预算。Windows 入口默认仍为 50x60，并新增 1–500 VU、30–300 秒单轮参数及最多 12 轮/总计 1200 秒的 `Rounds` 计划；多轮在一次账户 seed、直连准备和全局 flock 内逐轮运行并独立报告，全部完成后才统一清理和发布 manifest。远端监控以启动意图、互斥锁、取消标记及 PID/starttime 身份绑定收敛断链竞态，未确认启动也必须先取消后才能接受 identity 缺失。入口继续以 lease-bound guardian 执行全部 direct/fixture mutation，内层命令 120 秒硬截止、失锁即停载并等待实际 flock 释放后重获；fixture 以同一服务环境快照的数据库 URL 与 HMAC key 摘要绑定，目标漂移时在写 Token/SQL 前拒绝。真实 guardian 强杀故障注入已验证子进程持锁、自动收敛、旧租约隔离与重新取锁。k6 全局及逐请求都禁止重定向，避免直连测试静默转入反代。本轮 report/cleanup、账户归零、服务恢复、临时状态与全局锁释放均已核验；Caddy 未接入。 |
| /root/gesture_editor_inline_fields | P1 手势编辑器首组三字段单行排布 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、本表 | 已完成，未提交 | 名称、触发方式、适用范围已在桌面宽度排为一行，窄屏（≤520px）折回单列；字段、说明、精度控制和手势录制未改。独立结构校验、测试文件语法与差异检查通过。`node --test ui/tests/news-ui.test.cjs` 的新增断言通过，但整组另有两项既有断言受任务外生成脚本改动阻断。已本地 fast 构建、覆盖桌面快捷方式目标并实际打开手势编辑器确认该布局；运行库伴随文件校验通过。 |
| /root/gesture_hint_commits_action | P0 手势提示与松手执行一致 | `apps/desktop-ui/src/legacy-ts/{gesture-ui/{gesture-ui.ts,gesture-ui.test.mts},reader-gesture/{reader-gesture.ts,reader-gesture.test.mts}}`、必要生成物、本表 | 已完成，未提交 | 提示一经显示即锁定该可执行动作，松手优先执行锁定动作；取消、失焦仍不执行。提示在按住期间持续显示，松手后隐藏；主窗口也补采松手坐标。定向 22 项、TypeScript 类型检查、73 个 Legacy 输出构建与校验、运行库伴随文件校验和差异检查通过；完整前端构建仍被工作树中既有 `ui/index.html` 的源码策略变更拦截。已 fast 构建、覆盖并启动桌面快捷方式实际目标。未改配置或同步契约。 |
| /root/runtime_controller | P0 本机情报模型运行时切换、完整性校验与超时诊断 | `scripts/local-intelligence-{runtime,judge,retrieval-models,editor}.ps1`、本表 | 已完成，未提交 | 已加入分阶段状态与耗时、按角色进行完整 SHA-256 校验、27B 等待 GPU 显存释放；不改 host、worker、server 或 contracts。 |
| /root/page_turn_real_timing | P0 普通/跨章翻页真实位置更新与首绘计时 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-bug-trace,reader-page-layout-annotations}*`、`ui/tests/reader-bug-trace.test.cjs`、对应生成物与本表 | 已完成，未提交 | 保留原 520ms 最终状态检查；新增 `position_updated` 与 `first_paint`，跨章逐帧等待真实位置改变，快速连续翻页标记 `paint_superseded`。typecheck、28 项 TS、定向 UI、6 项生成物校验和主程序构建通过；未采集正文、路径或账户信息。 |
| /root/page_info_settings_popover | P0 页码与章节进度设置弹层、末项联动与阅读页 10px 间距 | `ui/{reader.html,tests/reader-preferences.test.cjs,tests/reader-toolbar.test.cjs}`、`apps/desktop-ui/src/legacy-ts/{reader-preferences-ui,reader-settings-ui,reader-shell,reader-i18n}/`、必要生成物与本表 | 已完成，未提交 | 已修复全书页码隐藏状态、子项全关自动关闭总开关及旧状态迁移，并恢复拖动排序与避让动画；typecheck、73 项生成物校验、10 项 TS 测试和 65 项 UI 测试通过，主程序已构建并实机核对。 |
| /root/dedupe_vector_index | P0 情报工作机跨来源正文去重与持久向量召回 | `src/intelligence_worker/{processing.rs,mod.rs}`、本表 | 进行中，未提交 | 规范化完整正文 SHA-256 以首见处理身份跨来源映射；ETag-only 更新或转载别名不重新进入 8B 队列。按 canonical body SHA + 0.6B model+artifact SHA 持久缓存 embedding，全库 canonical 条目稠密召回后仅重排前 64 条；正文样本含首/中/尾。已实机验证到 0.6B 重排→8B 判断，27B 可写入综合事件。当前收紧全文事实、复核和综合输出预算；综合预算按可用证据量动态伸缩并为 4K 本地上下文保留空间。 |
| /root/session2_server_delivery | P0 情报服务端生命周期收敛、正式切换与受控联调 | `server/reader-sync-api-rs/{src/{intelligence.rs,intelligence_archive.rs,intelligence_retention.rs,intelligence_object_outbox.rs,host_inference.rs,lib.rs},tests,scripts,migrations}`、`contracts/intelligence/`、`docs/testing/intelligence-deployment-readiness.md`、本表 | 正式 v5 已上线；等待会话 1 的 DPAPI 安全输入入口后签发短期 capability 并完成两份合成 fixture 联调 | 已实测 36 migrations、PostgreSQL 路由/主机状态机、MinIO durable outbox/Range/GC、备份恢复与旧候选迁移护栏；正式服务完成受保护备份、备份副本迁移、对象存储配置与切换，HTTPS 资讯 SSE 未认证请求已受保护。不得把测试库或回环候选直接挂入既有公开 `/v1/*`。只处理服务端发布、投递、留存、历史回源、对象存储与部署；不改本机采集/模型流水线或客户端 UI。 |
| /root/session2_archive_relay_windows_path | P0 受控历史回源 Windows 隔离 profile 长路径修复 | `src/intelligence_worker/archive_relay.rs`、直接 Rust 测试、本表 | 已完成，未提交 | outbox 改为由 job 与包摘要共同派生的稳定 128-bit 短名，避免 Windows 隔离临时根目录触及路径上限；定向 worker 5/5 和真实受控 relay 上传/下载哈希/ACK 通过。未改采集、模型、UI、服务端协议或凭据。 |
| /root/host_relay_fix | P0 host-inference relay V1 状态机、幂等与安全清理闭环 | `server/reader-sync-api-rs/src/host_inference.rs`、host relay 服务端测试、必要 migration/契约说明/本表 | 已完成，提交 `7e15f8a` | 仅处理私有密文中继：持久化幂等回执、CLAIMED/RUNNING/CANCEL_REQUESTED/FAILED、主机确认取消及安全失败、账号/主机隔离和 PostgreSQL E2E 均已验证；不改公开资讯、outbox、对象存储、本机模型或客户端 UI。 |
| /root/structured_citations | P0 结构化综合报道引用投影纯模块 | `src/intelligence_worker/synthesis.rs`、本表 | 已完成，未提交 | 仅新增无网络、无 UI、无磁盘副作用的 blocks→segments→noteIds 校验/投影和内嵌测试；模型只能给受控 sourceId/noteId、禁止 URL，mediaId 必须来自受控输入。6 项独立 Rust 测试与 rustfmt/diff 检查通过；不改 worker 调度、processing、publication、UI、服务端或 contracts。 |
| /root/client_media | P0 情报客户端源码恢复、正式图片下载与账号隔离缓存 | `src/intelligence_client.rs` 及直接 Rust 测试、本表 | 已完成，未提交 | 已从本机会话历史恢复被意外覆盖的未跟踪模块；正式包先经 schema/canonical SHA 校验，再按 Range 下载图片、核对 MIME/精确分块长度/SHA-256，写入账号+服务隔离 SQLite 后才 ACK。视频仅投影 HTTPS 链接、不下载；缓存重读会重验图片。15 项客户端定向测试、rustfmt 与差异检查通过；不改 UI、main、contracts、服务端或 worker。 |
| /root/object_store_real_e2e_gate | P0 真实 S3/MinIO 验收必须显式运行 | `server/reader-sync-api-rs/{src/intelligence_object_outbox.rs,tests/object_store_e2e.rs,scripts/run-object-store-e2e.sh}`、`docs/testing/intelligence-deployment-readiness.md`、本表 | 已完成，未提交 | 普通测试明确忽略真实对象存储；确认脚本在缺确认/缺受保护配置时拒绝，显式运行才执行 PUT、Range、删除与 durable outbox 晋升。rustfmt、定向测试、Clippy、脚本语法/拒绝路径及差异检查通过；尚未在受保护环境执行真实 E2E。 |
| /root/object_store_state_dualwrite | P0 情报图片对象存储状态注入与可靠双写 | `server/reader-sync-api-rs/{src/{state.rs,lib.rs,intelligence.rs,intelligence_object_outbox.rs,intelligence_object_store.rs},migrations/0032_intelligence_object_write_outbox_v1.sql,tests/{http_baseline.rs,postgres_e2e.rs}}`、本表 | 已完成，未提交 | 默认 disabled 保持 PostgreSQL bytea；S3 在启动时构造并注入，资产完成同事务落 PostgreSQL 与 write-outbox，后台成功 PUT 后才标记 S3 元数据，失败退避重试；现有 GC outbox 亦由同一 worker 删除。未改 archive 路径或部署。局部格式/diff 检查通过；全 server check 暂受并行 host_inference/intelligence_archive 未完成编译错误阻断。 |
| /root/intelligence_host_control_plane | P0 开发/处理机本地情报主机与可视化观察页 | `src/intelligence_host/{mod.rs,dashboard.rs,audit.rs}`、`src/bin/kunpeng-intelligence-host.rs`、`apps/intelligence-host/{dashboard.html,dashboard.css,dashboard.js}`、`scripts/{start-intelligence-dashboard.ps1,open-intelligence-management.ps1,install-intelligence-management-shortcut.ps1}`、`src/intelligence_worker/{archive.rs,mod.rs,collection.rs,content_archive.rs,processing.rs,publication.rs}`、本表 | 进行中，未提交 | 这不是阅读器客户端功能：开发/处理机独立运行 `kunpeng-intelligence-host`，显式 `--dashboard [port]` 或桌面启动器仅监听 `127.0.0.1`；桌面入口静默启动服务且只打开观察页，不自动采集或推理。页面同时提供按需、每次最多 24 条的逐条处理投影：标题/来源与抓取、全文、媒体、去重、向量/8B、27B、发布状态；不返回正文、URL、提示词/推理、向量或凭据。后台按采集→补全文→7B/8B→0.6B 召回/重排→27B→发布/中转处理；模型路由必须含真实文件 SHA-256。仍待真实27B、账号分发、PG/对象存储恢复和故障场景端到端验收。 |
| /root/host_loop_lifecycle | P0 独立情报主机持续处理控制 | `src/intelligence_host/{mod.rs,dashboard.rs}`、`apps/intelligence-host/{dashboard.html,dashboard.js}`、本表 | 已完成，已定向验证 | 独立 loopback 情报工作台已补持续处理的启动、停止、状态与跨进程互斥；不改阅读器客户端、worker 处理逻辑、服务端或契约。 |
| /root/triage_crash_idempotency | P0 8B 关系判定崩溃边界幂等恢复 | `src/intelligence_worker/processing.rs`、直接 Rust 测试、本表 | 已完成，待精确提交 | 在 8B 合法 JSON 收到后、关系 decision 入库前持久化受指纹、模型、提示词和输入摘要绑定的已校验判定；重启优先复用 staging，不记录正文、URL、凭据或原始模型响应。模拟 staging 后重启时 8B 调用为零，正式关系原子写入并删除 staging；损坏 staging 安全丢弃。未改采集、UI、服务端或协议。 |
| /root/fulltext_audit_telemetry | P0 全文补全健康聚合 | `src/intelligence_host/mod.rs`、`apps/intelligence-host/dashboard.js`、本表 | 进行中，未提交 | 仅从既有回填状态表读取等待、可重试、退避、受限主机和固定失败类别的聚合计数；不得读取/返回正文、URL、主机名、原始错误或凭据。 |
| /root/fulltext_queue_audit | P0 全文队列统计与调度口径审计 | `src/intelligence_worker/collection.rs`、`src/intelligence_worker/content_archive.rs`、`src/intelligence_host/mod.rs`、`apps/intelligence-host/dashboard.js`、本表 | 已完成，提交 `e4cb440` | 已确认“可回填全文”是有公开来源的总量，“当前模型待正文/历史缺正文”按模型状态分开统计；“可重试”仅表示文章级退避到期，仍可被来源熔断暂缓。修复熔断来源在批次截断后才过滤导致的空跑：现先过滤已熔断来源，再公平截断，仍在派发前复查。新增 32 个熔断来源后健康来源仍被选择的回归；不读取/显示正文、URL、主机名、原始错误或凭据。 |
| /root/fulltext_pipeline | P0 全文回填与永久内容归档可靠性 | `src/intelligence_worker/{collection.rs,content_archive.rs,archive.rs}` 及直接 Rust 测试、本表 | 已完成，未提交 | 正文按 BOM/换行/尾随空白规范化后再 CAS 与 SHA-256；旧文章缺 URL 时从永久 collection record 回退。回填失败持久记录安全分类、次数、下次重试、失败/成功时间，指数退避封顶 1 小时；图片仍本地 CAS、视频仍只存 HTTPS。collection 15/15、content archive 5/5、rustfmt/diff 检查通过。真实小批由既有 worker 完成：完整正文 2→9，1 条进入退避；不输出正文或 URL。 |
| /root/backfill_throughput | P0 历史全文回填吞吐与来源公平性 | `src/intelligence_worker/collection.rs` 及其直接 Rust 测试、本表 | 已完成，未提交 | 每轮候选由 8 提升至 32；先扫描 128 条就绪候选并按发布者轮转，最多 4 个不同发布者并发抓取，同一网络主机在一波中始终只有一个请求。单一来源仍顺序回填；公开 URL 边界、持久失败分类与每条指数退避不变。新增来源公平和同源不并发回归；worker 93/93 通过。 |
| /root/host_relay_pairing_commands | P1 桌面情报主机安全配对命令与 TS port | `src/{host_inference_lifecycle.rs,ai_reader.rs,ai_reader/commands.rs,main.rs}`、`apps/desktop-ui/src/legacy-ts/main-semantic/{semantic-port.ts,semantic-port.test.mts}`、本表 | 进行中，未提交 | 仅账户认证的 offer 创建、一次性邀请投影、确认/列表/撤销与预检；私钥、Bearer、配对 secret 不跨 WebView，不启用主机路线或任务派发。 |
| /root/secure_pair_ui | P1 Windows 情报 worker DPAPI 配对输入入口 | `ui/{index.html,styles.css,tests/sync-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、对应生成物与本表 | 已完成，未提交 | 仅在账户已登录后的“同步内容”页临时接收 HTTPS baseUrl、publish/relay capability 与登录启动选项；输入只单次传给既有 native `pair_intelligence_worker`，不写 localStorage/URL/日志，之后只显示脱敏状态与本地撤销。不得修改 server、host/worker 处理或契约。`legacy-ts` build/verify、同步 UI 定向 17/17、静态安全测试 18/18、`cargo build --bin ebook-reader-tauri` 通过；未安装覆盖主应用。 |
| /root/host_relay_capability | P1 桌面情报主机安全配置与运行时能力预检 | `src/{host_inference_lifecycle.rs,host_inference_crypto.rs,ai_reader/capabilities.rs,ai_reader.rs,ai_reader/commands.rs,main.rs}`、本表 | 已完成，未提交 | DPAPI 本机配对材料、仅已登录账户的公开能力协商、五分钟成功状态与 `intelligence_host_preflight` 已接线；未配对/不可达/能力或修订不匹配均禁用。未做 offer/claim UI 或实际明文任务执行。 |
| /root/host_relay_client_crypto | P1 桌面端主机推理 HPKE 信封与中继 HTTP 客户端基础 | `Cargo.toml`、`Cargo.lock`、`src/host_inference_crypto.rs`、`src/host_inference_relay_client.rs`、`src/main.rs`、本表 | 已完成，未提交 | RFC 9180 Auth HPKE typed 信封、canonical AAD、篡改/身份替换/跨任务与方向重放防护，以及 mockable transport 的密封任务提交、轮询/取回、取消与确认已完成。定向单测共 8/8 通过；不接 UI、capability、服务端或远程行为，不序列化/记录私钥、明文或 Bearer 凭据。 |
| /root/host_relay_worker_executor | P1 情报主机本地密文任务执行状态机 | `src/intelligence_worker/host_executor.rs`、`src/intelligence_worker/mod.rs`、`src/bin/kunpeng-intelligence-worker.rs`、本表 | 已完成，未提交 | 已交付仅本机、无 UI 的 HPKE 执行状态机和 transport trait 测试；远端执行仍关闭，待安全配对私钥持久化及 host-scoped 任务状态/撤销读取 API 接入后才可启用，不记录密钥、令牌或明文。 |
| /root/wire_news_preferences | P1 正式资讯缓存的本地偏好排序接线 | `apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.{ts,test.mts}`、`apps/desktop-ui/src/legacy-ts/main-semantic/{semantic-port.ts,semantic-port.test.mts}`、本表 | 已完成，未提交 | 只读取已登录账户的已校验正式发布缓存和本机收藏；路由关闭/不可用时不调用模型，评分仅排序不隐藏，分数本机可丢弃缓存。已通过定向 TS 测试和 typecheck。 |
| /root/client_sse_subscriber | P0 情报客户端受保护 SSE 订阅与安全缓存刷新 | `src/{intelligence_client.rs,main.rs}`、相邻单元测试、本表 | 已完成，未提交 | Rust 内部持有凭据/服务地址，页面不订阅且不触发模型或网络；严格三字段 wake-up、单调 cursor、账号/令牌变更停旧会话及 1/2/5/10/30s 退避。首次客户端定向 14/14 与 desktop check 通过；后续重复测试受共享 Rust incremental cache 崩溃影响，主会话用 `CARGO_INCREMENTAL=0` 复验。 |
| /root/intelligence_delivery_ui_wakeup | P0 已接收正式资讯的界面自动刷新补齐 | `src/intelligence_client.rs`、`apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.{ts,test.mts}`、本表 | 已完成，未提交 | 仅在 Rust 已完成账号隔离缓存、校验与 ACK 后发送无内容 UI 唤醒；页面只重新读取本地缓存，不订阅 SSE、不发起采集、模型请求或二次网络刷新。前端定向 23/23、原生客户端 19/19、typecheck 与 Windows fast 构建通过；已覆盖并启动本机验收版。 |
| /root/deployment_readiness_audit | P0 情报分发 V1 部署前离线门与真实环境验收清单 | `server/reader-sync-api-rs/scripts/{check-intelligence-deployment-readiness.sh,run-postgres-e2e.sh,run-postgres-backup-restore-rehearsal.sh}`、`server/reader-sync-api-rs/tests/postgres_intelligence_e2e.rs`、`docs/testing/intelligence-deployment-readiness.md`、本表 | 已完成，未提交 | 离线门明确 PostgreSQL bytea（无对象存储）并可用 `--require-object-storage` 硬拒绝；真实受保护 PG 时，runner 额外验证 intelligence schema/blob 列，备份恢复汇总覆盖正式包、图片、历史上传与SSE。离线门、契约、shell、拒绝路径、定向测试/Clippy、diff 通过；未连接、部署或接触远端。 |
| /root/worker_pipeline_migration | P0 情报 RAG/7B8B/27B/全文分块从页面迁移至 headless worker | `src/bin/kunpeng-intelligence-worker.rs`、`src/intelligence_worker/{mod.rs,processing.rs}`、本表 | 已完成，未提交 | 新增显式 `--process-once`/常驻 `--process-loop`：只处理永久档案中已 7B/8B 初筛保留且正文完整的条目。0.6B Embedding+Reranker 仅作候选召回，8B 输出关系 JSON；27B 分块提事实、按持久质量门全量/重大低置信/10%确定性抽检复核，并生成事件修订。模型缓存键包含输入哈希、模型、提示词、schema 与处理器版本；全部限 loopback，输出只含脱敏计数。worker 42/42、check、fmt、差异检查通过；不改 UI/服务端/契约。 |
| /root/worker_installation_lifecycle | P0 Windows 情报 worker 配对凭据与登录/关闭生命周期 | `src/intelligence_worker_lifecycle.rs`、`src/main.rs`、`src/bin/kunpeng-intelligence-worker.rs`、`src/intelligence_worker/mod.rs`、Windows 打包/测试文档、本表 | 已完成，未提交 | 仅本机：DPAPI 保护 publish/relay 配对凭据、可局部撤销、Run 登录启动与主窗口关闭后独立运行；`--service-loop` 每轮在 worker 内读取密文，使用 relay capability 且有 Windows mutex 单实例保护。无 UI/服务端/契约变更。worker 53/53、sidecar check 与范围差异检查通过；桌面 check 被任务外 `ai_reader` 缺失 `intelligence_local_model_preflight` 导入阻断。 |
| /root/intelligence_isolated_fixture | P0 Windows 合成资讯归档隔离与受控发布前置 | `src/{profile.rs,intelligence_worker_lifecycle.rs,bin/kunpeng-intelligence-{host,worker}.rs,intelligence_worker/mod.rs,intelligence_host/mod.rs}`、本表 | 已完成，未提交 | Windows 隔离 profile 已拒绝 reparse point、收紧隔离 DACL、向子进程转发后剥离参数、使用 profile-scope mutex（默认名称不变）、禁止隔离 profile 写登录启动，DPAPI 配置经 profile config 路径隔离。受控合成归档的发布、受权读取与 SSE、历史回源上传/下载 SHA-256 校验及 ACK 已完成；短期 capability、合成账户和测试数据均已撤销或清理。未读取或发布默认永久情报档案，未恢复客户端同步设置入口。 |
| /root/sse_stream_server | P0 情报账号隔离 SSE 投递通知 | `server/reader-sync-api-rs/{src/{intelligence.rs,lib.rs,middleware.rs},migrations/0029_intelligence_delivery_stream_v1.sql}`、相关测试、本表 | 已完成，未提交 | 新增 durable account+cursor delivery-event 表；publication 与对应唤醒事件在同一事务提交。Bearer + intelligence capability gate 后的 SSE 仅输出 deliveryId/cursor/kind，支持 Last-Event-ID/query cursor、15 秒注释心跳；无订阅注册表，断连即释放任务。路由绕开普通 15 秒请求超时且不占常规读槽；OpenAPI 与匿名 401 覆盖。服务端 intelligence 11/11、HTTP 定向 2/2、Clippy、fmt 与范围 diff 通过；不部署或接触外部服务。 |
| /root/legacy_ui_test_migration | P0 情报中心旧 UI 测试迁移为只读缓存行为 | `apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.test.mts`、本表 | 已完成，未提交 | 移除 37 个将页面误作采集/模型 worker 的历史测试，替换为正式缓存读取、显式刷新、空态/错误态、现有阅读器打开公开引用及只读命令边界；保留确定性证据归并、模型输出边界与质量门选择测试。定向 13/13、typecheck、Legacy TS 构建与差异检查通过。 |
| /root/worker_collection_ingest | P0 情报独立采集入永久档案 | `src/bin/kunpeng-intelligence-worker.rs`、`src/intelligence_worker/{mod.rs,collection.rs}`、本表 | 已完成，未提交 | 新增仅显式启用的 `--collect-once` 与严格绝对本地输入适配器；核心为无网络/无 UI 的可注入 collector port，永久记录 sourceId/GUID/归一化 URL/title/publishedAt/ETag/Last-Modified/status/language/batchId。相同来源+GUID+归一化 URL 且相同指纹只刷新采集元数据，不新建文章或队列；正文存在时入不可变正文/段落档案。无配置不读取、不写入、不领取队列，输出仅脱敏聚合。37 项 worker 测试、`cargo check --bin kunpeng-intelligence-worker -j 1` 与范围差异检查通过；未联网、未改页面、triage 或 relay。 |
| /root/smart_management_capability_ui | P1 智能管理能力路由首版 | `ui/index.html`、`apps/desktop-ui/src/legacy-ts/main-semantic/{semantic-ui.ts,semantic-rules.ts}`、其直接测试与本表 | 已完成，未提交 | 将技术性搜索方案改为自动能力路由；27B 只作为深度理解，不作为向量建库模型；底层仍原子切换标准/高精度/M3，且新库准备期间明确保留旧库服务。`npm run typecheck`、直接 TS 13 项、静态 UI 22 项、Legacy TS 构建与差异检查通过。 |
| /root/worker_archive_relay | P0 情报历史请求发布主机出站中转 | `src/bin/kunpeng-intelligence-worker.rs`、`src/intelligence_worker/{mod.rs,archive_relay.rs}`、本表 | 已完成，未提交 | `--relay-once` 只在显式启用和 HTTPS 发布服务器+本机 relay capability 凭据同时存在时运行；出站长轮询、claim、永久 catalog 按日期/事件/系列最小包、outbox 原子落盘与 SHA-256 复核、上传及 not-found/failed 状态均可注入测试。输出始终脱敏聚合，不改 UI、服务端、contracts、triage 或真实网络部署。33 项 worker 测试、`cargo check --bin kunpeng-intelligence-worker -j 1`、未启用实跑和差异检查通过。 |
| /root/client_intelligence_ui | P0 情报中心正式包本地缓存读取与显式刷新 | `src/intelligence_client.rs`、`src/main.rs`、`apps/desktop-ui/src/legacy-ts/main-news/intelligence-workspace-ui.{ts,test.mts}`、本表 | 已完成，未提交 | 新增 Rust-only 已校验包投影：仅当前账号+服务范围隔离缓存，读取时二次严格校验后才返回正式事件正文与公开引用，绝不返回 token、服务地址、缓存路径、投递状态或原始包。唯一既有情报中心打开仅调用缓存状态/内容命令，不联网；“刷新”才调用 native 拉取。未登录、无权限与空缓存均有明确中文提示；不改 `ui/index.html`/`styles.css`，不创建第二套 UI。Rust 定向 7/7、TS 定向 2/2、typecheck、仅本文件 rustfmt 与差异检查通过。 |
| /root/server_retention_cleanup | P0 情报正式内容 30 日过期与物理清理闭环 | `server/reader-sync-api-rs/{migrations/0027_intelligence_retention_v1.sql,src/intelligence_retention.rs,tests/intelligence_retention.rs}`、本表 | 已完成，未提交 | 已实现 PURGING→投递/图片引用→图片 mark-and-sweep→正式内容/草稿/收据删除→COMPLETE 的单事务步骤；只保留日期、类型、修订、包哈希、发布主机和运行计数。5 项无 DB 规则/SQL 静态测试、Clippy、fmt 和差异检查通过；不改 `lib.rs`/`intelligence.rs`，主入口接线由 `/root` 串行处理。 |
| /root/server_archive_calendar_heartbeat | P0 情报归档日历与发布主机心跳端点 | `server/reader-sync-api-rs/src/intelligence_archive.rs`、本表 | 已完成，未提交 | 新增 content-free calendar（仅日期/数目）与 relay credential heartbeat：校验有效、未撤销的 `intelligence:relay` 凭据及精确安装实例，使用 relay receipt 作幂等写且响应不含正文/选择器/安装 ID。6 项模块、69 项服务库测试、定向 Clippy、格式与差异检查通过；全量 Clippy 仍被任务外 `intelligence_retention.rs` 5 项 lint 阻断。`/root` 需串行接入路由与 OpenAPI。 |
| /root/client_distribution_cache | P0 情报分发 V1 桌面端受保护客户端缓存 | `src/{intelligence_client.rs,sync.rs,main.rs}`、相邻单元测试、本表 | 已完成，待主入口统一注册 | Rust 侧复用同步受保护 token 拉取 capabilities/feed/publication，逐包严格 schema/canonical SHA-256 校验后以服务地址+账号哈希隔离落盘，SQLite 成功提交后才 ACK。状态/刷新命令只返回聚合结果，不序列化 token 或服务 URL；由 `/root` 统一注册主入口，现有页面不会隐式触发网络。5 项定向测试、`cargo check --bin ebook-reader-tauri -j 1` 与差异检查通过。 |
| /root/reader_memory_audit | P1 阅读页伴读设定库第一版 | `src/{reader_media.rs,main.rs}`、`apps/desktop-ui/src/legacy-ts/reader-shell/reader.ts`、`ui/reader.html`、必要直接测试、本表 | 已完成，未提交 | 已按内容 ID（无则图书 ID）本地保存画风、负面提示词及人物设定，读取/保存均原子写入应用配置目录且不进入同步；阅读页“伴读设定”可编辑，手动与章节自动提示词均携带设定。定向 TS 7/7、Rust 2/2、`git diff --check` 通过。 |
| /root/archive_backup | P0 情报永久档案安全备份最小闭环 | `src/{intelligence_backup.rs,main.rs}`、对应直接测试、本表 | 已完成，未提交 | 显式现存第二磁盘/NAS 目录下创建唯一暂存快照，经 SQLite `quick_check`、源稳定性、逐文件 SHA-256 与目标文件哈希核验后同目录原子发布；拒绝相对、缺失、根/主目录和与档案重叠的目标，且不删除或覆盖源/目标既有内容。3 项直接测试与 `cargo check --bin ebook-reader-tauri -j 1` 通过。 |
| /root/intelligence_ui_readonly | P0 情报中心打开与刷新只读不变量 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、本表 | 已完成，未提交 | 已切断页面 load/refresh 对采集、快照保存、入队与模型命令的隐式触发，改为读取既有快照/审计并如实展示后台 worker 状态；定向 3/3、typecheck、差异检查通过。整份旧 UI 测试仍有约 30 条将 `open()` 视为旧采集/模型 worker 启动入口，与本只读不变量冲突，未在本任务范围内迁移。 |
| /root/reader_window_geometry_growth_fix | P0 Windows 主窗口与阅读页反复关闭重开尺寸/位置漂移 | `src/{main.rs,book.rs,window_commands.rs,diagnostics.rs,app_commands.rs}`、相关回归测试、本表 | 已完成，项目版与已安装版均已更新，未提交 | 阅读页外框物理矩形已稳定；进一步确认书架主窗口仍把逻辑客户区大小和外框坐标混合保存，在 Windows 200% DPI 的无边框窗口上导致边框/位置偏差累积。主窗口现保存/恢复完整物理外框，存在物理记录时不再预先用逻辑尺寸创建；问题记录新增主窗口启动恢复、显示、关闭保存及 50/250/800ms 原生读回。旧记录一次性安全收敛到 `(772,50) 3051×2100`，随后两轮完整退出/启动的保存、请求、立即恢复和延迟读回全部一致。窗口 21/21、诊断 11/11、`cargo check -j 1`、Fast 构建及三目标 SHA-256 校验通过，哈希 `069AC81B8202A78383F8A6DD3D0A81C8E926BC160C01D1A4514A69C448E40210`。不改视觉、阅读数据、同步或跨端契约。 |
| /root/intelligence_distribution_v1 | P0 情报分发系统 V1：ADR 与独立跨端契约 | `docs/adr/0036-intelligence-distribution-v1.md`、`contracts/intelligence/`、契约 fixture/测试、本表 | 进行中 | 固定唯一永久发布主机、30 日服务器热数据、不可变发布包、账号隔离投递与历史中转；不复用同步实体、不连接或部署服务器。 |
| /root/client_stream_audit | P0 情报桌面客户端 V1 审计与历史中转接收 | `src/intelligence_client.rs`、`src/main.rs`、`apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.{ts,test.mts},news-ui.ts}`、本表 | 已完成，未提交 | 正式缓存仍按 account+service hash 隔离、二次验签、落库后 ACK；补齐历史 calendar/request/status/download native 命令，临时包 SHA/schema/selector 验证及本地提交后 ACK。正式包保留 block→note 映射，在原有阅读器以行内“注”链接显示；原站打开失败仍保留已校验短证据。页面不触发 worker。服务端尚无 `/v1/intelligence/stream`，故没有伪造 SSE/轮询；需服务端先定义并实现 Bearer SSE 协议再接入。定向 Rust 11/11、TS typecheck、UI 13/13、diff check 通过；全目标 clippy 被任务外现有 lint 阻断。 |
| /root/archive_protocol_audit | P0 情报历史中转/发布上传协议 V1 审计与补全 | `server/reader-sync-api-rs/src/intelligence*.rs`、`server/reader-sync-api-rs/migrations/0028_intelligence_resumable_uploads_v1.sql`、服务端 intelligence 测试、本表 | 已完成，待 `/root` 串行接线路由/OpenAPI | 历史包改为 1 MiB 分块、可按返回 offset 续传、128 MiB 上限、SHA-256 完成校验；正式图片新增 25 MiB 分块暂存、哈希校验，并在 publication complete 同事务校验 MIME/长度、创建引用和延长到发布过期。ACK 后若同一共享 job 已无任何可下载账号请求立即清空正文（强于 5 分钟上限），其他账号仍可下载，未 ACK 24 小时路径保留。新增离线守卫；22 项服务库/5 项离线测试和 Clippy 通过。未改 `lib.rs`，未联网或部署。 |
| /root/intelligence_e2e_harness | P0 情报分发 V1 离线路由/契约验收审计 | `server/reader-sync-api-rs/tests/{http_baseline.rs,intelligence_offline_contract.rs}`、本表 | 已完成，未提交 | 新增离线守卫：所有 archive/relay 路由匿名请求立即 401（含 `jobs?wait=25`，不等待），OpenAPI 全端点 Bearer 声明；静态守卫验证无资讯能力时明确 403、过期/草稿不可读 404 路径、账号隔离与重复 ACK/请求 receipt、以及 25 秒独立 long-poll lane。定向 4/4 + HTTP 2/2、`npm run test:intelligence-contracts`、rustfmt 与差异检查通过；不连数据库、不部署。 |
| /root/quality_gate_validation | P0 情报 7B/8B→27B 复核质量门与审计度量 | `src/ai_reader/intelligence_store.rs` 的关系复核规则、SQLite 度量及直接单测、本表 | 已完成，未提交 | SQLite 持久化 full/sample 状态与 epoch；关系 `relation_accuracy` 已纳入白名单和真实计量。只有首 500 关系复核、总体/关系/重大召回/正向合并均 ≥97%、零误合并、JSON 合规且连续 3 个关系批稳定后才进入 sample；任意 `false_merge` 或 sample 期核心准确率 <95% 自动新 epoch 回 full。输出剩余全量数、稳定批、10% 最低随机抽检率与状态理由；19 项情报存储测试通过。未改 UI、服务端、worker 采集或契约。 |
| /root/citation_bundle_validation | P0 情报发布包事实引用与不可变性契约校验 | `contracts/intelligence/{README.md,intelligence-v1.schema.json}`、`contracts/fixtures/intelligence-publication-bundle.v1.json`、`scripts/test-intelligence-contracts.mjs`、`server/reader-sync-api-rs/src/intelligence.rs` 的 publish validator/直接单测、`src/intelligence_client.rs` 接收校验/直接单测、本表 | 已完成，未提交 | 已将 sourceSha256 + paragraph SHA-256 evidence、同 event note/media 可解析性、模型文本 URL 拒绝、HTTPS 视频、canonical SHA-256 与 append-only publication ID 接入契约、服务端发布校验及客户端缓存接收；契约、服务端 8/8、客户端 8/8、服务端 Clippy 与范围差异检查通过。未改 UI/路由/网络。 |
| /root/server_history_relay | P0 情报历史中转状态机与发布主机长轮询 | `server/reader-sync-api-rs/{migrations/0026_intelligence_archive_relay_v1.sql,src/{intelligence_archive.rs,middleware.rs},tests/http_baseline.rs}`、本表 | 已完成，未提交 | 账号隔离 ArchiveRequestV1 以同账号归一化请求合并，共用历史抓取 job 但各账号独立下载/ACK；完整 REQUESTED→…→PURGED 及 HOST_OFFLINE、NOT_FOUND、FAILED、REQUEST_EXPIRED 状态、7 日请求/5 分钟租约/24 小时临时包/ACK 后 5 分钟清除均已落库。发布主机仅接受独立 `intelligence:relay` credential；每个写端点使用域隔离幂等收据。`jobs?wait≤25` 使用模块内两槽 semaphore，middleware 不占普通 15 秒读槽。`cargo test --lib` 61 个库测试通过；`cargo clippy --lib -D warnings`、格式和差异检查通过。全目标 Clippy 被任务外 `intelligence_retention.rs` 的 const trait 错误阻断；HTTP 基线前 27/28 通过，任务外 password/reset DB 边界测试在本工具 30 秒单次上限内未结束。未连接数据库或部署。 |
| /root/server_delivery_assets | P0 情报分发 V1：账号偏好、投递/设备状态与鉴权图片资产 | `server/reader-sync-api-rs/{migrations/0025_intelligence_delivery_v1.sql,src/{lib.rs,intelligence.rs},tests/http_baseline.rs}`、本表 | 已完成，未提交 | 独立账号表与路由已具备：偏好读写、发布包 ACK、设备登记、由有效发布引用约束的图片读取（单段 Range）；所有客户端接口先做 Bearer/资讯权限校验，写操作按账号+操作+幂等键保存收据。服务端 58 单元、28 HTTP/OpenAPI、Clippy、契约测试与差异检查通过；未连接数据库或部署。`lib.rs` 已释放给历史中转串行接线。 |
| /root/intelligence_server_skeleton | P0 情报分发服务端第一批安全骨架 | `server/reader-sync-api-rs/{migrations/0023_intelligence_capability_v1.sql,src/{lib.rs,auth.rs,error.rs,intelligence.rs},tests/http_baseline.rs}`、本表 | 已完成，未提交 | 已新增独立 capabilities 路由；缺少 Bearer 为 401、无能力为 403 `INTELLIGENCE_ACCESS_DENIED`、有能力仅返回无内容的 V1 能力对象。账号字段由 migration 默认 false，且进入每次权威 Bearer 鉴权查询。3 项单元、2 项 HTTP/OpenAPI、全服务测试 53+26、Clippy、迁移静态检查与差异检查通过；未连接数据库或部署。 |
| /root/intelligence_server_publications | P0 情报分发服务端 V1 发布包与用户读取 | `server/reader-sync-api-rs/{Cargo.toml,migrations/0024_intelligence_publications_v1.sql,src/{lib.rs,credentials.rs,error.rs,intelligence.rs},tests/http_baseline.rs}`、本表 | 已完成，未提交 | 已提供独立草稿/正式发布/发布凭据/幂等表，及 init、complete、feed、publication 四路由；公共读取强制 `expires_at > now()`，发布包验证 canonical SHA-256 与精确 30 天有效期。普通用户 token 对发布返回 403；未完成草稿不出现在读取查询。4 项模块、27 项 HTTP/OpenAPI、服务端全量 55+27、Clippy 与差异检查通过（使用本机 Rust 1.97.1）；未连接数据库、未部署。 |
| /root/client_intelligence_routing | P0 客户端智能能力路由与增量阅读记忆 | `src/ai_reader{.rs,/reading_memory.rs,/commands.rs}`、`apps/desktop-ui/src/legacy-ts/reader-shell/{reader.ts,reader.test.mts}`、相关 Rust/TS 测试、本表 | 进行中 | 搜索核心保留 0.6B 检索组件，8B 用于智读/书库理解，27B 仅作为可选深度理解；新增只基于已读范围的本机阅读记忆，不改变同步实体、正文上传或现有唯一 UI。 |
| /root/capability_routes_backend | P1 智能管理本机能力路由持久化与 Tauri 边界 | `src/ai_reader/capabilities.rs`、`src/ai_reader{.rs,/commands.rs}`、`src/main.rs` 两个 handler、直接单元测试、本表 | 已完成，未提交 | 五项本机 capability（search/understanding/news_preference/deep_analysis/companion）仅写入本机 SQLite metadata；返回 auto/local/host/cloud/off 的可用性，当前 host 明确标为尚未接入远程推理而不可选。3 项直接测试、`cargo check --bin ebook-reader-tauri -j 1` 与范围差异检查通过；不改同步实体、契约、UI 或远程服务。 |
| /root/permanent_archive | P0 情报本机永久档案 SQLite 迁移最小闭环 | `src/ai_reader/intelligence_store.rs` 及其直接单元测试、必要时 `src/profile.rs`、本表 | 已完成，未提交 | 旧缓存库以 SQLite 一致快照迁入 `app_data_dir/intelligence-hub/catalog.sqlite3`，经完整性检查后同目录原子发布；旧库不删除，仅作首次迁移兼容读取源。重复启动保留已发布档案，遗留有效暂存文件可恢复发布。3 项迁移测试、17 项情报存储测试、`cargo check -j 1`、定向格式和差异检查通过；不改 UI、契约、服务端或主入口。 |
| /root/permanent_archive_worker | P0 独立本机情报后台工作节点骨架 | `src/bin/kunpeng-intelligence-worker.rs`、`src/intelligence_worker/{archive.rs,mod.rs}`、`src/ai_reader/intelligence_store.rs` 的档案路径接线、对应测试、本表 | 已完成，未提交 | 新 bin 仅复用永久档案路径/迁移模块，不加载桌面入口、UI、模型或网络；`--status` 只读聚合队列，`--once` 仅在显式环境开关后领取一条 30 秒租约。无配置/档案/队列均输出脱敏 JSON 并成功退出。`cargo check --bin kunpeng-intelligence-worker -j 1`、10 项 worker 测试、格式和差异检查通过；桌面全包检查受任务外 `src/ai_reader/reading_memory.rs` 的 `LowerHex` 编译错误阻断。 |
| /root/local_archive_media | P0 情报永久档案正文版本、稳定段落与媒体 CAS | `src/intelligence_worker/{archive.rs,mod.rs,content_archive.rs}`、本表 | 已完成，未提交 | `persist_article_content_at` 以文章记录指纹拒绝旧正文覆盖，永久写入 `blobs/text`、`blobs/html` 与 `blobs/images/sha256`；目录仅保存 SHA-256 扇出安全相对路径。正文/HTML 版本不可变，段落 ID 基于 article+规范化内容+重复序号稳定生成；保存原图及实际 640/1280 WebP 派生，视频仅允许无凭据 HTTPS URL。永久目录骨架已包含 blobs/indexes/archive/packages/outbox/audit；4 项媒体/版本测试加目录测试，worker 29/29、`cargo check --bin kunpeng-intelligence-worker -j 1`、定向 rustfmt 与差异检查通过。抓取阶段随后调用该 API；本任务不联网、不改 UI/服务端/契约。 |
| /root/worker_pipeline_transfer | P0 worker 单条初筛执行迁移 | `src/intelligence_worker/`、`src/bin/kunpeng-intelligence-worker.rs`、对应测试、本表 | 已完成，未提交 | `--once` 仅在显式启用且同时提供本机 7B/8B loopback 配置时领取一条；复用兼容 Provider 传输，严格校验单项 JSON，并原子写回 keep/filter。模型/响应失败会释放租约、递增尝试并按既有指数退避重排；未配置不领取。输出始终是脱敏聚合。23 项 worker/bin 测试、`cargo check --bin`、未配置实跑和差异检查通过。 |
| /root/cuda_provider_fix | P0 修复桌面快捷方式目标遗漏 ONNX CUDA Provider 伴随 DLL，恢复 CUDA 状态与本机运行组件加载入口 | `scripts/{build-fast,build-release,test-desktop-runtime-companions}.ps1`、相邻 GPU 测试与本表 | 已完成，未提交，已部署 | 根因是旧交付脚本仅复制 EXE，快捷方式目标旁没有 ONNX CUDA Provider；现从同一构建目录复制 `onnxruntime*.dll`、只清除该族陈旧伴随 DLL，并在交付后校验 CUDA/shared Provider 的 SHA-256。`cargo test semantic::gpu::tests` 7/7、实际本机 CUDA Provider 注册忽略测试、交付伴随 DLL 校验和 `git diff --check` 通过。已现场点击加载 Anaconda 的完整 CUDA 12/cuDNN 9 组件并重新检测，智能管理显示“GPU 就绪 · NVIDIA GeForce RTX 5070 Ti”。不改搜索切换、H3、同步或契约。 |
| /root/smart_settings_local_models | P0 智能设置二次改版：GPU 状态、搜索库直换模型、Qwen 27B 深度方案、本地 MiniMax-H3；删除快捷书单设置入口并恢复菜单栏收藏夹；书库问答、AI 图书标签与 AI 推荐书单支持 8B+ 本地模型 | `ui/{index.html,styles.css,reader.html,favorites.css}`、`apps/desktop-ui/src/legacy-ts/{main-semantic,reader-shell,main-favorites,library-ai,main-settings}/`、`src/{reader_media.rs,main.rs,semantic*,ai_reader*}`、本地运行脚本、相邻测试、必要生成物与本表 | 已完成 | 智能管理已收敛为“搜索与理解 / 阅读创作”两页；轻量方案已删除，智能搜索模型仅在主卡片切换，高级设置不再重复选模型；Qwen 27B 作为唯一回答与总结模型入口；MiniMax-H3 使用本地视频引擎，本地或云端大模型生成图片/视频提示词；GPU 状态明确区分显卡检测与 CUDA 推理组件加载。typecheck、定向前端 15/15、静态 UI 22/22、`cargo build --profile fast` 与实际界面验收通过；完整前端构建仅被本任务范围外的 `ui/reader-preferences.css` 既有视觉基线差异阻塞。桌面目标已覆盖并运行，SHA-256 `24069C4DA2B4FBD55E7A8D9AFD8210E2E51B29280A1ED88F070CA978A4F7E2BC`。不删除书单数据，不触碰同步契约。 |
| /root/favorites_interaction_followup | P0 收藏夹尺寸、双击多选与图书信息加入书单补齐 | `apps/desktop-ui/src/legacy-ts/{main-favorites/favorites-ui.ts,shelf-ui/shelf-ui.{ts,test.mts},main-book-info/book-info-organization.{ts,test.mts}}`、`ui/favorites.css`、必要生成物与本表 | 已完成 | 按用户最新要求把单击/双击判定调为 160 ms；首击同步发起 `prewarm_book`，立即准备续读章节、样式表与格式数据，160 ms 仅决定打开或多选。`typecheck`、7 项 typed、25 项 legacy UI、确定性生成物校验及 `cargo build --profile fast` 通过；桌面快捷方式目标已覆盖并运行，SHA256 `C5A3B043FDE834357D3C3A1A7E063E79986DA61D0A782CCA7BDC0D262AF5301E`。 |
| /root/favorites_unified_ui | P0 菜单栏统一收藏夹、书单快速加书与新闻收藏 | `apps/desktop-ui/src/legacy-ts/{main-favorites/,main-settings/{booklist-settings-ui,toolbar-settings-ui}.{ts,test.mts},shelf-ui/,main-book-info/,main-news/intelligence-workspace-ui.*}`、`src/app_settings*`、`contracts/{sync/entities.schema.json,fixtures/app-settings.v1.json}`、`docs/adr/0021-synced-app-settings.md`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 统一收藏夹运行时挂入唯一现有顶栏和信息浮层，不改被占用的 `ui/index.html`/`ui/styles.css`；书单详情支持全库搜索、多选添加，图书信息包含空书单；新闻仅本机保存最多 500 条、2 MiB 的安全元数据，综合报道按事件修订重开。收藏定向 69/69、Rust app_settings 14/14、typecheck、73 个 Legacy TS 入口构建/校验与 fast 构建通过；全量 typed 511/513，另两项为任务外问题记录 1 ms 时序断言和 Windows `spawnSync npx ENOENT`。已精确覆盖并启动快捷方式目标，SHA-256 `F598B223DC71FB3F8BDC046FBC1A940EB5988BA3C1FC38DAFB809DA28D43B45C`。 |
| /root/reader_layout_wait_optimization | P0 阅读首屏样式、字体、图片与后台预热竞争优化 | `src/{epub_runtime.rs,reader_protocol.rs,reader_protocol/html_resources.rs,window_commands.rs}`、`apps/desktop-ui/src/legacy-ts/reader-page-modules/reader-page-layout-annotations.{ts,test.mts}`、`ui/tests/reader-open-performance.test.cjs`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 只做不改变排版结果的优化：后台预热续读章顶层 CSS 原始字节，按书/mtime/路径做 10 本、4 MiB 有界缓存并计入 68 MiB 总预算；当前字体定向加载，仍等待正在加载的嵌套字体；正文 lazy 图片在分页前主动请求且仍等待固有尺寸；最近 10 本批量预热合并、延迟并在前台打开时让步，按真实 `last_read_at` 从新到旧处理。模块 TS 24/24、阅读性能与大章节 26/26、Rust EPUB 13/13、窗口 18/18、HTML 资源 6/6、typecheck、完整前端构建、`cargo check -j 1`、fast 构建及部署哈希核验通过；桌面目标 SHA-256 `54B1B3CB90C553F982931C5DBE39B224017E4DF2C6084CBB62622BA9AF38F314`。未改同步、契约、最终页数或续读语义。 |
| /root/reader_preload_benchmark_accuracy | P0 最近阅读缓存扩展到全格式并调整为 10 本 | `src/{app_state.rs,epub_runtime.rs,reader_protocol.rs,window_commands.rs}`、`apps/desktop-ui/src/legacy-ts/main-settings/reader-shell-preload-ui.{ts,test.mts}`、`ui/tests/reader-open-performance.test.cjs`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | EPUB/TXT/Markdown/MOBI/AZW 缓存解析结果和续读内容，PDF 做 4 MiB 首部与 256 KiB 尾部读前预热；统一最近 10 本、64 MiB 总内容预算，不增加阅读 WebView。设置页运行时删除旧的“保留最近 3 本书”说明和“仅 EPUB”标记。Rust 缓存定向 3/3、设置模块 5/5、打开性能 11/11、typecheck、73 个生成入口、完整前端构建、`cargo check -j 1`、fast 构建及哈希核验通过；桌面目标 SHA-256 `82ADFCEA4AFEB80DBD74D4A9C91BDBB2FD44C07AC7120AA9AA79B9C21EAB4FF2`。不改阅读数据、视觉布局、同步或跨端契约。 |
| /root | P0 情报中心全链路改版：7B/8B 小模型、Qwen3 新闻检索、持久队列、事件系列与审计 | `src/ai_reader{.rs,/commands.rs,/news_rag.rs,/intelligence_*}`、`apps/desktop-ui/src/legacy-ts/main-news/intelligence-*`、对应测试、`ui/{index.html,styles.css}`、必要生成物、本机模型运行脚本与本表 | 已完成；安装版已更新并现场验收，首次全量回填在后台继续 | 已建立本机情报 SQLite 和全量增量状态机：Qwen3-Embedding-0.6B 做中英 ANN 召回、Qwen3-Reranker-0.6B 重排、Embedding-8B 仅校准边界候选，Qwen3-8B 逐篇初筛并判定八类关系，Qwen 27B 分层抽检并按完整来源编辑综合报道；稳定事件、修订、系列、前情提要、内部历史链接和九阶段分页审计已接通。现场定位并修复超长 RSS 标题在逐篇初筛和关系判断入口阻塞整批租约的问题；审计 1–3 阶段固定使用全目录口径，失败状态、时间戳和 3 秒实时刷新均已核验。安装版复用 7413 条采集、7398 篇精确去重快照，决策从故障点 360 连续推进到审计页的 1644，随后后台续增至 1704；审计显示 7413 → 7398 → 1644、待处理 5754，未重新抓取或清零。前端定向 56/56、Rust 情报 37/37、类型检查、Rust 检查、源码策略、生产前端与 Windows Release 构建通过；桌面快捷方式目标已覆盖并启动，SHA-256 `CF7D60ABF97B3856ADA0CFFA92F48B6145102925B7D620997E31D00C9DBC04A6`。 |
| /root/shelf_recent_read_immediate_reorder | P0 最近阅读排序即时重排 | `src/window_commands.rs`、`ui/tests/shelf-ui.test.cjs`、必要生成物与本表 | 已完成，未提交，已部署桌面版 | 最新记录确认首次 pointerdown 已立即打开、`last_read_at` 同步更新；延迟来自 `shelf-book-read` 使用窗口定向事件而主窗口采用应用级监听。现改为应用总线，打开成功即在内存更新书卡并重排，不等待两秒后的书库写盘或下次焦点刷新。书架 25/25、主窗口 2/2、窗口 Rust 16/16 和差异检查通过；fast 构建已覆盖桌面目标，SHA-256 `D8FA54C0FF43AD5797284715A0EE7E2717D4CE6B3733C18B6F359CFC18597912`。不改视觉、同步或契约。 |
| /root/shelf_click_sort_runtime_fix | P0 书架首次单击打开与最近阅读排序实际生效 | `apps/desktop-ui/src/legacy-ts/shelf-ui/{shelf-ui.ts,shelf-ui.test.mts}`、`apps/desktop-ui/src/legacy-ts/main/shelf-ui-rules.{ts,test.mts}`、`src/{window_commands.rs,diagnostics.rs}`、`apps/desktop-ui/src/legacy-ts/main-settings/problem-trace-ui.{ts,test.mts}`、必要生成物与本表 | 已完成，未提交，已部署桌面版 | 鼠标主键在 `pointerdown` 直接打开并只抑制同一序列的派生 click，不再用时间窗口吞独立重试；旧排序按 v2 一次迁为最近阅读，之后尊重用户选择。原生窗口事件改走应用总线，Windows 约 1 秒内确认主窗口/WebView2 焦点并记录脱敏证据。前端 30/30、窗口 16/16、诊断 9/9、typecheck、Legacy 73 项构建/校验和差异检查通过；fast 构建已覆盖桌面快捷方式目标，SHA-256 `7C8056BA3A15B8B5598AD2C296E72CED4BD8B6CAE040E77A5F3EEBF13C7899C6`。新进程实时问题记录确认排序为 `read`、单击开启；Computer Use 未返回该自定义进程窗口，未自动点击书卡。不改视觉、同步或契约。 |
| /root/focus_restore_fix | P0 书架按最近浏览时间排序并补齐同书重开时间更新 | `src/window_commands.rs`、`apps/desktop-ui/src/legacy-ts/main/shelf-ui-rules.{ts,test.mts}`、`apps/desktop-ui/src/legacy-ts/shelf-ui/{shelf-ui.ts,shelf-ui.test.mts}`、`ui/tests/shelf-ui.test.cjs`、本表 | 已完成，未提交，待 /root 整合 | 无显式偏好时默认按 `last_read_at` 降序，显式其他排序仍保留；同书隐藏窗口与现存窗口重开均在成功后更新最近阅读并即时触发书架刷新。严格 TS 8/8、书架静态 25/25、Rust 窗口 15/15、typecheck、Legacy 73 项构建/校验、`cargo check -j 1`、格式与差异检查通过；全仓 Clippy 仅受任务外 `newsnow.rs`/`diagnostics.rs` 既有告警阻断。未改视觉、同步或契约。 |
| /root/reader_close_focus_same_book_stability | P0 阅读页关闭后的书架焦点、单击重开、同书定位与最近阅读排序稳定 | `src/window_commands.rs`、`src/diagnostics.rs`、`apps/desktop-ui/src/legacy-ts/{reader-shell,reader-page-modules,reader-trace,main-settings,main,shelf-ui}/` 内已分配文件、相邻测试、必要生成物与本表 | 已完成，未提交，已部署桌面版 | 已补齐关闭结算期间打开请求重放、Windows 阅读窗口前台唤起、同书锚点恢复、问题记录脱敏时序，并将无显式偏好时的默认排序改为最近阅读；复用隐藏/现存阅读窗口也会更新最近阅读时间。定向 TS 13/13、Rust 窗口 15/15、诊断 8/8、typecheck、Legacy 73 项和阅读页 6 项构建/校验、差异检查通过；fast 构建已覆盖桌面快捷方式目标并校验哈希，只运行新路径进程。不改视觉、同步或契约。 |
| /root/same_book_jump_fix | P0 同书隐藏窗口重开位置稳定与关闭结算请求重放 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-runtime.ts}`、`apps/desktop-ui/src/legacy-ts/reader-shell/reader.ts`、相邻测试与本表 | 已完成，未提交 | 锚点稳定恢复与临时保存抑制已完成；关闭结算期间到达的切书请求改为单槽最新意图队列，重复同目标不延长有效期，关闭完成后只重放一次，超过 3 秒、绑定失效或非关闭阶段忙碌请求明确丢弃。问题记录只保存阶段、结果、原因、序号和耗时。定向 reader-shell 5/5、相关 Legacy UI 90/90、typecheck、Legacy 生成物构建/校验和 diff 检查通过；不改视觉、同步或契约。 |
| /root/problem_trace_focus_relayout | P0 补齐关闭交接、书架首次输入与同书重排诊断 | `apps/desktop-ui/src/legacy-ts/main-settings/problem-trace-ui.{ts,test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-trace/reader-bug-trace.{ts,test.mts}`、本表 | 已完成，未提交，待主会话整合 | 已记录书卡 pointerdown/click 是否到达、关闭后原生焦点结果与文档焦点复核，并在阅读页检查点到达后重新合并主窗口时序，避免 reader-only 快照覆盖；同书恢复只接收固定焦点类别及 bounded viewport/page/anchor/resize 数值。定向测试 21/21、定向 ESLint、typecheck 与差异检查通过；未改阅读逻辑、视觉或契约。 |
| /root/shelf_single_click_restore | 恢复书架一次单击直接打开 | `apps/desktop-ui/src/legacy-ts/shelf-ui/{shelf-ui.ts,shelf-ui.test.mts}`、`ui/tests/shelf-ui.test.cjs`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 历史保存的双击偏好按版本迁移一次到单击默认，迁移后仍可显式切回双击。严格 TS 4/4、书架 25/25、类型检查、Legacy 构建通过；Computer Use 实机一次点击即打开“南明史”。同名旧安装进程已精确关闭，快捷方式目标运行中。 |
| /root/problem_trace_native_diagnostics | 将原生调试信息并入问题记录并移除 debug.log | `src/{diagnostics.rs,runtime_support.rs,main.rs}`、相邻测试、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 原生日志已改为内存中有界、脱敏、结构化事件环并通过 `runtime_diagnostics` 进入问题记录；不保存原始消息、路径、网址、凭据或正文。Rust 诊断 7/7、清理 1/1、`cargo check -j 1`、fast 构建通过；实机快照 schema 3 含 16 条原生事件，旧 `debug.log` 已删除且未重建。桌面目标 SHA-256 `2241118731D9B8E619C557FDDFCAC9009915E176861CAD66A5FCD0A6C6005F9C`。 |
| /root/reader_fast_graceful_close | P0 主窗口关闭前同步保存全部阅读窗口几何并缩短桌面退出等待 | `src/{main.rs,window_commands.rs}`、桌面 `close-kunpeng-reader.ps1`（仓库外）与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 主窗口关闭先同步采集/落盘所有绑定阅读页，再隐藏后台实例；脚本忽略 WebView2 无标题辅助窗口。桌面关闭脚本现同时精确识别桌面快捷方式指向的项目版和 Windows 标准安装目录中的安装版，避免从开始菜单启动后无法关闭；只处理通过完整路径校验的阅读器进程。Rust 窗口定向 10/10、fast 构建通过；最新端到端实测中 `.cmd` 成功关闭安装版，进程从 1 降为 0，4 个本地模型进程未受影响。SHA-256 `B6312B81CD5C2657829EE1B65DA98F28131FE13FE9FDC5F92A6E0CDE3A19B907`。 |
| /root/reader_graceful_desktop_close | P0 桌面关闭脚本等待阅读窗口保存 | 桌面 `close-kunpeng-reader.ps1`（仓库外）与本表 | 已完成，未提交 | 仅向快捷方式精确目标进程发送正常关闭请求；等待书库写盘及窗口隐藏，超时不强杀。PowerShell 语法与安全路径检查通过；未关闭当前阅读器进行自动实机验收。 |
| /root/reader_window_geometry_physical_restore | P0 记录并修复阅读窗口物理尺寸与位置恢复 | `src/{book.rs,window_commands.rs}`、`apps/desktop-ui/src/legacy-ts/main-settings/problem-trace-ui.{ts,test.mts}`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 记录脱敏的关闭采集、写盘、恢复请求/结果和显示后读回；恢复新记录使用物理像素并按保存矩形命中的显示器限缩。Rust 窗口定向 9/9、问题记录 9/9、`cargo check -j 1`、生成物校验和 fast 构建通过；快捷方式目标已覆盖并启动，SHA-256 `A2B253250E629FA107404FCC99E06B5EA66AE5ED0BDD4E735DEDAB882C77F948`。 |
| /root/server_private_ops_discovery | 让新会话自动发现鲲鹏服务器私密运维资料 | `~/.codex/private/kunpeng-reader.md`、`~/.codex/AGENTS.md`、个性化更新备注、根 `AGENTS.md` 与本表 | 已完成，未提交 | 真实主机、账号、密钥和远端路径只保存在仓库外；私密文件已补全新会话读取、安全 SSH 选项与禁止外传规则。全局、个性化更新备注和项目指令均已登记同一路径与保密边界；ACL 仍仅授权当前 Windows 用户。 |
| /root/reader_window_geometry_restore | P0 修复阅读窗口关闭后大小与位置未恢复 | `src/window_commands.rs`、相关回归测试、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 已确认 Windows WebView2 隐藏后可能回报默认位置；现改为窗口仍可见时采集几何、隐藏后落盘，EPUB/PDF 继续分离保存并在新建或池化窗口中恢复。`cargo fmt --check`、`cargo check -j 1` 通过；定向测试被任务外 `src/ai_reader.rs` 在 Release 完成后新增但未实现的测试命令导入阻断。已精确覆盖并启动快捷方式目标，SHA-256 `B462E5EE0295B435FEFD81A1CE8C093C178A30B132FE4C5B269291C8B548448F`。 |
| /root/reader_undo_event_delivery | P0 保证阅读页关闭事件进入主窗口撤销栈 | `src/window_commands.rs`、相关回归测试、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 最新记录显示阅读页关闭成功，但书架“上一步”最终 `no_match` 且无提示，证明撤销栈未收到阅读页。现已将关闭事件移到隐藏/书架焦点恢复之后，以应用级广播发送，并新增 `undo_checkpoint=sent/failed/missing_book` 诊断。回归测试 1/1、`cargo check -j 1`、`cargo fmt --check` 与 Release 构建通过；已精确覆盖并启动快捷方式目标，SHA-256 `0C32C1B72F8DB8BB9A583133D6A39DC0C2246AD8B838CF5D1777B9BA8A2B4030`。 |
| /root/reader_undo_hidden_reveal | P0 上一步恢复同一本隐藏阅读窗口 | `src/window_commands.rs`、相关回归测试、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 问题记录显示 `undo_last` 已匹配、`open_book` 已调用并进入 `same_book`，真正失败点是隐藏缓存窗口未回到前台。现已补全显示、取消最小化、任务栏恢复、阅读壳恢复、WebView/普通聚焦与 Windows 原生前台激活，并记录各步布尔结果。回归测试 1/1、`cargo check -j 1`、`cargo fmt --check` 与 Release 构建通过；已精确覆盖并启动快捷方式目标，SHA-256 `F5F175B8F38711E0879185666360F197F83B8DAD6B280B9F5267C130EC1F9B77`。 |
| /root/reader_undo_close_order | P0 关闭阅读页后“上一步”优先重新打开阅读页 | `apps/desktop-ui/src/legacy-ts/gesture-ui/{gesture-ui.ts,gesture-ui.test.mts}`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 收到阅读页关闭事件时先结算 MutationObserver 尚未处理的主窗口页面变化，再将阅读页压入统一撤销栈顶，避免旧子页面随后覆盖“最近一步”。定向测试 13/13、类型检查、ESLint、Legacy 构建与生成物校验、Release 构建通过；已精确覆盖并启动快捷方式目标，SHA-256 `305F6D84A0A3231FDDBAC89EBB06D533AF83E7131577821DFDB3D3765CBD1672`。 |
| /root/reader_gesture_window_route_fix | P0 修复阅读手势关闭命令取错原生窗口 | `src/{window_commands.rs,main.rs}`、相关回归测试、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 初版 `Window` 取到宿主主窗口，首修 `WebviewWindow` 又在实机参数注入阶段返回 `failed_window`；最终改为 Tauri 保证可从当前 IPC 注入的 `Webview`，以 WebView 标签区分主界面/阅读页，再操作其宿主原生窗口。回归测试 1/1、`cargo check -j 1`、`cargo fmt --check` 与 Release 构建通过；已精确覆盖并启动快捷方式目标，SHA-256 `515060D6E4D3A003BCBFCC139A4D7733279BF8346CA9390BA149160995C5B126`。 |
| /root/reader_gesture_execution_trace | P0 补齐阅读手势提示、结束判定、执行路由与结果诊断 | `apps/desktop-ui/src/legacy-ts/reader-gesture/{reader-gesture.ts,reader-gesture.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}`、必要生成物与本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 同一手势编号已串联开始、提示、松手最终判定、执行路由与布尔结果；只记录动作、分数、阈值、采样点数、耗时和历史类型，不记录轨迹坐标或自定义手势名称。定向测试 14/14、类型检查、ESLint、Legacy 构建与生成物校验通过；Release 构建完成并精确覆盖快捷方式目标，SHA-256 `C26419EB42B516492296FEFDCAA4AF00690492FDAC719CF35EA23EC575F5FED3`。未改契约。与滚动分页任务仅在白名单集合窄范围合并，保留其已有几何字段。 |
| /root/reader_close_gesture_reliability | P0 阅读页即时关闭、返回手势稳定性与问题记录保护 | `apps/desktop-ui/src/legacy-ts/reader-shell/{reader.ts,reader.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-gesture/{reader-gesture.ts,reader-gesture.test.mts}`、`src/{app_commands.rs,window_commands.rs}`、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 阅读窗口先隐藏，再由缓存 WebView 在 420 ms 内确认位置并后台写盘；原生命令也先隐藏、激活书架再保存几何和统计。手势结束若仅末尾轻微漂移，会沿用最近 8 个采样点内已显示的有效识别。空白缓存页不再覆盖两分钟内的有效脱敏记录。定向前端 10/10、Rust 全量 596/596、类型检查、ESLint、Legacy 输出校验与 Release 构建通过；全量 TypeScript 434/436，另两项为既有 Node 26 `spawnSync npx ENOENT`。并行 Release 在同一源码与已验证生成物上完成后覆盖并启动桌面快捷方式实际目标，最终 SHA-256 `30BD770273DFAC788252F78D5789187AAC4F17A8F89221C9C586D3B1371A7DB5`。 |
| /root | P0 情报事件判定器与可审计处理流程 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,intelligence-event-decision-rules.ts,intelligence-event-decision-rules.test.mts,intelligence-audit-ui.ts,intelligence-audit-ui.test.mts}`、`ui/{index.html,styles.css}`、`src/{ai_reader.rs,ai_reader/commands.rs,ai_reader/provider.rs,main.rs}`、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 规则/RAG 只召回候选；硬主体、股票代码和财报期冲突直接拦截；本机可选小模型逐对判定，Qwen 再按全文证据复核并写综合报道。完整关联组才合并，未判定/失败一律保留独立事件。判定及正文均按稳定指纹缓存，增量刷新只处理新内容；“处理审计”页展示六阶段公共元数据，不含正文、网址、路径或密钥。定向前端 39/39、`npm run typecheck`、`npm run desktop-ui:build`、`cargo test intelligence_ -j 1` 15/15、`cargo check -j 1` 通过；已覆盖启动桌面快捷方式实际目标，SHA-256 `9854ACF13B07291E749F177A4C594FB39F277063B5827FF499C2D243AC876C32`。 |
| /root | P0 情报处理审计流程工作台重设计与点击性能修复 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-audit-ui.ts,intelligence-audit-ui.test.mts}`、`ui/{index.html,styles.css}`、`scripts/frontend-source-policy.json`、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 保持唯一既有审计页面，改为分阶段流程总览与按需加载的单阶段详情；打开时固定渲染 6 个流程节点和当前阶段前 10 条记录，继续展开才加载后续条目。审计定向 2/2、相关情报 39/39、类型检查、前端源码策略及完整构建、Tauri NSIS 构建通过；已覆盖启动快捷方式实际目标，SHA-256 `8DECF2288AA720A2C69D36418B435E61655D25F792D0E87E266998609F2FBD2E`。不触及资讯处理、模型、同步或原始内容。 |
| /root/article_enrichment | P0 情报候选正文与媒体本机增强 | `src/newsnow.rs`、`src/main.rs`、本表 | 已完成，未提交 | 新增逐来源正文/媒体增强命令：清洗公共正文、首图本地数据 URL、图片/视频 URL 和有界应用缓存；拒绝私有目标及重定向，单源失败退回 RSS 证据。`cargo test newsnow::tests:: -- --nocapture` 45/45、`cargo check -j 1` 通过；不改同步、书库、备份或远程服务。 |
| /root/source_links | P0 简报引用来源的阅读器内跳转 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、本表 | 已完成，未提交 | 受 /root 委派。综合报道末尾仅为已校验 HTTPS 来源生成可点击锚点；prepared-article 阅读器拦截标记后复用现有 `newsnow_open_article` 内部打开链路，不调用外部 `open_url`。情报工作区 30/30、类型检查、Classic 产物构建和校验通过；完整 typed 门禁另有两项任务外 `spawnSync npx ENOENT`。不改来源内容、原生 WebView、同步或外部浏览器行为。 |
| /root/news_rag | P0 情报候选本地语义召回与聚类 | `src/ai_reader/news_rag.rs`、`src/semantic.rs`、`src/ai_reader.rs`（仅模块接线/调用点）、本表 | 已完成，未提交 | 提供 2–120 条公开候选的独立、本地、纯内存向量召回与稳定事件分组接口；不读取或写入书库 shard/HNSW/缓存、书籍正文、同步或备份。含输入上限、日期/词面防误并组及纯算法回归测试；`rustfmt --check` 与差异检查通过。定向 cargo test 暂被并行 `src/newsnow.rs` 的任务外 SHA-256 格式化编译错误阻断。 |
| /root | P0 情报中心简报直接展示聚合结果 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/styles.css`、`ui/tests/intelligence-workspace.test.cjs`、`scripts/frontend-source-policy.json`、本表 | 已完成，本机 release 已更新，未提交 | 简报卡片、详情与来源列表均不再提供原文跳转；只展示本机模型对多来源证据的合并标题、摘要、重要性、理由和来源数量。普通资讯列表仍保留既有打开能力。专项 TS 28 项、静态 UI 2 项、ESLint、类型检查、前端构建、来源策略和差异检查通过；Windows release 已重启。未改同步、书库、备份或远程服务。 |
| /root | P0 情报中心每日 20–30 条简报与历史回顾首版 | `src/ai_reader{.rs,/commands.rs}`、`apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,intelligence-digest-history-rules{,.test}.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，本机 release 已更新，未提交 | 当日规则候选稳定取前 25（范围 20–30），本机 Qwen 按两条事件一批完成可追溯编辑；每天本地固化一份快照，历史支持只读回顾，保留 90 天，不进入同步、备份、书库或远程服务。定向 TS 30 项、Rust 3 项、Rust 全量 592 项、完整 Legacy UI 427 项、前端生产构建、Clippy/format/类型检查均通过；总 typed 门禁仅受当前 Node 26 对两条既有测试 `spawnSync("npx")` 的 ENOENT 阻断。 |
| /root | P0 本机 Qwen 27B Q3 情报全链路验收 | 仓库外本机推理运行时/模型、`src/ai_reader.rs`、`apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、必要本机配置、本表 | 已完成，未提交 | 已部署仅回环 Qwen3.8-27B-UD-Q3_K_XL（SHA-256 已核验）、全层 GPU、Flash Attention、Q8 KV 与 MTP；候选模型服务能接受既有 Qwen3.5 配置名并回报实际模型名。简报提示词限制为最多两条且字段短小，避免 384 token 截断；固定公共候选经实际 OpenAI 兼容请求验证为 UI 可解析 JSON。新 release EXE 已替换并重启；Rust 简报边界 2 项测试和 release 构建通过。不改书库、同步实体、远程服务或模型权重。 |
| /root/github_desktop_packages | 删除 README 的 Android 说明并修复桌面三端构建产物 | `README{,.en}.md`、`package.json`、`server/reader-sync-api-rs/tests/postgres_e2e.rs`、GitHub Actions 构建与发布产物、本表 | 进行中，未提交 | 仅保留 Windows、macOS、Linux 项目说明；修复干净检出环境的生成物前置步骤及固定 Rust 工具链 Clippy 门禁。 |
| /root/intelligence_toolbar_settings | 菜单栏设置加入情报中心 | `docs/adr/0021-synced-app-settings.md`、`contracts/{sync/entities.schema.json,fixtures/app-settings.v1.json}`、`src/app_settings{.rs,/rules.rs}`、`apps/desktop-ui/src/legacy-ts/main-settings/{toolbar-settings-ui.ts,toolbar-settings-ui.test.mts}`、`ui/tests/toolbar-settings-ui.test.cjs`、本表 | 已完成，未提交 | 既有情报中心入口已纳入菜单栏完整排序与显示/隐藏设置；旧八项设置自动在资讯后补齐。定向 TypeScript 3 项、Legacy UI 5 项、Rust 14 项、类型检查、ESLint、契约 fixture、完整前端与 macOS Release 构建、覆盖安装及实机验收通过；全量 typed/legacy 与 Clippy 仍受任务外已登记旧脚本等价测试、同步旧断言和 `private_sync.rs` 告警阻断。 |
| /root/intelligence_snapshot_incremental_and_article_fallback | P0 情报快照续跑/增量更新与外部正文显示修复 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、`src/{newsnow.rs,main.rs}`、`ui/tests/{intelligence-workspace,news-ui}.test.cjs`、本表 | 已完成，未提交 | 情报中心首次采集每批会写入受限的本机应用缓存；首次中断可续跑，完成后打开先显示综合简报，只轮换一批来源增量更新。快照不进入书库、备份或同步。外部网页在导航开始或 700ms 兜底后显示，避免永久停在加载态；定向 Rust/TypeScript/静态测试、完整前端构建、macOS Release 覆盖安装和启动通过。 |
| /root/news_article_loading_fallback | P0 外部资讯原文白屏期间保留可返回交互 | `src/newsnow.rs`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`ui/tests/news-ui.test.cjs`、本表 | 已完成，未提交 | 外部子 WebView 完成加载前及后续跳转加载中保持隐藏，让主阅读页的返回、Esc 与手势始终可用；加载完成才显示网页。Rust 定向测试、TypeScript、前端完整构建、来源页专项与 macOS Release 覆盖安装启动通过；一项旧资讯预取断言仍与其他会话已改的抓取接口不匹配。 |
| /root | P0 修复资讯二次打开导航串行与情报快照增量续用 | `src/newsnow.rs`、`apps/desktop-ui/src/legacy-ts/main-news/{news-ui.ts,intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/tests/{news-ui,intelligence-workspace}.test.cjs`、本表 | 已完成，本机 release 已更新，未提交 | 子 WebView 关闭时只隐藏、后续只提交一次外站导航，消除本地壳/外站的串行竞争；主阅读器加载壳立即可见。情报快照接受来源重排、按来源有界持久化且保存失败明确提示；同日完整快照仍只轮换一批增量来源。TypeScript 29 项、新闻 UI 21 项、Rust 定向、类型检查、前端产物与 Windows Release 启动通过。未改契约、同步或视觉。 |
| /root | P0 资讯二次打开问题记录时序补全 | `apps/desktop-ui/src/legacy-ts/{main-settings/problem-trace-ui.ts,main-news/news-ui.ts}`、相邻测试、本表 | 已完成，本机 release 已更新，未提交 | 记录资讯 WebView 的点击、原生命令、导航开始/完成与关闭耗时；只保存脱敏阶段、结果、序号和耗时，不记录网址、标题、正文、路径或凭据。资讯时序在 300ms 合并后写入本机最新问题记录，便于复现后直接读取。类型检查、6 项定向记录测试、前端产物校验与 Windows Release 启动通过。 |
| /root | P0 资讯预加载 WebView 释放上次导航 | `src/newsnow.rs`、相邻测试、本表 | 已完成，本机 release 已更新，未提交 | 脱敏时序显示点击和 IPC 均低于 35ms；慢点发生在复用 WebView2 的外站导航启动前。关闭或切换时均中止上一次未完成页面加载，防止其继续占用同一预加载窗口；不记录或输出外站 URL。Rust 格式检查、两项原生定向测试与 Windows Release 启动通过。 |
| /root | P0 资讯返回后交互卡顿与索引扫描降载 | `src/{search.rs,library_commands.rs,startup.rs}`、`apps/desktop-ui/src/legacy-ts/main-news/{news-ui.ts,intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、相邻测试、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 已降低启动索引扫描优先级并补齐脱敏资讯时序。最新复现确认原生关闭命令仅 8–16ms；根因是从正文返回时，情报中心对仍在内存中的完整快照重复执行整条 load 链路并在主线程重建简报。现在完整快照已水合时直接恢复现有 DOM/状态，首次进入或用户显式刷新才加载/增量更新；未完成采集仍保持续跑。情报工作区 29 项定向测试、类型检查与静态 UI 2 项通过；Release 已按桌面快捷方式实际目标覆盖并启动，SHA-256 `8A9A06F92AD47F3C27523DD8DCC842ACB5B3C6D192F18C74AE3ECF08B85EC941`。 |
| /root/interstellar_progress_v1 | P1 情报中心星际旅行进度首版 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、必要生成物与本表 | 已完成，已提交（55f1a20d） | 在现有唯一情报中心中扩展第四种布局；首版展示 17% 人工基线、三项支柱、六类瓶颈、人类/AI 归因、速度快照规则，并对当前资讯执行带航天技术门槛的候选筛选，不自动把新闻计入进度。类型检查、8 项定向测试、完整前端构建、macOS Release 覆盖安装和实机验收通过；全量 typed 测试 394 项仅有其他会话的新闻规则等价测试 1 项失败。 |
| /root/reader_chapter_opening_snapshot_v10 | P1 预制相邻下一章章首过渡快照，减少跨章首屏等待 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 仅在 macOS 滚动兼容分页下复用已预取正文制作非交互的章首临时快照；真实章节仍按现有精确分页落地，设置、视口和章节键变化自动失效。TypeScript 专项 24/24、Legacy 阅读器回归 62/62、typecheck、ESLint、完整前端构建、macOS 覆盖安装与签名验证通过。 |
| /root/reader_latency_range_chunks_v9 | P0 撤回会造成正文重叠的跨章首屏 Range 分段测量 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 用户实机发现文字片段重叠；已立即回退至已验证的逐行源偏移测量，不保留该优化路径。29 项 TypeScript 阅读器专项、typecheck、ESLint、完整前端构建、macOS 覆盖安装与签名验证通过。 |
| /root/reader_latency_exact_short_nodes_v8 | P1 兼容排版短节点首屏精确测量加速 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 单行文本节点现直接复用其行框，短多行普通文本也进入按行源偏移反查；仅跨行退化矩形和异常 caret 才逐字回退。专项 77 项、typecheck、ESLint、完整前端构建、签名验证、macOS 覆盖安装和启动均通过。 |
| /root/reader_latency_exact_first_screen_v7 | P1 兼容排版长章节章首精确测量加速 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 长文本节点现用整段行框＋caret 源偏移反查替代首屏逐字 Range 测量；任一行边界不可靠时仍自动逐字回退。新增专项回归，typecheck、ESLint、TS/Legacy 阅读器专项 77 项、完整前端构建、签名验证及 macOS 覆盖安装启动均通过。未改视觉、分页语义、脚注或用户数据。 |
| /root/reader_latency_queue_adjacent_prefetch_v6 | P1 兼容排版跨章输入排队与相邻章节预取 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-page-modules,reader-large-chapter}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 保持单一阅读页面和现有排版语义；跨章加载期间保留最后一次净翻页意图，预取相邻章节正文，当前章连续页预取采用独立队列而不因快速点按被取消。TS/专项 77 项、完整前端打包、签名、覆盖安装和启动均通过。 |
| /root/reader_trace_autosave_page_latency_v5 | P1 兼容排版翻页预取与问题记录自动持久化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}`、`ui/tests/{reader-bug-trace,reader-toolbar,reader-large-chapter,reader-page-modules}.test.cjs`、`src/app_commands.rs`、`AGENTS.md`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 已取消跨章落地后 320 ms 的点击盲区，以坐标去重同一次 macOS 指针事件；章首精确扫描先覆盖一屏、仅在页尾异常留白时扩展，下一页/再下一页预取提前到 36/72 ms。最新两分钟脱敏快照现原子覆盖持久化到应用缓存，关闭前也会强制检查点；新会话按 `AGENTS.md` 直接读取快照和 `debug.log`。定向 TypeScript 18/18、相关阅读器 109/109、Rust 定向 4/4、Rust 全量 577/577、typecheck、ESLint、前端完整构建、Release 打包/签名/覆盖安装和持久化文件实机校验均通过。全量 typed/legacy 各剩一项任务外资讯规则断言，Release 总门禁被其他会话的 `private_sync.rs:948` 默认值重赋值 Clippy 告警阻断；本任务未改契约、视觉或用户数据。 |
| /root/reader_compat_cross_chapter_latency_v4 | P1 兼容排版跨章与顺序翻页响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 最新问题记录基线：普通首次顺序翻页约 40–274 ms；章节 121 首次首屏 1128 ms、整章 1308 ms，正文读取仅 2 ms；章节 122 落地 171 ms 后收到补点并进入第二页。现跳过已就绪活动字体的无效全局字体等待，macOS 快速章节阈值从 16 KiB 降至 4 KiB，当前页显示后 80 ms 空闲预取下一页，并在跨章落地后 320 ms 内仅消费同方向补点。定向 TypeScript 22/22、相关阅读器 50/50、目标 ESLint、typecheck、前端源码门禁、全部前端产物、许可证门禁及完整 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改契约、视觉设置或用户数据。 |
| /root/reader_compat_chapter_first_paint_v3 | P1 兼容排版大章跨章首帧与章首空白修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 基线：普通页 45–70 ms、缓存页 13–22 ms、小章跨章 133 ms，54 KiB 大章完整分页 566–690 ms。现按虚拟排版收紧的标题空段动态扩展精确候选带，修复首页仅 17 行的大块留白；向前进入大章时先提交精确首屏并记录 `chapter_first_page`，下一帧再完成整章页表。定向 20/20、相关阅读器 50/50、目标 ESLint、typecheck、全部前端产物、完整 macOS Release 构建与覆盖安装启动通过；另有任务前已存在的旧脚注正则断言 1 项失败。本任务未改契约、阅读设置或用户数据。 |
| /root/reader_compat_page_latency_v2 | P1 兼容排版逐页与跨章延迟优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 问题记录基线：普通页 48–160 ms，跨章 573 ms、返回 925 ms，正文读取仅 2–3 ms。现在精确排版只处理粗分页标出的当前页候选节点，同时 LRU 复用最近 48 个精确页；保留无索引时的全文兼容回退。定向 19/19、阅读器回归 110/111（唯一失败为任务前已有的旧脚注正则断言）、目标文件 ESLint、全部前端产物生成/校验、完整 macOS Release 构建与覆盖安装通过；已启动 `/Applications/鲲鹏阅读器.app`。全仓 typecheck 受共享工作区 `news-ui.ts` 的未完成 `canInvoke` 变更阻断，视觉门禁受共享 `ui/index.html` 未登记改动阻断；本任务未改两者、未改契约。 |
| /root | P1 跨设备继续阅读与可选自定义订阅同步 | `contracts/sync/{README.md,entities.schema.json}`、`contracts/fixtures/sync-handoff-and-subscriptions.v1.json`、`docs/adr/0021-synced-app-settings.md`、`src/{db.rs,db/entities.rs,db/backup_port.rs,private_sync.rs,library_commands.rs,newsnow.rs,main.rs,sync.rs}`、`apps/desktop-ui/src/legacy-ts/{sync-ui/sync-ui.ts,main-news/news-ui.ts}`、`ui/index.html`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 新增仅含稳定内容 ID 的续读交接实体；自定义 RSS/Atom 地址按独立、默认关闭的选项同步，均不含正文、文件、路径、缓存或阅读历史。短暂断网后的手动/启动同步加入无交互、指数退避恢复，恢复成功会清除失败 UI；钥匙串拒绝、认证和限额错误不重试。客户端/服务端单测、Clippy、完整前端与 macOS 签名覆盖安装启动通过。 |
| /root | P1 登录历史按点击展开与登录后账户概览保留 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`ui/tests/sync-ui.test.cjs`、必要生成物与 macOS 构建、本表 | 已完成，已提交（55f1a20d） | 登录表单的程序化聚焦不再打开账号历史；用户点击账号框才展开。认证成功后保留账户面板并选中概览，首轮同步状态可见。同步 UI 定向 12 项、Legacy 同步页 17 项、72 项脚本构建/校验、完整 macOS Release 构建与覆盖安装通过；全局 TypeScript 检查仍被其他会话的阅读器测试只读属性赋值阻断。 |
| /root/reader_compat_boundary_latency | P1 兼容排版跨章首屏响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 问题记录显示正文接口仅 3–5ms，约 0.33–0.37s 延迟集中在 macOS 兼容分页。现保证缓存目标首屏先提交两帧，再开始阻塞式整章重排；首次跨章跳过不可用且随后会被丢弃的隐藏态分页。验证：定向 18/18、阅读器回归 95/95、typecheck、lint、source policy、生成物校验、`npm run desktop-ui:build`、`scripts/install-macos-app.sh --build` 均通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改契约。 |
| /root/reader_compat_cross_chapter_footnote | P1 兼容排版跨章注文解析与无效查找优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 原书注文存在；虚拟角标现已拆解 `#c目标章~片段` 并直取目标章，明确目标不存在时也只检查该章，不再串行扫描全书；无目标章的旧格式仍保留兼容回退。9 项定向及 95 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_compat_note_pointerup | P1 macOS 兼容排版脚注点击优先级修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 新增诊断确认 macOS 快速 pointerup 在 click 前把虚拟“注”当正文翻页；已将虚拟注释标记排除出快速翻页路径，交回脚注监听器。9 项定向及 94 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_footnote_trace | P1 脚注点击脱敏诊断链 | `apps/desktop-ui/src/legacy-ts/{reader-page-modules/{reader-page-bug-trace.ts,reader-page-bug-trace.test.mts,reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts},reader-messages/{reader-message.ts,reader-message.test.mts},reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 已记录脚注标记命中、链接/片段存在、虚拟监听器拦截、注文本章/跨章查找与弹窗显示结果；三层白名单仅允许布尔值、阶段和数量，不记录正文、注文、片段名或链接。24 项定向与 108 项相关阅读器测试、typecheck、ESLint、生成物验证、前端源码门禁、完整前端及 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/reader_compat_footnote_click | P1 兼容排版脚注标记点击修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 虚拟排版片段现在会恢复包裹它的脚注链接，“注”点击由脚注弹窗消费，不再落入左右翻页热区。24 项定向测试及 226 项相关阅读器回归通过；typecheck、ESLint、生成物验证、前端源码门禁、完整前端与 macOS Release 构建通过，已覆盖并启动 `/Applications/鲲鹏阅读器.app`。全量 typed 测试中的 1 项任务外资讯规则等价性测试仍受其他未提交修改影响。 |
| /root/reader_compat_boundary_cache | P1 兼容排版章首章尾往返响应优化 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与完整前端/macOS 构建、本表 | 已完成，已提交（55f1a20d） | 已缓存最多 4 个与当前视口和阅读设置精确绑定的稳定章边界快照；再次往返立即显示目标章页，真实章节仍按原门禁重排并无缝替换。54 项定向与 110 项相关阅读器回归、typecheck、ESLint、前端源码门禁、完整前端及 macOS Release 构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root | P0 账户页钥匙串按需读取 | `src/{sync.rs,secret_store.rs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`ui/tests/sync-ui.test.cjs`、本表 | 已完成，已提交（55f1a20d） | 打开账户页不再请求额度或同步 token；只有显式同步才允许钥匙串读取，且一次取消不会阻止后续手动重试。已完成 Rust/UI 定向验证、完整 macOS 构建并覆盖安装。 |
| /root | P0 同步凭据恢复、账户页在线探测与 inventory 向后兼容修复 | `src/{secret_store.rs,sync.rs,sync/commands.rs,main.rs,profile.rs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、必要同步测试、本表 | 已完成，已提交（55f1a20d） | 无交互钥匙串探测不再缓存 macOS“需要交互授权”为进程级拒绝；显式同步成功会把旧 v2-v4 开发签名钥匙串条目迁移至当前 v5 槽位，使同一已安装版本后续重启可静默恢复。账户页会安全探测服务在线状态；旧服务 inventory 400 时只延后新可选类别。Rust 全量 577 项、Clippy、类型检查、同步 UI 13 项、完整前端构建及 macOS 签名覆盖安装启动通过。 |
| /root | 账户概览同步成功提示去重 | `ui/index.html`、`apps/desktop-ui/src/legacy-ts/sync-ui/sync-ui.ts`、同步 UI 测试、本表 | 已完成，已提交（55f1a20d） | 顶部已表达同步成功；概览底部保留失败与进行中信息，不再重复显示成功结果。同步 UI 13 项、类型检查、完整前端构建、macOS 签名覆盖安装启动和差异检查通过。 |
| /root | P0 macOS 钥匙串一次授权后跨重启与重装稳定 | `src/{profile.rs,secret_store.rs,sync.rs}`、`scripts/install-macos-app.sh`、钥匙串定向测试、本表 | 已完成，待长期实机观察，未提交 | v9 同步凭据使用仅信任已安装应用的 ACL，安装脚本复用持久本机签名身份，避免每次构建生成新 cdhash 权限。已通过配置 9 项、钥匙串 5 项、同步 28 项测试及完整 macOS Release 构建；实机连续验证普通重启、重新构建覆盖安装、重装后再次重启三种场景，均无钥匙串弹窗且后台同步成功（最后 19:53:35）。未删除旧钥匙串项、账户或书库数据。 |
| /root/reader_compat_tail_probe | P1 兼容排版章首页尾精确行扫描修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/reader-large-chapter.test.cjs`、必要生成物与前端构建、本表 | 已完成，已提交（55f1a20d） | 已将 macOS 兼容排版页尾精确测量带由“仅段距”扩大为“一整行＋段距＋字形余量”，使基础分页已消费的页尾行也能进入精确重绘。23 项专项和 109 项相关阅读器测试、typecheck、ESLint、前端源码门禁、完整前端与 macOS Release 构建均通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。未改视觉设置、翻页语义或协议。 |
| /root | P1 来源目录紧凑说明与无上限选择 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/{legacy-ts/main-news/{news-ui.ts,news-ui.test.mts},legacy-ts/main-rules/news-rules.ts,features/news/{news-rules.ts,news-rules.test.mts}}`、`src/{newsnow.rs,app_settings/{rules.rs,patch.rs}}`、`contracts/sync/entities.schema.json`、`docs/adr/0021-synced-app-settings.md`、`README.md`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 删除来源页顶部链路说明，改为短悬停提示和完整手势说明；去掉 24 条选择限制并保持来源请求的 12 路并发节流。同步设置 schema 与本地规则已升级，避免跨设备截断选择；类型/Rust/前端验证及 macOS 覆盖安装启动通过。 |
| /root | P1 来源悬停说明与检查移除 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/{main-news/news-ui.ts,gesture-ui/gesture-ui.ts}`、`src/{newsnow.rs,main.rs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 已删除来源检查 UI、状态及原生命令；来源悬停显示完整概览，来源卡片阻断信息提取手势，空白区域页面说明保留。类型检查、定向新闻测试、Clippy、完整桌面构建与 macOS 安装验收通过。 |
| /root | P1 来源目录紧凑布局与手势说明 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 自定义 RSS/Atom 改为默认收起的紧凑编辑区；来源悬停/键盘聚焦显示简介；信息提取手势在来源上显示该来源的概览、在空白处显示来源页总览。已通过类型检查、定向来源页测试、前端构建及 macOS 安装验收。 |
| /root/page_open_fix | P0 主窗口页面入口诊断 | `apps/desktop-ui/src/legacy-ts/main-app/{app.ts,main-app.test.mts}`、必要前端构建/测试、本表 | 已完成，已提交（55f1a20d） | 以当前唯一 `ui/index.html` 与 72 个 generated classic 脚本在 Chromium 中模拟 Tauri 验证：统计、书库问答、旧资讯、设置与情报中心入口均可打开，无主窗口初始化异常。用户所述“页面打不开”不在主导航复现，已转交按情报中心的资讯详情/原文来源路径处理；未改产品代码。 |
| /root/source_ui_catalog | P1 来源目录单页视觉重设 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 直接重设唯一既有“管理来源”页为自适应分类目录；保留搜索、已选上限与按需可用性检查，新增 provider 汇总/筛选挂点及 `data-provider` 分组样式，不创建第二套页面。来源页专项静态测试与前端单 UI 门禁通过。 |
| /root | P1 多提供方来源筛选与目录行为 | `apps/desktop-ui/src/legacy-ts/main-news/{news-ui.ts,news-ui.test.mts}`、`src/newsnow.rs`、`ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 在唯一既有来源管理页中接入 provider/kind 的筛选、汇总和受限可用性检查；新增 Horizon 的 ReliefWeb、CISA 与 WorldMonitor 的 USGS、NASA EONET、GDACS 真实公开来源。来源页按提供方与中文类型检索/分组，保留 24 个同时启用的上限；来源检查沿用每源 6 秒、最多 12 路并发。五个新增公开端点实测均返回 HTTP 200；完整桌面前端构建、33 项 newsnow 单测、Clippy、签名安装及启动通过。 |
| /root/horizon_worldmonitor_sources | P1 Horizon 与 WorldMonitor 公开来源目录 | `src/newsnow.rs`、必要 `newsnow` 单元测试、本表 | 已完成，已提交（55f1a20d） | 新增 Horizon 的 ReliefWeb 人道动态、CISA 网络安全公告，以及 WorldMonitor 的 USGS 显著地震、NASA EONET 自然事件、GDACS 灾害预警；均为公开免凭据直连源，并输出 `reader|horizon|worldmonitor` provider 与稳定 kind。复用现有 6 秒、12 路来源检查；弃用实测超时的 GDELT。33 项 newsnow 单测、Clippy、格式和差异检查通过。 |
| /root/custom_source_ui | P1 自定义 HTTPS RSS/Atom 来源单页接入 | `ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、本表 | 已完成，已提交（55f1a20d） | 在唯一既有“管理来源”页加入本地持久化的自定义 RSS/Atom 表单与列表：仅无凭据 HTTPS URL（≤2048）、名称≤80、分类≤48、本机最多200条；支持启停/删除，所有启用来源仍与内置/贴吧共用24条上限。已启用自定义源以 `custom/rss` 纳入统一目录、搜索、分类、健康检查，以及 list/refresh/availability 请求的 `customSources`；定义不跨设备同步，避免另一设备取得无法解析的裸 ID。类型检查、前端整包构建及专项来源回归通过。 |
| /root | P1 全量公开来源与可配置来源 | `src/newsnow.rs`、`apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`ui/{index.html,styles.css,tests/news-ui.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 已接入 614 条 WorldMonitor 公开直连 HTTPS RSS 与 4 条 Horizon 无密钥 RSS/Atom，内置目录共 657 条；唯一来源页支持本地自定义 RSS/Atom（受 URL/私网/重定向、2 MiB、6 秒、24 个启用上限约束）。只验证用户启用的来源，逐项反馈可用/空/不可用；不进行全量外网探测。桌面前端与 Rust 回归已通过，待安装验收版。 |
| /root | P1 情报中心来源复用与可用性页 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 复用唯一既有“管理来源”选择器；情报中心读取当前选择、可打开既有安全正文阅读器，并提供来源目录与已选源可用性检查。Horizon/WorldMonitor 当前无自有采集源，页面明确其复用链路。类型检查、前端构建、定向 UI/原生测试、Clippy 与 macOS 覆盖安装启动通过。 |
| /root | P5 2核4G 同步服务稳定吞吐突破 | `server/reader-sync-api-rs/src/{auth.rs,rate_limit.rs,sync.rs,error.rs,middleware.rs,account_mutation.rs,config.rs}`、`server/reader-sync-api-rs/migrations/0022_*`、`server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,test-capacity-monitor.py,run-capacity-test.sh,test-run-capacity-test.sh}`、`src/sync{.rs,/retry.rs}`、相关测试、容量文档、本表 | 已完成，已提交（55f1a20d） | 认证/准入/业务 SQL 复用每请求同一连接，checkpoint 改账户单行高水位，push 合并 SQL 往返；准入租约自适应且竞争不再持连接等待。负载模型现用固定 seed 哈希相位、首请求错峰、3 秒排空和严格请求账目门禁；旧 20 分钟报告有 13 个 cutoff，按现行门禁只是历史诊断。新增 error/busy/database/queue 来源指标、有界写等待位 48 和数据库 acquire 超时 300 ms。最终默认执行预算收敛为普通读/checkpoint/写=12/18/10，合计 40；桌面同步客户端截止为 20 秒，长于服务端默认 15 秒，超时/5xx 最多三次抖动重试，push 保持 mutation ID 幂等。新预算的隔离 40 VU 三轮共 250,896 请求全成功，0 个 503/无响应/Busy/数据库失败/队列拒绝，平均 1,393.87 成功 RPS、P99 51.63 ms。隔离测试服务已生效，正常服务未改；完整 macOS 应用已构建、覆盖安装并启动。未改 v5 wire 语义。 |
| /root | P0 退出后登录失败可见性与流程修复 | `ui/{index.html,styles.css,tests/sync-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 登录请求不再在认证前关闭账户面板；在登录卡片中显示进行中/失败原因，避免失败无提示而无法诊断。本机到服务的隧道、服务健康/就绪、登录流程回归、完整 macOS Release 覆盖安装及签名均通过。 |
| /root | P1 同步首次上传与日额度一致性 | `server/reader-sync-api-rs/src/{sync.rs,assets.rs,auth.rs}`、`ui/{index.html,tests/sync-ui.test.cjs}`、`contracts/sync/README.md`、`docs/adr/{0019-account-security-quotas-and-verified-sync.md,0028-registration-and-resource-abuse-hardening.md,0029-rust-sync-api-v4.md}`、服务端测试、本表 | 已完成，已提交（55f1a20d） | 每日“实际接受写入”已提升至 25 MiB（下载恢复不计入），账户页明确显示“今日上传”；总存储 25 MiB、请求/注册防护不变。服务与 macOS Release 已部署、签名和健康检查通过。 |
| /root | P1 单账户同步准入边界 | `server/reader-sync-api-rs/src/config.rs`、本表 | 已完成，已提交（55f1a20d） | 将默认的已认证单账户一分钟上限由 120 调整为 600，避免首次同步被误判为滥用；全局 75 并发背压不变。配置测试、Clippy、服务健康/就绪检查通过。 |
| /root | P1 同步单飞进行中状态 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、本表 | 已完成，已提交（55f1a20d） | 将已有同步单飞返回的忙状态显示为进行中，避免误标为同步失败；不改变同步协议或服务端。typed/legacy 同步 UI 回归、类型检查与 macOS Release 覆盖安装启动通过。 |
| /root | P0 首次大同步与卡住状态修复 | `src/sync/{client.rs,retry.rs}`、`server/reader-sync-api-rs/src/{sync.rs,auth.rs,config.rs,lib.rs}`、`server/reader-sync-api-rs/migrations/0021_storage_ledger_history_cleanup_v5.sql`、`server/reader-sync-api-rs/tests/postgres_e2e.rs`、`apps/desktop-ui/src/legacy-ts/{main-app/app.ts,sync-ui/{sync-ui.ts,sync-ui.test.mts}}`、本表 | 已完成，已提交（55f1a20d） | 修正升级后账本触发器继续引用已移除恢复历史列，避免实体/资产写入 503；连续迁移已在正常服务应用，迁移开关恢复关闭。首次 push/pull/replay、数据重置/会话撤销、资产断点续传及账本在隔离 PostgreSQL 全部通过；客户端会在后台单飞结算事件后更新 UI，429 快速结算。 |
| /root | P5 账户新密码边界调整 | `server/reader-sync-api-rs/src/account_validation.rs`、服务端认证测试、`apps/desktop-ui/src/legacy-ts/sync-ui/`、`contracts/auth/`、ADR-0028、本表 | 已完成，已提交（55f1a20d） | 新注册、手机号注册、重置及改密统一为 8–32 Unicode 字符；既有登录兼容不变。服务端边界/HTTP、前端 typed/legacy、schema 解析与完整 macOS Release 覆盖安装均通过；当前开发服务已替换并以协议 5 健康/就绪检查验证。 |
| /root | P1 登录卡片与账户概览尺寸对齐 | `ui/styles.css`、`ui/tests/sync-ui.test.cjs`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 登录外框为与概览一致的 760×302，360px 居中表单；注册页改为 520px 内容自适应卡片，窄屏保持自适应。17 项 sync UI、完整 desktop-ui 构建、macOS Release 覆盖安装、签名与实机验收通过。 |
| /root | P1 未登录同步摘要去重 | `apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、本表 | 已完成，已提交（55f1a20d） | 未登录概览仅保留“尚未同步”一行，隐藏标题已表达的重复“尚未登录”；不改账户、认证或同步请求。5 项 typed sync UI、17 项 legacy sync UI、完整桌面构建、macOS Release 覆盖安装、签名与实机验收通过。 |
| /root | P5 75 并发读写背压预算 | `server/reader-sync-api-rs/src/config.rs`、`server/reader-sync-api-rs/DEPLOYMENT.md`、`server/reader-sync-api-rs/scripts/{run-capacity-test.sh,test-run-capacity-test.sh}`、本表 | 已完成，已被 40 预算取代 | 历史的普通读/checkpoint/写=33/24/18（合计 75）已由后续隔离证据收敛为 12/18/10（合计 40）；不得再将本行的 75 执行预算当作当前配置。 |
| /root | P5 可指定并发的一分钟隔离同步冒烟 | `server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,capacity-client-monitor.py,run-capacity-test.sh,test-run-capacity-test.sh}`、本表 | 已完成，已提交（55f1a20d） | 新增 `--smoke CONCURRENCY` 一键非容量冒烟（1–500）：独立测试账号、60 秒、隔离服务/测试库和三端资源观测；不改变 20 分钟容量曲线。100 并发：61,856/61,900 成功、1,030.93 成功请求/秒、P50/P95/P99=65.66/100.44/130.91ms，44 个受控 503；75 并发：55,575/55,575 成功、926.25 成功请求/秒、P50/P95/P99=54.33/83.02/105.60ms，无无响应或拒绝。 |
| /root/login_registration_redesign | P1 未登录账户入口与登录注册二级页 | `ui/{index.html,styles.css,tests/sync-ui.test.cjs}`、`apps/desktop-ui/src/legacy-ts/sync-ui/{sync-ui.ts,sync-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 已精简登录卡片并删除账号上方标题说明和返回概览；登录主操作改为低强调灰色；注册点击后立即进入独立状态并显示邮箱。保留邮箱验证码注册协议、原账户概览和单一 UI。定向 lint、typecheck、源码门禁、17 个 legacy 测试、362 个 typed 测试、macOS Release 构建、签名安装及正式应用实机验收均通过；完整 `scripts/check.ps1` 仅剩其他会话涉及的反单体预算与手势反馈断言失败。 |
| /root | P5 本机 SSH 隧道同步临时端点 | `build.rs`、`src/runtime_support.rs`、本表；仓库外本机 LaunchAgent/安装入口 | 已完成，已提交（55f1a20d） | 已配置自动重连的本机 SSH 回环隧道；构建期仅允许严格的 `127.0.0.1` HTTP 端点、拒绝公网 HTTP。仓库外 `run-kunpeng-macos-install.sh --build` 为本机验收构建注入该地址，避免退出登录后丢失服务配置；公开构建仍不内置地址。Release 覆盖安装、签名和隧道健康检查通过。 |
| /root | P5 P99 队列/连接/SQL 归因与分级背压收敛 | `server/reader-sync-api-rs/{src/{middleware.rs,config.rs,state.rs,lib.rs,auth.rs,rate_limit.rs,sync.rs},scripts/{capacity-k6.js,capacity-k6-report.py,capacity-monitor.py,run-capacity-test.sh,test-capacity-monitor.py,test-run-capacity-test.sh}}`、必要服务端测试、本表 | 已完成，已提交（55f1a20d） | 仅隔离候选服务：按模板路由/读写/轻读槽记录已接纳排队、快速拒绝、连接获取和 SQL 区间；成功 2xx 延迟与总体延迟分列，固定 330 秒短测要求每阶段有样本，否则整轮无效。候选完成一轮完整报告；不记录 URL 参数、账号、Token、请求体或数据库连接资料。 |
| /root | P5 带数据同步传输 5 分钟隔离冒烟 | `server/reader-sync-api-rs/scripts/{data-transfer-k6.js,data-transfer-report.py,run-capacity-test.sh,test-data-transfer-report.py}`、本表 | 已完成，已提交（55f1a20d） | 仅可销毁测试库和独立服务；固定 300 秒、约 256 KiB 同步实体交替 push/pull。最终有效记录为 1,024/1,024 成功、无超时/429/503，已采集 wire 字节、P50/P95/P99、服务/PG/客户端资源；不接入反代或正式服务。 |
| /root | P5 同步 checkpoint 桌面接入与隔离短测 | `src/{sync.rs,sync/protocol.rs}`、必要同步测试、`server/reader-sync-api-rs/scripts/{capacity-k6.js,capacity-k6-report.py}`、本表 | 已完成，已提交（55f1a20d） | 无待上传变更、完整 cursor 且一小时内做过完整清点时走轻量 checkpoint；任何否定/错误回退完整 v5 同步。已部署独立测试服务并完成固定 330 秒短测。 |
| /root | P1 手势恢复动作重启丢失修复 | `src/app_settings/{rules.rs}`、`src/app_settings.rs`、`contracts/sync/entities.schema.json`、`docs/adr/0021-synced-app-settings.md`、本表 | 已完成，已提交（55f1a20d） | v5 app-settings 已接受规范 `undo_last`；旧 `reopen_last` / `restore_jump` 可读并写回归一。应用设置 14 项、手势 7 项、类型检查、前端与 macOS Release 构建、覆盖安装启动通过。完整 rustfmt 仍被同目录既有共享格式差异阻断，未重排任务外内容。 |
| /root | P1 阅读偏好高级页排版引擎界面重设 | `ui/{reader.html,reader-preferences.css,tests/reader-preferences.test.cjs}`、`apps/desktop-ui/src/legacy-ts/reader-preferences-ui/{reader-preferences-ui.ts,reader-preferences-ui.test.mts}`、`scripts/frontend-source-policy.json`、本表 | 已完成，已提交（55f1a20d） | 已删除弹窗内重复标题行；排版引擎改为与跳转回退同风格的图标+标题+双选控件，保留 legacy/modern 设置语义。已同步更新经用户授权的 UI 视觉基线；类型检查、偏好回归、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root | P1 新版排版章节内翻页计数修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与前端构建、本表 | 已完成，已提交（55f1a20d） | 新版 EPUB 多栏布局在 WebKit Range 几何退化为首栏时，改以 scrollWidth 计数作只增不减回退，避免章节误判为单页而让点击/滑动跨章；不改手势映射。专项 3 项、类型检查、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root | P1 新版排版空白尾栏修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、必要生成物与前端构建、本表 | 已完成，已提交（55f1a20d） | 新版 WebKit Range 几何失效不再参与尾栏修剪，避免把 scrollWidth 已确认的章节页数裁回单页；保留 legacy 的可见内容尾栏修剪。专项 3 项、类型检查、前端完整构建与 macOS Release 覆盖安装启动通过。 |
| /root/reader_compat_bottom_space | P1 兼容排版页尾空白收敛 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts}`、`ui/tests/{reader-large-chapter,reader-page-modules}.test.cjs`、必要生成物与前端构建、本表 | 已完成，已提交（55f1a20d） | 已移除固定整行保护，并让大章节精确逐字重绘向视口外探测可回收段距，避免页尾空白、整行漏读及下一页重复；相关 56 项测试、typecheck、lint、完整 Release 构建通过，已覆盖 `/Applications/鲲鹏阅读器.app` 并在第 50/84 章第 1→2 页实机验证连续且无切字。全量 `scripts/check.ps1` 仅被任务外既有 `news-rules-equivalence` 期望差异阻塞（实际 `['b','a','3']`，期望 `[]`）。 |
| /root/reader_compat_chapter_turn | P1 兼容排版跨章闪页与章首页快照修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-transition.ts,reader-page-transition.test.mts}`、`ui/tests/reader-toolbar.test.cjs`、必要生成物与前端构建、本表 | 已完成，已提交（55f1a20d） | 关闭翻页动画时的跨章遮挡层已改为复制 macOS 兼容排版真正可见的逐行虚拟页，不再短暂显示页界不同的原始滚动正文；章首页页尾与撤下遮挡后的正文保持一致。新增专项回归，相关 94 项阅读器测试、typecheck、ESLint、完整前端和 macOS Release 构建通过，已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/pdf_gesture_forwarding | PDF 书页接入阅读器手势 | `apps/desktop-ui/src/legacy-ts/pdfview/pdfview.ts`、`ui/pdfview.js`、`ui/tests/pdf-engine-bridge.test.cjs`、本表 | 已完成，已提交（55f1a20d） | PDF iframe 已通过固定来源桥转发右键拖动的 start/move/end/cancel，覆盖 Pointer Events、WebKit 鼠标兼容和失焦取消；专项 4 项、Typed 355 项、完整桌面前端构建、Lint、类型检查、Release 打包/签名/覆盖安装及真实 PDF 打开通过。全仓检查另被占用中的 `window_commands.rs` 三项 Clippy 拦截；Legacy 全量另有两项任务外迁移断言失败。 |
| /root/server_sync_hotpath_impl | P5 同步服务 PostgreSQL 热路径尾延迟优化 | `server/reader-sync-api-rs/src/sync.rs`、`server/reader-sync-api-rs/migrations/0016_sync_hot_path_v5.sql`、必要服务端测试与本表 | 已完成，已提交（55f1a20d） | push 收据改为精确 90 天读/写边界与每进程 5 分钟一次、最多 2,000 行的全局清理；inventory 空结果改为单一 account-left-join 快照并加入覆盖索引；pull 使用同语义的 stored `envelope_bytes`。迁移静态检查、35 服务单元、22 HTTP、16 PG 门禁、Clippy 与差异检查通过；未部署或连接数据库。 |
| /root/capacity_pg_stats | P5 容量测试 PostgreSQL 脱敏观测与内存指标校正 | `server/reader-sync-api-rs/scripts/{capacity-monitor.py,run-capacity-test.sh,test-capacity-monitor.py,test-run-capacity-test.sh}`、本表 | 已完成，已提交（55f1a20d） | 仅隔离容量测试链路；从运行中的可销毁服务推导并前缀校验其测试库，报告聚合前后 SQL 计数、连接/等待及 PSS；不含 SQL、凭据、地址或账户数据。`test-capacity-monitor.py`、Shell 语法、Python 编译、容量脚本自测与差异检查通过；未连接、部署或执行远端测试。 |
| /root/client_sync_core | 客户端同步核心安全快路径 | `src/{sync.rs,data_migration.rs}` 及必要同步核心测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 稳态 inventory/reconcile 改为只读 LWW manifest，不解码实体 JSON；pull/reconcile/push conflict 在与 cursor/实体同一事务中写 runtime projection marker，稳态无变更时跳过完整 runtime 投影和重复 JSON→SQLite 迁移，异常中断仍会在下次 no-op pull 恢复。保留初次投影、cursor/LWW、每轮 inventory 自愈和顺序 push。`cargo test` 548 项、`cargo clippy --all-targets -- -D warnings`、格式和差异检查通过。 |
| /root/sync_dirty_scheduler | 客户端持久脏同步调度 | `src/{app_state.rs,sync.rs,db.rs,db/metadata.rs}`、必要调用点/测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 复核已有 generation 调度并补齐“重启不主动读取钥匙串”：自动启动只可使用当前进程中已解锁的同 marker token cache；手动同步失败也会持久化同一指数退避，拒绝钥匙串时不会再次自动弹窗。保留本地 upsert/tombstone/导入同事务 generation、45/10 秒 trailing、5 分钟上限、同步期新写保留、恢复重绑 notifier 与远端 projection 静默。`cargo fmt --all -- --check`、`cargo test sync::`（52）、`cargo test db::metadata::`（6）、`cargo check`、`cargo clippy --all-targets -- -D warnings`、差异检查均通过。 |
| /root/catchup_cursor | k6 推进式 catchup 容量档位与 cursor-zero 对抗回放隔离 | `server/reader-sync-api-rs/scripts/{capacity-k6.js,run-capacity-test.sh,capacity-k6-report.py,test-run-capacity-test.sh}`、`docs/testing/sync-capacity-baseline.md`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 单 controller VU 的 `http.batch` 每轮使用阶段指定数量的不同账户，持久保存 2048 个账户各自的 `nextCursor`；报告明确阶段数是 `http-in-flight` 而非 k6 VU。显式 `cursor-zero-replay` 标为对抗且不可作默认容量结论。脚本自测、演练工具总自测、Python 编译、Shell 语法、JS 解析和差异检查通过；本机未安装 k6，真实短测待具备 k6 的压测机执行。 |
| /root/server_queue_metrics | P5 服务请求队列与处理时延观测 | `server/reader-sync-api-rs/src/middleware.rs`、必要 middleware 测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 队列等待、在执行槽内 handler 耗时、活跃/排队数及按路由的状态码均用 MatchedPath 模板和 read/write 类别标注；不记录 URL 参数、身份、Token 或请求体。定向 middleware 4 项、格式和范围差异检查通过；全目标 Clippy 暂被并行 `sync.rs` 缺失字段阻断。 |
| /root | P5 多账号同步 P95/P99 六项优化 | `server/reader-sync-api-rs/src/{auth.rs,rate_limit.rs,state.rs,config.rs,sync.rs,assets.rs,recovery.rs}`、`server/reader-sync-api-rs/migrations/`、`server/reader-sync-api-rs/{tests,scripts,README.md}`、`docs/testing/sync-capacity-baseline.md`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已完成认证单 CTE、跨实例保守租约式单账号准入、pull 单查询、事务用量账本、批量 push/history 与 cursor 稳态档位。压测一键脚本现使用 2048 个账户、并发去重与测试迁移。隔离 catchup 短测完成：500 并发 P95 1012.61ms、P99 6637.29ms、0 个429/断连、1 个503；未触碰正常服务/数据库或公网入口。 |
| /root | P5 单账户写入快速限流及多账户 k6 容量复测 | `server/reader-sync-api-rs/src/{account_mutation.rs,sync.rs,assets.rs,recovery.rs,lib.rs}`、`server/reader-sync-api-rs/{scripts,tests,README.md}`、`docs/testing/sync-capacity-baseline.md`、`AGENTS.md`、`docs/coordination/ACTIVE_WORK.md` | 短测完成，未提交 | 保持跨实例账户数据锁，但改为立即 503/Retry-After；k6 以 500 个独立、可销毁测试账户和保活连接执行既定 20 分钟曲线，并采样客户端/API/PostgreSQL 负载。2026-08-14 短测完整跑完 330 秒、358,529 请求，仅 1 次无响应；完整 20 分钟容量验收仍待单独执行。 |
| /root | P5 读写分级、背压与异步维护优化 | `server/reader-sync-api-rs/src/{middleware.rs,config.rs,state.rs,sync.rs,assets.rs,lib.rs}`、必要迁移/测试/容量文档、本表 | 已完成，已提交（55f1a20d） | 读写现有独立执行槽和独立有界等待位，满/超时即返回可重试 503；过期收据和孤儿资产已从 push 关键路径移至有界后台任务。保持同账户写入锁、协议与配额精确性；候选仅部署到隔离测试服务并已运行固定 330 秒短测。 |
| /root/read_write_backpressure | P5 服务端读写分级与有界背压 | `server/reader-sync-api-rs/src/{middleware.rs,config.rs,state.rs,lib.rs}`、必要服务端测试/文档、本表 | 进行中，未提交 | 在父任务授权范围内补齐独立读写执行槽与有限等待队列；仅记录模板路由和读写类别，不记录请求参数、身份或内容。 |
| /root/async_maintenance | P5 同步写后资产/收据异步维护 | `server/reader-sync-api-rs/src/{sync.rs,assets.rs}`、服务端测试/容量文档、本表 | 已完成，已提交（55f1a20d） | push 事务只保留 LWW、配额账本和幂等 receipt；单个后台循环每 5 分钟最多清理 2,000 条过期 receipt、每小时最多检查 100 个资产账户。账户锁忙即跳过，无同账户队列积压，失败下周期重试；指标仅使用固定任务标签/聚合数量。`cargo test --lib` 37、HTTP 25、格式和差异检查通过；全库 Clippy 仅被任务外 `config.rs` 的 doc_markdown 阻断。无需 migration。 |
| /root/sync_write_batch | P5 同步写入/恢复历史 SQL 往返收敛 | `server/reader-sync-api-rs/src/{sync.rs,recovery.rs}`、必要 migration/服务端测试、本表 | 已完成，已提交（55f1a20d） | `0017` 将实体、资产与恢复历史的存储账本触发器改为 statement-level transition-table 聚合，批量源表写入由每实体账本更新收敛为每账户一次；恢复 history 用持久 `last_recorded_at` 原子分配单调时间戳，删除每 push 的 `MAX(history)` 扫描，并把保留裁剪和元数据更新合为一条 SQL。`cargo fmt --check`、36 单元、22 HTTP、16 PG 门禁、Clippy、迁移/演练工具自检和差异检查通过；本机无 PostgreSQL 可执行文件，真实 migration 留待隔离测试库验收；未部署或连接服务。 |
| /root | P5 注册账号与新密码长度统一 | `server/reader-sync-api-rs/src/{account_validation.rs,auth.rs,registration.rs,phone_registration.rs,password_reset.rs}`、`server/reader-sync-api-rs/tests/http_baseline.rs`、`contracts/auth/{registration-v2.md,password-reset-v2.md,phone-registration-v1.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 新账号保持 3–32 个 ASCII 标识字符；新密码统一为 12–128 个 Unicode 字符。登录保留旧密码兼容边界，避免锁死已注册用户。服务端 35 个单元、22 个 HTTP、14 个受保护 PG 门禁测试和 Clippy 均通过。 |
| /root/main_ui_ts_migration | 原版主窗口首批严格 TS 无视图迁移 | `apps/desktop-ui/src/legacy-ts/main/**` | 进行中，未提交 | 只迁移纯规则、状态与边界，不改 `ui/`、HTML、CSS、构建配置或 Rust，不定义第二套 UI。 |
| /root/interstellar_source_integration_v2 | 星际旅行来源目录接入与本地模型评分预留 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 复用现有资讯来源管理与 `newsnow_sources` 目录，透明展示已启用来源、目录分类覆盖和由当前候选资讯反向标记的通用来源；不改 Rust、同步或跨端契约，不让来源或新闻自动改变进度。定向 10 项、类型检查、前端整包构建及 macOS Release 覆盖安装和实机验收通过。 |
| /root/crewed_interstellar_and_links | 载人星际目标与情报中心入口修复 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 场景已切为载人近邻恒星际飞行与 8% 人工基线，补入闭环生命保障、人体适应/生物工程及乘员权益门槛。情报中心的详情与来源跳转可在普通资讯入口关闭时仍显式打开同一资讯页；定向 10 项、类型检查、前端整包构建和 macOS Release 覆盖安装通过，实机确认“查看资讯详情”与“管理来源”均可打开。 |
| /root/intelligence_direct_article_links | 情报中心资讯条目直接打开正文 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 简报、关联信号和星际候选条目主点击现直接打开同一资讯正文；右侧上下文与显式“查看资讯详情”保留。新增直接点击简报与星际候选的定向测试；9 项定向、类型检查、前端整包构建和 macOS Release 覆盖安装通过。 |
| /root/intelligence_article_return | 情报中心正文返回路径 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts,news-ui.ts}`、`ui/tests/intelligence-workspace.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 从情报中心打开正文时携带返回来源；正文返回、Esc、手势返回及正文加载失败均回到原情报中心布局。普通资讯入口与来源管理返回行为保持不变。9 项定向测试、类型检查、前端整包构建及 macOS Release 覆盖安装通过。 |
| /root/intelligence_article_transition_and_gesture | 情报中心正文直达与嵌入网页手势一致性 | `apps/desktop-ui/src/legacy-ts/main-news/news-ui.ts`、`src/newsnow.rs`、相关测试、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 情报中心打开文章跳过资讯流加载，不再让普通资讯页在正文前短暂呈现；嵌入式正文的返回手势使用当前用户精度而不是固定阈值，返回后保留原情报中心布局。定向前端 3 项、Rust 1 项、类型检查、源码与完整前端构建以及 macOS 覆盖安装通过；全量旧资讯回归有 2 项被任务外并行改动的旧断言阻断。 |
| /root/intelligence_briefing_pipeline_v1 | 情报中心全目录采集与综合简报首版 | `apps/desktop-ui/src/legacy-ts/main-news/{intelligence-workspace-ui.ts,intelligence-workspace-ui.test.mts}`、`ui/{index.html,styles.css,tests/intelligence-workspace.test.cjs}`、`scripts/frontend-source-policy.json`、`docs/coordination/ACTIVE_WORK.md` | 已完成，未提交 | 情报中心改用 657 个公开来源目录而非普通资讯已选清单；每 12 源一批抓取并在每批后即时做跨批 URL 去重、标题级合并、重要性排序和主题要点整理，条目始终保留原文入口。实机已见第 2/55 批增量回填：首批 343 条抓取结果压缩为 332 条去重资讯、5 个主题。类型检查、11 项 typed、2 项静态、完整前端与 macOS Release 覆盖安装通过；未改同步或跨端契约。 |
| /root/reader_ui_ts_migration | 原版阅读器首批严格 TS 无视图迁移 | `apps/desktop-ui/src/legacy-ts/reader/**` | 进行中，未提交 | 只迁移阅读外壳的纯规则、状态、guard、导航、偏好和消息边界，不改 `ui/`、HTML、CSS、构建配置或 Rust。 |
| /root/worker_gap_audit | 情报 worker 真实公开源采集补齐 | `src/intelligence_worker/{collection.rs,mod.rs}`、相邻测试、本表 | 已完成，未提交 | `HttpCollector` 读取绝对本地 sources JSON，在无页面进程中抓取 RSS/Atom/JSON/公开网页、禁重定向并拒绝明显私网地址；抓全文、保存 ETag/Last-Modified/状态及脱敏失败原因。`--collect-loop` 按来源间隔独立续跑。`cargo test --bin kunpeng-intelligence-worker -j 1` 39/39 通过。 |
| /root/fingerprint_reconciliation | P0 本机情报正文版本指纹一致性补偿 | `src/intelligence_worker/{content_archive.rs,mod.rs}`、相邻定向测试、本表 | 已完成，已提交（b797ccb） | 补偿仅在当前 `body` 规范化 SHA-256 与旧 current complete 证据正文哈希完全一致时复制不可变版本、段落和媒体映射到新记录指纹；正文不同/缺失仍保持不完整，不能被 claim。每次 triage claim 前最多补偿 64 条历史中断项；content archive 7/7、worker 124/124 与 worker build 通过。禁止改服务端、UI、`collection.rs`，不读取或输出正文、URL、凭据。 |
| /root | P0 滚动分页注文重排诊断与修复 | `apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-layout-annotations.ts,reader-page-layout-annotations.test.mts,reader-page-bug-trace.ts,reader-page-bug-trace.test.mts,reader-page-runtime.ts,reader-page-runtime.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-trace/{reader-bug-trace.ts,reader-bug-trace.test.mts}`、`src/app_commands.rs`、本表 | 进行中，未提交 | 同步稳定滚动页的行框与页表，定位翻页后首条注文不稳定与延迟半行；问题快照改按最后一次用户操作锚定，禁止采集正文、链接、路径、账户或凭据。 |
| /root | P0 Windows 阅读器卡死、首屏关键路径优化与逐段问题记录 | `src/{window_commands.rs,epub_runtime.rs,epub_runtime/text_conversion.rs,reader_protocol{.rs,/html_resources.rs}}`、`ui/tests/reader-open-performance.test.cjs`、`apps/desktop-ui/src/legacy-ts/{shelf-ui/shelf-ui.ts,reader-shell/reader.ts,main-settings/{problem-trace-ui,reader-shell-preload-ui}.{ts,test.mts},reader-page-modules/reader-page-layout-annotations.{ts,test.mts}}`、相邻测试、必要生成物与本表 | 当前繁简结果后台预转换缓存已部署，等待同书复测，未提交 | 连续开书卡死修复已通过本机压力复测。样式内联后首轮 12 次完整打开由平均 225.7 ms / 中位 158.1 ms / 最大 753.7 ms 降至 185.7 ms / 107 ms / 593.9 ms，样式阶段由平均 106.3 ms、最大 629.8 ms 降至 1.4 ms、10.8 ms。首章直达初版复测 12 次为平均 181.7 ms / 中位 122.8 ms / 最大 606.4 ms；问题快照确认用户启用了 `t2s`，而初版只内嵌 `original`，因此仍回退额外 `/chapter` 请求。繁简直达版复测 12 次为平均 179.5 ms / 中位 140.3 ms / 最小 64.2 ms / 最大 397.5 ms。补齐命中指标后的同书复测有 10 次完整 EPUB 直达命中，平均 135.9 ms / 中位 101.1 ms / 最大 333.0 ms；直达正文阶段平均 106.8 ms / 最大 271.2 ms，唯一回退样本为 515.4 ms。现新增 8 MiB、最多 10 本的转换正文 LRU；后台最近阅读预热读取当前 `t2s`/`s2t` 设置，提前生成对应续读章节，点击和 `/chapter` 回退均复用转换结果，旧文件代次、书籍淘汰、清理和总 64 MiB 预算同步生效。阅读外壳 URL 携带 `tc`，正文页按同一转换键写入现有四章 LRU；缓存未命中或转换后章节超过 512 KiB 时仍不做额外磁盘读取并安全回退。Rust EPUB 17/17、打开链路 18/18、阅读页 30/30、预加载与问题记录 18/18、类型检查、生成物校验、格式与 diff 检查通过。Fast 产物与桌面快捷方式目标 SHA-256 均为 `E2103DAA91903C77F962056E19154B09C9F493D19EB65FDFD2464745CA07C1AF`，应用 PID 23748 已启动且响应；不恢复 debug.log。 |
| /root | P0 阅读窗口关闭前保存大小与位置 | `src/window_commands.rs`、相关回归测试、本表 | 已完成，本机桌面快捷方式目标已更新，未提交 | 已确认关闭前保存的 EPUB 几何存在；同一本隐藏窗口复用分支此前没有再次应用该几何，Windows 可能在隐藏期间重置窗口状态。现将同书复用、预加载与冷启动统一为显示前安全恢复。8 项窗口生命周期回归、文件格式检查、`cargo check -j 1` 与 Fast 构建通过；已覆盖并启动快捷方式目标，SHA-256 `7F557987BC6DF4E79631262382CA1FC0E9B1FF6C784F8FC36FF31B5226362A47`。 |
| /root | P0 阅读偏好全链路生效修复 | `apps/desktop-ui/src/legacy-ts/reader-settings-ui/{reader-settings-ui.ts,reader-settings-ui.test.mts}`、`apps/desktop-ui/src/legacy-ts/reader-page-modules/{reader-page-runtime.ts,reader-page-runtime.test.mts}`、必要的 `ui/tests/reader-*.test.cjs`、本表 | 已完成，未提交 | Windows 的 no-referrer 正文 iframe 现仅将固定 `http://reader.localhost` 映射到固定 `http://tauri.localhost`，恢复严格来源校验下的设置/正文控制消息。新增无 referrer 的发送者与来源正反向测试，并记录脱敏设置发送/应用阶段。类型检查、定向 8 项、65 项阅读 UI 回归、完整前端构建和 fast 桌面 EXE 覆盖启动通过。 |
| /root/host_inference_contract | P1 情报主机端到端加密推理中继契约 | `contracts/intelligence/host-inference-v1.*`、`contracts/fixtures/intelligence-host-inference-v1.json`、`docs/adr/0037-*.md`、契约测试与本表 | 进行中，未提交 | 仅定义账户配对、主机 capability、加密任务/结果信封、TTL、取消和销毁语义；不改服务端/客户端行为，不把正文、提示词、结果、密钥或模型/索引写入同步实体。 |
| /root/ts_build_pipeline | 原版页面严格 TS 到 classic IIFE 的确定性构建管线 | `package.json`、`package-lock.json`、`tsconfig.json`、`apps/desktop-ui/vite*.ts`、`apps/desktop-ui/scripts/**`、新增前端源码门禁/校验脚本 | 进行中，未提交 | 构建产物仍供唯一原版页面加载；不改 `ui/`、Tauri/Rust、文档，不引入 React/TSX 或双入口。 |
| /root/reader_loading_delay | 阅读窗口加载指示器延迟显示 | `ui/reader.html`、`apps/desktop-ui/src/pages/reader/reader.css`、`ui/tests/reader-loading-delay.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 现行与迁移中阅读页的加载遮罩均延迟 100ms 显示；定向 46 项、类型检查、四入口构建、Rust 483 项与前端 Lint 通过。完整检查仅被任务外尚未收口的 React 源码迁移门禁拦截；Release 整包构建、签名、覆盖安装和实机正文就绪检查通过。未改图书加载、排版、翻页、数据或协议。 |
| /root/reader_full_page_text_fix | 整页翻页图片页正文缺失回归修复 | `ui/{reader-page-runtime.js,reader-page-layout.js,tests/reader-large-chapter.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 连续图片来源页不再改真实媒体高度；带正文的旧式背景图 table 会拆为普通图片和原正文节点。定向 15 项、完整工程检查（476 项 Rust 及 TS/语法）和 Release 安装通过；《爱丽丝》实机确认问题页下一页完整正文立即显示。 |
| /root/reader_background_art_text_fix | EPUB 背景插图与正文重叠修复 | `ui/{reader-page-layout.js,tests/reader-large-chapter.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将含正文的 EPUB 背景图表拆为独立插图和普通正文流；专项 15 项通过、Legacy UI 409/410（唯一失败为任务外 React 第二 UI 治理迁移）、完整 Release 构建/签名/安装通过，同书同章实机确认不再重叠。 |
| /root | P3 同步只读 SQLite 观测边界收敛 | `src/sync.rs`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | 仅将 4 个已存在的只读锁快照改接 `with_db_read`，补齐锁等待/锁内区间指标；不改变大 JSON 构造、账户 generation、网络、备份/恢复或同步 wire。 |
| /root | P5 CI 上传物离线暂存门禁接线 | `.github/workflows/sync-api-v5.yml`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | CI 在上传前必须用已验证的暂存工具生成、复验仅含二进制和溯源清单的目录；不连接部署环境。 |
| /root | P2 AI 阅读证据纯规则拆分 | `src/ai_reader.rs`、`src/ai_reader/reading_evidence.rs`、`docs/coordination/ACTIVE_WORK.md` | 进行中，未提交 | 仅移动已读章节排序、选区邻近、章节范围提示、去重与上限；不改 Tauri、SQLite、书架、网络、provider 或同步。 |
| /root/p5_offline_bundle | P5 离线候选物受限暂存包 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 新增最小传输目录门禁：仅复制已复验的候选二进制与清单、拒绝符号链接/额外文件/内容漂移，并在复制后复验；无上传、服务器、数据库、SMTP、反代或部署能力。`bash -n`、独立 bundle 自测、演练工具自测、迁移静态检查与差异检查通过。 |
| /root | P5 受保护空 PostgreSQL 演练尝试 | 受保护服务器临时数据库/角色/源码目录、`docs/coordination/{CURRENT,ACTIVE_WORK}.md` | 已安全停止 | 仅创建前缀受限的临时库和最小权限角色；Docker Hub 获取 Rust 1.97.1 镜像超时，测试和迁移均未开始。已立即销毁临时库、角色和源码目录；旧服务、反代和正式数据未改。需可用的受信 Rust x86_64 构建镜像或 CI 候选物后重试。 |
| /root/sqlite_next_safe | P3 实体 JSON 读取 SQL 计时补齐 | `src/db/entities.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 单条与批量 JSON 投影现记录固定 `entity_json`/`entity_json_many` SQL 标签与结果行数，不采集键或 JSON；不改查询语义、同步、备份、连接池或 AppState 生命周期。定向 3 项实体读取测试、全目标 Clippy 与范围差异检查通过。 |
| /root/reader_image_overlay_fix | 爱丽丝插图翻页首帧与全局弹窗顺序修复 | `ui/{reader-page-layout.js,reader-page-runtime.js,overlay-stack.js,tests/{reader-large-chapter,overlay-stack}.test.cjs}`、`docs/{architecture/desktop-frontend-and-rust.md,coordination/ACTIVE_WORK.md}` | 已完成，已进入当前提交 | 程序化翻页后主动稳定媒体合成；连续图片裁切按首次原高幂等计算；信息窗与操作窗按打开顺序叠放，确认/反馈仍保留更高语义层。完整检查、Release 构建和安装通过，连续模式目标页首帧已实机检查。 |
| /root | P5 v5 发布清单跨目录 fixture 完整性 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 编译期引入的 v5 契约 fixture 已纳入发布清单的 Git 清洁度与 SHA-256 复验，避免服务目录之外的输入漂移；自测覆盖 fixture 漂移拒绝。未连接服务器、数据库或 SMTP。 |
| /root | P5 v5 服务 Linux 可复现构建产物 | `.github/workflows/sync-api-v5.yml`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交 | CI 现以 Rust 1.97.1 在 Ubuntu 24.04 锁定构建服务端，运行格式、测试、Clippy、迁移/演练工具，并上传通过源码/二进制/迁移/Cargo.lock 复验的 Linux x86_64 候选产物；不连接服务器、数据库或 SMTP，不配置反代或切流。提交 `17261520`。 |
| --- | --- | --- | --- | --- |
| /root | P1 MiniMax-H3 一键本地 ComfyUI/GGUF 配置 | `scripts/local-minimax-h3.ps1`、`src/reader_media.rs`、`apps/desktop-ui/src/legacy-ts/main-semantic/{semantic-ui.ts,semantic-port.ts}`、`ui/index.html`、本表 | 已完成，本机未提交；后台下载中 | 修复“检测中”禁用的一键按钮：无需等待状态检测即可启动，失败在卡片中显示，后台自动轮询安装；手动 ComfyUI/工作流接入收进高级选项。当前已按 16GB 显存 Q3 低显存档开始下载。 |
| /root/public_source_catalog | P1 Horizon/WorldMonitor 可直连公开来源目录与自定义 RSS 安全边界 | `src/newsnow.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已扩展本地资讯目录、通用 HTTPS RSS/Atom 解析与每请求自定义源验证；不改 UI、同步、服务端或外部部署。 |
| /root/reader_annotation_rules | P1 阅读页高亮菜单纯矩形规则拆分 | `ui/{reader-page-annotations.js,reader-page-highlight-rules.js,tests/{reader-page-modules,reader-toolbar}.test.cjs}`、`src/reader_page.rs`、`ui/tests/governance.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将高亮矩形包络、指针最近命中、同页行分组及跨页/末行菜单锚点移至冻结、无 DOM/存储/IPC 依赖的编译期规则脚本；批注渲染、选区、分页、事件、IPC 和 EPUB/PDF 命令式阅读引擎留在原模块。定向 157 项与全量 409 项 Legacy UI、`cargo test reader_page` 和差异检查通过；全仓 rustfmt 仅被任务外正在修改的 update 模块差异阻断。 |
| /root/p5_deploy_manifest | P5 Rust 服务离线发布产物溯源门禁 | `server/reader-sync-api-rs/{scripts,README.md,DEPLOYMENT.md}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交 | 新增仅本地的二进制/源提交树/Cargo.lock/迁移 SHA-256 清单创建与复验；拒绝服务目录未跟踪、未提交或二进制不匹配，避免错误归属。服务源码及 v5 fixture 已作为提交 `dc7502fc` 入库，脚本自测、迁移/拒绝路径自测、服务端测试、格式、Clippy 与差异检查通过；未连接服务器、数据库或读取部署机密。 |
| /root/update_rules_split | P2 更新版本与发布元数据纯规则拆分 | `src/update.rs`、`src/update/rules.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将版本比较、发布 JSON 字段兼容、tag/changelog 解析、HTTPS endpoint 与仓库标识校验移至无网络/Tauri/构建注入依赖的 rules；父模块保留 HTTP、超时、构建配置和命令。5 项更新定向测试、格式、全目标 Clippy 与差异检查通过；未加入部署端点。 |
| /root/registration_ui_audit | 注册端点缺失的安全用户提示审计 | `src/sync/validation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 默认端点不存在时改为明确说明客户端未配置同步服务、不能注册或登录，并建议联系服务提供方或使用已配置版本；不泄露地址、不添回退、未改端点、服务端或 contracts。`cargo fmt --all -- --check`、3 项定向校验测试和差异检查通过。 |
| /root/p3_metadata_metrics | P3 高频 metadata 前缀查询 SQL 计时 | `src/db/metadata.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | `metadata_with_prefix` 现从 SQL 执行到结果收集记录固定的 `metadata_with_prefix` 标签及行数，不记录调用前缀或内容；新增定向测试。`cargo fmt --check`、定向 1 项、全目标 Clippy 与范围差异检查通过；未改 entities、schema、索引、连接池、同步或备份。 |
| /root | 注册默认同步端点构建注入 | `build.rs`、`src/runtime_support.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已提供仓库外 `KUNPENG_DEFAULT_SYNC_URL` 的 HTTPS 构建注入机制；未设置时客户端明确提示尚未配置服务。线上仍是旧协议，正式 macOS 应用未注入任何端点，等待 v5 服务部署、受保护 E2E 与注册验收后才可启用。构建注入验证、472 项 Rust、408 项 Legacy UI、81 项 Typed、桥接构建、Clippy、格式和差异检查通过。 |
| /root/p4_runtime_boundary | P4 PDF 受控渲染器替代打开请求生命周期保障 | `packages/pdf-engine/{src/index.ts,test/performance-baseline.test.mts}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 修正被替代打开请求在 finally 中误清后继 PDF.js loading task 的竞态；close/abort 现原子取得任务所有权后恰好 destroy 一次。新增无内容双打开合成回归，确认旧请求结算不丢失新任务、close 仍清理后继任务。`npm run test:typed`（81 项）、`npm run desktop-ui:build`、PDF bridge 3 项与差异检查通过；未用真实样本、未改 PDF.js Canvas、视觉、手势或页面入口。 |
| /root/gesture_rules_split | P1 手势提示配置纯规则拆分 | `ui/{gesture-ui.js,gesture-hint-rules.js,index.html,tests/gesture-hint-rules.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将提示框配置规范化、形状路径和自由绘制点压缩移至冻结的 classic-script 规则；外壳保留手势输入、Canvas、DOM、存储、IPC 和阅读器引擎命令式执行，并在规则缺失时逐项保持等价回退。定向 62 项、全量 403 项 Legacy UI、Node 语法和范围差异检查通过。 |
| /root/app_settings_rules_audit | P2 应用设置纯规范化规则拆分 | `src/app_settings.rs`、`src/app_settings/rules.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 新闻、工具栏、手势和阅读排版的无状态 JSON 规范化已迁至 `rules`；父模块保留 v5 像素字段/退役字段清理、payload 合并、Tauri 命令和 SQLite 生命周期。新增 2 项纯规则测试；`cargo fmt --check`、10 项 app-settings 测试、全目标 Clippy 和差异检查通过。 |
| /root/reader_page_layout_split | P1 阅读页滚动分页纯几何规则拆分 | `ui/{reader-page-layout.js,reader-page-scroll-rules.js,tests/reader-page-modules.test.cjs}`、`src/reader_page.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 滚动分页的索引、对齐起点、切片底边和断点查找已移至冻结、无 DOM/设置/IPC 的编译期 classic-script 规则；布局模块仅桥接既有全局 API，保留 EPUB/PDF、视觉层、选区、手势和 Canvas 命令式引擎。Node 语法、31 项阅读页模块、24 项治理与差异检查通过；Rust 定向测试被其他会话未完成的 `background_tasks`/`db` 编译错误阻断。 |
| /root/background_registry_split | P2 后台任务持久化注册表低耦合拆分 | `src/background_tasks.rs`、`src/background_tasks/registry.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将持久化任务快照的序列化/加载、恢复归一化、终态、日志与历史裁剪辅助迁至低耦合 registry 模块；调度器、Tauri 命令、后台优先级和锁生命周期保留父模块。29 项后台任务定向测试与全目标 Clippy 通过；全仓 rustfmt 被任务外 `src/db.rs` 的既有格式差异阻断，范围差异检查通过。 |
| /root/i18n_more_split | P1 i18n 语义运行时文案目录纯规则拆分 | `ui/{app-i18n.js,app-i18n-semantic-runtime-catalog.js,index.html,tests/app-i18n.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将语义模型、检索分级、GPU/下载/索引运行态的无 DOM/存储/IPC 十语种 catalog 移至预加载 classic script；入口保留严格 applier 形状校验与无前置脚本时的等价本地回退。定向 10 项、全量 401 项 Legacy UI、Node 语法与差异检查通过；未改 AI 调用、账户/同步语义、React 或契约。 |
| /root/rust_module_audit | P2 书架展示投影低耦合拆分 | `src/library_commands.rs`、`src/library_commands/presentation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 审计后迁移书名首字母及其 GBK/标点规则至无 Tauri、SQLite、文件写入或同步依赖的纯模块；命令签名、书库锁、同步和阅读窗口生命周期保持不变。定向 2 项测试、全目标 Clippy、格式和差异检查通过。其余书架命令均直接编排持锁持久化、同步墓碑或阅读窗口生命周期，不作无价值拆分。 |
| /root/search_next_split | P1 搜索历史纯规则拆分 | `ui/{search.js,search.html,search-history-rules.js,tests/search-history-rules.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 最近搜索规范化、去重、长度上限与常搜排序已移至冻结、无 DOM/存储/IPC 依赖的 classic script；搜索外壳保留 localStorage、DOM、事件和检索命令，并在脚本缺失时走同形状回退。定向 15 项与全量 396 项 Legacy UI、Node 语法检查通过；未改搜索命令、页面入口、React 或契约。 |
| /root/news_next_split | P1 资讯瀑布流纯布局规则拆分 | `ui/{news-ui.js,news-layout-rules.js,index.html,tests/news-layout-rules.test.cjs,tests/news-ui.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已将列数、卡片估高和稳定最短列分配移至冻结、无 DOM/存储/IPC 依赖的 classic script；外壳保留 DOM 创建、图片队列、来源分组、页面与资讯命令，并在规则脚本缺失时逐项使用原公式。定向 19 项与全量 399 项 Legacy UI、Node 语法与差异检查通过。 |
| /root/sync_next_split | P2 同步地址校验与账户 scope 纯规则拆分 | `src/sync.rs`、`src/sync/validation.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | sync/auth base 规范化、本机 HTTP 边界、账户 scope 与 data generation 校验已迁至无 HTTP/SQLite/Tauri/恢复依赖的模块；v5 wire/HTTP、账户恢复、命令签名及 sync generation 语义未变。34 项 `cargo test sync::`、全目标 Clippy、rustfmt 与差异检查通过。 |
| /root/builtin_chinese_dictionary | 恢复合规的内置汉语词典与中英释义切换 | `src/dict.rs`、`src/dict/{zh_zh.tsv.gz,ZHWIKTIONARY_SOURCE.md}`、`tools/generate_zhwiktionary_asset.py`、`THIRD_PARTY_NOTICES.md`、`README.md`、`ui/tests/reader-toolbar.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 旧萌典繁转简资产因禁止改作条款保持移除；现以固定哈希的中文维基词典/Kaikki CC BY-SA 4.0 快照生成 108848 个中中查询键，并保留 CC-CEDICT 中英释义。修正中文英文释义字段映射后，划词卡恢复“中文/英文”切换，生词本双语标签实机可见。3 项真实词典测试、400 项 Legacy UI、全目标 Clippy、许可证门禁、确定性生成/差异检查与 Release 整包构建通过；已覆盖并启动 `/Applications/鲲鹏阅读器.app`。 |
| /root/backup_next_split | P2 备份剩余低耦合边界审计 | `src/backup.rs`、`src/backup/`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 审计确认 catalog/retention/snapshot/restore/transaction-log 已覆盖现存无状态边界；剩余函数直接执行文件暂存/替换/回滚、durable manifest 恢复分支或 SQLite 独占关闭/重开，继续拆分会割裂同一原子事务，故未作无价值移动。36 项备份定向测试、全目标 Clippy 与差异检查通过。 |
| 未登记的既有修改 | 工作区存在多个历史未提交改动 | 以 `git status --short` 为准 | 注意 | 新会话不得假定这些文件可自由修改。 |
| /root/db_next_split | P2 SQLite 便携核心数据包纯规则拆分 | `src/db.rs`、`src/db/portable_package.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 核心数据包 JSON 格式、资源上限和校验已迁至无 SQLite/文件依赖模块；AppDb 保留查询、导入事务、WAL、备份与恢复生命周期。新增纯规则有效 v2 包与标准化敏感字段拒绝测试；24 项 db 定向测试、全目标 Clippy、格式和差异检查通过。 |
| /root/ai_next_split | P2 AI 阅读证据上下文纯规则拆分 | `src/ai_reader.rs`、`src/ai_reader/context.rs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已迁移无状态的证据上下文格式化、来源 ID 解析与引用充分性规则；修正上下文上限边界（包括末尾分隔符），新增独立 2 项测试。父模块保留命令、数据库、语义检索和 provider 编排，不改协议或 Tauri 签名。定向 60 项 AI 测试、全目标 Clippy、格式与差异检查通过。 |
| /root/reader_next_rules | P1 阅读器显式导航历史纯规则拆分 | `ui/{reader.js,reader.html,reader-navigation-rules.js,tests/reader-navigation-rules.test.cjs,tests/reader-toolbar.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 已把导航点规范化、去重、有界历史与分页消失判断移至冻结、无 DOM/存储/IPC 的经典脚本；外壳保留 DOM、计时、事件、命令和等价缺失脚本回退。未触及 EPUB/PDF 分页、选区、手势、PDF.js Canvas、设置或同步契约。聚焦 47 项和全量 392 项 Legacy UI、Node 语法及范围差异检查通过。 |
| /root/sqlite_preflight_immutability | P3 SQLite 只读预检副本不变性自检 | `scripts/test-sqlite-reader-db-preflight.sh`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 临时测试数据库现断言预检前后 SHA-256 一致，且前后均不存在 WAL/SHM sidecar；不修改 SQLite、同步或备份生命周期。`bash -n`、脚本自测和差异检查通过。 |
| /root/i18n_next_split | P1 i18n 资讯文案目录纯规则拆分 | `ui/{app-i18n.js,app-i18n-news-surface-catalog.js,index.html,tests/app-i18n.test.cjs}`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 资讯页十语种纯 classic-script catalog 已在 `app-i18n.js` 前加载；兼容入口保留独立加载时的等价回退与目录形状校验。9 项 i18n 定向、390 项 Legacy UI、语法与差异检查通过。 |
| /root/shelf_next_split | P1 书架自定义滚动条纯几何规则拆分 | `ui/shelf-ui.js`、`ui/shelf-ui-rules.js`、`ui/tests/shelf-ui.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 无状态 thumb 比例、轨道点击和拖动映射已进入既有 `ReaderShelfRules`；外壳负责 DOM/拖拽状态，规则函数未加载时逐项走原公式。未改 `ui/index.html`、书架命令、契约或 React。25 项书架定向、390 项 Legacy UI、Node 语法和差异检查通过。 |
| /root/reader_startup_blocker | 阅读页启动阻断与关闭保险 | `ui/{reader.html,reader.js,reader-startup-guard.js,tests/reader-startup-guard.test.cjs}`、`ui/tests/reader-toolbar.test.cjs`、`docs/coordination/ACTIVE_WORK.md` | 已完成，已提交（55f1a20d） | 实机根因是 `reader-settings-ui.js` 的全局函数与 `reader.js` 新拆分别名同名，macOS WebKit 在解析阶段报语法错误，导致 frame 停在 about:blank 且主关闭逻辑未安装。主脚本现直接通过 `ReaderJumpBackRules` 模块对象访问，已扫描阅读页 27 个经典脚本无其他顶层重名；启动守卫覆盖同步脚本、book_info、有效 EPUB/PDF URL 与引擎 ready，并提供原生关闭兜底。全量 388 项 Legacy UI 回归、Node 语法、差异检查及 Release 整包构建通过；已按规范覆盖 `/Applications/鲲鹏阅读器.app` 并实机验证正文、设置面板和关闭窗口。 |
| /root/sync_v5_desktop | P5 桌面 Rust sync v5 Axum wire 对齐 | `src/sync.rs`、`src/sync/client.rs`、`src/sync/protocol.rs`、`src/reader_backgrounds.rs` 与其内嵌测试 | 已完成，已提交（55f1a20d） | v5 DTO 现在要求 camelCase 的 dataGeneration、嵌套 user、恢复元数据与资产续传偏移，拒绝 Python/snake_case 形状；push/pull/inventory/reconcile/资产和通用 sync 请求均使用 v5 header。32 项同步、2 项资产、全目标 Clippy、格式和差异检查通过。 |
| /root/news_legacy_rules_split | P1 资讯来源/内容纯规则拆分 | `ui/news-ui.js`、`ui/news-rules.js`、`ui/tests/news-rules.test.cjs` | 已完成，已提交（55f1a20d） | 已抽取无 DOM/存储/IPC 的 HTTPS 与图片边界、结果包/预览判断、来源/Tieba 约束；现有资讯壳在规则脚本未预加载时保留等价内嵌回退，未改入口、React、样式或资讯命令。3 项定向规则、13 项资讯回归与全量 381 项遗留 UI 测试、语法及范围差异检查通过。 |
| /root/stats_legacy_rules_split | P1 统计日期范围与导航纯规则拆分 | `ui/stats-ui.js`、`ui/stats-rules.js`、`ui/tests/stats-rules.test.cjs` | 已完成，已提交（55f1a20d） | 已抽取无 DOM/存储/IPC 的日期范围、锚点和导航边界；现有 stats-ui 在规则脚本未预加载时保持等价内嵌回退，未改入口、React、样式或统计命令。聚焦 16 项与全量 372 项遗留 UI 测试、语法及范围差异检查通过。 |
| /root/search_legacy_rules_split | P1 搜索结果纯规则拆分 | `ui/search.js`、`ui/search-result-rules.js`、`ui/tests/search-result-rules.test.cjs` | 已完成，已提交（55f1a20d） | 已抽取转义、CJK 高亮与排序的无 DOM/存储/IPC 规则；独立搜索窗口保留等价内嵌回退，未改 `search.html`、React、搜索命令或 WebView 加载顺序。新增 4 项规则测试、相邻 4 项会话测试和全量 369 项遗留 UI 测试通过。 |
| /root/db_backup_port_split | P2 SQLite 备份端口边界拆分 | `src/db.rs`、`src/db/backup_port.rs` | 已完成，已提交（55f1a20d） | 已抽取 AppDb 的 SQLite 快照与旧实体清理端口；`backup.rs` 的目录暂存、文件回滚和独占连接生命周期未改。`rustfmt --check`、21 项 db 定向测试、全目标 Clippy 与范围差异检查通过。 |
| /root/asset_sync_integration | 背景资产同步 v4 闭环 | `server/reader-sync-api-rs/src/sync.rs`、`src/reader_backgrounds.rs`、对应测试 | 已完成，已提交（55f1a20d） | 桌面端 init/PUT/GET 使用 `/v1/sync/assets/*` 与协议 v4 头，init JSON 使用 `dataGeneration`；服务端重置 E2E 覆盖已完成资产、恢复历史和当日额度清理。服务端 40 项、桌面定向 2 项、两端 Clippy、格式和差异检查通过；未改 contracts、未连接或部署服务。 |
| /root/sync_v4_get_header_audit | P5 桌面同步 GET v4 请求头审计 | `src/sync.rs`、`src/sync/client.rs`、对应内嵌测试 | 已完成，已提交（55f1a20d） | 审计确认账户通用 JSON 请求和资产 GET 已有路径受限 v4 头；补齐直连 `push`、`pull`、`inventory`、`reconcile` 请求，共用单一头名/版本常量。32 项 sync、2 项资产定向测试、格式、全目标 Clippy 及范围差异检查通过；未改认证、公共端点或 UI。 |
| /root/sync_client_split | P2 同步 HTTP 客户端传输边界拆分 | `src/sync.rs`、`src/sync/client.rs` | 已完成，已提交（55f1a20d） | 已抽取带超时的 ureq client 与认证 JSON 请求/响应解码，保留既有端点、认证、v4 header 作用域和外层中文错误映射；32 项 sync 定向测试、全目标 Clippy、格式和范围差异检查通过；未改 contracts。 |
| /root/sync_commands_split2 | P2 同步薄命令适配边界拆分 | `src/sync.rs`、`src/sync/commands.rs` | 已完成，已提交（55f1a20d） | `sync_get_settings`、注册/登录与安全/额度概览的 Tauri 适配编排已迁移；父模块保留领域、HTTP、账户切换和 DB 逻辑。Tauri handler 所需私有宏符号留在父模块的单行转发器，命令签名不变。18 项 sync 测试、全目标 Clippy、全仓格式检查和范围差异检查通过；未改 contracts。 |
| /root/sync_reconcile_split | P2 同步 reconcile 本地决策边界拆分 | `src/sync.rs`、`src/sync/reconcile.rs` | 已完成，已提交（55f1a20d） | 已迁移 manifest 请求投影及服务端要求补传时的本地实体选择；父模块保留 HTTP、SQLite、数据迁移、账户恢复与公开命令。3 项 reconcile 定向测试、18 项 sync 测试、全目标 Clippy 与差异检查通过；未改 contracts。 |
| /root/sync_protocol_split | P2 同步协议纯数据边界拆分 | `src/sync.rs`、`src/sync/protocol.rs` | 已完成，已提交（55f1a20d） | 已迁移无状态 wire 模型、库存摘要、游标和批处理；父模块保留账户恢复、HTTP 编排及 reconcile 流程。6 项协议定向测试、18 项现有 sync 测试、全目标 Clippy 与范围差异检查通过；未改 contracts。 |
| /root/sync_modularization | P2 同步 retry 低耦合边界拆分 | `src/sync.rs`、`src/sync/retry.rs` | 已完成，已提交（55f1a20d） | 仅迁移重试策略、退避和诊断；未触碰账户恢复、协议语义、命令或 UI。`cargo fmt --check`、435 项 Rust 测试、Clippy 通过。 |
| /root | P3 同步 SQLite 访问边界收敛 | `src/sync.rs` | 已完成，已提交（55f1a20d） | 登录、注册、设置、登出及需认证请求的短 SQLite 临界区已统一为 `with_db_read/write`，网络调用前释放锁；保留备份/恢复独占连接生命周期、协议与账户语义。31 项同步测试与 Clippy 通过。 |
| /root | P2 SQLite metadata 边界拆分 | `src/db.rs`、`src/db/metadata.rs` | 已完成，已提交（55f1a20d） | metadata CRUD、设备 ID 与同步作用域已拆分；格式、21 项 db 测试和 Clippy 通过；不改变连接生命周期。 |
| /root | P3 应用命令 SQLite 边界收敛 | `src/app_commands.rs` | 已完成，已提交（55f1a20d） | 翻译凭据读写改用 `with_db_read/write`，网络翻译前已释放 SQLite 锁；定向测试和 Clippy 通过。 |
| /root | P2 AI provider 低耦合拆分 | `src/ai_reader.rs`、`src/ai_reader/provider.rs` | 已完成，已提交（55f1a20d） | 网络协议、端点、响应解析和脱敏错误已拆分；42 项 AI 测试和 Clippy 通过。 |
| /root/ai_error_types_audit | P2 AI provider 内部结构化错误审计 | `src/ai_reader/provider.rs`、其内嵌测试 | 已完成，已提交（55f1a20d） | 传输、响应读取、HTTP 拒绝、JSON 解析及空回答已收敛为 provider 私有结构化错误；`call`/`call_async` 仍在模块边界输出原有中文 `Result<String, String>`，未触碰 Tauri command、父模块或契约。7 项 provider、42 项 AI 测试及全目标 Clippy 通过。 |
| /root | P3 数据命令 SQLite 边界收敛 | `src/data_commands.rs` | 已完成，已提交（55f1a20d） | 数据包导入/导出改用 `with_db_read/write`；大 JSON 写盘在释放锁后执行，恢复排他连接生命周期未变。 |
| /root | P1 i18n catalog 拆分 | `ui/app-i18n.js`、`ui/app-i18n-reranker-catalog.js` | 已完成，已提交（55f1a20d） | 保持兼容入口/API，纯 reranker catalog 已移出；51 项相关旧 UI 测试通过。 |
| /root/i18n_legacy_split2 | P1 i18n statistics catalog 拆分 | `ui/app-i18n.js`、`ui/app-i18n-stats-catalog.js`、`ui/tests/app-i18n.test.cjs` | 已完成，已提交（55f1a20d） | 已新增图表、详情、热图的无 DOM/存储/IPC 经典脚本目录，并给兼容入口增加可选委托与严格形状检查；因 `ui/index.html` 被 `/root/single_ui` 占用，尚未加入实际 script 标签，现有 WebView 仍走等价内置回退。8 项 i18n 测试、Node 语法检查与范围差异检查通过。 |
| /root | P2 备份 snapshot 边界拆分 | `src/backup.rs`、`src/backup/snapshot.rs` | 已完成，已提交（55f1a20d） | manifest 兼容性和完整性校验已拆分；父模块仍独占文件系统、SQLite 生命周期和恢复事务；28 项备份测试及 Clippy 通过。 |
| /root/backup_transaction_log_split | P2 备份恢复事务日志边界拆分 | `src/backup.rs`、`src/backup/transaction_log.rs` | 已完成，已提交（55f1a20d） | 耐久 manifest/日志序列化、完整性刷新及加载路径/白名单校验已迁移；父模块保留文件暂存、复制/重命名、回滚编排和 SQLite 生命周期。30 项备份测试、定向 rustfmt、全目标 Clippy 和差异检查通过；全仓 `cargo fmt --check` 仍受任务外 `src/private_sync.rs` 格式差异阻断。 |
| /root/backup_restore_split | P2 备份恢复计划边界拆分 | `src/backup.rs`、`src/backup/restore.rs` | 已完成，已提交（55f1a20d） | 已迁移无状态的恢复计划路径、事务日志计划投影、同目录约束、运行时投影名单与 SQLite 目标识别；父模块保留文件、SQLite 独占生命周期、复制重命名与回滚。33 项备份测试、全目标 Clippy、定向 rustfmt 与范围差异检查通过；全仓 `cargo fmt --check` 仍受任务外 `src/sync.rs`/`src/sync/protocol.rs` 格式差异阻断。 |
| /root | P5 Axum v4 合约缺口核对 | `server/reader-sync-api-rs/` | 已完成，已提交（55f1a20d） | 已实现 feedback、密码变更、数据重置、密码找回、背景资产分块与 90 天实体恢复历史。恢复只记录实际接受的非密钥包版本，校验压缩内容、保留窗口前锚点；确认恢复生成墓碑、递增世代并撤销会话。数据重置会物理清空同步状态、资产和恢复历史。所有功能尚未部署。 |
| /root/postgres_rehearsal_tools | P5 PostgreSQL 演练受保护入口 | `server/reader-sync-api-rs/scripts/`、`server/reader-sync-api-rs/README.md`、`docs/testing/sync-api-v5-postgres-rehearsal.md` | 已完成，已提交（55f1a20d） | 已增加离线迁移静态检查、显式破坏性确认/测试库前缀门禁，以及不连接数据库的拒绝路径自检；未执行真实 PostgreSQL E2E、部署或备份恢复。脚本自检、服务端测试和 Clippy 通过。 |
| /root | P2 AI history/retrieval 低耦合拆分 | `src/ai_reader.rs`、`src/ai_reader/history.rs`、`src/ai_reader/retrieval.rs` | 已完成，已提交（55f1a20d） | 纯检索策略已拆分；Tauri commands、配置与网络调用仍留在父模块。已通过全量 Rust 测试、Clippy 及旧 UI 回归。 |
| /root/ai_commands_split | P2 AI 配置命令适配边界拆分 | `src/ai_reader.rs`、`src/ai_reader/commands.rs` | 已完成，已提交（55f1a20d） | 配置档案的 6 个 Tauri command 已迁移为薄 State 适配器；父模块保留领域校验和持久化，命令名称/参数/返回值不变。`cargo fmt --check`、51 项 AI 测试、全目标 Clippy 及差异检查通过。 |
| /root | P2 SQLite entities 边界拆分 | `src/db.rs`、`src/db/entities.rs` | 已完成，已提交（55f1a20d） | 同步实体查询/投影辅助已迁移；连接生命周期、schema 和 metadata 边界保留。已通过全量 Rust 测试和 Clippy。 |
| /root/db_migration_split | P2 SQLite 旧库紧凑迁移边界拆分 | `src/db.rs`、`src/db/migration.rs` | 已完成，已提交（55f1a20d） | 旧 keyword 索引库的 checkpoint、快照校验和可回滚文件替换已迁入迁移模块；AppDb API、schema/entities/metadata、便携导入语义与备份恢复生命周期未变。`cargo fmt --check`、21 项 db 定向测试和全目标 Clippy 通过。 |
| /root/db_schema_split | P2 SQLite schema 初始化边界拆分 | `src/db.rs`、`src/db/schema.rs` | 已完成，已提交（55f1a20d） | DDL、WAL/连接 PRAGMA 和 `dirty` 兼容列初始化已迁移；AppDb API、文件生命周期和备份/恢复编排未变。22 项 db 定向测试、全目标 Clippy、格式和范围差异检查通过。 |
| /root/sqlite_metrics_audit | P3 SQLite 锁等待与慢操作观测收敛 | `src/app_state.rs`、`src/db.rs`、`src/diagnostics.rs` | 已完成，已提交（55f1a20d） | 已将锁等待、helper 锁内区间和有明确 SQL 边界的 AppDb 操作分为独立、带类型的脱敏诊断；不改变单连接/WAL 或备份恢复生命周期。定向 27 项测试、全目标 Clippy 与范围差异检查通过。 |
| /root | P4 EPUB/PDF 真实样本验收 | `packages/reader-engine/`、`packages/pdf-engine/`、`docs/testing/` | 待隔离 profile 人工验收 | 自动验收基线和仓库外 SHA-256/性能记录工具已完成；当前仓库不保存 EPUB/PDF 二进制，不能伪造真实打开、截图、计时或内存结果。已验证隔离配置能从空书架经真实 UI 导入受控 EPUB；Computer Use 不能按 PID 附着隔离进程，剩余阅读/PDF/性能/视觉验收须在直接启动的隔离进程中人工完成。 |
| /root/sqlite_explain_preflight | P3 授权大书库 SQLite 只读预检 | `scripts/sqlite-reader-db-preflight.sh`、`scripts/test-sqlite-reader-db-preflight.sh`、`docs/testing/sqlite-reader-db-preflight.md` | 已完成，已提交（55f1a20d） | 只读预检要求显式授权确认、仓库外新建输出，并在 `-readonly` 与 `query_only` 双重门禁下运行 quick_check、聚合及固定 EXPLAIN 探针。报告只留汇总数与计划分类；脚本自检验证脱敏、拒绝路径与不修改临时 DB。 |
| /root/reader_legacy_split2 | P1 阅读器 AI 历史纯规则拆分 | `ui/reader.js`、`ui/reader.html`、`ui/reader-ai-history-rules.js`、`ui/tests/reader-ai-history-rules.test.cjs`、`ui/tests/reader-toolbar.test.cjs` | 已完成，已提交（55f1a20d） | 历史 ID、墓碑合并、会话摘要和长度限制已迁至冻结、无 DOM/存储/IPC 依赖的经典脚本；外壳仅保留存储、同步与 DOM 编排。聚焦 50 项与全量遗留 UI 361 项测试通过；未改 EPUB/PDF、分页、选区、手势或 PDF.js Canvas。 |
| /root/shelf_legacy_split2 | P1 书架首屏封面加载纯规则拆分 | `ui/shelf-cover-loading-rules.js`、`ui/tests/shelf-cover-loading-rules.test.cjs` | 已完成，已提交（55f1a20d） | 已抽取首屏封面数量、上下限及 eager/lazy 加载优先级的无副作用规则；`ui/index.html` 由 `/root/single_ui` 占用，故没有接入或改动 `shelf-ui.js`，避免引入未加载依赖。定向 26 项旧 UI 测试和 Node 语法检查通过。 |
| /root/axum_deploy_audit | P5 Rust 服务离线部署配置自检 | `server/reader-sync-api-rs/src/bin/`、`server/reader-sync-api-rs/scripts/`、`server/reader-sync-api-rs/{README.md,DEPLOYMENT.md}`、`docs/testing/sync-api-v5-postgres-rehearsal.md` | 已完成，已提交（55f1a20d） | `config_check --offline` 只解析运行配置而不连接 PostgreSQL 或监听端口；拒绝路径覆盖缺少数据库、过短 HMAC、未显式允许的公网 bind。迁移静态检查、工具自检、32 项服务端测试与全目标 Clippy 通过；未执行真实 E2E、备份恢复、部署或切流。 |
| /root/sync_load_rehearsal | P5 受保护 PostgreSQL 幂等/限流负载演练 | `server/reader-sync-api-rs/{scripts,tests}/`、`docs/testing/sync-api-v5-postgres-rehearsal.md`、`README.md` | 已完成，已提交（55f1a20d） | 新增独立确认词、测试库前缀及 URL 静默门禁；固定本地 Router 演练验证 12 个并发相同 mutation 只留一条实体/收据，以及匿名反馈第 4 次返回 429、只保存前三条。拒绝路径自检、2 项定向测试（未提供数据库时跳过连接）、全目标 Clippy、格式与范围差异检查通过；未连接、部署或接触外部服务器。 |
| /root/reader_sample_schema_check | P4 仓库外阅读样本记录校验 | `scripts/{validate,test-validate}-reader-sample-record.mjs`、`scripts/record-reader-sample.mjs`、`docs/testing/reader-pdf-*`、`package.json` | 已完成，已提交（55f1a20d） | 记录升级为 schema v3，强制格式所需计时/视觉证据，独立校验器拒绝未知字段、路径/URL/内容泄漏及伪造的汇总数；全程不读取或提交真实 EPUB/PDF、路径、正文、截图或录像。`npm run test:reader-sample-record`、Node 语法检查和范围差异检查通过。 |
| /root/reader_prefs_rules_split | P1 阅读偏好颜色纯规则拆分 | `ui/reader-preferences-ui.js`、`ui/reader-preference-color-rules.js`、`ui/reader.html`、`ui/tests/reader-preference-color-rules.test.cjs` | 已完成，已提交（55f1a20d） | 已提取冻结、无 DOM/存储/IPC 依赖的 Hex/HSL 规则；偏好页在脚本预加载时委托，在脚本缺失时保持等价内嵌回退。未触碰 EPUB/PDF、分页、选区、手势、PDF Canvas、样式或主窗口入口。定向 21 项、全量 380 项遗留 UI 测试、Node 语法与范围差异检查通过。 |
| /root/backup_split_audit | P2 备份目录册低耦合边界拆分 | `src/backup.rs`、`src/backup/catalog.rs`、对应备份测试 | 已完成，已提交（55f1a20d） | 已抽取恢复点目录发现、状态投影、稳定排序与安全 ID/路径约束；父模块仍独占创建、轮转删除、恢复事务、文件替换、SQLite 生命周期与回滚。35 项备份定向测试、454 项全量 Rust 测试、全目标 Clippy、格式及差异检查通过。 |
| /root/reader_inner_engine_preload | 阅读器内层引擎预热、内存观测、分层测速、测速后启动失败与连续开书卡死诊断 | `src/{main.rs,window_commands.rs,library_commands.rs,epub_runtime.rs,diagnostics.rs}`、`apps/desktop-ui/src/legacy-ts/{reader-shell/reader.ts,reader-trace/reader-bug-trace.ts,shelf-ui/shelf-ui.ts,main-settings/{reader-shell-preload-ui,problem-trace-ui}.ts}`、相关测试、`docs/coordination/ACTIVE_WORK.md` | 已修复连续开书后 PDF 冷建窗被误判为提前关闭的竞态；本机桌面快捷方式目标已更新，未提交，待用户复测 | 阅读器生命周期事件按窗口标签定向发送，避免测速窗口污染预加载池；外层唯一开关同时控制外壳、内层引擎和最近阅读缓存，三项状态合并显示并按 120 MiB 总预算观测。连续开书由全书架单飞控制，忙碌时仅保留最后一本；目标首屏真正显示后才完成，切换异步串行。空闲外壳在首屏稳定后补充，连续下一次打开最多等 800ms 获取已预热外壳。问题记录新增图片阻塞与资源超时指标。PDF 绕过重排格式的内层引擎并使用独立普通窗口；跨书切换标记现在保持到替换窗口完成原生创建、几何恢复和显示，错误返回自动清理，因而不会再把冷建窗的短暂隐藏判为用户关闭。测速页区分 EPUB 完全冷开、预加载命中和书架实际打开耗时；PDF 明示不参与 EPUB 预加载。Rust 窗口 25/25、格式检查及 fast 构建通过；桌面目标 SHA-256 `99BCA3C90AE667E7EBE60D84F321DED8DFB76E1ABD75DB5424B127BE4B1B94D1`，PID 4736 正常响应；未改同步或跨端契约。 |
| /root/publication_pipeline | P0 本机情报实时事件发布与日报就绪投影 | `src/intelligence_worker/{publication.rs,mod.rs}`、对应 Rust 测试、本表 | 已完成，提交 `c7ae685` | 基于既有 `kind=event` V1 契约，服务循环每轮先冻结/尝试投递一个当天已完成事件修订，再独立固化前一 UTC 日日报；无 capability 仅本机落草稿，绝不伪造发布。新修订使用新不可变 ID，关联图片仍逐 SHA-256 验证。新增 `--publish-event-once` 的脱敏聚合状态。未改 server/contracts、凭据或工作台 UI。 |
