# Architecture Decision Records（ADR）

本目录记录会影响多个客户端、同步服务或长期维护方式的决定。文件命名：`NNNN-简短英文或中文标题.md`，例如 `0001-contract-first.md`。

每份 ADR 至少包含：背景、决定、兼容性影响、替代方案、迁移/回滚方案、验证方式和状态（提议/已接受/已废弃）。

纯视觉微调不需要 ADR；同步实体、删除语义、冲突合并、认证方式、跨端存储边界等必须记录。
