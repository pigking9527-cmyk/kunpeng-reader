# 鲲鹏阅读器 v1.14.0

发布日期：2026-08-08

## 重点更新

- 新增完整“阅读偏好”页面，支持总体/单书独立设置、自定义配色与背景图、分页策略、工具栏按钮显隐和高级选项。
- 新增书内跳转回退：目录、脚注和书内链接跳转后可从正文右侧逐级返回；图标支持自动隐藏规则和 1～10 级大小。
- 底部整书进度条维护独立的完整跳转历史，拖动时使用本地预览，减少圆球被旧进度拉回和弹动。
- 新增启动增强：关闭窗口后可保留后台进程快速恢复，并可单独决定是否继续语义索引等高消耗任务；Windows 另提供开机自启。
- 智读新增三档侧栏宽度、脑图收起，以及最近 100 条自动同步/手动选择云端记录；本机历史不设活跃记录上限。
- 自定义主题、排序与背景图支持 Windows、macOS、Linux 同步；背景图使用独立资产传输，不再把大 Base64 原图塞入主题或阅读页。
- Windows GPU 组件检测改为异步、无命令行闪窗，并支持恢复上次下载状态和按需安装大型 CUDA 运行库；不可用时自动回退 CPU。
- Linux 主窗口与阅读窗口新增八方向边框拉伸；更新提示仅在当前平台存在对应发布资产时显示。

## 修复与稳定性

- 修复词义提示、可能义项、结合当前句子和上位词开关无法保持关闭状态。
- 修复简繁、目录、章节、朗读、批注、生词本和页码等工具栏按钮设置隐藏后仍显示的问题。
- 修复阅读偏好滚动条样式、嵌套滚动边界和外部点击收起设置等问题。
- 修复 EPUB 空 spine/空首章、跨章定位闪现、双页续读锚点及关闭重开后的阅读位置偏移。
- 加强阅读窗口关闭状态和书架打开重试，减少关闭阅读页后第一次点击图书无响应。
- 修复启动增强状态下窗口偶发无法关闭或再次唤醒，以及切换开机自启时闪出命令行。
- 修复 GPU 已下载后仍要求再次安装、检测卡顿、说明文字过长和设置页面高度跳动。
- 修复应用动画开启但翻页动画仍被系统减少动态效果静默关闭的问题。

## 同步与数据安全

- 用户标签和收藏夹拆分为独立同步实体，旧客户端不能再用空数组覆盖新版分类关系。
- 云端保存最近 90 天的压缩实体变化历史，并保留窗口前恢复锚点；恢复后撤销旧登录，防止旧设备立即覆盖恢复结果。
- 整库快照按最近 7 个日备份、4 个周备份和 12 个月备份轮换，并执行一致性与 SHA-256 校验。
- 自定义背景图支持哈希去重、分块上传、断点续传、Range 下载、大小限制和完整性校验。
- 新账号完成邮箱验证后才允许同步；服务端增加账号存储/每日写入预算、共享限流、封禁审计和异常记录。

## 发布资产

- Windows x64：单文件便携版、NSIS 安装包、`SHA256SUMS.txt`
- macOS Apple Silicon：App ZIP、DMG、`SHA256SUMS-macOS.txt`
- Linux x86_64：AppImage、deb、`SHA256SUMS-Linux.txt`
- Android：继续使用 v0.3.0 Profile 测试包

## 已知说明

- Windows 安装包尚未进行 Authenticode 签名，首次运行可能显示系统安全提示。
- macOS 使用临时签名，尚未完成 Developer ID 签名和 Apple 公证。
- Linux 当前兼容基线为 Ubuntu 24.04 / glibc 2.38+；CUDA 运行依赖不可用时自动使用 CPU。