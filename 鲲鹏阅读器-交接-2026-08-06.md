# 鲲鹏阅读器交接说明

日期：2026-08-06  
仓库：`C:\Users\pigki\Documents\Codex\2026-07-21\claude-projects\ebook-reader-tauri`  
桌面端当前构建：v1.12.0

## 服务器与同步 API

| 项目 | 信息 |
| --- | --- |
| 云服务器 | 京东云 `117.72.220.69`，Ubuntu 24.04 |
| SSH | `deploy@117.72.220.69:22` |
| 身份文件 | `C:\Users\pigki\.ssh\id_ed25519` |
| 权限 | `deploy` 已配置免密 `sudo` |
| 同步 API 代码 | `/srv/apps/reader-sync-api` |
| SQLite 数据库 | `/srv/data/reader-sync/entities.db` |
| 公开 API（交接时提供） | `http://117.72.220.69` |
| 客户端当前正式同步基线 | `https://117.72.220.69`（见 `docs/coordination/CURRENT.md`） |

连接示例：

```powershell
ssh -i C:\Users\pigki\.ssh\id_ed25519 deploy@117.72.220.69
```

服务器部署单元名称没有写入本交接资料；接手时先只读确认，避免误重启无关服务：

```bash
sudo systemctl list-units --type=service --all | grep -Ei 'reader|sync|api'
ps -ef | grep -E 'reader-sync|uvicorn|gunicorn|node' | grep -v grep
sudo ss -ltnp
```

确认实际服务单元后，再使用 `sudo systemctl status <unit>`、`sudo journalctl -u <unit> -n 200` 查看状态和日志。部署或迁移前先备份 `entities.db`；不要把数据库、SSH 私钥、Token、密码或 API Key 提交到 Git。

## 当前工作区与构建产物

- GitHub `main` 发布提交：`9107af7 Merge pull request #16 from pigking9527-cmyk/codex/release-v1.12.0`；功能提交为 `6d47107 feat: release v1.12.0 desktop experience and data controls`。
- 共享工作区在发布完成时另有尚未提交的后续开发改动（自动导入监听、重读字数统计等）。继续工作前先 `git status --short`，不要用 `reset --hard`、`checkout --` 或清理命令覆盖。
- 当前本机构建产物：`鲲鹏阅读器.exe`（仓库根目录）。
- 本次构建 SHA-256：

  ```text
  688A284804758EED4593048D3A84D903377A0D23161F9B15D6E64B2D37D3E345
  ```

- 若要交付 Windows 验收包，构建后覆盖项目内 `鲲鹏阅读器.exe`。若此**确切路径**正在运行，可只关闭路径完全匹配的进程；不要关闭其它同名或未知进程。

## 已实现的桌面功能

### 书库问答与智读

- 书库问答在本机语义索引中检索；支持全书库、标签/收藏夹筛选、单书深度问答和跨书比较。
- 广泛问题在检索材料足够时会补充多本书、多处正文依据；材料少时不强制凑多本书。
- 回答采用“**一句重点。** 后接具体书名、人物/情节、来源”的依据格式；禁止把模型审校过程、`某人`、`某来源`等占位语输出给用户。
- 支持 Enter 提问、Shift+Enter 换行，以及仅剪切/复制/粘贴的右键菜单；问答历史保留问题和回答。
- 智读、书库问答历史可选同步；同步历史不包含书籍正文、本机路径、原书文件或向量索引。

### 资讯

- 资讯是主窗口内的浏览器渲染页面；资讯流与资讯正文为独立页面，正文仅显示抽取后的标题、正文、正文图片、来源和时间，并可返回资讯流。
- 来源可在“管理来源”中选择；支持贴吧自定义多个吧名、3DM 游戏新闻、游民星空新闻及既有公开来源。
- 布局支持列表/方格、混合/按来源排序。卡片有图时显示封面，无图时不留占位。
- **资讯数量不设阅读器侧总上限或单来源上限。** 所选来源实际返回多少条就展示多少条；来源返回 1,000 条也保留。贴吧仍会压低陈旧置顶内容，优先近期帖子。
- 页面不再显示“已更新 N 条资讯”。
- 应用空闲时每 5 分钟刷新已启用来源；封面按批后台预取、压缩并写入本机缓存。手动刷新也会先预取首批封面，避免卡片后续异步插图造成跳动；缓存会继续分批补齐更多图片。

关键入口：

| 范围 | 文件 |
| --- | --- |
| 资讯来源、缓存、封面预取、正文提取 | `src/newsnow.rs` |
| 资讯页面、布局、来源管理 | `ui/news-ui.js` |
| 资讯样式 | `ui/styles.css` |
| RAG、智读、书库问答、分类 | `src/ai_reader.rs` |
| Tauri 命令注册 | `src/main.rs` |
| 书库问答 UI | `ui/library-ai.js`、`ui/library-ai-entry.js` |
| 同步实现 | `src/private_sync.rs`、`server/reader-sync-api/` |
| 跨端基线 | `AGENTS.md`、`docs/coordination/CURRENT.md`、`contracts/`、`docs/adr/` |

## 同步边界

- 同步：阅读进度、书签、高亮、批注、评分、手工标签、收藏夹、生词本、统计、模型书籍分类标签；用户开启后可同步智读/书库问答历史和加密后的密钥包。
- 不同步：书籍正文、原书文件、原始封面、语义模型、语义索引、本机路径、明文 API Key。
- 修改跨端同步语义时，必须先更新 ADR、`contracts/`、fixture 与兼容测试；不要把桌面 SQLite 当作跨端协议。

## 验证与构建

v1.12.0 发布门禁已通过：Rust 336/336、前端 173/173、同步服务 25/25；`scripts/check.ps1 -Release`、GitHub Windows CI、macOS Apple Silicon 工作流和 Linux x86_64 工作流全部成功。Linux 额外通过动态链接检查和启动烟测，macOS 通过临时签名核验。

正式 Release：`https://github.com/pigking9527-cmyk/kunpeng-reader/releases/tag/v1.12.0`

- Windows：便携版、NSIS 安装包、`SHA256SUMS.txt`。
- macOS Apple Silicon：App ZIP、DMG、`SHA256SUMS-macOS.txt`。
- Linux x86_64：AppImage、deb、`SHA256SUMS-Linux.txt`。
- 正式更新端点 `https://117.72.220.69/updates/latest` 已返回 `1.12.0`。

```powershell
$env:ORT_LIB_PATH=(Resolve-Path -LiteralPath 'target\local-ort-link').Path
Remove-Item Env:HTTP_PROXY,Env:HTTPS_PROXY,Env:ALL_PROXY -ErrorAction SilentlyContinue
$env:CARGO_NET_OFFLINE='false'
cargo fmt -- --check
cargo test newsnow
node --test ui\tests\news-ui.test.cjs
cargo build --profile fast
```

发布前还应运行：

```powershell
.\scripts\check.ps1 -Release
```

交接时该完整脚本有一个既有前端国际化测试断言待处理：它仍期待旧的 `data-i18n="libraryDescription"` 标记。Rust Clippy、Rust 测试和资讯专项测试均已通过；修复该断言后再将完整脚本视为绿灯。

## 接手顺序

1. 先检查工作区状态和未提交修改，再读取 `AGENTS.md`、`CURRENT.md`、相关 ADR/契约。
2. SSH 只读核对服务器服务单元、监听端口、日志与数据库备份策略。
3. 修改桌面 UI 或资讯功能后运行专项测试；涉及同步/API 语义时先改契约和 ADR。
4. 发布 Windows 版前运行完整检查、生成校验和，并只上传正式构建产物。
